---
course: "GPU 性能工程"
unit: "04｜测量Roofline与瓶颈定位"
title: "测量、Roofline 与瓶颈定位｜GPU 性能工程 04"
kicker: "GPU 性能工程｜第 04 单元"
summary: "这页把一个 kernel 的时间、工作量和搬运字节放到同一张账上，先判断它更像带宽受限还是计算受限，再把“慢”压成一个能复测的假设。它接着上一页的访存形状判断，往下一页的 Triton 块级优化走；这里先不展开更细的计数器解释或微架构细节。"

side_note: "把同一次实验里的时间、FLOP、字节数和 profiler 读数放在一起看。口径对不齐，后面的优化判断就容易跑偏。"

can_do:
  - "能写出一个最小但可信的 kernel 计时口径，知道为什么要同步、预热、重复运行和固定输入形状。"
  - "能用 FLOP、字节数、时间和算术强度算出一个算子的上限，并判断它更接近带宽墙还是计算墙。"
  - "能把 profiler 里看到的现象翻成下一步动作：先查数据是否搬得碎，再查复用是否足够，最后回到端到端复测。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一单元"
    href: "../03_内存层次访存合并与带宽/学习页.html"
  - text: "下一单元"
    href: "../05_Triton编程模型与块级思维/学习页.html"

---

# 这页要练的三件事 {#goals}
<!-- tag: measurement -->
<!-- layout: grid -->

**把时间测准**
能区分 CPU 侧 launch 时间、GPU kernel 时间和端到端时间，知道何时需要 CUDA event、同步、warmup 和重复运行。

**把上限算清**
能从 FLOP、字节数和时间算出实际吞吐、有效带宽、算术强度和 Roofline 上限，并保留单位。

**把指标变成动作**
能把 Nsight Systems 的时间线和 Nsight Compute 的单 kernel 指标，写成下一轮可验证的优化假设。

> **先放一边的细节** 先不展开具体 GPU 的所有硬件 counter、tensor core 指令编码、cache slice、bank conflict 和厂商库内部实现。这里先把测量链路学稳，后面的 Triton 和注意力优化会继续使用这套判断语言。


---

# 先把“慢”拆成几种可验证的情况 {#overview}
<!-- tag: overview -->
<!-- layout: grid2 -->

## box soft
### 先拿一个固定例子
这页沿用上一节的两个例子：FP32 向量加法和一个 FP16 矩阵乘。向量加法每个元素读两个输入、写一个输出，FLOP 很少，适合观察带宽限制。矩阵乘会反复使用 tile，FLOP 很多，适合观察计算上限和实际实现差距。
同一个计时结果，只有放进工作量和字节账本里才有意义。一个 kernel 跑 1 ms 可能很好，也可能很差；要看它应该搬多少字节、应该做多少计算，以及它离机器的可持续上限有多远。

## box info
### 一条顺手的排查顺序
推荐顺序是：先测端到端时间线，确认热点在哪；再给热点写 microbenchmark，测 kernel 自身；接着算 FLOP/byte 和 Roofline；最后回到 profiler 指标，判断下一轮实验应该改访存、并行度、计算路径还是 launch 粒度。

步骤回答的问题常用工具或记录下一步输出


端到端时间线时间主要花在哪个阶段Nsight Systems、应用日志、CUDA API 时间线热点 kernel 或同步/拷贝空洞
microbenchmark热点本身是否稳定、可复现CUDA event、固定 shape、warmup、重复运行可信 kernel 时间分布
工作量账本它理论上做了多少计算、搬了多少数据FLOP、读写字节、dtype、shape实际吞吐和算术强度
Roofline 对照它更像带宽受限还是计算受限峰值计算、可持续带宽、AI理论上限和效率差距
指标确认差距来自哪里Nsight Compute、stall、occupancy、memory throughput下一轮可验证假设


---

# 先把计时测准 {#timing}
<!-- tag: timing -->
<!-- layout: grid2 -->

## box
### CPU 计时为什么容易误判
CUDA kernel launch 对 CPU 通常是异步的。CPU 侧用墙钟包住一次 launch，如果没有同步，测到的可能停在提交工作这一层，GPU 真正执行 kernel 的时间还没有纳入。端到端分析可以看墙钟，但 kernel microbenchmark 应该把异步边界写清楚。
一个可信计时至少要说明：输入 shape、dtype、warmup 次数、重复次数、同步位置、统计口径，以及是否包含 host-device copy。没有这些信息，优化前后的百分比很容易是噪声。

## box warn
### 计时口径分开记
端到端时间用于判断用户是否真的变快；kernel 时间用于判断局部实现是否变快；profiler 时间用于解释为什么快或慢。不要把三个口径混成一句“快了 30%”。

CUDA event 计时骨架复制

```python
# PyTorch sketch: measure GPU work, not only CPU launch overhead.
for _ in range(warmup):
 kernel(x, y, out)
torch.cuda.synchronize()

start = torch.cuda.Event(enable_timing=True)
end = torch.cuda.Event(enable_timing=True)
times_ms = []

for _ in range(iters):
 start.record()
 kernel(x, y, out)
 end.record()
 torch.cuda.synchronize()
 times_ms.append(start.elapsed_time(end))

print({
 "shape": tuple(x.shape),
 "dtype": str(x.dtype),
 "median_ms": median(times_ms),
 "min_ms": min(times_ms),
 "max_ms": max(times_ms),
})
```

对象读什么写什么错了的症状下一步检查


输入 shape当前 batch、长度、矩阵规模benchmark 记录换 shape 后结论失效固定一组代表性 shape，再看分布
warmup缓存、编译、调度初始状态稳定后的运行状态第一次特别慢，均值被拉偏单独丢弃 warmup 轮次
同步点GPU 队列是否完成可读的耗时记录只测到 launch 提交时间用 event 或 synchronize 明确边界
统计口径每次运行耗时median/min/max只报单次结果，无法判断波动至少看 median 和 min/max


---

# Roofline 怎么算 {#roofline}
<!-- tag: roofline -->
<!-- layout: grid2 -->

## box soft
### 先把三个量写清
Roofline 的横轴是算术强度，也就是每搬 1 byte 数据能做多少 FLOP。纵轴是性能，通常写成 FLOP/s。给定机器峰值计算性能和可持续内存带宽，某个算子的理论上限可以先用一个小公式估出来。
Roofline 模型用来给判断划边界，精度来自后续 profiler 复查。它的价值是把“我觉得慢”改写成“这个点在带宽墙下面、计算墙下面，还是远离两个屋顶”。远离屋顶时，下一步才去看 occupancy、stall、tensor core、访存布局、同步和 launch 粒度。

## box info
### ridge point 只负责分界
ridge point = 峰值计算 / 可持续带宽。AI 低于它时，带宽更可能先成为上限；AI 高于它时，计算路径更值得检查。它只给方向，不替代 profiler 和真实实验。

$$
AI = \frac{FLOPs}{Bytes},\quad
P_{roof} = \min(P_{peak},\; BW_{sustained} \times AI),\quad
P_{actual} = \frac{FLOPs}{Time}
$$
 `AI` 是算术强度；`P_peak` 是目标硬件的峰值计算性能；`BW_sustained` 是可持续带宽；`P_actual` 是实测吞吐。算完以后要保留单位，例如 FLOP/byte、TFLOP/s、TB/s。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

例子工作量账本AIRoofline 判断第一检查项


FP32 vector add每元素 2 FLOP，约 12 byte约 0.17 FLOP/byte通常偏带宽受限连续访问、有效带宽、launch 是否过碎
FP16 matmul大矩阵有大量乘加和 tile 复用可能数百 FLOP/byte更可能看计算路径tensor core、tile、数据布局、occupancy
高 AI 低吞吐 kernel账本看起来接近计算密集高于 ridge point不应只怪 HBM指令路径、stall、寄存器 spill、同步


---

# 读 profiler {#profiler}
<!-- tag: profiler -->
<!-- layout: grid2 -->

## box
### 先看时间线，再看单 kernel
端到端慢时，先看时间线。你要找的是 kernel 是否真的占主要时间，还是被 host 逻辑、同步、CPU 调度、memcpy、launch 空洞或小 kernel 串行化吃掉了。一个 kernel 局部快了 30%，如果它只占端到端 3%，整体收益会很小。

## box
### Nsight Compute 再回答“热点为什么慢”
单 kernel 报告适合解释瓶颈：实际带宽、SM 利用率、warp stall、occupancy、寄存器压力、shared memory 使用、访存事务和计算管线使用。读这些指标时先带着 Roofline 假设进去，不要在一堆 counter 里漫游。

现象更可能的原因先做的小实验回看哪一节


memory throughput 接近可持续上限算法本身带宽密集，或数据复用不够减少读写字节、融合相邻操作、改变数据布局03 的字节账本和本页 Roofline
AI 高但吞吐远低于 Roofline计算路径没打满、tile 形状差、寄存器压力或 stall检查 tensor core 使用、block size、寄存器 spill、occupancy本页 profiler 和 05 Triton
小 kernel 很多，时间线有空洞launch overhead 或同步过多合并操作、减少同步、端到端重测本页 timing 和 workflow
单 kernel 快，端到端几乎不变热点占比小，或新瓶颈转移到别处看 Systems 时间线，比较整体阶段占比workflow 和下一步实验
访存请求拉散线程映射和数据布局错位沿 threadIdx.x 写地址表，改连续维度映射03 访存合并


---

# 每轮只改一个变量 {#workflow}
<!-- tag: workflow -->
<!-- layout: grid2 -->

## box soft
### 把实验写成一条记录
性能排查最怕同时改 shape、block size、数据布局和测量口径。这样即使变快，也不知道是哪一步起作用。每一轮都应该写清：当前症状、证据、假设、最小改动、预期指标变化、回退条件。
例如“vector add 慢”太粗；“有效带宽只有目标 GPU 可持续带宽的 20%，且访存地址沿 threadIdx.x 跨步，先改线程映射让地址连续”更接近可执行假设。

## box warn
### 端到端复测别省
局部 kernel 变快只是中间结果。真正交付前，要回到实际输入分布、batch、并发和完整模型路径里复测。性能工程最终服务真实工作负载，单点最高分只提供线索。

字段写什么示例


症状慢在哪里，来自哪个口径Nsight Systems 显示 softmax kernel 占端到端 18%
证据计时、FLOP、字节、profiler 指标AI 低，实际带宽远低于可持续带宽
假设最可能的瓶颈原因线程映射导致跨步访问，transaction 被拉散
最小改动只改一个因素让 threadIdx.x 对齐连续维度，其他配置不动
预期变化哪些指标应该变有效带宽上升，global load 效率改善
复测局部和端到端都要看microbenchmark 变快后再看模型整体延迟

## box soft
### 完整案例：一个 fused softmax 为什么局部变快，端到端不动
假设你在 attention 里替换了一个 fused softmax kernel。microbenchmark 里，单个 kernel 从 `42 us` 降到 `28 us`，看起来很漂亮；但真实推理请求的 p95 延迟只从 `31.2 ms` 到 `31.0 ms`。这时先暂停下结论，把读数放到同一张账本里：端到端里这个 kernel 占比是多少，前后是否多了 layout transform，是否因为 batch 和 sequence length 分布不同，导致 microbenchmark 的 shape 偏离主流 shape。
一个常见错误是只保留最快的 microbenchmark 结果。比如你用固定 `B=1, H=32, S=2048` 测了 1000 次，看到中位数下降，于是提交优化。但线上实际请求可能有大量 `S=256` 到 `S=768` 的短序列，softmax kernel 本身只占 4% 到 7%，真正的空洞在 launch、mask 构造和 host/device 同步之间。此时单 kernel 快 30%，端到端也只会动一点点。正确动作是回到 Nsight Systems：看时间线中 softmax 前后的 kernel、memcpy、同步点和 CPU 提交间隔，再看这个 kernel 在真实请求路径里到底占多少。
复测时可以按三层记录：第一层是单 kernel，用同一组输入、同一 stream、同一计时口径比较前后；第二层是局部子图，把 softmax 前后的 mask、scale、dropout 或 layout transform 一起纳入；第三层是端到端，把真实请求分桶后分别看 p50、p95 和 GPU 时间线。三层结果如果方向一致，优化判断就比较稳；如果第一层变快、第二层变慢，说明融合边界或布局转换吃掉收益；如果第二层变快、第三层没动，说明真实热点在别处，下一轮要重新排优先级。
记录里还要写清回退条件。比如“长序列收益超过 5%，短序列无退化，端到端 p95 至少下降 1%”可以作为继续推进的条件；如果短序列因为额外 mask 处理变慢，就要保留旧路径或按 shape 分派。这个条件让后续复测有明确裁判，也能避免只挑好看的图。下一次实验从这条记录继续，避免重新猜；同时留下命令、截图、驱动和库版本，方便别人复现。
检查项可信读法容易误判的读法下一步


shape 分布按真实 batch、head、sequence length 分桶统计只选一个最大 shape 当代表至少测主流 shape、长尾 shape 和最坏 shape
端到端占比先算优化对象占总延迟比例只看 kernel 自己的加速比用 Amdahl 思路估算可见收益上限
时间线空洞看 kernel 前后是否有 host 等待和同步把所有空洞都归因于 GPU 算得慢先减少 launch、同步或无用拷贝
数据布局确认优化没有引入额外 transpose、contiguous 或 copy只看新 kernel 内部更快把 layout transform 单独计入账本
如果这张表填完后发现 softmax 在真实路径只占 5%，那么单 kernel 加速 33% 的端到端上限大约也只有 1.7%。这个结果仍有价值，它说明下一轮要找更大的热点，或者把多个小操作融合成一次时间线更紧凑的路径。如果表里显示 layout transform 吃掉了收益，就要把数据布局设计和调用边界一起改；如果表里显示短序列 launch 空洞多，就优先考虑 batching、fusion 或减少 host 同步。你在练习区做 Q14 时，应该能用这条链条回答“为什么 microbenchmark 好看但端到端不动”。


---

# 证据放一起 {#ledger}
<!-- tag: ledger -->
<!-- layout: grid2 -->

## box soft
### 为什么要放在一起
计时回答“多久”，Roofline 回答“上限在哪”，profiler 回答“为什么慢”。如果三种证据不放在一张表里，常见结果是：时间看的是一个 shape，字节算的是另一个 shape，结论却拿去解释同一个 kernel。
这页的做法很直接：先把证据写成行，再把每行对应到下一步。这样后面换成 Triton 或注意力内核时，你会知道自己究竟是在改访问形状、改复用，还是只是改了测量口径。

## box info
### 本页的锚点
同一个低 AI 向量加法，贯穿计时、Roofline、profiler、排查和复测。它会先给你一个带宽型答案；同一个 fused softmax，会先给你一个 launch 型答案；同一个高 AI matmul tile，会把你带向计算路径。不要把这三个 case 混成一个“慢”。

证据对象先读什么先写什么错了的症状下一查


kernel 时间warmup、同步、重复次数median/min/max只看 CPU launch，GPU 真正执行没算进去回到同步点和计时口径
工作量账本FLOP、读写字节、shapeAI、实际吞吐字节没算准，Roofline 位置会跑偏核对公式和单位
profiler 结论时间线、stall、吞吐、occupancy瓶颈假设只记 counter，不记因果把 counter 翻回代码和索引表达式
下一轮实验当前症状和最小改动只改一个因素多个参数一起动，结果没法归因保留对照版本，再测一次

<!-- interactive: ['shape-tracer'] → keep as HTML -->


---

# 三种常见慢法 {#lab}
<!-- tag: failure lab -->

下面三个模拟输出不绑定某个固定 GPU 的真实 benchmark，作用是训练读法：先看症状，再决定下一步该查哪里。点开不同 case，注意输出里的证据怎样改变你的假设。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

case证据不要急着做什么先做什么


带宽墙低 AI，带宽已经高盲目加更多线程减少字节、融合、提高复用
launch 太碎小 kernel 多，端到端没变快只继续优化单 kernel看时间线，减少 launch 和同步
远低于 Roofline高 AI，吞吐离屋顶很远直接归因于 HBM看计算路径、stall、occupancy、寄存器


---

# 下一步：把测量结果接到 Triton {#bridge}
<!-- tag: bridge -->
<!-- layout: grid2 -->

## box soft
### 先准备这四样东西
进入 Triton 前，至少准备四样东西：热点 kernel 的可靠时间、FLOP/byte 账本、profiler 读数，以及一个明确假设。这样你在调 block size、mask、program id、num_warps 或 tile 形状时，才知道自己是在追带宽、追计算路径，还是在减少 launch 和同步。

## box info
### 还不稳就先别写 Triton
如果你还不能区分低 AI、高 AI、远低于屋顶这三种情况，先别急着写 Triton。回到本页练习区，把向量加法、矩阵乘和端到端没有变快这三个 case 讲清楚，再往后走。这里先不追更底层的硬件计数器、block 细节和库内部实现；先把时间、字节、AI 和时间线对上，再进入下一轮实验。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box soft
### 答题时先看什么
这组题不考背诵。重点是把一次性能判断拆成四个动作：计时口径是否可信，FLOP 和字节数是否算清，Roofline 位置是否说得出，profiler 指标能不能落到下一步检查。

## box info
### 先看什么
每道题先想清楚你信哪一个读数，再说它把你带向哪类瓶颈。只写“优化访存”或“提高并行度”，还不够。

<!-- end-practice-intro -->

<!-- practice-group: 一、先把计时测准 -->
<!-- quick-review: 答题提示 异步执行是计时误差的主要来源，先处理同步和重复运行。 -->

### 1 choice

**Q:** Q1. 选择：测单个 CUDA kernel 时间时，哪种口径更可信？

- [ ] A. CPU time.time() 包住 launch
- [ ] B. CUDA event + synchronize
- [ ] C. 只看第一次运行的耗时

Answer: B. CUDA event + synchronize
Explain: CUDA event 记录在 GPU 时间线上，再配合同步等待完成，比直接用 CPU 墙钟更稳。

### 2 true/false

**Q:** Q2. 判断：GPU kernel launch 通常是同步执行，所以 CPU 计时不需要等待。

- [ ] 对
- [ ] 错

Answer: false
Explain: kernel launch 往往先入队，CPU 可能提前返回；不等待就可能只测到提交开销。

### 3 fill

**Q:** Q3. 填空：正式计时前先跑几轮，通常叫 `____`。


### 4 explain

**Q:** Q4. 设计一个最小可信计时流程。

Explain: 合格答案要包含固定 shape 和 dtype、预热、使用 CUDA event 或显式同步、重复运行多次、报告中位数或稳定区间。更好的答案会同时说明：如果是端到端流程，要把数据拷贝、同步和 launch 开销单独标出来。

<!-- end-practice-group -->

<!-- practice-group: 二、Roofline 算账 -->
<!-- quick-review: 答题提示 先算算术强度，再拿它乘持续带宽，最后和峰值算力取较小值。 -->

### 5 choice

**Q:** Q5. 选择：算术强度很低时，Roofline 通常先碰到哪面墙？

- [ ] A. 带宽墙
- [ ] B. 计算墙
- [ ] C. PCIe 墙一定先出现

Answer: A. 带宽墙
Explain: 低算术强度表示每搬一个字节做的计算少，更容易被内存带宽限制。

### 6 fill

**Q:** Q6. 填空：Roofline 的斜线段通常等于算术强度乘以 `____`。


### 7 calculation

**Q:** Q7. 小算账：FLOP=`2e9`，Bytes=`12e9`，Time=`2ms`。

Explain: 算术强度是 2e9 / 12e9，约 0.167 FLOP/Byte。实际吞吐是 2e9 / 0.002s，等于 1e12 FLOP/s，也就是 1000 GFLOP/s 或 1 TFLOP/s。如果持续带宽按 2 TB/s 估算，带宽屋顶约 0.167 * 2 TB/s = 0.333 TFLOP/s；实际点若高于这个粗算屋顶，就说明字节估计、带宽口径或单位需要复查。合格答案要同时写公式、单位和复查动作。

### 8 choice

**Q:** Q8. 选择：ridge point 主要用来判断什么？

- [ ] A. kernel 需要多少个 block
- [ ] B. L2 cache 的容量
- [ ] C. 从带宽受限转向计算受限的大致分界

Answer: C. 从带宽受限转向计算受限的大致分界
Explain: ridge point 是峰值算力除以持续带宽得到的强度阈值。

<!-- end-practice-group -->

<!-- practice-group: 三、读 profiler -->
<!-- quick-review: 答题提示 Systems 看时间线，Compute 看单个 kernel 里的计数器。 -->

### 9 choice

**Q:** Q9. 选择：想先看端到端时间线和 launch 是否碎，优先用哪个工具？

- [ ] A. Nsight Systems
- [ ] B. Nsight Compute
- [ ] C. 只看 nvidia-smi

Answer: A. Nsight Systems
Explain: Nsight Systems 更适合先看 CPU/GPU 时间线、launch 间隔和拷贝重叠。

### 10 choice

**Q:** Q10. 选择：单个 kernel 想看访存效率、占用率和热点 source line，优先用哪个工具？

- [ ] A. Nsight Systems
- [ ] B. Nsight Compute
- [ ] C. git diff

Answer: B. Nsight Compute
Explain: Nsight Compute 面向单个 kernel 的详细指标和 source 关联。

### 11 true/false

**Q:** Q11. 判断：一次 profile 看到低 occupancy，就一定应该先把线程数调到最大。

- [ ] 对
- [ ] 错

Answer: false
Explain: 低 occupancy 只是信号；还要看寄存器、shared memory、访存等待和实际吞吐。

### 12 checkbox

**Q:** Q12. 多选：哪些现象支持“launch 太碎或同步太多”的假设？

- [ ] GPU 时间线上出现大量很短 kernel
- [ ] kernel 之间有明显空洞或 host 等待
- [ ] 单个长 kernel 已经贴近计算峰值

Explain: 前两项支持这个假设：大量短 kernel 会放大 launch 开销，kernel 之间的空洞或 host 等待说明时间线没有被连续利用。第三项更像单个 kernel 已经算得很满，不直接支持 launch 碎片。

<!-- end-practice-group -->

<!-- practice-group: 四、闭环判断 -->
<!-- quick-review: 答题提示 每个结论后面都要跟一个能复测的动作。 -->

### 13 choice

**Q:** Q13. 选择：算术强度高，但实际点远低于计算屋顶，下一步更应该先查什么？

- [ ] A. 只把 batch size 改大
- [ ] B. 指令效率、数据复用、occupancy 和依赖等待
- [ ] C. 立刻换显卡

Answer: B. 指令效率、数据复用、occupancy 和依赖等待
Explain: 高算术强度远低于屋顶，通常要回到 kernel 内部看执行效率，继续只算字节很难定位原因。

### 14 synthesis

**Q:** Q14. 诊断：microbenchmark 变快了，端到端没变快。你会怎么排查？

Explain: 先确认这个 kernel 在端到端时间里占比是否足够大；再看优化是否引入额外数据拷贝、同步或布局转换；接着用 Nsight Systems 看时间线有没有新的空洞；最后复测真实输入，单个 microbenchmark 只能作为局部证据。答案要体现“回到端到端目标”的闭环。

<!-- end-practice-group -->

<!-- practice-group: 五、先后顺序 -->
<!-- quick-review: 答题提示 先 Systems，后 Compute；先口径，后结论。 -->

### 15 choice

**Q:** Q15. 选择：要先看端到端时间线和 launch 空洞，优先跑哪个工具？

- [ ] A. Nsight Systems
- [ ] B. Nsight Compute
- [ ] C. 先看显卡型号

Answer: A. Nsight Systems
Explain: 先用 Systems 找热点和空洞，再决定要不要深入到单个 kernel。

### 16 choice

**Q:** Q16. 选择：单个 kernel 想看实际带宽、stall 和 source 关联，优先跑哪个工具？

- [ ] A. Nsight Systems
- [ ] B. Nsight Compute
- [ ] C. 只看日志

Answer: B. Nsight Compute
Explain: Compute 面向单个 kernel 的细节，适合解释为什么离屋顶远。

### 17 fill

**Q:** Q17. 填空：一次完整复测至少要留下命令、`____`、驱动或库版本和下一步。


<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- **NVIDIA CUDA C++ Programming Guide**
[https://docs.nvidia.com/cuda/cuda-c-programming-guide/](https://docs.nvidia.com/cuda/cuda-c-programming-guide/)
用于核对 CUDA 执行、同步、event、memory hierarchy 和 kernel launch 相关术语。访问日期：2026-06-21。
- **NVIDIA CUDA C++ Best Practices Guide**
[https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/](https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/)
用于核对计时、带宽、有效吞吐和性能测量的公开表述。访问日期：2026-06-21。
- **NVIDIA Nsight Systems Documentation**
[https://docs.nvidia.com/nsight-systems/](https://docs.nvidia.com/nsight-systems/)
用于核对端到端时间线、CPU/GPU 活动、kernel launch 和数据拷贝分析的工具边界。访问日期：2026-06-21。
- **NVIDIA Nsight Compute Profiling Guide**
[https://docs.nvidia.com/nsight-compute/ProfilingGuide/index.html](https://docs.nvidia.com/nsight-compute/ProfilingGuide/index.html)
用于核对单个 kernel 指标、section、source 关联和 profiler 读法。访问日期：2026-06-21。
- **Roofline: An Insightful Visual Performance Model for Multicore Architectures**
[https://crd.lbl.gov/assets/pubs_presos/roofline-sc09.pdf](https://crd.lbl.gov/assets/pubs_presos/roofline-sc09.pdf)
用于核对 Roofline 模型中的算术强度、带宽斜线、计算屋顶和 ridge point 概念。访问日期：2026-06-21。
- **GPU 性能工程｜内存层次、访存合并与带宽**
[../03_内存层次访存合并与带宽/学习页.html](../03_内存层次访存合并与带宽/学习页.html)
用于承接上一单元的字节账本、访存形状和有效带宽判断。
- **GPU 性能工程｜Triton 编程模型与块级思维**
[../05_Triton编程模型与块级思维/学习页.html](../05_Triton编程模型与块级思维/学习页.html)
用于说明本页诊断结论如何进入下一单元的块大小、tile 和程序实例设计。

---
