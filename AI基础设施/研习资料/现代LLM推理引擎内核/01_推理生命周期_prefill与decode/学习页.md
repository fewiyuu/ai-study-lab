---
course: "现代 LLM 推理引擎内核"
unit: "01｜推理生命周期 prefill 与 decode"
title: "推理生命周期 prefill 与 decode"
kicker: "第 01 单元"
summary: "这一页把一次生成请求拆成 prefill、decode、采样和停止四个动作，先看清它们各自读什么、写什么，以及 TTFT 和 ITL 是怎么在这里分开的。下一页会接着把 decode 里不断增长的 KV cache 算成显存账本。"

side_note: "先把一条请求的时间线看清，再去看 KV cache 与显存账本。"

can_do:
  - "你能把一条生成请求分成 tokenization、prefill、decode、采样和停止五步，并说出每一步读什么、写什么。"
  - "你能分开解释 TTFT、ITL 和 throughput，知道首 token、相邻 token 和整体产出分别看什么。"
  - "你能读懂 chunked prefill、`max_num_batched_tokens`、`max_num_seqs` 和 `scheduler_reserve_full_isl` 在调度里各自管什么。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "下一单元"
    href: "../02_KV_cache与显存账本/学习页.html"

---

# 学习目标 {#goals}
<!-- tag: goal -->

画出时间线
把一次生成请求拆成 tokenization、prefill、decode、采样和停止，说明每一步读什么、写什么。


分开看指标
能区分 TTFT、ITL 和 throughput，知道它们分别描述首 token、相邻 token 和整体产出速度。


读懂调度语义
看到 chunked prefill、`max_num_batched_tokens`、`max_num_seqs` 和 `scheduler_reserve_full_isl` 时，知道它们在限制什么。

> [!NOTE] 学完这一页，先做哪一步：
> 拿到一个慢请求，先判断慢在首 token 前，还是慢在持续输出中，再决定下一步看排队、prefill 还是 decode。


---

# 先把整条主线看见 {#overview}
<!-- tag: overview -->

LLM 推理先把 prompt 变成可复用的注意力状态，再一 token 一 token 地生成后续内容。前半段叫 prefill，后半段叫 decode。两段都在跑模型，但读写的对象和瓶颈不同。

## box soft
### prefill 做什么
prefill 读取完整 prompt，计算各层的 key / value，并把它们写进 KV cache。首 token 之前的等待，大头通常在这里。

## box soft
### decode 做什么
decode 读取已有 KV cache 和当前 token，每轮只推进一个新 token，并把新 token 的 key / value 追加进去。流式输出的节奏主要由这里决定。

这也是为什么同一个请求会同时出现两个体验：首 token 很慢，但后面一旦开始流式输出，看起来就快了；也可能首 token 不慢，但中间一直卡顿，因为 decode 的小步循环被队列和资源压力打断了。


---

# 阶段账本 {#ledger}
<!-- tag: ledger -->

把请求当成一笔在不断变化的账，就能看清每一步在读什么、写什么、什么时候会出错。下面这张表把 prefill、decode、采样和停止放到同一张账本里。

阶段
进入前是什么
进入后是什么
主要读取
主要写入 / 变更
错了会怎样


tokenization
原始文本
token 序列
分词器、模板、特殊 token
输入 token 序列
前缀看起来一样，实际 token 变了，后面复用会失效


prefill
完整 prompt token
已写入 KV cache 的 prompt 状态
模型权重、完整 prompt、当前 batch
每层的 K / V、首 token logits
首 token 变慢，或者 KV cache 写入不完整


decode
已有 KV cache 和上一轮 token
新增一个 token 后的状态
历史 K / V、当前采样结果
新 token 的 K / V、流式输出
输出节奏断续，ITL 上升


sampling
模型给出的 logits
一个被选中的 token
temperature、top-p、top-k、stop 条件
下一轮 decode 的输入 token
模型“算完了”但 token 还没选好，状态就会看错


stop / release
还占着 batch 和 KV cache
请求结束、资源释放
停止符、长度上限、业务停止条件
释放占位、回收缓存、结束流式输出
已结束请求还占着资源，后面会误判可用容量

> [!NOTE] 先检查哪一步：
> 如果一个请求慢，先问它卡在 tokenization、prefill、decode 还是 sampling，不要直接把问题归到模型本身。


---

# prefill 和 decode 的读写顺序 {#mechanism}
<!-- tag: trace -->

这一段只围绕一个锚定例子：同一条请求，prompt 里有 3000 个 token，最终要生成 400 个 token。这个例子足够长，能把 prefill、decode 和流式输出之间的关系拉开，又不会长到看不清主线。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

步骤
状态从哪里来
状态写到哪里
如果这步放错了，会误判什么


prefill
完整 prompt 和已分配的 batch
prompt 对应的 KV cache
会误以为首 token 慢只是算力问题，忘了它先在写状态


decode
历史 KV cache 和上一轮 token
新增 token 的 KV cache
会误以为 decode 只是读，不会继续吃显存


sampling
当前 logits 和约束条件
下一轮 token
会把“模型已经算完”误当成“token 已经确定”


stop
停止符、长度上限、业务规则
释放资源、结束流式输出
会把已完成请求继续算进并发和显存预算

最小生命周期伪代码复制

```python
def infer(request):
 tokens = tokenize(request.text)
 kv = prefill(tokens)
 outputs = []
 while not stop_condition(outputs, request):
 logits = decode_step(kv, outputs[-1] if outputs else None)
 next_token = sample(logits, request.temperature, request.top_p)
 outputs.append(next_token)
 kv = append_kv(kv, next_token)
 release(kv)
 return detokenize(outputs)
```

这段伪代码最重要的地方顺序：先 prefill，再 decode，再 sample，再 append KV，最后释放。如果把 sample 放到 decode 前面，或者把 release 提前，状态就会错位。


---

# 指标读法 {#metrics}
<!-- tag: metrics -->

同一个请求可以“感觉很快”，但具体慢在哪一步要拆开看。TTFT、ITL 和 throughput 关注的对象不同，混在一起看会把排查方向带偏。

$$\text{TTFT} = \text{queue} + \text{prefill} + \text{first sample}$$
 $$\text{ITL} \approx \text{decode loop} + \text{queue interference}$$
 $$\text{throughput} = \frac{\text{generated tokens}}{\text{wall time}}$$
 这里的 queue 指排队时间，prefill 指完整 prompt 的处理，first sample 指首 token 采样，decode loop 指后续每轮生成。式子只帮助你把指标和阶段对齐，真正的 profiling 还是要看日志和曲线。

指标
它在看什么
最常见的高值原因
先查什么


TTFT
从请求进入到首 token 出来
排队长、prompt 长、prefill 被切太碎
看 queue、prompt token 数、chunked prefill 和 token budget


ITL
相邻 token 之间的时间
decode 被挤压、batch 抖动、KV 读取压力大
看 decode 节奏、队列干扰和流式输出是否断续


throughput
单位时间产出的 token 总数
batch 太小、资源空转、抢占太多
看 batch 利用率和重复 preemption

> [!NOTE] 一条实用判断：
> TTFT 高，不一定是模型慢；它可能是 queue 或 prefill。ITL 高，也不一定是采样慢；它可能是 decode 被别的请求打断了。


---

# 阅读路线 {#bridge}
<!-- tag: bridge -->

这页的作用，是给后面三类内容搭桥：先到显存账本，再到块管理，最后到调度循环。只要你能把请求生命周期说清楚，后面每个优化是在改哪一段，就不会看乱。

先去看什么
下一页会把 decode 不断增长的 KV cache 算成显存账本，看看为什么请求越长、并发越高，显存压力就越大。


中间会遇到什么
再往后会看到 PagedAttention 和块管理。那一页会回答：这些 KV cache 到底怎么摆、怎么共享、怎么避免尾部浪费。


最后接到哪里
更后面的 continuous batching 会把这些块放进调度循环，解释为什么多请求服务每轮都要重新排队。

问题
这页回答什么
下一页回答什么
再下一页回答什么


请求为什么分成两段
prefill 和 decode 的生命周期差异
KV cache 为什么会随 token 增长
块怎么放、怎么共享、怎么省掉浪费


为什么 TTFT 和 ITL 要分开看
首 token 与后续 token 的瓶颈不同
显存账本和容量限制如何影响两者
调度循环怎样在多请求之间分配预算

> **读公开资料的顺序** 先确认你看到的是阶段、预算还是状态迁移，再回文档找对应术语。先定语义，再看参数名，能少走很多回头路。

看到什么
先回哪类资料
先核对什么


TTFT 高
vLLM Optimization and Tuning
chunked prefill、decode 优先、`max_num_batched_tokens`


ITL 高或输出断续
SGLang Benchmark and Profiling
TTFT、ITL、TPOT、throughput 的口径


`preempted_for_recompute`
vLLM Scheduler API
request 状态、回队、抢占和准入边界

> [!NOTE] 这里先别追的东西：
> 先把 tokenizer、KV cache、PagedAttention、continuous batching 和 kernel 优化的边界分开。这一页先处理请求生命周期和指标读法，显存账本、块管理和 kernel 优化留到后面。


---

# 公开资料怎么读 {#source-route}
<!-- tag: route -->

这页很多判断都要回到公开资料核对。看到 TTFT、ITL、chunked prefill、preemption 或 `scheduler_reserve_full_isl` 时，先看 vLLM 文档确认调度语义；看到 benchmark 数字时，回 SGLang 文档看它记录的指标口径。先对齐术语，再看结论。

看到什么
先回哪类资料
先核对什么


TTFT 高
vLLM Optimization and Tuning
chunked prefill、decode 优先、`max_num_batched_tokens`


ITL 高或输出断续
SGLang Benchmark and Profiling
TTFT、ITL、TPOT、throughput 的口径


`preempted_for_recompute`
vLLM Scheduler API
request 状态、回队、抢占和准入边界

> [!NOTE] 下一步怎么做：
> 先选一条症状，再只读对应的那一份资料。这样能更快把问题边界收紧，也方便回查。


---

# 这里先不追的几件事 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
### 先放一边
KV cache 的字节账本、PagedAttention 的块表、continuous batching 的队列和 GPU kernel 的访存细节。它们重要，但这页先不展开。

## box soft
### 这页要完成的动作
你要能看懂一条请求从 prompt 进入到 token 流式输出的完整顺序，并且能把 TTFT、ITL 和 throughput 分开说清楚。

> [!NOTE] 下一步怎么接：
> 如果问题出在首 token 前，先回到 prefill 和 queue；如果问题出在持续输出中，先回到 decode 和 ITL，再去看下一页的显存账本。

这页先不追 speculative decoding、beam search、speculative accept/reject、模型并行和具体 kernel 优化。它们都会在后面的单元或更深的工程资料里回来，但在当前阶段，先把“请求怎么流动、指标怎么看、状态怎么变”弄稳更重要。


---

# 三个读法 {#case}
<!-- tag: cases -->

同一条请求慢，不代表慢的地方相同。下面三个场景把最容易混掉的判断拆开：首 token 慢、输出中间慢、总吞吐低。每个场景都只盯一个问题，先别急着把所有参数都拉进来。

场景一：TTFT 高，ITL 正常
更像是 queue 太长、prompt 太长，或者 prefill 被切得太细。你先看首 token 前的等待，不要先盯 decode。


先查什么为什么


waiting queue请求可能还没真正进 running。
prompt token 数prefill 要先把完整上下文写一遍。
chunked prefill切块过细会拉长首 token 前的等待。


场景二：首 token 快，后面断续
更像是 decode 被别的请求打断，或者 KV cache 读取压力上来了。这里要看持续输出阶段。


先查什么为什么


decode 节奏一轮一 token 的循环被打断，会让 ITL 抖。
队列干扰别的长请求可能在抢预算。
KV cache 压力历史状态越长，读取越重。


场景三：throughput 低
更像是 batch 太小、资源空转，或者抢占太多。这里看整台服务的总产出。


先查什么为什么


batch 利用率资源没被吃满时，吞吐会掉。
重复 preemption反复抢占会把工作打碎。
长 prompt 占比长 prefill 过多会压住 decode。

> [!NOTE] 这三个读法的共同点：
> 先从症状回到阶段，再回到状态账本。这样后面的参数选择才不会乱。


---

# 终端判断 {#diagnosis}
<!-- tag: terminal lab -->

下面这三个日志只是在读法上给你一个落点。你不用背输出内容，只要会根据输出判断应该往哪一层查，就算真正接上了调度语义。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

看见 TTFT 高
先回到 queue 和 prefill，看是不是 prompt 太长、chunk 太碎，或者完整输入根本还没被放行。


看见反复抢占
先回到 KV headroom 和 reserve 策略，看是不是准入过激，导致请求总是在回队或重算。


看见等待变长
先回到调度窗口，看是不是长请求太多、短请求被挤到队尾，或者 token budget 留得太少。

> [!NOTE] 排障起手式：
> 先看是 TTFT、ITL、waiting queue 还是 repeated preemption 在变坏，再回到参数表。先不要把问题归给 kernel。


---

# 请求推演 {#plan}
<!-- tag: planning -->

把生命周期放到实际请求里看，最容易看清 queue、prefill 和 decode 是怎么相互挤压的。下面把一个长 prompt 和一个短 prompt 放到同一个调度窗口里，看看 TTFT、ITL 和窗口余量会怎么变。

轮次
长请求状态
短请求状态
窗口变化
会影响什么


t0
长 prompt 进入 waiting，等待 prefill 预算
短请求还没到
token budget 先被预留给 decode
decode 节奏被保住，TTFT 还没开始计时


t1
长 prompt 进入 prefill，但只走一个 chunk
短请求到达 waiting
剩余预算被切出来给短请求的首 token
短请求的 TTFT 可能比长请求更好


t2
长请求继续分段 prefill
短请求已经进入 decode
running 序列变多，KV headroom 变紧
ITL 主要看 decode 是否被挤压


t3
长请求如果继续占位，就可能触发 preemption 或回队
短请求继续流式输出
窗口开始偏向短请求的持续输出
吞吐是否稳定，取决于抢占和回队的频率

## box soft
### 长请求带来的直接后果
长 prompt 会先把 prefill 压长，再把 KV cache 拉大。如果窗口不留足余量，它会一边拖 TTFT，一边把后面的请求挤出 batch。

## box info
### 短请求为什么有时更快
短请求需要的 prefill 预算更少，首 token 更容易抢到位置。但如果前面堆了太多长 partial prefills，短请求也会被卡在 waiting 里。

把一轮预算拆给长短请求复制

```python
def split_budget(token_budget, running_decode, reserve, long_prefill_remaining, short_request_tokens):
 left = max(0, token_budget - running_decode - reserve)
 decode_share = min(running_decode, token_budget)
 long_share = min(long_prefill_remaining, max(0, left - short_request_tokens))
 short_share = min(short_request_tokens, max(0, left - long_share))
 return {
 "decode_share": decode_share,
 "long_prefill_share": long_share,
 "short_request_share": short_share,
 }
```

> [!NOTE] 这段代码想让你看见什么：
> 一轮预算按阶段和优先级分配，先保 decode，再把余下部分切给 prefill。长请求和短请求的差别在于它们吃预算的方式不同。


---

# 参数取舍 {#parameter}
<!-- tag: tuning -->

参数的方向取决于你要保护哪一种等待。`max_num_batched_tokens` 决定这一轮能塞多少 token，`max_num_seqs` 决定这一轮能挂住多少条序列，`scheduler_reserve_full_isl` 决定你会不会先把完整输入长度算进去。

参数
它在控制什么
调大时常见结果
调小时常见结果


`max_num_batched_tokens`
单轮可用的 token 预算
吞吐可能更高，但长 prefill 更容易挤压 decode
ITL 往往更稳，但 TTFT 可能更长


`max_num_seqs`
单轮可处理的序列数量
并发更高，但 KV headroom 更紧
队列更容易增长，但单轮压力更小


`scheduler_reserve_full_isl`
是否先按完整输入长度做准入
更保守，过度准入更少
更激进，但后面更容易出现抢占或重算

## box soft
### 常见误读
很多人把 `max_num_seqs` 当成 token budget，或者把 `max_num_batched_tokens` 当成并发数。前者会把容量看偏，后者会把调参方向看错。

## box info
### 和 TTFT、ITL 的关系
如果你想稳 ITL，就不要让长 prefill 一口气占满预算；如果你想压 TTFT，就要给首 token 留出足够的可见窗口。两者很少能同时拉满。

> [!NOTE] 实操顺序：
> 先看症状落在 TTFT 还是 ITL，再回到参数。顺序反过来，通常只会把问题调成另一个样子。

举个具体例子：如果一条请求有 3000 个 prompt token、400 个输出 token，`max_num_batched_tokens` 设得太小，TTFT 往往会更稳，但长请求要跑更多轮；设得太大，长 prefill 会更容易压住 decode，短请求看起来也会一起变慢。练习时就拿这组数字回看上面的状态账本和终端日志。下面这些公开文档按 2026-06-21 可见内容整理，后续版本如果改名或迁移，先回链接核对参数语义。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

症状
先怀疑什么
下一步先看哪里


TTFT 高，但后续输出节奏还算稳
排队、prompt 太长、prefill 被切太碎
看 waiting queue、prompt token 数和 chunked prefill 预算


首 token 不慢，但中间一直卡
decode 被挤压、队列抖动、KV 读取压力大
看 decode 节奏、抢占次数和 KV headroom


整台服务吞吐低
batch 太小、资源空转、抢占太多
看 batch 利用率、preemption 和长请求占比

<!-- practice-intro -->
## box soft
### 怎么答才算会
先说清楚请求在哪个阶段，再说它读什么、写什么，最后再给指标判断。只写“快/慢”不算会。

## box info
### 检查重点
你要能分清 prefill、decode、sampling 和 stop，还要能把 TTFT、ITL 和 throughput 对上。

<!-- end-practice-intro -->

<!-- practice-group: 第一组：概念和顺序 -->
<!-- quick-review: 快速复盘 prefill 先写完整 prompt；decode 每轮追加一个 token；sampling 选出下一 token；stop 负责释放资源。 -->

### 1 choice

**Q:** prefill 阶段最准确的描述是哪一个？

- [ ] . 一次性生成完整答案
- [ ] . 读取完整 prompt 并写入对应的 KV cache
- [ ] . 只负责选择下一个 token

Answer: . 读取完整 prompt 并写入对应的 KV cache

### 2 true/false

**Q:** decode 阶段只读不写，不会继续增加 KV cache。

- [ ] 对
- [ ] 错

Answer: false

### 3 ordering

**Q:** 把这四步按顺序写出来：sampling、prefill、decode、stop。

Explain: 顺序是 prefill → decode → sampling → stop。更完整的说法是：prefill 先写 prompt 的 KV，decode 继续追加新 token 的 KV，sampling 负责选出下一 token，stop 负责结束并释放资源。

### 4 true/false

**Q:** 模型输出 logits 之后，下一个 token 就已经自动确定了。

- [ ] 对
- [ ] 错

Answer: false

<!-- end-practice-group -->

<!-- practice-group: 第二组：指标读法 -->
<!-- quick-review: 快速复盘 TTFT = queue + prefill + 首次采样；ITL 主要看 decode 节奏；throughput 看总产出。 -->

### 5 choice

**Q:** TTFT 主要在看什么？

- [ ] . 从请求进入到首 token 出来的时间
- [ ] . 相邻 token 之间的时间
- [ ] . 单位时间总共生成了多少 token

Answer: . 从请求进入到首 token 出来的时间

### 6 choice

**Q:** ITL 高的时候，最先要怀疑哪类问题？

- [ ] . prompt 很短也可能出现 ITL 高
- [ ] . decode 被别的请求打断、队列抖动或 KV 读取压力大
- [ ] . 只要首 token 很快就不用看 ITL

Answer: . decode 被别的请求打断、队列抖动或 KV 读取压力大

### 7 scenario-transfer

**Q:** 一个请求 TTFT 很高，但 ITL 很正常。写出两个更可能的原因。

Explain: 两个常见原因是：1）请求在队列里等得久；2）prompt 很长，prefill 花了更多时间。答题时如果还能补上“chunked prefill 太细”或“token budget 不够”，说明你已经能把问题放回调度里看了。

### 8 choice

**Q:** throughput 变高，最直接说明了什么？

- [ ] . 首 token 一定更快了
- [ ] . 每个 token 的等待一定更短了
- [ ] . 单位时间内总共产出的 token 更多了

Answer: . 单位时间内总共产出的 token 更多了

<!-- end-practice-group -->

<!-- practice-group: 第三组：调度语义和操作桥 -->
<!-- quick-review: 快速复盘 chunked prefill 让 decode 先走；`max_num_batched_tokens` 管每轮预算；`max_num_seqs` 管序列数，token budget 看另一项。 -->

### 9 choice

**Q:** chunked prefill 最主要在改什么？

- [ ] . 把 logits 直接变成答案
- [ ] . 让长 prompt 切块进入队列，并尽量保住 decode 节奏
- [ ] . 让模型参数自动变小

Answer: . 让长 prompt 切块进入队列，并尽量保住 decode 节奏

### 10 choice

**Q:** 下面哪个参数最直接限制单轮可以处理多少条序列？

- [ ] . `max_num_seqs`
- [ ] . `max_num_batched_tokens`
- [ ] . `scheduler_reserve_full_isl`

Answer: . `max_num_seqs`

### 11 choice

**Q:** `scheduler_reserve_full_isl=True` 主要在防什么？

- [ ] . 只看第一段 token 就放行，后面才发现完整输入装不下
- [ ] . 所有请求都会自动变短
- [ ] . throughput 会自动翻倍

Answer: . 只看第一段 token 就放行，后面才发现完整输入装不下

### 12 code-reading

**Q:** 如果你想确认 `PREEMPTED_FOR_RECOMPUTE` 的含义，最先该回到哪类公开资料？

Explain: 最先应回到 vLLM 的 Scheduler API 或相关调度文档，因为它直接定义了 preempted request、`PREEMPTED_FOR_RECOMPUTE` 和请求回队的语义。答题时只要能把“调度语义”说清楚，就说明你知道问题该往哪层查。

<!-- end-practice-group -->

<!-- practice-group: 第四组：故障和路线 -->
<!-- quick-review: 快速复盘 TTFT 看 queue 和 prefill；ITL 看 decode；吞吐看 batch 和抢占。 -->

### 13 choice

**Q:** TTFT 高但 ITL 正常时，最先该看什么？

- [ ] . waiting queue 和 prefill
- [ ] . 已经结束的请求数
- [ ] . 模型词表大小

Answer: . waiting queue 和 prefill

### 14 choice

**Q:** 如果日志里反复出现 `preempted_for_recompute`，最先该怀疑什么？

- [ ] . 温度参数太高
- [ ] . 输出 token 太短
- [ ] . KV headroom 不够，或者准入太激进

Answer: . KV headroom 不够，或者准入太激进

### 15 scenario-transfer

**Q:** 如果短请求一直排在长请求后面，写出两个更可能的原因。

Explain: 两个常见原因是：1）token budget 太窄，长请求总是先占满窗口；2）长 partial prefill 太多，短请求一直被挤到后面。答题时如果还能提到 `max_num_partial_prefills` 或 `max_long_partial_prefills`，说明你已经把问题放回调度策略里看了。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- [vLLM - Optimization and Tuning](https://docs.vllm.ai/en/stable/configuration/optimization/)：用来对齐 chunked prefill、decode 优先、`max_num_batched_tokens` 以及 TTFT / ITL / 吞吐的取舍。
- [vLLM - Engine Arguments](https://docs.vllm.ai/en/stable/configuration/engine_args/)：用来对齐 `--max-num-batched-tokens`、`--max-num-seqs`、`--max-num-partial-prefills`、`--max-long-partial-prefills`、`--long-prefill-token-threshold` 和 `--scheduler-reserve-full-isl` 的语义。
- [vLLM - Scheduler API](https://docs.vllm.ai/en/stable/api/vllm/v1/core/sched/scheduler/)：用来对齐 preempted request、`PREEMPTED_FOR_RECOMPUTE` 和请求回队的调度语义。
- [SGLang - Benchmark and Profiling](https://docs.sglang.ai/developer_guide/benchmark_and_profiling.html)：用来对齐 TTFT、ITL、TPOT、throughput，以及在线 serving 测试该怎么看。
- [SGLang - Welcome](https://docs.sglang.ai/)：用来对齐 serving framework 的定位，以及低延迟、高吞吐推理服务的上下文。

---
