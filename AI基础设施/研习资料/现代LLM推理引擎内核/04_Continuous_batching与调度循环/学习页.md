---
course: "现代 LLM 推理引擎内核"
unit: "04｜Continuous batching 与调度循环"
title: "Continuous batching 与调度循环"
kicker: "第 04 单元"
summary: "这一页只看 continuous batching 与调度循环：请求怎样在 waiting、running、preempted 和 finished 之间迁移，token budget 怎样决定本轮能推进多少 prefill / decode，chunked prefill 为什么可以和 decode 同轮共存。它承接 03 单元的块管理，也会把你送到 05 单元的算子层；更下游的路由、网关、多机扩展留到后面的单元。"

side_note: "本页把学习目标压到能动手检查的最小闭环。先顺着主线读，再用练习区自查。"

can_do:
  - "你能看一条调度轨迹，分出 waiting、running、preempted 和 finished 各自什么时候变化。"
  - "你能读懂 `max_num_batched_tokens`、`max_num_seqs`、`max_num_partial_prefills`、`scheduler_reserve_full_isl` 这些参数会把性能推向哪一边。"
  - "你能从 TTFT、ITL、吞吐、等待队列长度和 preemption 次数里，判断问题是在预算、队列还是抢占。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一单元"
    href: "../03_PagedAttention与块管理/学习页.html"
  - text: "下一单元"
    href: "../05_推理算子FlashAttention与量化/学习页.html"

---

# 先把问题定住 {#goals}
<!-- tag: goals -->
<!-- layout: grid2 -->

## box soft
### 这页先抓什么
先看请求怎样进入 waiting queue，怎样被 scheduler 选进 running queue，怎样在 token budget 不够时继续排队，怎样在显存压力下被抢占后回到等待队列。只要这条状态线没断，后面的参数和 benchmark 才有地方放。

## box info
### 先别展开什么
先不要把注意力放到路由、网关、分布式通信、speculative decoding、kernel fusion 或 MoE expert parallelism 上。那些内容会在后面的系统单元或算子单元里再回来。

> [!NOTE] 读完这一节，你要能做什么：
> 拿到一条请求轨迹，先说出它现在在哪个队列，再判断这轮能不能进 batch，最后说明它如果被抢占，是 swap 回去还是 recompute。


---

# continuous batching 在改什么 {#overview}
<!-- tag: overview -->

静态 batch 的思路是先凑齐，再一起跑完。continuous batching 的思路是每一轮迭代都重排一次队伍：先让正在 decode 的请求继续前进，再把剩余 token budget 分给 chunked prefill，必要时再把长请求切块、把低优先级请求抢占出去。这样做的目的，是让 decode 不被长 prefill 长时间挡住，同时让新请求更快进入服务。

| 层次 | 静态 batch | continuous batching | 你要盯住的变化 |
| --- | --- | --- | --- |
| 请求进入 | 等一批请求凑齐后再开始 | 新请求可以在每轮迭代之间进入 | 观察 waiting queue 的长度和进入时机 |
| prefill | 常常整段一次性推进 | 长 prompt 可以被切成多个 chunk | 观察 chunked prefill 是否抢占了过多预算 |
| decode | 容易和 prefill 一起被大 batch 牵着走 | 通常优先保住 decode 的节奏 | 观察 ITL 是否因为长 prefill 被拖慢 |
| 状态更新 | 一轮里状态变化少 | 每轮都可能有完成、继续、回队、抢占 | 观察 running / waiting / preempted 的迁移 |

> [!NOTE] 这页的锚点例子：
> 一组请求 A、B、C、D 会在不同轮次里交替出现。你后面看到的表格和代码，都会围着这组请求转。


---

# 状态账本 {#ledger}
<!-- tag: ledger -->

调度器最重要的是知道每个请求现在处于什么状态，接下来要读什么、写什么、丢什么。下面这张表把状态迁移摆平，后面的预算和抢占都可以回到这里检查。

对象
进入前是什么
进入后是什么
读什么
写什么 / 变更什么
错了会怎样


waiting request
还没拿到本轮预算
继续等待，或被挑进 batch
队列顺序、优先级、前缀命中情况
排队位置、是否 chunked prefill
会把“没进 batch”误当成“服务挂了”


running request
已经在上一轮进入 batch
继续 decode，或推进一个 prefill chunk，或结束
当前剩余 token、KV cache 可用空间
新增 token 的状态、完成标记、流式返回
会把 decode 继续推进误看成“没有新状态”


partial prefill request
长 prompt 还没走完
保留已完成的 chunk，等待下一轮继续
剩余 prompt tokens、chunk 上限、token budget
已完成 chunk 的进度
会把长 prompt 当成一次性输入，预算算大


preempted request
本轮资源不够，先被让出位置
回 waiting，或走 swap / recompute
抢占模式、KV cache 压力、优先级
是否保留块、是否重算、是否重新排队
会把抢占当成错误，忽略它其实是在保住整体吞吐


finished request
还占着运行位和 KV 块
释放资源，退出调度循环
停止条件、采样结果、流式输出状态
释放 KV block、清掉 running 占位
会把已完成请求当成仍在占预算

> [!NOTE] 先检查哪里：
> 如果你在日志里只看到“慢”，先不要碰 kernel。先回到这张表，看请求到底是 waiting、running、partial prefill 还是 preempted。


---

# 调度循环怎么跑 {#mechanism}
<!-- tag: mechanism -->

这一段把调度器拆成五步：新请求进入、预算检查、组 batch、执行 forward、回写状态。读的时候不要只盯最后一次 forward，要看每一步到底读了什么、写了什么、丢了什么。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

最小调度循环伪代码复制

```python
while engine_running:
 retire_finished_requests()
 free_kv_for_finished_requests()

 decode_batch = pick_decode_requests(running_queue, token_budget)
 prefill_batch = pick_prefill_requests(
 waiting_queue,
 token_budget,
 chunked_prefill=True,
 )

 batch = decode_batch + prefill_batch
 outputs = model_forward(batch)
 update_request_states(outputs)

 if kv_pressure_high():
 preempt_low_priority_requests(mode=preemption_mode)
```

> [!NOTE] 这段代码要你盯住的 4 个动作：
> 先退场已完成请求，再挑 decode，再把剩余预算留给 prefill chunk，最后回写状态。只要这四步中的任一步顺序错了，队列表现就会变形。


---

# 调度窗口怎么看 {#window}
<!-- tag: window -->

调度窗口可以理解为“这一轮允许被看见和被推进的请求集合”。它受三类限制共同影响：token 预算、序列上限和 KV cache headroom。窗口太窄，短请求也会排队；窗口太宽，长 prefill 和过多 running 请求会把 KV 压紧，后面只能靠抢占收场。

做线上判断时，不要只看平均输入长度。最好同时看 p95 prompt 长度、当前 decode 并发、KV 使用率峰值和 repeated preemption 数量；这四个量合在一起，才能说明到底是哪一层限制先撞墙。

限制
它在保护什么
窗口变窄时
窗口变宽时


token budget
单轮 forward 的计算量和 prefill / decode 比例
prefill chunk 更小，TTFT 可能上升
吞吐可能上升，但 decode 更容易被长 prefill 挤压


`max_num_seqs`
同一轮能挂住多少条序列
waiting queue 更容易增长
并发更高，KV cache 压力也更高


KV headroom
后续 token 写入和请求恢复的空间
准入更保守，短期吞吐受限
更容易 over-admission，抢占和重算成本会上来

> [!NOTE] 读日志时的抓手：
> 如果 waiting 增长，看窗口是否太窄；如果 repeated preemption 增长，看窗口是否放得太宽；如果 TTFT 和 ITL 互相拉扯，回到 token budget 和 chunked prefill 的比例。
 别把瞬时峰值当成稳定瓶颈，至少跨几个连续调度轮次一起看。


---

# 一轮一轮看 4 个请求怎么走 {#example}
<!-- tag: example -->

下面这个例子只跟踪 A、B、C、D 四个请求。A 和 B 先在 decode 阶段继续生成；C 是一个长 prompt；D 是一个短 prompt，但到得更晚。这样安排是为了让你一次看到 decode 优先、chunked prefill、等待队列和抢占四件事。

轮次
本轮输入
预算状态
调度决策
结果
要看出的原则


t0
A、B 正在 decode；C 刚到，prompt 很长；D 还没到
token budget 足够容纳少量 decode 和一段 prefill chunk
A、B 继续 decode，C 只推进一个 chunk
A 可能完成；B 继续；C 保留 prefill 进度
decode 不该被长 prefill 一口气挤掉


t1
C 还没走完；D 到达，且输入更短
剩余预算更紧
优先保住正在 decode 的请求，再把剩余 token 分给 D 和 C 的下一段 chunk
D 可能比 C 更快进入 running
短请求不应永远排在长 prefill 后面


t2
B 还在生成；C 继续分段 prefill；D 转入 decode
running 数接近上限
不再放新请求进来，直到下一轮有人完成或释放位
waiting 增长，但 batch 保持稳定
`max_num_seqs` 在这里开始起约束作用


t3
C 的 prefill 还没结束，但 KV cache headroom 变紧
需要给后来的请求留空间
低优先级或长尾请求 C 被抢占，之后按 recompute 或 swap 恢复
C 回到 waiting；别的请求继续前进
抢占的作用是保住整体吞吐

> [!NOTE] 这张表最重要的结论：
> 一轮里既可以有 decode，也可以有 chunked prefill，还可以有抢占。continuous batching 关注每轮怎么重新组批，让不同状态的请求按预算继续前进。


---

# 参数怎么把系统推向不同方向 {#tuning}
<!-- tag: tuning -->

调参时不要只问“这个值大不大”，要问它在减少哪一种等待。`max_num_batched_tokens` 更多是在调每轮 token 预算，`max_num_seqs` 更多是在调并发请求数，`max_num_partial_prefills` 和 `max_long_partial_prefills` 影响长 prompt 能同时被切几段，`scheduler_reserve_full_isl` 则决定你在准入时会不会把整段输入长度一起算进去。

$$B = \text{max\_num\_batched\_tokens}$$
 $$\text{chunk} = \min(P_{\text{remaining}}, \max(0, B - D - R))$$
 这里 \(B\) 是这一轮的总 token 预算，\(P_{\text{remaining}}\) 是还没走完的 prompt tokens，\(D\) 是这轮已经给 decode 留出的 token，\(R\) 是安全余量或保留头寸。这个式子用来帮助你判断为什么长 prefill 会和 decode 争预算。

参数
主要控制什么
调大 / 调小会怎样
常见误读


`max_num_batched_tokens`
单轮总 token 预算
调小通常更利于 ITL，调大通常更利于 TTFT 和吞吐
把它当成“吞吐越大越好”的单旋钮


`max_num_seqs`
单轮最多处理多少个序列
调小会限制并发，调大能让更多请求进来，但也更吃 KV cache
把它当成 token budget 的替代品


`max_num_partial_prefills`
同时进行中的部分 prefill 数
调大能让更多长 prompt 同时切块推进，调小能让排队更保守
把长 prompt 和短 prompt 看成同一种调度成本


`max_long_partial_prefills`
超过阈值的长 prompt 同时推进的数量
调小后，短 prompt 更容易插队；调大后，长 prompt 更容易并行切块
忽略长请求和短请求的队列差异


`long_prefill_token_threshold`
多长算“长 prompt”
阈值降低会让更多请求走长 prompt 路径，阈值升高则相反
默认阈值一定适合自己的长度分布


`scheduler_reserve_full_isl`
准入时是否先检查整段输入是否装得下
打开后更保守，能减少 over-admission 和 KV thrashing
只看第一块就够了


`preemption_mode`
被抢占时是 swap 还是 recompute
swap 保留块，recompute 丢块重来
抢占只是“暂停”，不会改变后续成本


`scheduling-policy`
先来先服务还是优先级调度
`priority` 能让高优先级请求先走，但更容易牺牲公平性
公平和低延迟永远可以同时拉满

> [!NOTE] 调参顺序建议：
> 先看 token budget 和 `max_num_seqs`，再看长 prefill 的并行数，最后才看抢占模式。顺序反了，容易把问题从预算层拖到抢占层。


---

# 排障时先看哪里 {#diagnosis}
<!-- tag: diagnosis -->

你看到的症状
更像哪类问题
先看什么
下一步再看什么


TTFT 变长，但 decode 还算稳定
prefill 过大或 chunk 太少
`max_num_batched_tokens`、`max_num_partial_prefills`、`max_long_partial_prefills`
prompt 长度分布和 chunked prefill 是否真的启用


ITL 变差，decode 经常被打断
prefill 挤压了 decode
token budget 是否给 prefill 留得太多
有没有把长 prompt 和短 prompt 一起塞进了同一轮


waiting queue 越来越长
并发上限太低，或者调度太保守
`max_num_seqs`、`scheduling-policy`
是否应该让短请求更容易插队


同一批请求反复被抢占
KV cache 压力过大或 over-admission
`scheduler_reserve_full_isl`、`preemption_mode`、KV headroom
是 swap 还是 recompute 更适合当前负载


长请求总是走不完
长 prefill 被切得太碎，或者长请求过多
`long_prefill_token_threshold`、`max_long_partial_prefills`
要不要减少长请求并行数，换短请求优先

<!-- interactive: ['terminal-lab'] → keep as HTML -->

> [!NOTE] 排障起手式：
> 先看是 TTFT、ITL、waiting queue 还是 repeated preemption 在变坏，再回到参数表。不要一上来就把问题归给 kernel。


---

# 资料路线与下一步动作 {#bridge}
<!-- tag: bridge -->

这一节把“看到什么问题，先打开哪份文档”说清楚。你如果在 vLLM 里调参数，先看 optimization 和 engine args；你如果在 SGLang 里做 benchmark，先看 benchmark 和 profiling；你如果在长上下文负载上被 KV cache 卡住，再看 HiCache。

材料
先抓什么
下一步动作


vLLM Optimization and Tuning
chunked prefill、`max_num_batched_tokens`、TTFT / ITL / 吞吐的取舍
把自己的请求分布和默认预算对比一次


vLLM Engine Arguments
`--max-num-seqs`、`--enable-chunked-prefill`、`--preemption-mode`、`--scheduler-reserve-full-isl`
确认启动参数里哪些是默认值，哪些是你真的改过


vLLM Scheduler API
`PreemptionMode`、`PREEMPTED_FOR_RECOMPUTE`、reset / queue 逻辑
看一次抢占到底是保块还是重算


SGLang Welcome + Benchmark and Profiling
offline throughput、TTFT、TPOT、ITL、在线与离线 benchmark 的区别
先跑 `bench_serving`，再决定是否改参数


SGLang HiCache System Design + Best Practices
三层 KV cache、长上下文、多轮会话、复用收益
如果工作负载是长上下文或多轮，先看缓存层级，再看 batch 大小

> [!NOTE] 下一步怎么接到后面的课：
> 如果你关心的是算子和 kernel，去 05 单元；如果你关心的是把输出格式限制住，去 06 单元；如果你要做引擎选型和源码对照，去 07 单元。


---

# 这里先不追的几件事 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
### 先放一边
网络路由、RPC、speculative decoding、FlashAttention kernel、MoE expert parallelism、分布式 gateway。它们都重要，但这页先不展开。

## box soft
### 这页要完成的动作
你要能从一个症状出发，先说清楚请求在哪个状态、这一轮 token budget 怎么分、是否启用了 chunked prefill、为什么会发生 preemption，以及下一步该调哪一个参数。

> [!NOTE] 边界一句话：
> 如果问题出在 queue、budget 或 preemption，就留在这一页；如果问题出在路由、模型结构或 kernel，就去别的单元。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box soft
### 怎么答才算会
先把对象说清楚，再说预算和队列，再说为什么这一轮该让谁先走。只给结论、不讲状态和预算，通常说明你还没把这一页的机制读进去。

## box info
### 检查重点
你要能说出：谁在 waiting，谁在 running，谁会被 chunked prefill，谁会被抢占，哪些参数在控制 TTFT、ITL 和吞吐之间的取舍。

<!-- end-practice-intro -->

<!-- practice-group: 第一组：状态和主线 -->
<!-- quick-review: 快速复盘 每轮都可能重排队伍；decode 优先，长 prefill 切块，抢占用于保住整体吞吐。 -->

### 1 choice

**Q:** continuous batching 的核心变化是什么？

- [ ] . 每个请求都必须等到全部结束后再进入下一轮。
- [ ] . 每一轮迭代后都可以重新调整 batch，新的请求也能进来。
- [ ] . 只要启用 chunked prefill，就不再需要调度器。

Answer: . 每一轮迭代后都可以重新调整 batch，新的请求也能进来。

### 2 true/false

**Q:** `max_num_seqs` 可以直接代替 `max_num_batched_tokens`，因为它们控制的是同一件事。

- [ ] 对
- [ ] 错

Answer: false

### 3 synthesis

**Q:** 用 waiting / running / preempted / finished 这四个词，讲一条请求从进入到结束会怎么变。

Explain: 一个合格答案至少要包含：请求先进入 waiting queue；被 scheduler 挑中后进入 running；如果本轮 token budget 或 KV headroom 不够，可能被 preempted，之后回到 waiting，或者按 swap / recompute 恢复；当采样结束或达到停止条件时进入 finished，并释放资源。答题时要同时提到“预算”和“资源释放”。

<!-- end-practice-group -->

<!-- practice-group: 第二组：一轮里怎么分配 -->
<!-- quick-review: 快速复盘 decode 先保节奏，剩余预算给 chunked prefill；抢占是为了保住整体吞吐。 -->

### 4 checkbox

**Q:** 下面哪些动作可以在同一轮里同时发生？

- [ ] . 一部分请求继续 decode。
- [ ] . 新到的长 prompt 只推进一个 chunk 的 prefill。
- [ ] . 前一轮被抢占的请求回到 waiting queue。
- [ ] . 只要进入这一轮，所有请求都必须在这一轮结束。

Explain: A、B、C 都可以同时发生；D 不对。continuous batching 的重点是在同一轮里混合 decode、chunked prefill 和状态回写，让不同状态的请求按预算继续前进。

### 5 choice

**Q:** `scheduler_reserve_full_isl=True` 主要是在防什么？

- [ ] . 只看第一段 chunk 就放行，后来才发现整段输入装不下。
- [ ] . 所有请求都会自动变成更短的输出。
- [ ] . 每轮 token budget 会自动翻倍。

Answer: . 只看第一段 chunk 就放行，后来才发现整段输入装不下。

### 6 fill

**Q:** 如果 `max_num_batched_tokens = 1024`，这轮已经给 decode 留出 512 个 token，又保留 128 个 token 的安全余量，剩余 prompt 还有 700 个 token，那么这一轮最多可以给 prefill 分多少 token？

Answer: 384

<!-- end-practice-group -->

<!-- practice-group: 第三组：调参 -->
<!-- quick-review: 快速复盘 `max_num_batched_tokens` 管单轮预算，`max_num_seqs` 管并发，长 prompt 的并行数要单独控。 -->

### 7 choice

**Q:** 下面哪个参数最直接限制每一轮能处理多少个序列？

- [ ] . `max_num_batched_tokens`
- [ ] . `max_num_seqs`
- [ ] . `long_prefill_token_threshold`

Answer: . `max_num_seqs`

### 8 choice

**Q:** 当很多长 prompt 一直占着 chunked prefill 时，下面哪个设置更容易让短请求插队？

- [ ] . 提高 `max_long_partial_prefills`，让更多长请求同时切块。
- [ ] . 提高 `long_prefill_token_threshold`，让更多请求都算作长请求。
- [ ] . 适当降低 `max_long_partial_prefills`，给短请求留出队头空间。

Answer: . 适当降低 `max_long_partial_prefills`，给短请求留出队头空间。

### 9 synthesis

**Q:** 为什么把 `max_num_batched_tokens` 调小，常常会改善 ITL，却可能让 TTFT 变差？

Explain: 把 `max_num_batched_tokens` 调小，单轮里能塞进来的 prefill chunk 变少，decode 请求被长 prefill 打断的次数也会下降，所以 ITL 往往更稳；但同样的长 prompt 需要更多轮才能跑完，首个 token 或完整请求完成前要等更久，所以 TTFT 可能变差。答题时要把“少打断 decode”和“prefill 分轮更细”都说出来。

<!-- end-practice-group -->

<!-- practice-group: 第四组：诊断和路线 -->
<!-- quick-review: 快速复盘 反复抢占看 KV headroom 和 reserve；要查 recompute 语义，就去 scheduler API。 -->

### 10 choice

**Q:** 如果你看到 `preempted for recompute` 反复出现，而且 waiting queue 也越来越长，最先该怀疑什么？

- [ ] . 采样温度太高。
- [ ] . `max_num_seqs` 一定太小。
- [ ] . KV cache 压力过大，或者准入太激进。

Answer: . KV cache 压力过大，或者准入太激进。

### 11 debug-plan

**Q:** 如果开启 chunked prefill 之后 TTFT 变差，先检查哪三件事？

Explain: 可以先查：1）`max_num_batched_tokens` 是否过小，导致 prefill chunk 被切得太细；2）`max_num_partial_prefills` 和 `max_num_long_partial_prefills` 是否让长请求并行过多，挤压了短请求；3）`scheduler_reserve_full_isl` 是否没打开，或者请求长度分布变化后仍按老预算准入。只要能把“预算、长请求并行、准入检查”三件事说清楚，就算合格。

### 12 choice

**Q:** 如果你要确认 `PREEMPTED_FOR_RECOMPUTE` 的含义，最先该看哪份资料？

- [ ] . vLLM Scheduler API。
- [ ] . SGLang HiCache Best Practices。
- [ ] . vLLM Engine Arguments 里的 LoRA 选项。

Answer: . vLLM Scheduler API。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- **Optimization and Tuning - vLLM Documentation**
解释 chunked prefill、`max_num_batched_tokens`，以及为什么小 budget 往往更利于 ITL，大 budget 往往更利于 TTFT 和吞吐。Snapshot: 2026-06-21.
- **Engine Arguments - vLLM Documentation**
解释 `--max-num-seqs`、`--enable-chunked-prefill`、`--preemption-mode`、`--scheduler-reserve-full-isl` 等启动参数的含义。Snapshot: 2026-06-21.
- **scheduler - vLLM Documentation**
解释 `PreemptionMode`、`PREEMPTED_FOR_RECOMPUTE`、reset / queue 相关逻辑，以及抢占时是保块还是重算。Snapshot: 2026-06-21.
- **Welcome to SGLang**
说明 SGLang 作为低延迟、高吞吐 serving framework 的定位，以及它和 RadixAttention、prefix caching、multi-GPU parallelism 的关系。Snapshot: 2026-06-21.
- **Benchmark and Profiling - SGLang Documentation**
说明 `bench_serving`、`bench_offline_throughput`、TTFT、TPOT、ITL 这些在线 / 离线 benchmark 的使用边界。Snapshot: 2026-06-21.
- **HiCache System Design and Optimization**
说明 HiCache 的系统架构、workflow、三层缓存和配置参数，以及它如何扩展 KV cache 的驻留层级。Snapshot: 2026-06-21.
- **HiCache Best Practices**
说明 HiCache 更适合长上下文和多轮对话场景，以及在什么负载下层次化缓存才值得付出搬运成本。Snapshot: 2026-06-21.

---
