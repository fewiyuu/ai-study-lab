---
course: "智能体运行承载工程"
unit: "04"
title: "状态、会话与可恢复执行"
kicker: "状态恢复"
summary: "这一页接着 03 的工具面和工作空间往下走，专门拆长任务的恢复语义：会话历史给模型看，检查点给运行时继续跑，幂等键保护外部系统，追踪记录让人知道为什么会恢复成这样。"

side_note: "本页只拆四条线：session、checkpoint、幂等键、trace。先看恢复链路，再看退款审批案例。"

can_do:
  - "能把同一个长任务拆成 session、checkpoint、业务副作用和 trace 四条线。"
  - "能判断一个恢复点该用同一 thread_id、同一 session，还是同一幂等键。"
  - "能对重复跑一遍会不会写坏做出具体检查，也能指出要看哪一层。"

course_links:
  - text: "上一页：03 工具面与工作空间"
    href: "../03_工具面与工作空间/学习页.html"
  - text: "课程地图"
    href: "../index.html"
  - text: "下一页：05 外部协议与连接器"
    href: "../05_外部协议与连接器/学习页.html"

---

# 学习目标 {#goals}
<!-- tag: goal -->

这一页只解决长任务里的恢复问题：哪些内容给模型看，哪些内容给运行时续跑，哪些内容必须交给业务系统去重，哪些内容只是给人排查用。先把这四条线分开，后面的概念才不会缠在一起。

对象
主要负责
读什么
写什么
错位时的症状


session
给下一次模型调用拼历史。
上一轮消息、工具回包、摘要。
新增对话项。
用户觉得“上一句怎么没接上”。


checkpoint / thread
记住执行走到哪一步。
当前状态、下一节点、恢复配置。
StateSnapshot、任务队列、恢复点。
重启后只能从头来，或者恢复到错的节点。


业务副作用
保证写入重复时不多写。
业务键、审批状态、已有记录。
幂等 upsert、outbox、提交状态。
恢复一次就多一张退款单、多一封邮件。


trace
解释为什么会恢复成这样。
trace id、span、group_id、metadata。
运行记录和排查证据。
动作做过了，但没人说得清发生了什么。

## box info
### 先知道，先别追
这一页先不展开外部协议、连接器、完整 durable workflow、版本迁移和多 agent 协作。它们会在后面的单元回来。现在先把 session、checkpoint、幂等键和 trace 的边界立住。

你现在应该能做什么：听到“保存上下文”时，先问对方是在说 session、checkpoint、幂等键，还是 trace。


---

# 状态账本 {#state-map}
<!-- tag: map -->

这里沿用同一个锚点例子：客服智能体处理工单 `T-456`，目标订单是 `O-789`，流程要先产出建议，再等待人工批准，最后写退款并留追踪记录。这个例子足够小，能把恢复链路看清楚，也足够真实，不会把问题简化没了。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

状态层
读取什么
写入或改变什么
有效时机
错位症状


会话历史
上一轮对话、工具返回、摘要。
新增消息项。
下一次模型调用前后。
模型接话断掉，用户要重复说明。


执行检查点
状态值、next、任务、配置。
StateSnapshot、恢复点。
图边界、interrupt、resume。
恢复到错的节点，或者找不到同一条执行线。


业务状态
审批结果、订单号、退款申请键。
退款单、审批标记、去重记录。
审批后或幂等写入时。
重复提交、重复通知、重复扣款。


追踪记录
trace id、span、group_id、metadata。
排查证据和导出结果。
运行期间与结束后。
只能看见结果，看不见路径。

你现在应该能做什么：拿到一个长任务时，先把它拆成“给模型看、给运行时存、给业务系统去重、给人排查”四段。


---

# 会话 {#session}
<!-- tag: memory -->

OpenAI Agents SDK 里的 session 解决的是“下一次模型应该看到什么”。它会在 run 前取历史，在 run 后把新增项写回去。这样做的价值很直接：同一个工单继续问时，不用每轮手工拼历史，但也不会把执行恢复、业务去重和排查记录一起搅进来。

OpenAI Agents SDK 会话最小用法
复制

```python
from agents import Agent, Runner, SQLiteSession

agent = Agent(name="Support", instructions="回答要简洁，先给结论。")
session = SQLiteSession("ticket-T-456", "conversations.db")

first = await Runner.run(agent, "这张订单能退款吗？", session=session)
second = await Runner.run(agent, "继续上一步，补一段处理结论。", session=session)

# 第二次 run 会自动带上同一 session 的历史，不需要手工拼接输入项。
```

对象
读取什么
写什么
什么时候会过期
错了会怎样


session
历史消息、工具回包、摘要。
新用户输入、助手响应、工具项。
换了 session id 或换了后端存储。
模型看不到同一段对话。


Runner.run
当前输入和 session 历史。
新的 run 结果和新增 items。
run 结束之后。
把执行恢复误当成会话续接。

## box info
### 会话边界
session 只管模型输入历史，不管图状态从哪一步继续。它能帮用户接话，不能替 checkpoint 找回执行位置。

你现在应该能做什么：看到“继续刚才的话题”时，优先检查 session；看到“中断后继续执行”时，先别动 session，先找 checkpoint。


---

# 检查点 {#checkpoint}
<!-- tag: state -->

LangGraph 这一类持久执行把同一条执行线固定在 thread_id 上，再把图状态保存成 checkpoint。恢复时看同一条 thread，就能回到同一条状态线；这和聊天历史是两回事。

字段
它保存什么
它给谁用
错位症状


values
当前状态值，例如 approved、refund_status。
下一个节点和恢复逻辑。
恢复后状态像被重置。


next
下一批要执行的节点。
调度器和 resume 过程。
恢复后跳错节点。


config / thread_id
恢复同一条执行线的稳定指针。
checkpointer 查状态。
找不到原来的 checkpoint。


metadata / writes
节点写入和运行元数据。
排查和回放。
看不出上一次到底改了什么。


tasks
待执行或中断的任务。
恢复后的继续执行。
该执行的节点没被调度回来。

LangGraph interrupt / resume 骨架
复制

```python
from langgraph.types import Command, interrupt

def approval_node(state):
 decision = interrupt({
 "question": "是否批准发送退款通知？",
 "ticket_id": state["ticket_id"],
 "order_id": state["order_id"],
 })
 return {"approved": bool(decision)}

config = {"configurable": {"thread_id": "refund-T-456"}}

# 首次运行会停在 interrupt，checkpoint 会把当前状态记下来。
first = graph.stream_events({"ticket_id": "T-456", "order_id": "O-789"}, config=config, version="v3")

# 恢复时必须用同一个 thread_id，resume 值会成为 interrupt(...) 的返回值。
resumed = graph.stream_events(Command(resume=True), config=config, version="v3")
```

## box warn
### 这一步的关键
interrupt 之后恢复，节点通常会从开头再跑一遍，所以 interrupt 前面的代码必须能重跑，或者不要做不可逆写入。

你现在应该能做什么：看到一个恢复问题时，先问 thread_id 有没有保持不变，再问 checkpoint 里到底存了什么状态。


---

# 恢复实验 {#resume}
<!-- tag: lab -->

下面这个实验把恢复中最容易出错的三种情况放在一起：同一条 thread 继续跑、thread 换了、interrupt 顺序换了。你不需要背概念，直接看恢复后到底落到哪里。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

## box info
### 观察重点
恢复实验只看三件事：同一 thread 是否还在、checkpoint 是否还在、interrupt 的顺序是否还在。只要其中一项变了，恢复结果就可能换线。

你现在应该能做什么：把“恢复失败”先拆成 thread_id 问题、checkpoint 问题、还是 interrupt 顺序问题。


---

# 副作用 {#side-effects}
<!-- tag: idempotency -->

checkpoint 只管图状态，不会替外部系统去重。退款、发信、发消息、写审批单这些外部副作用，要靠幂等键或 outbox 才能安全重跑。先问这一步能不能重复跑，再问重复跑会不会多写一次。

处理路线
先读什么
写什么
重复执行后的结果
更稳的做法


先 create 再 interrupt
订单和审批建议。
新退款单。
恢复后再跑一次，可能又创建一张。
把写入后移，或者改成幂等 upsert。


先审批后写入
审批结果和业务键。
带幂等键的退款写入。
重复跑时能落到同一条记录。
优先采用这个顺序。


先 outbox 再 worker
意图记录和去重键。
待投递事件。
worker 重试时只会看到同一条意图。
适合外部系统慢、偶发失败多的场景。

退款幂等键与写入方式
复制

```python
refund_request_id = f"refund:{ticket_id}:{order_id}"

result = refund_api.upsert_refund(
 key=refund_request_id,
 order_id=order_id,
 reason="approved_by_agent",
)

# key 一样，业务系统应返回同一条结果，而不是再新建一条退款单。
```

## box warn
### 常见误区
“有 checkpoint 了，所以外部写入也安全” 这个判断是错的。checkpoint 管的是执行位置，幂等键管的是外部系统是否会重复提交。

你现在应该能做什么：看到一个会写外部系统的节点时，直接问它有没有幂等键，或者有没有换成 upsert / outbox。


---

# 恢复桥接 {#bridge}
<!-- tag: bridge -->

把 session、checkpoint、幂等键和 trace 放在同一张桥接表里，能直接看出它们什么时候接、什么时候断。这个表把谁保存什么、谁负责继续、谁负责去重、谁负责排查，放成一份操作清单。

字段
存放位置
读写责任
断点后症状


session_id
会话存储
给下一次模型输入补历史。
用户补一句，模型像没接上。


thread_id
checkpoint 配置
让运行时找到同一条执行线。
重启后恢复不到原来的节点。


refund_request_id
业务系统
给外部写入做幂等去重。
恢复一次就多提交一次。


group_id
trace 入口
把多次 run 归到同一业务任务。
同一工单的证据链断开。

把四个 ID 串起来的最小流程
复制

```python
def handle_refund(agent, session, graph, ticket_id, order_id):
 thread_id = f"refund-{ticket_id}"
 refund_request_id = f"refund:{ticket_id}:{order_id}"

 with trace("refund_workflow", group_id=ticket_id, metadata={"ticket_id": ticket_id}):
 draft = await Runner.run(agent, "生成退款建议", session=session)
 decision = await graph.invoke(
 {"ticket_id": ticket_id, "order_id": order_id, "refund_request_id": refund_request_id},
 config={"configurable": {"thread_id": thread_id}},
 )
 if decision["approved"]:
 refund_api.upsert_refund(key=refund_request_id, order_id=order_id)

 flush_traces()
```

## box info
### 为什么这段桥接重要
这段最小流程把四种责任分开了：session 只管模型看到什么，thread_id 只管同一条状态线，refund_request_id 只管外部写入是否重复，group_id 只管人怎么把多次运行串起来。恢复事故常见的根因是字段放错层，同一条任务线因此被拆散。

你现在应该能做什么：拿到一个可恢复任务时，直接画出四个 ID 的落点，并说明哪个字段一旦错位会导致什么后果。


---

# 追踪 {#trace}
<!-- tag: observability -->

trace 负责解释“为什么会变成这样”。它把一条长任务拆成多个 span，记录每次 run、工具调用、人工介入和恢复过程。到了排查阶段，你会想把同一个工单的多次 run 归到同一个 group_id 下面，这样恢复前后留下的证据才连得起来。

trace 和 group_id 的写法
复制

```python
from agents import Runner, trace, flush_traces

async def handle_ticket(agent, session, ticket_id):
 try:
 with trace(
 "refund_workflow",
 group_id=ticket_id,
 metadata={"ticket_id": ticket_id, "mode": "recoverable"},
 ):
 draft = await Runner.run(agent, "生成退款建议", session=session)
 final = await Runner.run(agent, f"复核这段建议：{draft.final_output}", session=session)
 return final.final_output
 finally:
 flush_traces()
```

对象
它记录什么
它最适合回答的问题
不能替代什么


trace
一整次运行的证据链。
这条任务怎么走到这里。
checkpoint。


span
一个动作或子调用。
哪一步慢、哪一步错。
业务幂等键。


group_id
把多条 trace 归到同一业务任务。
同一工单的多次 run 怎么串起来。
session_id。


metadata
业务标签和运行备注。
这次为什么会重跑、属于哪个 ticket。
状态存储。

## box info
### trace 边界
trace 负责证据链，不能替代 session 的历史，也不能替代 checkpoint 的状态。

你现在应该能做什么：排查一次恢复事故时，先找 trace，再根据 trace 回到 thread_id、session 和幂等键。


---

# 落地检查 {#checklist}
<!-- tag: checklist -->

如果你要把一个会中断的 Agent 改成可恢复执行，先按这五步检查。顺序很重要：会话先对齐模型输入，thread_id 先对齐执行线，幂等键先对齐外部写入，trace 先对齐排查，最后再跑恢复测试。第一轮最容易错的地方，通常是把 session 继续拿去存执行状态，或者在 interrupt 前就做了不可逆写入。

检查项
先看什么
合格标准
最先会失败的地方
下一步


会话
session 存储和历史追加。
同一会话能续上上一轮对话。
模型重复问同一问题。
确认 session 不是拿来存执行位置的。


执行线
thread_id 和 checkpoint。
resume 能回到同一条状态线。
恢复后落到新线程。
检查 thread_id 是否稳定。


外部写入
退款单、发信、审批单。
重跑后落在同一条业务记录。
重复提交或重复通知。
补幂等键、upsert 或 outbox。


追踪
trace、group_id、flush_traces。
能串起同一工单的多次 run。
看得到结果，看不到路径。
把 flush 放在退出前。


恢复测试
interrupt 前后各跑一次。
同一条线恢复后行为一致。
节点从头重跑导致副作用翻倍。
补一个最小失败案例。

## box info
### 下一步先看什么
官方文档先看 Sessions、Durable Execution、Persistence、Interrupts 和 Tracing。实际排查时，先找 session_id、thread_id、幂等键和 group_id 四个字段，再决定问题该往哪一层修。要是还看不出来，先故意让同一节点重跑一次，问题通常会在这一轮里暴露出来。

你现在应该能做什么：拿到一个新 Agent，按这五项找出最先会失败的地方，而不是先去加更多提示词。


---

# 退款案例 {#example}
<!-- tag: case -->

把四条线放回同一个流程里看，最容易看出它们各自负责什么。客服智能体先给退款建议，等人工确认，再写退款申请，最后把这一串动作记到 trace 里。

字段
例子
作用
错位后果


session_id
ticket-T-456
让同一工单的对话历史接着走。
用户补一句，模型却像没听见。


thread_id
refund-T-456
让同一条执行线找到 checkpoint。
恢复不到原来的中断点。


refund_request_id
refund:T-456:O-789
给外部退款系统做幂等去重。
恢复一次就多提交一次。


group_id
T-456
把同一工单的多条 trace 归在一起。
多次 run 找不到同一条证据链。

退款审批状态拆分示意
复制

```python
state = {
 "ticket_id": "T-456",
 "order_id": "O-789",
 "refund_request_id": "refund:T-456:O-789",
 "approved": None,
 "refund_status": "not_submitted",
}

def review_refund(state):
 decision = interrupt({
 "ticket_id": state["ticket_id"],
 "order_id": state["order_id"],
 "refund_request_id": state["refund_request_id"],
 })
 return {"approved": decision}

def submit_refund(state):
 if not state["approved"]:
 return {"refund_status": "rejected"}

 result = refund_api.upsert_refund(
 key=state["refund_request_id"],
 order_id=state["order_id"],
 reason="approved_by_agent",
 )
 return {"refund_status": result.status}
```

会话保存什么用户和智能体围绕工单的对话、工具回包、后续补充说明。
检查点保存什么状态值、next、thread 配置、恢复点和待执行任务。
业务系统保存什么退款请求键、审批结果、提交状态和通知记录。

## box info
### 边界提醒
这一页只讲恢复和排查的基础面。外部协议、连接器、完整运行平台和版本迁移，留到后面的单元再看。

你现在应该能做什么：把一个“等待人工批准”的流程拆成 session、thread、幂等键和 trace 四个字段，并说明各自放在哪里。


---

# 常见错误 {#mistakes}
<!-- tag: mistakes -->

## box info
### 先别追的东西
这里先不追外部协议、持久工作流框架、多 agent 协同和迁移策略。把恢复边界看明白以后，再去看那些更大的运行系统，理解会稳很多。

## box
### 错误 1：把聊天历史当恢复状态
只把历史消息再喂给模型，恢复点就会丢掉，工具调用也更容易重复。
**修法：**聊天历史交给 session，执行位置交给 checkpoint。

## box
### 错误 2：interrupt 前先做非幂等写入
先 create_refund 再 interrupt 等审批，恢复时节点从头再跑，create 可能再执行一次。
**修法：**把写入后移，或者改成 upsert 和幂等键。

## box
### 错误 3：resume 时换了 thread_id
恢复时用了新 id，相当于开始一条新执行线。旧 checkpoint 找不到，流程只能重新开始。
**修法：**把 thread_id 存进业务任务记录，前后端都用同一个值。

## box
### 错误 4：同一节点里 interrupt 顺序变动
多个 interrupt 的恢复值通常按位置对应。顺序一变，恢复输入就会落到错的字段上。
**修法：**保持顺序稳定，复杂表单拆节点，或者一次收集完整结构。

## box
### 错误 5：把 trace 当状态库
trace 能看见发生过什么，但不能替你恢复执行位置。状态还是要放在 checkpoint 和业务系统里。
**修法：**trace 只做证据链，真正的状态放 checkpoint 和业务系统。

## box
### 错误 6：任务结束了，trace 还没导出
后台 worker 先退场，trace 还留在缓冲里，排查时就只剩半截记录。
**修法：**在 trace 上下文结束后及时 flush_traces。

你现在应该能做什么：看到恢复事故时，先分成“历史没接上、位置没接上、写入没去重、记录没导出”四类，再找对应修法。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box soft
### 怎么答才算会
先分清对象，再说边界，再说失败症状。选择题用来卡住职责，填空题用来卡住术语，开放题用来检查你能不能把恢复链路说完整。

## box info
### 检查重点
重点看你有没有把 session、checkpoint、幂等键和 trace 分开；有没有把恢复位置和对话历史分开；有没有把外部副作用和排查证据分开。

<!-- end-practice-intro -->

<!-- practice-group: 一、概念辨析 -->
<!-- quick-review: 先想一句话：这东西是给模型看的、给运行时看的，还是给人排查看的。 -->

### 1 choice

**Q:** 1. 下列哪项最像 session 的职责？

- [ ] 让下一次模型调用自动带上同一会话的历史。
- [ ] 保证外部退款接口不会重复写入。
- [ ] 让进程重启后从任意代码行继续执行。

Answer: 让下一次模型调用自动带上同一会话的历史。

### 2 true/false

**Q:** 2. trace 可以作为唯一的恢复状态库。

- [ ] 对
- [ ] 错

Answer: 错

### 3 fill

**Q:** 3. LangGraph 恢复同一条执行线时，最关键的稳定指针通常是哪一个？

- [ ] thread_id
- [ ] group_id
- [ ] session_id

Answer: thread_id

### 4 explain

**Q:** 4. 用一句话区分 session 和 checkpoint。

Explain: 合格答案会说：session 负责给模型续上下文，checkpoint 负责给运行时续执行位置。前者解决“下一轮模型要看到什么”，后者解决“中断后从哪里继续”。

<!-- end-practice-group -->

<!-- practice-group: 二、恢复语义 -->
<!-- quick-review: 恢复题先想三件事：同一条线在不在，checkpoint 在不在，interrupt 顺序在不在。 -->

### 5 choice

**Q:** 5. LangGraph interrupt 恢复时，最需要警惕哪件事？

- [ ] 节点通常会从开头重跑，interrupt 前的代码可能再次执行。
- [ ] 所有局部变量都会永久保留，不会重跑。
- [ ] thread_id 会自动换成新的值。

Answer: 节点通常会从开头重跑，interrupt 前的代码可能再次执行。

### 6 true/false

**Q:** 6. 同一节点里的多个 interrupt 在恢复时可以按名称自由匹配，因为 resume 值会自动找字段名。

- [ ] 对
- [ ] 错

Answer: 错

### 7 fill

**Q:** 7. 恢复 interrupt 时，通常调用 Command(____=外部输入)。


<!-- end-practice-group -->

<!-- practice-group: 三、副作用与幂等 -->
<!-- quick-review: 先看外部写入有没有幂等键，再看 checkpoint 是否只是保存了执行位置。 -->

### 8 choice

**Q:** 8. 哪个操作更适合放在 interrupt 前？

- [ ] db.upsert_refund(key=refund_request_id, ...)
- [ ] email_api.send_without_idempotency_key(...)
- [ ] create_refund(order_id)

Answer: db.upsert_refund(key=refund_request_id, ...)

### 9 explain

**Q:** 9. 为“工单 T-456 对订单 O-789 发起退款”设计一个幂等键，并说明它为什么要和订单、工单一起生成。

Explain: 合格答案会把幂等键设计成能从业务对象稳定推导出来的值，例如 refund:T-456:O-789。它要同时绑定工单和订单，这样同一业务动作在重试时仍然命中同一条外部写入记录，不会因为上下文变化被当成新请求。

### 10 explain

**Q:** 10. 为什么 checkpoint 不能替代幂等键？

Explain: 合格答案会说：checkpoint 保存的是图状态和下一步执行位置，只能让流程知道从哪里继续；幂等键负责外部系统是否把同一笔写入当成重复请求。两者解决的是不同层的问题，不能互相替代。

<!-- end-practice-group -->

<!-- practice-group: 四、追踪与综合设计 -->
<!-- quick-review: 先想 trace 负责证据，group_id 负责串线，flush_traces 负责把记录送出去。 -->

### 11 choice

**Q:** 11. 在 tracing 里，哪个字段最适合把同一工单的多条 trace 关联起来？

- [ ] group_id
- [ ] started_at
- [ ] max_tokens

Answer: group_id

### 12 fill

**Q:** 12. 在后台 worker 中，任务结束后尽快把 trace 导出去，通常要调用：


### 13 scenario-transfer

**Q:** 13. 给退款流程写一个最小 ID 对照表，写出 session_id、thread_id、refund_request_id、group_id 各自放在哪里。

Explain: 合格答案通常会写：session_id 给会话历史用，thread_id 给 checkpoint 恢复用，refund_request_id 给业务系统幂等去重用，group_id 给 trace 聚合用。最好再补一句，这四个字段各管一层，不要混用。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- **OpenAI Agents SDK Sessions**[官方文档](https://openai.github.io/openai-agents-python/sessions/)：用于理解 session 如何在 run 前后维护多轮历史，以及它和恢复执行的边界。
- **OpenAI Agents SDK Tracing**[官方文档](https://openai.github.io/openai-agents-python/tracing/)：用于理解 trace、span、group_id、metadata 和后台导出的基本约定。
- **LangGraph Durable Execution**[官方文档](https://docs.langchain.com/oss/python/langgraph/durable-execution)：用于理解检查点、线程标识、恢复和长任务中断后的继续执行。
- **LangGraph Persistence**[官方文档](https://docs.langchain.com/oss/python/langgraph/persistence)：用于理解 thread、checkpoint、state history、pending writes 和 replay 的关系。
- **LangGraph Interrupts**[官方文档](https://docs.langchain.com/oss/python/langgraph/interrupts)：用于理解 interrupt、Command(resume=...)、同一 thread_id 和节点重跑规则。
- **Temporal**[官方文档](https://docs.temporal.io/)：用于补足长运行流程、任务历史和恢复执行的通用语义边界。

---
