---
course: "Claude Code 工作流"
unit: "15"
title: "15 Git Worktree任务隔离｜Claude Code 工作流"
kicker: "Git worktree"
summary: "本节学习 Git Worktree：在同一个 Git 仓库下创建多个工作目录，每个目录可以 checkout 不同分支。普通分支切换只能让你在一个目录里交替做任务；worktree 则让 `repo/`、`dev-branch-3/`、`dev-branch-4/` 同时存在，每个目录绑定自己的分支，适合人类或 AI coding agent 并行推进多个任务。"

side_note: "Worktree 的价值不是“多一个 Git 命令”，而是把分支隔离升级成目录隔离，让多个任务可以同时展开。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../14_自主Agent与看板轮询/学习页.html"
  - text: "下一节"
    href: "../16_可审计任务追踪/学习页.html"
---

# 1. 普通分支切换只能“轮流做” {#why}
<!-- tag: checkout pain -->

传统开发里，你通常在一个仓库目录中使用 `git switch -c dev-branch-1` 开始任务一。新任务来了，你再切回 `main`，基于主分支创建 `dev-branch-2`。这当然能工作，但同一时刻这个目录只能处在一个分支上。

问题出现在任务交错时：任务一改了一半，任务二突然很急；你要么提交半成品，要么 stash 临时改动，要么冒着脏工作区切分支的风险。对于 AI coding agent，这个问题更明显：多个 agent 如果共用同一个目录，它们会互相踩文件、互相改变分支、互相污染状态。

## box warn
### 普通 checkout
- 一个工作目录只能对应一个当前分支。
- 多任务需要频繁切换分支。
- 未提交改动容易阻塞或污染切换。

## box
### Git Worktree
- 一个仓库可以挂出多个工作目录。
- 每个目录绑定不同分支。
- 不同任务可以在不同目录同时推进。

你现在应该能做什么：识别“分支切换”解决的是版本线问题，而 worktree 进一步解决“工作目录隔离”问题。


---

# 2. Worktree 是“共享 Git 数据的独立工作目录” {#concept}
<!-- tag: one repo many trees -->

`git worktree` 允许同一个仓库创建多个工作目录。每个工作目录看起来像一个独立项目目录，可以打开、编辑、运行、测试，也可以处在自己的分支上。但它们背后仍然共享同一个 Git 对象数据，不需要完整克隆多份仓库。

一个实用直觉是：普通 clone 像复制一整套仓库；worktree 像从同一个仓库“挂出”多个工作台。每个工作台上可以摆不同任务的文件状态，但提交历史、对象数据和分支信息仍由同一个 Git 仓库管理。

## box soft
### 流程图式文字
已有仓库 → 选择起点分支 `main` → 为任务创建新分支 → 用 `git worktree add` 挂出新目录 → 在新目录运行编辑和测试 → 任务合并后 `git worktree remove` 清理目录 → 如需继续查看，用已有分支重新挂载。

## box
### 隔离是否成立
- 每个任务有独立工作目录。
- 每个工作目录绑定明确分支。
- 命令在对应目录中运行。
- 删除 worktree 前确认改动已提交或不再需要。
- 清理目录不误删远程分支或本地分支。

单目录切分支
多个 worktree
remove 后
恢复目录


单目录切分支
说明会显示在这里。
**repo/**
当前分支随 checkout 变化。
**dev-branch-3/**
尚未创建。
观察重点：worktree remove 删除的是工作目录，不是分支本身。只要分支还在，就可以重新 add 一个 worktree 恢复目录。

你现在应该能做什么：用自己的话说清“多个工作目录”和“多个仓库克隆”的区别。


---

# 3. 最小命令流程：add、list、remove、恢复 {#commands}
<!-- tag: commands -->

视频里先初始化一个仓库，创建 `README.md` 并提交到主分支。日常真实开发中，你更多是从远程 clone 一个现成 repo。无论是 `git init` 还是 `git clone`，只要你在一个 Git 仓库里，就可以使用 worktree。

基础准备：仓库里有一个提交Shell

```text
mkdir repo
cd repo
git init
git branch -M main
echo "hi readme" > README.md
git add README.md
git commit -m "init readme"
```

有了基线提交后，你可以创建一个新 worktree。命令一般写成：`git worktree add <目录路径> -b <新分支名> <起点分支>`。这里的目录路径是新工作目录的位置，分支名是这个目录要绑定的新分支，起点分支通常是 `main`。

正确示例：为新任务创建独立目录和分支Shell

```text
# 在 repo/ 目录中执行
git worktree add ../dev-branch-3 -b dev-branch-3 main

# 进入新目录后，当前分支就是 dev-branch-3
cd ../dev-branch-3
git branch --show-current
```

如果你再创建 `dev-branch-4`，旁边就会多出另一个目录。此时你可以在 `repo/` 里看主分支，在 `dev-branch-3/` 里做任务三，在 `dev-branch-4/` 里做任务四。它们真的有不同文件树。

查看当前有哪些 worktreeShell

```text
git worktree list

# 可能看到：
# /path/to/repo abc1234 [main]
# /path/to/dev-branch-3 def5678 [dev-branch-3]
# /path/to/dev-branch-4 901abcd [dev-branch-4]
```

任务完成、PR 合并后，可以删除本地 worktree 目录。注意：`git worktree remove` 主要移除工作目录登记和目录本身，不等于自动删除这个分支。视频里 remove 后再看 branch，分支仍然保留，这就是“删除目录不影响分支，可随时恢复”的意思。

删除 worktree 目录，但分支仍在Shell

```text
git worktree remove ../dev-branch-3
git branch

# dev-branch-3 分支仍然可以存在。
```

如果误删或想重新打开这个分支，不需要 `-b` 创建新分支。因为分支已经存在，只需要把现有分支重新挂成一个 worktree。

恢复已有分支对应的工作目录Shell

```text
git worktree add ../dev-branch-3 dev-branch-3

cd ../dev-branch-3
git branch --show-current
# dev-branch-3
```

你现在应该能做什么：为一个新任务创建 worktree，为已有分支恢复 worktree，并用 `git worktree list` 检查当前隔离目录。


---

# 4. 为什么 AI coding agent 特别需要 worktree {#agent}
<!-- tag: agent isolation -->

人类开发者也会受益于 worktree，但 AI coding agent 的收益更直接。因为 agent 可以同时接多个任务、执行多个命令、修改多个文件。如果它们共用一个目录，任务之间很容易相互污染：一个 agent 切了分支，另一个 agent 的上下文就错了；一个 agent 改了文件，另一个 agent 的测试结果就不可靠。

Worktree 给每个任务一个独立物理目录。Agent A 在 `task-login/` 修改登录逻辑，Agent B 在 `task-report/` 修改报表导出。它们共享仓库历史，但工作区互不干扰。调试失败时，也能直接看某个目录里的改动、日志和状态。

场景不用 worktree 的风险使用 worktree 的好处

两个任务并行频繁 checkout，脏工作区冲突。每个任务一个目录，互不覆盖。
多个 Agent 协作Agent 之间切分支、改文件互相影响。每个 Agent 在自己的工作目录运行。
任务完成清理不清楚哪些临时文件属于哪个任务。remove 对应 worktree 即可清理目录。
审计任务结果多个任务改动混在一个工作区。按目录查看 diff、测试和提交。

这里也能看出本节和前面任务看板的连接：看板负责“有哪些任务”，worktree 负责“每个任务在哪个隔离环境里执行”。一个成熟的 agent harness 往往会把任务分配、工作目录、分支、提交和审计记录串起来。


---

# 5. 常见误区：目录、分支和仓库不是一回事 {#pitfall}
<!-- tag: pitfalls -->

学习 worktree 最容易混淆三个层次：工作目录、Git 分支、仓库数据。工作目录是你能打开和编辑文件的地方；分支是 Git 中的一条提交指针；仓库数据是 Git 保存对象、提交和引用的底层数据。

工作目录例如 `dev-branch-3/`。你在里面改文件、运行测试。
分支例如 `dev-branch-3`。它指向一串提交，可以被重新挂载。
仓库数据多个 worktree 共享同一套 Git 对象数据，不需要完整复制。

因此，`git worktree remove ../dev-branch-3` 删除的是工作目录，不是自动把 `dev-branch-3` 分支也删掉。反过来，如果你想创建全新的任务分支，用 `-b`；如果分支已经存在，就不要再 `-b`，直接指定已有分支。

错误示例：恢复已有分支时还使用 -b分支已存在

```text
# dev-branch-4 已经存在时，这样会失败或造成误解
git worktree add ../dev-branch-4 -b dev-branch-4 main

# 正确：直接挂载已有分支
git worktree add ../dev-branch-4 dev-branch-4
```

另一个误区是把 worktree 当作“多 clone 几份仓库”。多 clone 当然也能隔离目录，但每份 clone 都有自己的远程、配置和对象数据，管理成本更高。worktree 更适合从同一个本地仓库派生多个任务工作台。

## box soft
### Worked example：恢复已删除目录
你完成 `dev-branch-3` 后执行了 `git worktree remove ../dev-branch-3`。第二天需要重新查看这条分支，不要再写 `-b dev-branch-3`，因为分支已经存在。正确做法是 `git worktree add ../dev-branch-3 dev-branch-3`。这一步重新创建工作目录，但复用原来的分支指针。


---

# 综合练习 {#practice}
<!-- tag: 检查理解 -->

<!-- practice-group: 一、基础概念 -->

### ? 选择

**Q:** 1. Git Worktree 的核心作用是什么？

- [ ] . 同一仓库创建多个工作目录
- [ ] . 删除所有分支
- [ ] . 替代所有提交


### ? 短答

**Q:** 2. 普通 `git switch -c` 后在单目录开发多个任务时，最大限制是什么？


### ? 解释

**Q:** 3. 用一句话区分“工作目录”和“分支”。


### ? 判断

**Q:** 4. 多个 worktree 背后完全是互不相关的多个 Git 仓库。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 二、命令流程 -->

### ? 填空

**Q:** 5. 为新任务创建目录 `../dev-branch-3` 和新分支 `dev-branch-3`，命令应写什么？


### ? 填空

**Q:** 6. 查看当前所有 worktree 的命令是 ______。


### ? 短答

**Q:** 7. `git worktree remove ../dev-branch-3` 删除了什么？没有自动删除什么？


### ? 命令

**Q:** 8. `dev-branch-4` 分支已存在，如何重新创建对应工作目录？


<!-- end-practice-group -->

<!-- practice-group: 三、任务隔离 -->

### ? 解释

**Q:** 9. 为什么 AI coding agent 比人类更需要 worktree 隔离？


### ? 场景

**Q:** 10. 举一个两个 Agent 共用同一目录可能互相污染的例子。


### ? 关联

**Q:** 11. 前一节 `.tasks` 看板和本节 worktree 隔离分别解决什么问题？


### ? 判断

**Q:** 12. 任务完成、PR 已合并后，可以考虑 remove 对应 worktree 目录。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 四、诊断与迁移 -->

### ? 诊断

**Q:** 13. 已有 `dev-branch-4`，却执行 `git worktree add ../dev-branch-4 -b dev-branch-4 main`，问题是什么？


### ? 纠错

**Q:** 14. “remove worktree 会删除分支，所以不能用。”这句话哪里错？


### ? 比较

**Q:** 15. worktree 和多 clone 仓库相比，管理上有什么差别？


### ? 流程

**Q:** 16. 写出一个从创建 worktree、进入目录、完成后删除目录的最小流程。


### ? 表达

**Q:** 17. 面试中如何解释 Git Worktree 对 Agent Harness 的价值？


### ? 费曼解释

**Q:** 18. 用 6-9 句话讲清楚：Git Worktree 如何实现任务隔离。


<!-- end-practice-group -->


---

