---
course: "Claude Code 工作流"
unit: "18"
title: "18 消息规范化｜Claude Code 工作流"
kicker: "Message normalization"
summary: "项目更新后，章节数从 12 扩展到 19，系统加固部分新增权限、Hook、记忆、错误恢复、调度和 MCP。消息规范化出现在工具使用章节：以前直接把内部 `messages` 发给模型，现在先执行 `normalize_messages(messages)`，把内部消息整理成 API 接受的格式，避免 400 Bad Request。"

side_note: "消息规范化是在发给模型前做协议预处理：删掉不能发的，补齐必须有的，合并能合并的。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../17_HarnessEngineering总览/学习页.html"
  - text: "下一节"
    href: "../19_权限系统/学习页.html"
---

# 1. 为什么复杂系统需要消息规范化 {#why}
<!-- tag: preflight -->

早期 Agent loop 很简单：用户发一条消息，模型返回文本或工具调用，系统执行工具，再把工具结果追加到 messages。此时直接把 messages 发给 API 往往能跑。

但系统变复杂后，消息里会混入更多内部结构：工具超时、用户取消、压缩替换、调试元数据、不同 SDK 返回的 block 对象、工具结果引用。内部结构对 Harness 有用，但 API 未必接受。API 是后端协议，它只接受规定字段和合法顺序；不合法就会返回 400。

这里有两个术语要分清：内部消息是 Harness 自己维护的运行时结构，可以带调试字段和对象引用；API 消息是发给模型服务的协议载荷，只能包含服务端声明接受的 role、content 和 block 字段。规范化就是从前者转换到后者。

## box warn
### 直接发送内部消息
可能包含 `_iteration`、SDK 对象、空 content、未配对 tool_use 等 API 不接受的内容。

## box
### 发送前规范化
把内部消息转换成 API 协议格式，提前消除可预期的 Bad Request。

你现在应该能做什么：判断消息规范化是“请求前预处理”，不是模型推理本身。


---

# 2. API 协议的三类约束 {#rules}
<!-- tag: protocol -->

视频提到三个典型要求。第一，user 和 assistant 消息需要严格交替；如果连续两个 user 或连续两个 assistant，可能要合并。第二，assistant 发出的 tool_use 必须有对应的 user tool_result；否则模型看到了工具调用，却没有看到工具结果。第三，只能发送协议定义字段，内部元数据会导致 400。

tool_use 和 tool_result 是一个闭环：assistant 说“我要调用某工具”，系统执行后必须以 user 角色把结果放回去，并用同一个 tool_use_id 对上。缺少这一步，模型下一轮就不知道工具到底成功、失败还是被取消。

约束为什么需要规范化动作

role 交替API 期望对话结构稳定。合并相邻同 role 消息。
tool_use 配 tool_result工具调用必须有结果闭环。收集已有 result id，检查缺失结果。
字段白名单API 不接受内部调试字段。过滤 `_` 开头或协议外字段。

协议直觉：只发送 API 能理解的消息JSON

```text
[
 {"role": "user", "content": "创建 greet.py"},
 {"role": "assistant", "content": [
 {"type": "tool_use", "id": "toolu_1", "name": "write_file", "input": {"path": "greet.py"}}
 ]},
 {"role": "user", "content": [
 {"type": "tool_result", "tool_use_id": "toolu_1", "content": "created"}
 ]}
]
```


---

# 3. 规范化流程：清理、校验、合并 {#pipeline}
<!-- tag: pipeline -->

规范化函数通常分三步。第一步遍历消息，构造 clean list：保留 role 和 content，把 content block 中 API 不接受的字段剥掉。第二步收集已有 tool_result 的 `tool_use_id`，用于判断 assistant 消息中的 tool_use 是否都有结果。第三步合并相邻同 role 消息，让 user/assistant 更接近 API 期望的交替结构。

内部消息
过滤元数据
检查工具结果
合并role


内部消息
说明会显示在这里。
1. 过滤内部字段
2. 收集 tool_result id
3. 合并相邻同 role
规范化不是越激进越好。它应该移除协议不接受的部分，同时保留模型继续推理所需的有效内容。

伪代码：normalize_messages 的基本意图Python

```text
def normalize_messages(messages):
 clean = []
 for message in messages:
 clean.append(strip_internal_metadata(message))

 existing_results = collect_tool_result_ids(clean)
 clean = drop_or_fix_unmatched_tool_use(clean, existing_results)

 return merge_adjacent_same_role(clean)
```

Worked example：连续 user 消息合并Before / After

```text
Before:
 user: "创建 greet.py"
 user: [{"type": "tool_result", "tool_use_id": "toolu_1", "content": "created"}]

After:
 user: [
 {"type": "text", "text": "创建 greet.py"},
 {"type": "tool_result", "tool_use_id": "toolu_1", "content": "created"}
 ]

注意：合并 role 不等于改写语义，只是把相邻同角色内容整理成协议可接受的形状。
```


---

# 4. 调试暴露的 bug：把有效 content 清空 {#bug}
<!-- tag: bug -->

视频调试里，第一次发送用户 prompt 没变化。第二轮 messages 有三条：用户 prompt、assistant 返回的 thinking/tool_use block、user 工具结果。规范化时，代码试图把 content block 当 dict 处理，并过滤 `_` 开头字段。但实际 SDK 返回的 thinking block、tool_use block 可能对象或特殊 block 类型。

结果是：assistant 的 content 被处理成空 list。随后系统把空 assistant content 发送给 API，触发 400 Bad Request。这个 bug 说明：规范化层如果写得不够鲁棒，反而会破坏原本有效的 tool_use 链路。

错误示例：只处理 dict，导致 block 被清空Bug

```text
for block in message["content"]:
 if isinstance(block, dict):
 block_item = {k: v for k, v in block.items() if not k.startswith("_")}
 content.append(block_item)

# 如果 block 是 SDK block 对象而不是 dict，这里就什么也不保留。
# assistant content 可能变成 []，下一轮请求直接 400。
```

更稳思路：先把 SDK block 转成协议 dict，再过滤字段Python

```text
def normalize_block(block):
 if hasattr(block, "model_dump"):
 block = block.model_dump()
 elif hasattr(block, "to_dict"):
 block = block.to_dict()
 elif not isinstance(block, dict):
 block = {"type": "text", "text": str(block)}

 return {k: v for k, v in block.items() if not k.startswith("_")}
```

排查这类 bug 的最小流程是：记录规范化前后的消息数量、每条消息的 role、content block 类型、tool_use_id / tool_result_id 列表；然后比较哪一步把有效 block 变成了空数组。不要只看最终 400，要沿数据流往前追。

你现在应该能做什么：看到 400 错误时，不只怪模型或 API，而会检查 messages 在规范化前后是否丢失了关键内容。


---

# 5. 常见误区：规范化不是简单删除 {#pitfall}
<!-- tag: pitfalls -->

消息规范化的目标是“转换成合法协议”，不是“看不懂就删掉”。尤其是 tool_use、thinking、text、tool_result 等 block，可能来自不同 SDK，有对象形式、dict 形式或 Pydantic model 形式。正确做法是先统一表示，再过滤字段。

误删内容把非 dict block 全部丢弃，导致 assistant content 为空。
漏删元数据把 `_iteration` 等内部字段发给 API，可能触发 400。
忽略配对tool_use 没有 tool_result，下一轮模型上下文断裂。

更稳的规范化还要保留足够日志：规范化前消息数量、规范化后消息数量、丢弃了哪些字段、是否出现空 content、是否存在未配对 tool_use。这样一旦出错，能快速定位是 API 协议问题还是规范化逻辑问题。

规范化层的边界也要明确：它可以修正格式、剥离内部字段、合并相邻角色，但不应该偷偷改用户意图、伪造工具结果或吞掉错误。遇到无法安全修复的结构，应返回明确错误，让上层决定重试、压缩还是请求用户确认。


---

# 综合练习 {#practice}
<!-- tag: 检查理解 -->

<!-- practice-group: 一、为什么规范化 -->

### ? 选择

**Q:** 1. 消息规范化最主要发生在什么时候？

- [ ] . 发送给模型 API 之前
- [ ] . 模型训练时
- [ ] . Git 提交之后


### ? 解释

**Q:** 2. 为什么内部 messages 直接发给 API 可能导致 400？


### ? 清单

**Q:** 3. 系统变复杂后，messages 里可能混入哪些内部结构？列 3 项。


### ? 短答

**Q:** 4. 已知 API 协议要求后，为什么在 Harness 侧先预处理有价值？


<!-- end-practice-group -->

<!-- practice-group: 二、协议规则 -->

### ? 填空

**Q:** 5. 本节提到的三类 API 协议约束是什么？


### ? 解释

**Q:** 6. 如果连续两个 user 消息，规范化可以怎么处理？


### ? 机制

**Q:** 7. 为什么 tool_use 必须有对应 tool_result？


### ? 判断

**Q:** 8. `_iteration` 这类内部字段应原样发给 API。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 三、流程与 bug -->

### ? 流程

**Q:** 9. 写出 normalize_messages 的三步基本流程。


### ? 解释

**Q:** 10. 收集 existing tool_result ids 时为什么适合用 set？


### ? 诊断

**Q:** 11. 调试里 assistant content 为什么会变成空 list？


### ? 修复

**Q:** 12. 更稳的 block 规范化应该先做什么，再过滤字段？


<!-- end-practice-group -->

<!-- practice-group: 四、诊断迁移 -->

### ? 清单

**Q:** 13. 遇到 API 400，你会检查 messages 的哪些点？列 5 项。


### ? 边界

**Q:** 14. 为什么 assistant content 为空可能导致请求失败？


### ? 设计

**Q:** 15. 为 normalize_messages 设计一组调试日志，应该记录什么？


### ? 迁移

**Q:** 16. 如果换了另一个模型 SDK，消息规范化层最容易出什么兼容问题？


### ? 表达

**Q:** 17. 用 3-5 句话解释消息规范化在 Agent Harness 中的价值。


### ? 费曼解释

**Q:** 18. 用 6-9 句话讲清楚：消息规范化为什么必要，以及调试中的 bug 说明了什么。


<!-- end-practice-group -->


---

