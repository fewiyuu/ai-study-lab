---
course: "Claude Code 工作流"
unit: "20"
title: "20 Hook系统｜Claude Code 工作流"
kicker: "Hook system"
summary: "Hook 是系统在固定主流程中预留的扩展口。Claude Code 工作流 这一节用 `.hooks.json` 配置 `session_start`、`pre_tool_use`、`post_tool_use` 三个时机，让你不修改 Agent loop 主体，也能在会话开始、工具执行前、工具执行后插入命令、记录日志、阻止操作或补充上下文。"

side_note: "Hook 的关键不是改主循环，而是在主循环预留的关键时机插入自定义逻辑。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../19_权限系统/学习页.html"
  - text: "下一节"
    href: "../21_记忆系统/学习页.html"
---

# 1. Hook 是固定流程里的扩展口 {#concept}
<!-- tag: extension point -->

Hook 的直觉：框架把主流程定死，在关键节点留下口子，让使用者插入自定义逻辑，而不必修改框架源码。Vue 的生命周期、Android Activity 生命周期、测试框架的 setup/teardown、Web 框架 middleware、模板方法模式里的抽象步骤，都可以用这个直觉理解。

Agent Harness 里的 Hook 也一样。Agent loop 仍然负责发请求、解析 tool use、执行工具、回填结果；Hook 只是在某些时机被调用。这样既保持主流程稳定，又允许用户或插件扩展行为。

边界也要清楚：Hook 是旁路扩展，不是新的主流程。它可以观察、记录、阻断或补充上下文，但不应该悄悄替代 Agent loop 的工具执行、权限检查和消息回填。

生命周期在 create、mount、destroy 等节点插入逻辑。
中间件请求和响应经过一层层 middleware。
模板方法主流程固定，局部步骤交给子类实现。

你现在应该能做什么：用“固定主流程 + 预留扩展点”解释 Hook。


---

# 2. 本节三个 Hook 时机 {#events}
<!-- tag: events -->

代码里初始化了一个 hooks 字典，包含三个 key：`pre_tool_use`、`post_tool_use`、`session_start`。每个 key 对应一个列表，可以挂多个 Hook。Hook 配置来自 `.hooks.json`。

Hook 配置示例JSON

```text
{
 "session_start": [
 {"command": "echo session start ok"}
 ],
 "pre_tool_use": [
 {"matcher": "bash", "command": "echo before bash"},
 {"matcher": "read_file", "command": "echo before read"}
 ],
 "post_tool_use": [
 {"matcher": "*", "command": "echo post tool use ok"}
 ]
}
```

Hook触发时机典型用途

`session_start`会话开始时加载环境、记录启动、注入初始上下文。
`pre_tool_use`工具执行前检查、阻止、改写、记录即将执行的操作。
`post_tool_use`工具执行后记录结果、上报可观测性、补充消息。

选择 Hook 时机可以按问题反推：需要准备环境，用 `session_start`；需要在危险动作前把关，用 `pre_tool_use`；需要记录实际结果或写审计，用 `post_tool_use`。同一需求不要随便挂到所有事件上，否则会重复执行或造成难查的副作用。


---

# 3. `run_hooks` 如何运行 Hook {#runtime}
<!-- tag: runtime -->

`run_hooks(event, context)` 是核心。它先创建 result：`blocked=false`、`messages=[]`。然后检查 workspace trust。如果当前工作区不可信，Hook 不运行。可信后，根据 event 取出对应 Hook 列表，按 matcher 判断是否匹配当前工具，再把 context 写入环境变量并运行 command。

session_start
pre_tool_use
run tool
post_tool_use


session_start
说明会显示在这里。
1. check workspace trust
2. matcher 筛选
3. 注入环境变量
4. 执行 command
5. 结果注入 messages/output
pre 可以阻止工具，post 可以把工具结果之后的额外信息补回上下文。

伪代码：Hook 运行结果Python

```text
result = {"blocked": False, "messages": []}

if not check_workspace_trust():
 return result

for hook in hooks[event]:
 if not matches(hook.get("matcher"), context):
 continue

 env["HOOK_EVENT"] = event
 env["HOOK_TOOL_NAME"] = context.get("tool_name", "")
 env["HOOK_TOOL_INPUT"] = json.dumps(context.get("tool_input", {}))
 env["HOOK_TOOL_OUTPUT"] = context.get("tool_output", "")[:10000]

 output = run_command(hook["command"], env=env)
 result = merge_hook_output(result, output)
```

Worked example：阻止访问密钥文件pre_tool_use

```text
context:
 event = "pre_tool_use"
 tool_name = "read_file"
 tool_input.path = ".env"

hook:
 matcher = "read_file"
 command = "check-sensitive-path"

hook output:
 {"blocked": true, "messages": ["Blocked reading .env because it may contain secrets."]}

Agent loop:
 不执行 read_file
 把 blocked message 作为工具结果或上下文反馈给模型
 让模型改用安全路径或请求用户确认
```


---

# 4. 信任标记、matcher、blocked 与 message {#trust}
<!-- tag: control -->

Hook 可以执行本地命令，所以必须先有工作区信任。视频中为了 debug 创建了类似 `.claude/.claude_trusted` 的标记文件，否则 `check_workspace_trust` 会拒绝运行 Hook。这是安全设计：不可信工作区里的 Hook 不应该随便执行命令。

matcher 决定 Hook 是否命中当前工具。`matcher="bash"` 只匹配 Bash；`matcher="read_file"` 只匹配读文件；`matcher="*"` 匹配所有工具。未匹配的 Hook 会 continue。

Hook 命令输出如果是普通文本，可以仅打印或记录；如果是结构化 JSON，可以携带 `blocked` 和 `messages`。`blocked=true` 时，pre Hook 可以阻止工具执行；messages 可以补充到模型上下文，让模型知道 Hook 发现了什么。

messages 的定位是“给模型看的补充信息”，不是给用户看的最终报告。它应该短、明确、可操作，例如说明为什么被阻止、下一步可选什么；不要把大量日志或完整文件内容塞进去，避免污染上下文。

结构化 Hook 输出：阻止危险操作JSON

```text
{
 "blocked": true,
 "messages": [
 "pre_tool_use blocked this command because it touches a forbidden path."
 ]
}
```

你现在应该能做什么：判断一个 Hook 是记录型、增强上下文型，还是阻断型。


---

# 5. 常见误区：Hook 不是把所有逻辑塞进主循环 {#pitfall}
<!-- tag: pitfalls -->

Hook 的价值是开闭原则：对主循环修改关闭，对扩展开放。你不需要改 Agent loop，就能在关键时机加日志、检查、通知、上下文注入、记忆写入或安全阻断。

## box warn
### 错误做法
每新增一个需求，就往主循环里加 if/else，最终 loop 变成杂乱控制中心。

## box
### Hook 做法
主循环只负责触发事件；扩展逻辑放进配置和 Hook command。

错误示例：不可信工作区直接运行 Hook安全风险

```text
# 风险：任意项目里的 .hooks.json 都能执行本地命令
for hook in hooks[event]:
 run_command(hook["command"])

# 更稳：先检查 workspace trust，再运行 Hook。
```

设计 Hook 时还要避免隐藏副作用。记录型 Hook 应尽量只写日志；阻断型 Hook 要返回明确原因；增强上下文型 Hook 要控制输出长度。Hook 一旦能修改文件、发网络请求或执行命令，就必须纳入权限和审计视野。


---

# 综合练习 {#practice}
<!-- tag: 检查理解 -->

<!-- practice-group: 一、Hook 直觉 -->

### ? 短答

**Q:** 1. 用一句话定义 Hook。


### ? 清单

**Q:** 2. 列出 3 个传统软件开发中的 Hook 类比。


### ? 解释

**Q:** 3. Hook 如何体现开闭原则？


### ? 比较

**Q:** 4. Hook 和 Web 中间件有什么相似之处？


<!-- end-practice-group -->

<!-- practice-group: 二、三个时机 -->

### ? 填空

**Q:** 5. 本节代码定义了哪三个 Hook event？


### ? 用途

**Q:** 6. `session_start` 适合做什么？


### ? 用途

**Q:** 7. `pre_tool_use` 为什么适合做安全检查或阻断？


### ? 用途

**Q:** 8. `post_tool_use` 适合记录哪些信息？


<!-- end-practice-group -->

<!-- practice-group: 三、运行机制 -->

### ? 解释

**Q:** 9. 为什么运行 Hook 前要检查 workspace trust？


### ? 解释

**Q:** 10. matcher 如何决定某个 Hook 是否运行？


### ? 清单

**Q:** 11. Hook command 可以从哪些环境变量拿到上下文？


### ? 机制

**Q:** 12. `blocked=true` 在 pre hook 中意味着什么？


<!-- end-practice-group -->

<!-- practice-group: 四、设计迁移 -->

### ? 设计

**Q:** 13. 设计一个 post_tool_use 日志 Hook，它应该记录什么？


### ? 迁移

**Q:** 14. 如何用 session_stop 或 post_tool_use Hook 写入长期记忆？


### ? 设计

**Q:** 15. 设计一个 pre_tool_use Hook，用来阻止访问某个敏感路径。


### ? 解释

**Q:** 16. 为什么 Hook 不应该让主循环变得更复杂？


### ? 表达

**Q:** 17. 用 3-5 句话解释 Hook 系统在 Agent Harness 中的价值。


### ? 费曼解释

**Q:** 18. 用 6-9 句话讲清楚：Hook 如何让 Agent 在不改主循环的情况下扩展能力。


<!-- end-practice-group -->


---

