---
course: "PyTorch 深度学习实践 刘二大人"
unit: "11 卷积神经网络高级篇：Inception、残差与读代码"
title: "11｜卷积神经网络高级篇：Inception、残差与读代码"
kicker: "PYTORCH STUDY PAGE"
kicker_en: "LESSON 11"
summary: "理解复杂 CNN 结构如何通过模块化减少重复，掌握 Inception 多分支、1x1 卷积、残差连接和阅读开源模型代码的基本方法。"

side_note: "来源：P11【11.卷积神经网络（高级篇）】。本页按当前 PyTorch 习惯整理，旧课写法中容易过时的部分已换成更稳的表达。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../10_卷积神经网络基础：通道、卷积核与池化/学习页.html"
  - text: "下一节"
    href: "../12_循环神经网络基础：序列、隐藏状态与RNNCell/学习页.html"
---

# 概念骨架 {#map}
<!-- tag: 先搭主线 -->

NCHW→卷积→通道→池化→展平→分类头

## box info
**先看输入**
确认数据形状、标签形式和 dtype。许多训练错误在进入模型前已经发生。


**再看变换**
逐层追踪输出形状和语义，别只看 API 名字。每一层都应该能回答“它把什么变成什么”。


**最后看目标**
确认 loss 的输入约定、输出是否是标量，以及评估指标是否对应真实任务。
判断句：如果你说不清某一行读入什么、输出什么、改变什么状态，就先不要急着调参。

**训练主线**：数据 → 模型预测 → 标量 loss → backward 写梯度 → optimizer.step 更新参数 → eval 检查泛化。

<!-- interactive: ['shape-tracer'] → keep as HTML -->


---

# 分支追踪 {#trace}
<!-- tag: 先分支，再合并 -->

| 步骤 | 读什么 | 写什么 | 先查什么 |
| --- | --- | --- | --- |
| 输入 | [B,C,H,W] | 同一批特征图 | 分支是否共享入口 |
| 分支输出 | [B,C1,H,W] / [B,C2,H,W] | 不同卷积核 | 高宽是否一致 |
| 拼接后 | [B,C1+C2,H,W] | 通道变厚 | 只沿 dim=1 合并 |
| 残差相加 | [B,C,H,W] | 逐元素相加 | 两边 shape 是否完全对齐 |
| 1x1 conv | [B,C',H,W] | 对齐通道或压计算量 | shortcut 是否需要调整 |

## box info
### 怎么读
先把每个分支写成一行：输入是什么，输出是什么，最后是拼接还是相加。只要这四件事写清，Inception 和残差块的 forward 就不会乱。

## box warn
### 出错会怎样
分支高宽不一致时，`torch.cat` 直接报错；shortcut 的通道或步幅不一致时，`+` 也会失败。不要先盯参数名，先查 shape。

## box soft
拿一个最小例子练习：分支 A 走 1x1，分支 B 走 3x3，分支 C 走池化，再把三路结果拼起来。你只要能说出每一路的输入和输出，就能判断为什么某条分支需要 padding，为什么某条 shortcut 需要 1x1 conv。
这一步的目标学会把结构翻译成数据流。以后再看到更复杂的 block，你也能先画图、再对齐、最后才读参数。


---

# 讲义 {#lecture}
<!-- tag: 慢读这一部分 -->

## box soft
### 1. 复杂网络先需要模块化
CNN 变深后，如果把所有卷积层都堆在一个 `forward` 里，代码会很长，也很难检查。模块化的做法是把重复结构写成小的 `nn.Module`，再在大模型中复用。
模块化为了降低认知负担。你可以先读一个 block 的输入输出，再读整体网络如何串联这些 block。后续读 ResNet、Inception、DenseNet 都需要这种方法。
你现在应该能把复杂模型拆成“模块内部”和“模块之间”两层来读。
读到这里，你应该能：用这一小节的判断规则去检查相关代码。

## box soft
### 2. Inception 的核心是多分支并行
Inception 模块的想法是：同一份输入可以走不同尺度的卷积分支，例如 1x1、3x3、5x5 和池化分支。每个分支提取不同感受野的特征，最后在通道维拼接起来。
这里最重要的 shape 判断是：分支输出的高宽必须一致，才能在通道维 `dim=1` 拼接。拼接后通道数等于各分支通道数之和。
你现在应该能解释 Inception 为什么是“宽”的结构，以及 `torch.cat(outputs, dim=1)` 为什么沿通道维拼接。
读到这里，你应该能：用这一小节的判断规则去检查相关代码。

## box soft
### 3. 1x1 卷积常用于改变通道数
1x1 卷积不看邻域，只在同一个空间位置上混合通道。它常用于降维或升维，减少后续大卷积的计算量，也能增加非线性组合。
例如先用 1x1 把通道从 192 降到 16，再接 5x5 卷积，就比直接对 192 通道做 5x5 便宜得多。这里的“降维”是降通道，不是降图像高宽。
你现在应该能说清 1x1 卷积通道变换工具。
读到这里，你应该能：用这一小节的判断规则去检查相关代码。

## box soft
### 4. 残差连接解决深层网络难训练问题
网络很深时，直接堆层可能出现训练困难。残差连接让模块学习 `F(x)`，再把输入 `x` 加回输出，形成 `F(x) + x`。如果额外层暂时学不到好东西，至少可以接近恒等映射。
残差相加要求形状一致。如果通道数或空间尺寸不同，需要用 1x1 卷积或 stride 调整 shortcut。否则直接相加会报 shape 错误。
你现在应该能判断残差连接中的主分支和 shortcut 是否能相加。
读到这里，你应该能：把训练 step 拆成前向、loss、清梯度、反向和更新。

## box soft
### 5. 读开源模型先看 forward 数据流
课程后半提醒不要只追求“跑通代码”，要先读代码。读复杂模型时，第一轮不要陷入每个参数，而是沿 `forward` 画数据流：输入经过哪些模块，在哪些地方分支，在哪些地方拼接或相加。
第二轮再看每个模块的输入输出通道。第三轮看训练代码和损失函数。这样读会比从文件第一行一路看到最后更稳。
你现在应该能拿一个陌生 CNN，先画出主干路径、分支点、合并点和分类头。
读到这里，你应该能：区分样本读取、batch 组织和模型训练三件事。

## box soft
### 6. 复杂结构仍服务于同一个训练目标
Inception、残差、1x1 卷积都是模型结构层面的改进。它们不会改变训练链路：输出 logits，交叉熵计算 loss，反向传播更新参数。
不要因为结构高级，就忽略基础检查。复杂网络出错时，shape、dtype、训练/评估模式、loss 输入约定仍然是第一批排查点。
你现在应该能把高级 CNN 结构放回普通 PyTorch 训练流程，而不是把它们当成孤立技巧。
读到这里，你应该能：把训练 step 拆成前向、loss、清梯度、反向和更新。

## box soft
### 完整知识链路
从单一路径 CNN 扩展到模块化、多分支和跳连结构。 它仍然落在同一条 PyTorch 主线上：先准备能被模型读取的数据，再让模型输出符合任务的张量，用合适的损失函数得到标量 loss，反向传播写入梯度，优化器更新参数，最后在评估模式下检查结果。换任务时，最先变化的是输入形状、模型输出含义和 loss 输入约定。


---

# 本页边界 {#boundary}
<!-- tag: 收住范围 -->
<!-- layout: grid2 -->

## box warn
### 本页只处理
多分支结构、通道拼接、残差相加、1x1 conv 对齐，以及按 forward 追数据流。你只要能把输入、输出和合并方式说清，就算抓住这一讲的主线。

## box info
### 后面再接什么
第 12 讲转到序列、hidden state 和 RNNCell，第 13 讲再看 Embedding、GRU 和变长序列分类。CNN 这一页先不混入序列模型的细节。

## box soft
如果你在读代码时卡住，回到这页只做三件事：先标出每个分支的输入输出，再看哪里做拼接或相加，最后检查哪一层负责对齐 shape。这个顺序比先背模块名字更稳。
做题时也照这个顺序写答案：先写合并方式，再写 shape 约束，最后写常见故障。这样你的回答会直接落到可检查的判断上，而不是停在概念层。
你现在应该能把 Inception、残差和 1x1 conv 放回同一张数据流图里读，不再只记名字。
如果你要自己改一个 block，先把每条分支的输出写成表，再决定是拼接还是相加，最后才去调 1x1 conv 或 shortcut。这样改代码时，你知道自己在修哪一个接口，而不是在黑盒里碰运气。
遇到陌生源码时，这个顺序还能直接迁移到别的 block：先确认输入 shape，再确认每支输出，再看合并方式，最后才判断是否需要对齐层。把它当成固定动作，比临场猜参数更可靠。


---

# 术语表 {#terms}
<!-- tag: 别混词 -->

| 术语 | 直觉与定义 | 常见误解 |
| --- | --- | --- |
| Inception | 多分支卷积模块，提取不同尺度特征后拼接。 | 误以为只是多堆几层。 |
| 1x1 卷积 | 在每个空间位置上混合通道，常用于通道变换。 | 以为没有空间窗口就没有意义。 |
| 残差连接 | 把输入通过 shortcut 加到模块输出上。 | 忽略相加前 shape 必须一致。 |
| 模块化 | 把重复网络结构封装为可复用 Module。 | 误以为只是代码风格问题。 |

### 易混概念对照

| 概念 | 准确含义 | 不要混成 |
| --- | --- | --- |
| cat | 通道拼接，通道数相加 | 不是逐元素相加 |
| add | 残差逐元素相加 | 要求 shape 一致 |
| 1x1 conv | 改变通道 | 通常不改高宽，除非 stride 改变 |


---

# 代码与例子 {#examples}
<!-- tag: 逐行读语义 -->

读代码时先标出输入、输出和状态变化。能解释这些，才算真正看懂。

Inception 拼接示意示例代码

```python
class InceptionBlock(nn.Module):
 def __init__(self, in_ch):
 super().__init__()
 self.b1 = nn.Conv2d(in_ch, 16, kernel_size=1)
 self.b2 = nn.Conv2d(in_ch, 24, kernel_size=3, padding=1)
 self.b3 = nn.Conv2d(in_ch, 24, kernel_size=5, padding=2)

 def forward(self, x):
 y1 = torch.relu(self.b1(x))
 y2 = torch.relu(self.b2(x))
 y3 = torch.relu(self.b3(x))
 return torch.cat([y1, y2, y3], dim=1) # 通道数相加
```

残差连接要求形状一致示例代码

```python
def forward(self, x):
 identity = x
 out = torch.relu(self.conv1(x))
 out = self.conv2(out)
 out = out + identity
 return torch.relu(out)
```

用 1x1 卷积调整 shortcut示例代码

```python
self.shortcut = nn.Conv2d(in_channels, out_channels, kernel_size=1, stride=2)
# 当主分支改变通道或空间尺寸时，shortcut 也要同步调整
```


---

# 误区诊断 {#pitfalls}
<!-- tag: 错因 / 后果 / 修法 -->
<!-- layout: grid2 -->

## box warn
### 分支高宽不一致还拼接
**后果：**torch.cat 会报 shape 错误。
**修法：**用 padding/stride 保持高宽一致。

## box warn
### 拼接维度写错
**后果：**沿 batch 或空间维拼接会破坏语义。
**修法：**CNN 通道拼接通常 `dim=1`。

## box warn
### 残差相加前 shape 不同
**后果：**无法逐元素相加。
**修法：**用 1x1 conv 调整 shortcut。

## box warn
### 以为 1x1 卷积没用
**后果：**忽略通道混合和降维作用。
**修法：**从通道维理解它。

## box warn
### 只跑代码不读结构
**后果：**换模型或排错时无从下手。
**修法：**先沿 forward 画数据流。

## box warn
### 把训练循环当成背诵模板
**后果：**模板能跑不等于能诊断。每一行都要知道它读什么、写什么、改变哪个状态。
**修法：**把每行标成前向、loss、清梯度、反向或更新。

## box warn
### 只看 loss 下降
**后果：**loss 下降只能说明优化目标变小，还要检查数据、标签、输出形状和评估指标是否对应任务。
**修法：**同时检查验证集指标和预测样例。

## box warn
### 混用训练和评估状态
**后果：**`model.train()` 和 `model.eval()` 会影响 Dropout、BatchNorm 等层，评估时还要配合 `torch.no_grad()`。
**修法：**训练和评估写成两个清楚的函数。


---

# 完整样例 {#worked-example}
<!-- tag: 一条完整教学链 -->

## box soft
### 完整样例：Inception 和残差在一条账本里怎么读
Inception 的关键“分支很多”。同一个输入会进入不同卷积核或不同尺度的路径，最后沿通道维拼在一起。只要分支在高宽上对齐，`torch.cat(..., dim=1)` 就能把它们接成一个更厚的特征图。
Residual 的关键也“相加前要先对齐”。`shortcut` 这条线把输入直接送到后面，和主分支逐元素相加。两边 shape 对不上时，`1x1` 卷积经常被拿来调通道和步幅。
读这类代码时，要先问自己：每个分支读什么、写什么、最后是拼接还是相加。把这个问题答清楚，后面的 `forward` 就不再像一串抽象 API，而是几条并行的数据流。
一旦你把 `cat` 和 `+` 分开，调试也就有了起点。拼接错多半是空间尺寸不一致；相加错多半是通道或步幅不一致。先把错归类，再看具体参数，修复会快很多。

## box info
### 输入和输出怎么对齐
先把输入 shape、标签 dtype、输出维度和训练目标写成同一张账本，后面再看 API。

## box warn
### 最常见的失配
这类页最常见的问题输入和输出的约定写反。先查 shape，再查 dtype，最后才查 loss。

对象shape / 语义要读什么常见失配分支 A[B,16,H,W]局部特征和其他分支拼通道分支 B[B,24,H,W]更大感受野保持高宽一致拼接[B,40,H,W]沿 dim=1 合并通道相加不是相加数值残差[B,C,H,W]逐元素相加两边 shape 必须一致

最小可执行片段复制

```python
import torch

y1 = branch1(x)
y2 = branch2(x)
y3 = branch3(x)
merged = torch.cat([y1, y2, y3], dim=1)
out = merged + shortcut
print(merged.shape, out.shape)
```

## box warn
### 故障现场
如果你看到训练能跑但结论不稳，先回到这个样例把 logits、标签和 shape 再核一次。


---

# 故障链 {#bridge-chain}
<!-- tag: 从症状回到接口 -->

## box soft
### 第二条教学链：先分支，再汇合，再对齐
高级 CNN 读起来比基础 CNN 更绕，原因不在公式，而在“流向变多了”。Inception 把同一个输入切成多条路，每条路读到的东西不同，但最后一定要在高宽上对齐后再拼接。你看到的一个模型里的多种感受野。
残差块更容易误解。它把原始输入保留下来，和主分支的结果做逐元素相加。只要这两边 shape 不一致，程序就会报错。于是你会发现，高级结构的调试顺序和基础结构一样，还是先看 shape，再看语义。
对读代码的人来说，最有用的习惯把每个分支的输入、输出和连接方式写出来。`cat` 代表拼接，`+` 代表相加，`1x1` 卷积经常是对齐工具。只要这三个概念被你分清，Inception 和 ResNet 的源码就会比注释更直白。
这页的价值不只是认识两个经典模块，而是学会一条通用的追踪方法：先把多分支输出写成表，再检查哪里是通道拼接、哪里是逐元素相加，最后把它们放回主干网络。这样你读更复杂的网络时，不会被结构名字带偏，而是直接抓数据流。

症状你先查什么为什么先查它修法并行分支[B,C,H,W]不同卷积核读每支自己的输出通道拼接[B,C1+C2,H,W]merge 通道只沿 dim=1 合并残差相加[B,C,H,W]逐元素相加两边 shape 必须一致1x1 conv[B,C',H,W]调整通道对齐 shortcut 或压计算量

故障排查片段复制

```python
branch_a = conv1(x)
branch_b = conv3(x)
branch_c = pool(x)
merged = torch.cat([branch_a, branch_b, branch_c], dim=1)
if merged.shape != shortcut.shape:
 shortcut = adjust(shortcut)
out = merged + shortcut
```

## box info
### 练习时的用法
先把上面的症状、表格和代码各读一遍，再回到练习区。这样你写答案时不是在猜，而是在复述已经排过的链路。


---

# 一致性补充 {#consistency-note}
<!-- tag: 把接口写稳 -->

## box info
### 学习时的落点
这一段把训练、评估、调试和迁移时应该固定住的检查顺序收束成一张表。
Inception 和 ResNet 这类块，最怕你把结构名记住了，却没把数据流记住。多分支结构里，输入会被复制到不同路径；残差结构里，原输入还会保留一条 shortcut。读源码时，如果你不能说清楚每条路读什么、写什么，后面就很容易在拼接和相加之间弄混。
一个实用的判断方法，是先看输出是拼接还是相加。拼接一般意味着通道维变厚，但高宽不变；相加一般意味着 shape 要完全对齐。于是你能反推出，拼接类模块在设计时更关注多尺度特征，而残差类模块更关注信息直通和梯度路径。
当你在调试高级 CNN 时，最常见的分支对齐错。某个分支少了 padding，另一个分支 stride 不一样，最后拼接或相加就炸了。别急着换块，先把每个分支的输入输出打印出来，再检查哪里没对齐。
如果你愿意把每个 block 都写成一张表，读代码的速度会快很多。表里只要有输入、输出、连接方式、常见错误这四项，绝大多数卷积结构都能被你拆开。结构再复杂，也只是这几种动作的重复和组合。
这也是为什么高级 CNN 依旧能用普通 CrossEntropy 训练：无论内部怎么分支、怎么跳连，最后都还是输出 logits。只要这个输出形状和任务类别数一致，损失函数就不需要为了结构复杂而改变。
在看开源模型时，最好先定位 `forward` 的入口和出口，再回头看中间每个分支。你不用一开始就理解所有模块，只要先知道哪条路是主干、哪条路是旁路、最后在哪里合并，后面每个层的作用就会自然清楚。
当你把这类结构读顺之后，再回去看 plain CNN，你会发现它其实就是多分支网络的简化版：少了旁路，少了拼接，但 shape 账本仍然是同一套。这个认识能让你在不同结构之间迁移得更快。
你还可以给高级 CNN 准备一份专属检查表：分支输入是否一致、分支输出是否同高宽、拼接后通道数有没有变厚、残差相加前有没有对齐。把这四步写在纸上，读任何 Inception 或 ResNet 代码都会更稳。
如果某个结构看着很复杂，先把它简化成‘并行计算 + 合并’这两个动作，再看它是拼接还是相加。这样你不会被模块名字吓住，而是会直接把注意力放到张量流向上。

场景先看什么为什么下一步分支输入[B,C,H,W]复制同一份张量注意每支自己的卷积参数拼接输出[B,C',H,W]沿通道合并高宽必须先对齐残差输出[B,C,H,W]逐元素相加shape 要完全相同1x1 对齐[B,C'',H,W]调整通道或步幅shortcut 常用它补接口

收束后的检查脚本复制

```python
y1 = self.branch1(x)
y2 = self.branch2(x)
y3 = self.branch3(x)
merged = torch.cat([y1, y2, y3], dim=1)
if merged.shape != x.shape:
 x = self.shortcut(x)
out = merged + x
print(merged.shape, out.shape)
```

## box warn
### 最后一遍检查
把这页的表、代码和练习题一起读完后，再回头看题目。这样你写答案时在复述一整条已验证过的数据流。


---

# 练习区 {#practice}
<!-- tag: 20 题 -->

<!-- practice-intro -->
## box
怎么写答案
选择和判断题先给结论；短答题写 1-3 句话；诊断题按“错因、后果、修法”回答；最后的费曼题用完整段落串起主线。

## box
检查重点
每个答案尽量落到形状、数据流、loss 输入约定或参数状态上。只写“因为不对”通常不够。

<!-- end-practice-intro -->

<!-- practice-group: 基础概念 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 1 选择

**Q:** 1. Inception 多分支输出通常沿哪个维度拼接？

- [ ] 通道维 dim=1
- [ ] batch 维 dim=0
- [ ] 高度维 dim=2

Answer: 通道维 dim=1
Explain: 按 NCHW 判断。

### 2 判断

**Q:** 2. 残差连接相加前两个张量 shape 可以随意不同。

Explain: 错误。残差连接通常做逐元素相加，两个张量的 batch、通道、高、宽都要能对齐；如果通道或空间尺寸不同，需要用 shortcut 分支、1x1 卷积或 stride 调整后再相加。

### 3 填空

**Q:** 3. 1x1 卷积常用于改变 ______ 数。

Explain: 1x1 卷积常用于改变通道数。它在每个空间位置上混合通道，通常不改变高宽，因此适合做降维、升维或 shortcut 的通道对齐。

<!-- end-practice-group -->

<!-- practice-group: 形状推导 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 4 短答

**Q:** 4. 三个分支输出通道分别是 16、24、24，高宽一致，拼接后通道数是多少？

Explain: 拼接后通道数是 16+24+24=64 。沿 NCHW 的通道维 dim=1 拼接时，高宽必须一致，通道数相加。

<!-- end-practice-group -->

<!-- practice-group: 代码阅读 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 5 短答

**Q:** 5. `torch.cat([y1,y2,y3], dim=1)` 为什么要求高宽一致？

Explain: torch.cat(..., dim=1) 只允许通道维不同；除拼接维以外的维度必须相同。因此三个分支的 batch、高、宽都要一致，否则无法把它们沿通道维合成一个张量。

### 6 短答

**Q:** 6. 残差代码里 `identity = x` 的作用是什么？

Explain: identity = x 保存输入，后面把主分支输出和这条 shortcut 相加。它让模块学习残差部分，而不是从零学习完整映射，也给梯度提供一条更直接的路径。

<!-- end-practice-group -->

<!-- practice-group: 错误诊断 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 7 诊断

**Q:** 7. Inception 拼接时报 size mismatch，优先检查什么？

Explain: 优先打印每个分支输出的 shape，重点看高宽是否一致。常见原因是某个分支的 kernel、padding 或 stride 让空间尺寸变了；修法是统一 padding/stride，或在拼接前把分支输出调整到同一高宽。

### 8 诊断

**Q:** 8. ResNet block 相加时报通道不一致，如何修？

Explain: 可以在 shortcut 分支加 1x1 conv ，把输入通道变成主分支输出通道；如果空间尺寸也变了，还要让 shortcut 使用相同 stride 或其他下采样方式，保证相加前 shape 对齐。

### 9 诊断

**Q:** 9. 把 `torch.cat` 误写成 `+` 会改变什么语义？

Explain: torch.cat 是把多个分支沿某个维度接起来，通道数通常会增加； + 是逐元素相加，shape 必须一致，通道数不会增加。把 cat 写成加法会把“保留多分支特征”变成“混合到同一形状”。

<!-- end-practice-group -->

<!-- practice-group: 场景应用 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 10 场景

**Q:** 10. 为什么 5x5 卷积分支前常放 1x1 卷积？

Explain: 5x5 卷积计算量大，先用 1x1 卷积降低通道数，可以减少后续 5x5 的参数和计算。它还会在每个空间位置上做通道混合，让分支进入大卷积前先压缩信息。

### 11 场景

**Q:** 11. 读陌生开源 CNN 时第一步看什么？

Explain: 第一步看 forward 的数据流：输入经过哪些模块、哪里分支、哪里拼接或相加、最后输出什么 shape。不要先陷进每个层的参数细节，先把张量流向和 shape 约束画出来。

<!-- end-practice-group -->

<!-- practice-group: 概念区分 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 12 匹配

**Q:** 12. 匹配 Inception / ResNet / 1x1 conv：多分支、跳连、通道变换。

Explain: Inception 对应多分支；ResNet 对应跳连或残差连接；1x1 conv 对应通道变换。读代码时可以先找这三类结构，再回头看每个分支的 shape 是否对齐。

### 13 短答

**Q:** 13. 拼接和残差相加的 shape 规则有什么不同？

Explain: 拼接只要求除拼接维外的维度一致，拼接维会相加；残差相加要求两个张量整体 shape 能逐元素对齐，输出 shape 通常不变。前者扩展特征，后者融合主分支和 shortcut。

<!-- end-practice-group -->

<!-- practice-group: 面试追问 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 14 短答

**Q:** 14. 残差连接为什么有助于训练深层网络？

Explain: 残差连接让模块可以学习“在输入上改多少”，如果某层暂时学不到有用变化，shortcut 仍能近似保留输入。它也让梯度有更短路径传回前面层，缓解深层网络训练困难。

### 15 短答

**Q:** 15. 模块化对读代码有什么帮助？

Explain: 模块化让你可以按局部输入输出读代码：每个 block 读什么 shape、写出什么 shape、内部是否分支或 shortcut。这样排错时能把问题定位到某个模块边界，而不是在整张网络里乱找。

<!-- end-practice-group -->

<!-- practice-group: 检查清单 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 16 清单

**Q:** 16. 调试多分支 CNN 时列 5 项检查。

Explain: 检查 5 项：每个分支输出 shape； cat 的维度是否是通道维；拼接后通道数是否等于各分支相加；shortcut 和主分支相加前 shape 是否一致；最后分类头是否按最终通道和高宽重算。

<!-- end-practice-group -->

<!-- practice-group: 代码修正 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 17 诊断

**Q:** 17. 两个残差分支空间尺寸不同，不能相加，给出一种修法。

Explain: 一种修法是在 shortcut 分支上使用和主分支一致的 stride 或下采样，让空间尺寸对齐；如果通道也不同，再加 1x1 卷积调整通道。关键是相加前两个张量 shape 能逐元素对应。

<!-- end-practice-group -->

<!-- practice-group: 迁移应用 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 18 短答

**Q:** 18. 为什么高级 CNN 结构仍然可以用普通 CrossEntropy 训练？

Explain: 因为不管中间结构多复杂，分类模型最后仍输出类别 logits。只要最后 shape 是 [batch,num_classes] ，标签是类别索引，就可以继续用普通 CrossEntropyLoss 训练。

<!-- end-practice-group -->

<!-- practice-group: 费曼解释 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 19 费曼

**Q:** 19. 用 6-9 句话解释：Inception 和残差连接分别解决什么问题。

Explain: Inception 用多个分支在同一层里观察不同尺度的特征，再沿通道维拼接，所以要求各分支高宽一致。1x1 卷积常放在大卷积分支前，用来压缩通道和减少计算。残差连接把输入通过 shortcut 加回主分支，帮助深层网络保留信息和传递梯度。残差相加要求两个张量 shape 对齐，不像拼接那样增加通道。读这类代码时，先画 forward 数据流，再标出每次 cat 或 add 的 shape 规则。报错时优先打印分支输出和 shortcut 输出。

<!-- end-practice-group -->

<!-- practice-group: 自我检查 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 20 短答

**Q:** 20. 学完本页后，你现在能做什么？

Explain: 你现在应该能读一个稍复杂的 CNN block：分辨多分支和残差连接，检查拼接维度，判断 shortcut 是否需要 1x1 卷积，并用 shape 打印定位 cat 或 add 的错误。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- [来源说明.md]()：本页的来源说明、锚点样本和教学意图。
- [10 讲](../10_卷积神经网络基础：通道、卷积核与池化/学习页.html)：CNN shape、通道和池化的基础读法。
- [12 讲](../12_循环神经网络基础：序列、隐藏状态与RNNCell/学习页.html)：后续序列模型的读法边界。
- [课程地图](../index.html)：本页在整门课中的位置。

---
