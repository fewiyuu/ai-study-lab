---
course: "PyTorch 深度学习实践 刘二大人"
unit: "13 循环神经网络高级篇：Embedding、GRU 与变长序列分类"
title: "13｜循环神经网络高级篇：Embedding、GRU 与变长序列分类"
kicker: "刘二大人课程精读"
kicker_en: "第 13 讲"
summary: "掌握名字分类任务的完整数据流：字符/名字编码、Embedding、GRU、最后隐藏状态、线性分类、padding/packing 与变长序列处理。"

side_note: "先把字符 id、Embedding、GRU、padding / packing 和最后隐藏状态连成一条线；更复杂的序列标注留到后面。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../12_循环神经网络基础：序列、隐藏状态与RNNCell/学习页.html"
---

# 概念骨架 {#map}
<!-- tag: 先搭主线 -->

序列→编码→隐藏状态→GRU/RNN→最后状态→分类

## box info
**先看输入**
确认数据形状、标签形式和 dtype。许多训练错误在进入模型前已经发生。


**再看变换**
逐层追踪输出形状和语义，别只看 API 名字。每一层都应该能回答“它把什么变成什么”。


**最后看目标**
确认 loss 的输入约定、输出是否是标量，以及评估指标是否对应真实任务。
判断句：如果你说不清某一行读入什么、输出什么、改变什么状态，就先不要急着调参。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

<!-- interactive: ['shape-tracer'] → keep as HTML -->

**循环更新**：hₜ = RNN(xₜ, hₜ₋₁)
序列分类常把最后隐藏状态送入线性层，得到类别 logits。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

<!-- interactive: ['shape-tracer'] → keep as HTML -->

<!-- interactive: ['shape-tracer'] → keep as HTML -->

<!-- interactive: ['shape-tracer'] → keep as HTML -->


---

# 变长追踪 {#trace}
<!-- tag: 把名字分类走完整 -->

| 步骤 | 读什么 | 写什么 | 为什么要看 |
| --- | --- | --- | --- |
| 字符 id | long 类型整数 | 词表索引 | 先把离散符号数字化 |
| Embedding | [B, S] | [B, S, E] | 把 id 查表成向量 |
| GRU | embedding 序列 | 最后 hidden | 读取序列依赖 |
| padding / packing | 变长 batch | 真实长度 lengths | 避免 pad 干扰 |

## box info
读法
这类题最先查的是词表、dtype、lengths 和 `hidden[-1]` 的 shape，而不是先追 `Linear`。


---

# 讲义 {#lecture}
<!-- tag: 慢读这一部分 -->

## box soft
### 1. 名字分类是整段序列分类任务
这一讲的任务是根据名字拼写判断所属语言或国家类别。输入看起来是一列名字，但每个名字本身是一串字符。模型要读完整个字符序列，再输出一个类别。
这类任务和 MNIST 一样是分类，但输入结构完全不同。MNIST 是图像张量，名字是变长字符序列。输出仍然可以是 `[batch, num_classes]` logits，标签仍然可以是类别索引。
你现在应该能把名字分类放进通用训练链路：序列输入、RNN/GRU 编码、线性层输出类别 logits、交叉熵训练。
读到这里，你应该能：沿时间步追踪输入、隐藏状态和最终输出。

## box soft
### 2. Embedding 把离散字符变成稠密向量
字符首先要映射成整数 id。`nn.Embedding(num_embeddings, embedding_dim)` 会把每个 id 查表成一个可学习向量。相比 one-hot，embedding 更低维，也能在训练中学到字符之间对任务有用的表示。
Embedding 的输入通常是 long 类型索引，输出会多出 embedding 维。例如输入 `[batch, seq_len]`，输出 `[batch, seq_len, embedding_dim]`。
你现在应该能说明为什么不能把字符串直接喂给 GRU，以及 embedding 在数据流中的位置。
读到这里，你应该能：用这一小节的判断规则去检查相关代码。

## box soft
### 3. GRU 用门控机制改善普通 RNN
普通 RNN 在长序列上容易遗忘或梯度变弱。GRU 引入门控机制，控制哪些信息保留、哪些信息更新。你不需要先手推门控公式，也要知道它的作用：让隐藏状态更新更可控。
PyTorch 的 `nn.GRU` 接收 embedding 后的序列，返回所有时间步输出和最后隐藏状态。做名字分类时，常取最后隐藏状态，再送入线性层得到类别 logits。
你现在应该能区分：Embedding 处理离散输入，GRU 处理序列依赖，Linear 处理分类输出。
读到这里，你应该能：用这一小节的判断规则去检查相关代码。

## box soft
### 4. 变长序列需要 padding 和长度信息
名字长短不一样，但一个 batch 里的张量必须能堆叠成矩形。常见做法是把短序列 padding 到同一长度，同时保留每个序列真实长度。
如果直接把 padding 当真实字符喂给 RNN，模型会读到很多无意义的 pad。`pack_padded_sequence` 可以告诉 RNN 每条序列真实长度，让它跳过 padding 部分。使用时要注意长度排序或设置 `enforce_sorted=False`。
你现在应该能说明 padding 解决 batch 对齐，packing 解决 RNN 不该学习 pad。
读到这里，你应该能：沿时间步追踪输入、隐藏状态和最终输出。

## box soft
### 5. 双向和多层会改变 hidden 形状
GRU 可以设置多层和双向。双向会同时从左到右、从右到左读序列，最后 hidden 的第一维会变成 `num_layers * num_directions`。分类时需要清楚取哪一层、哪一方向，或者把方向拼接起来。
如果设置 `bidirectional=True`，线性层输入维度常要变成 `hidden_size * 2`。这是高级 RNN 中常见 shape 坑。
你现在应该能在看到 GRU 参数后，推断分类层输入维度是否要乘以 2。
读到这里，你应该能：拿到一段代码后先检查输入、输出和层间维度是否对齐。

## box soft
### 6. 训练和推理要保留同一套词表
字符到 id 的映射必须固定。训练时用的词表，推理新名字时也要用同一套。否则同一个字符可能变成不同 id，embedding 查表含义就乱了。
还要处理未知字符。真实名字可能包含训练集中没见过的字符，通常需要 `<unk>` 之类的特殊 id。padding 也最好有单独 `<pad>` id，并在处理长度时排除它。
你现在应该能把序列分类的工程检查扩展到词表、padding、未知字符和长度。
读到这里，你应该能：把训练 step 拆成前向、loss、清梯度、反向和更新。

## box soft
### 完整知识链路
把 RNN 基础应用到真实的名字分类器。 它仍然落在同一条 PyTorch 主线上：先准备能被模型读取的数据，再让模型输出符合任务的张量，用合适的损失函数得到标量 loss，反向传播写入梯度，优化器更新参数，最后在评估模式下检查结果。换任务时，最先变化的是输入形状、模型输出含义和 loss 输入约定。


---

# 先别追 {#boundary}
<!-- tag: 把范围收住 -->
<!-- layout: grid2 -->

## box warn
### 这里先讲什么
Embedding、GRU、padding、packing 和变长序列分类。

## box info
### 这里先不讲什么
更完整的序列标注、解码器和 NLP 任务流水线，留给后面的专题。


---

# 术语表 {#terms}
<!-- tag: 别混词 -->

| 术语 | 直觉与定义 | 常见误解 |
| --- | --- | --- |
| Embedding | 把离散 id 映射为可学习稠密向量的查表层。 | 误以为输入可以是原始字符串。 |
| GRU | 带门控的循环网络单元，用于处理序列依赖。 | 和普通全连接层混淆。 |
| padding | 把短序列补齐到同一长度。 | 误以为 pad 是真实字符。 |
| packing | 把变长序列和真实长度交给 RNN，减少 padding 干扰。 | 以为只 padding 就够了。 |

### 易混概念对照

| 概念 | 准确含义 | 不要混成 |
| --- | --- | --- |
| Embedding | 字符 id -> 向量 | 不处理时间依赖 |
| GRU | 序列向量 -> 隐藏状态 | 不直接给类别概率 |
| Linear | 隐藏状态 -> 类别 logits | 不读取原始字符串 |


---

# 代码与例子 {#examples}
<!-- tag: 逐行读语义 -->

读代码时先标出输入、输出和状态变化。能解释这些，才算真正看懂。

名字分类模型骨架示例代码

```python
class NameClassifier(nn.Module):
 def __init__(self, vocab_size, embed_dim, hidden_size, num_classes):
 super().__init__()
 self.embedding = nn.Embedding(vocab_size, embed_dim, padding_idx=0)
 self.gru = nn.GRU(embed_dim, hidden_size, batch_first=True)
 self.fc = nn.Linear(hidden_size, num_classes)

 def forward(self, x, lengths):
 emb = self.embedding(x)
 packed = nn.utils.rnn.pack_padded_sequence(
 emb, lengths.cpu(), batch_first=True, enforce_sorted=False
 )
 _, hidden = self.gru(packed)
 last = hidden[-1]
 return self.fc(last)
```

Embedding 输入输出形状示例代码

```python
ids = torch.tensor([[1, 5, 9, 0], [2, 7, 0, 0]], dtype=torch.long)
emb = embedding(ids)
print(ids.shape) # [batch, seq_len]
print(emb.shape) # [batch, seq_len, embedding_dim]
```

双向 GRU 分类头要改维度示例代码

```python
self.gru = nn.GRU(embed_dim, hidden_size, batch_first=True, bidirectional=True)
self.fc = nn.Linear(hidden_size * 2, num_classes)
```


---

# 误区诊断 {#pitfalls}
<!-- tag: 错因 / 后果 / 修法 -->
<!-- layout: grid2 -->

## box warn
### 把原始字符串传给 Embedding
**后果：**Embedding 需要 long 类型 id。
**修法：**先建立字符表并编码。

## box warn
### padding 后不保留真实长度
**后果：**RNN 会把 pad 当成有效时间步。
**修法：**记录 lengths，必要时 pack。

## box warn
### 双向 GRU 后 Linear 维度没乘 2
**后果：**分类头输入维度不匹配。
**修法：**根据 directions 调整。

## box warn
### 推理时重建词表
**后果：**字符 id 映射变了，embedding 含义错乱。
**修法：**保存并复用训练词表。

## box warn
### 标签不是 long 类别索引
**后果：**CrossEntropyLoss 输入约定不匹配。
**修法：**国家/语言类别编码成 long。

## box warn
### 把训练循环当成背诵模板
**后果：**模板能跑不等于能诊断。每一行都要知道它读什么、写什么、改变哪个状态。
**修法：**把每行标成前向、loss、清梯度、反向或更新。

## box warn
### 只看 loss 下降
**后果：**loss 下降只能说明优化目标变小，还要检查数据、标签、输出形状和评估指标是否对应任务。
**修法：**同时检查验证集指标和预测样例。

## box warn
### 混用训练和评估状态
**后果：**`model.train()` 和 `model.eval()` 会影响 Dropout、BatchNorm 等层，评估时还要配合 `torch.no_grad()`。
**修法：**训练和评估写成两个清楚的函数。


---

# 完整样例 {#worked-example}
<!-- tag: 一条完整教学链 -->

## box soft
### 完整样例：Embedding + GRU + packing 怎么连起来
这一讲的名字分类器，第一步编码。字符先变成 long 型 id，再经过 Embedding 查表，得到稠密向量。这里的关键是：输入已经可以被模型读的索引张量。
名字长度不一，所以 batch 里通常要 padding 到同一长度。padding 只是为了堆叠成矩形，不是为了让模型学习这个占位符。真正该传给 RNN 的，是每条序列的真实长度，这样模型才知道哪些位置要忽略。
GRU 读完以后，最后一个 hidden 通常作为分类摘要。若设置双向，最后的 hidden 还要考虑方向数，线性层输入维度往往要乘 2。这里的 shape 变化是最常见的调试点。
如果词表在训练和推理时不一致，同一个字符可能落到不同 id 上，Embedding 查表的语义就会漂移。这个错误看起来像模型不稳定，实际上是编码规则改了。保存并复用同一套词表，比再调一次参数更重要。

## box info
### 输入和输出怎么对齐
先把输入 shape、标签 dtype、输出维度和训练目标写成同一张账本，后面再看 API。

## box warn
### 最常见的失配
这类页最常见的问题输入和输出的约定写反。先查 shape，再查 dtype，最后才查 loss。

字符 id[B,S]long 索引先数字化再查表Embedding[B,S,E]稠密向量把离散符号变成可学习表示GRU[B,S,H]序列编码读完整个序列依赖分类头[B,C]类别 logits最后 hidden 接 Linear对象shape / 语义要读什么常见失配

最小可执行片段复制

```python
import torch

ids = encode(names)
emb = embedding(ids)
packed = nn.utils.rnn.pack_padded_sequence(
 emb, lengths.cpu(), batch_first=True, enforce_sorted=False
)
_, hidden = gru(packed)
logits = fc(hidden[-1])
print(emb.shape, hidden.shape, logits.shape)
```

## box warn
如果你看到训练能跑但结论不稳，先回到这个样例把 logits、标签和 shape 再核一次。
故障现场


---

# 故障链 {#bridge-chain}
<!-- tag: 从症状回到接口 -->

## box soft
### 第二条教学链：词表、长度和方向数一起查
Embedding、GRU 和变长序列最容易让人感觉“都懂了”，一到代码就对不上。其实你只需要按顺序检查四件事：字符是否先被编码成 id，id 是否是 long，padding 后有没有记录真实长度，GRU 的方向数是否影响了最后 hidden 的维度。只要这四件事逐个过关，绝大多数报错就能定位。
变长序列的核心，让模型知道哪些位置只是占位。padding 只解决 batch 对齐，packing 才是让 RNN 少看无效 token。这个区别很工程，但它直接决定模型会不会把补零当成有效内容。
名字分类这种任务特别适合拿来练“从编码到分类头”的整条链路。你先把字符转 id，再查 embedding，再让 GRU 读序列，最后取最后 hidden 做分类。每一步的对象都不同，职责也不同；只要你能把职责讲清楚，代码就不会只剩下形状检查。
这一页最重要的收束点，是词表一定要复用。训练和推理如果用不同的字符表，同一个名字就会被编码成不同的 id，embedding 的意义会漂。这个问题表面上像是模型泛化差，实际上是输入语义变了。所以真正的工程检查，不只检查层，也要检查编码约定。

症状你先查什么为什么先查它修法字符raw string先编码不能直接进 EmbeddingEmbedding[B,S,E]稠密向量把 id 映射成可学表示GRU[B,S,H] / hidden序列编码读 sequence 依赖分类头[B,C]类别 logits最后 hidden 接 Linear

故障排查片段复制

```python
ids = encode(names)
emb = embedding(ids)
packed = nn.utils.rnn.pack_padded_sequence(
 emb, lengths.cpu(), batch_first=True, enforce_sorted=False
)
_, hidden = gru(packed)
if hidden.size(0) == 2:
 hidden = hidden.sum(dim=0)
logits = fc(hidden[-1])
```

## box info
### 练习时的用法
先把上面的症状、表格和代码各读一遍，再回到练习区。这样你写答案时不是在猜，而是在复述已经排过的链路。


---

# 一致性补充 {#consistency-note}
<!-- tag: 把接口写稳 -->

## box info
### 学习时的落点
这一段把训练、评估、调试和迁移时应该固定住的检查顺序收束成一张表。
Embedding + GRU 这一讲，最值得固定下来的编码顺序。先把字符转成 id，再查表成向量，再让 GRU 读序列，最后把 hidden 交给分类头。这个顺序一旦写对，后面的排错就很简单；写反了，整个模型都会像在读另一种语言。
长度信息是这类任务的第二个关键点。padding 负责补齐 batch，lengths 负责告诉模型哪些位置是假的。只做 padding 不做 lengths，你会让 RNN 把补零当成真实字符，训练结果可能看起来能收敛，但语义已经偏了。
双向 GRU 是第三个关键点。双向会把正向和反向的信息都读进来，所以最后 hidden 的方向数会变多，分类头输入维度常常要跟着翻倍。这个地方如果忘了改，报错一般很明确，但如果你只看模型结构名字，反而不容易第一时间想到它。
真正落地时，词表复用比你想的更重要。训练和推理如果不是同一套映射，同一个字符可能在两个阶段变成不同 id，Embedding 的含义就变了。那种‘模型时好时坏’的现象，很多并输入编码漂移。
所以这页的检查顺序应该是：字符是否编码、id 是否 long、padding 是否和 lengths 一起出现、GRU 的方向数是否影响分类头、最后有没有复用词表。把这一串检查养成习惯，你会少掉很多看似神秘的 bug。

场景先看什么为什么下一步字符输入raw string先编码不能直接进 EmbeddingEmbedding[B,S,E]向量表示查表后才可训练GRU[B,S,H]序列依赖方向数可能翻倍分类头[B,C]类别 logits最后 hidden 再接 Linear

收束后的检查脚本复制

```python
ids = encode(names)
assert ids.dtype == torch.long
emb = embedding(ids)
packed = nn.utils.rnn.pack_padded_sequence(
 emb, lengths.cpu(), batch_first=True, enforce_sorted=False
)
_, hidden = gru(packed)
if hidden.dim() == 3:
 hidden = hidden[-1]
logits = fc(hidden)
```

## box warn
### 最后一遍检查
把这页的表、代码和练习题一起读完后，再回头看题目。这样你写答案时在复述一整条已验证过的数据流。


---

# 练习区 {#practice}
<!-- tag: 20 题 -->

<!-- practice-intro -->
## box
怎么写答案
选择和判断题先给结论；短答题写 1-3 句话；诊断题按“错因、后果、修法”回答；最后的费曼题用完整段落串起主线。

## box
检查重点
每个答案尽量落到形状、数据流、loss 输入约定或参数状态上。只写“因为不对”通常不够。

<!-- end-practice-intro -->

<!-- practice-group: 基础概念 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 1 选择

**Q:** 1. `nn.Embedding` 的输入通常是什么？

- [ ] long 类型的整数 id
- [ ] 原始字符串列表
- [ ] float 概率

Answer: long 类型的整数 id
Explain: 查表层需要索引。

### 2 判断

**Q:** 2. padding 后的 `<pad>` 应该被当成真实字符学习。

- [ ] 错误：pad 只是补齐占位，通常要避免当真实字符学习
- [ ] 正确：pad 应该和真实字符一样学习语义

Answer: 错误：pad 只是补齐占位，通常要避免当真实字符学习
Explain: 判断 pad 的语义。

### 3 填空

**Q:** 3. 双向 GRU 的方向数是 ______。


<!-- end-practice-group -->

<!-- practice-group: 形状判断 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 4 短答

**Q:** 4. 输入 ids `[32,12]` 经过 embedding_dim=20 后形状是什么？

Explain: 形状是 [32,12,20] 。输入的每个 id 会查到一个 20 维向量，所以 batch 和序列长度不变，最后多出 embedding_dim 这一维。

### 5 短答

**Q:** 5. 单层单向 GRU 最后 hidden `hidden[-1]` 形状通常是什么？

Explain: 通常是 [batch,hidden_size] 。GRU 返回的 hidden 形状含层数和方向数；单层单向时取 hidden[-1] ，就得到每个样本最后的隐藏向量。

<!-- end-practice-group -->

<!-- practice-group: 代码阅读 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 6 短答

**Q:** 6. `pack_padded_sequence` 为什么需要 lengths？

Explain: pack_padded_sequence 需要 lengths 来知道每条序列真实有多长，从而跳过 padding 部分。没有真实长度，RNN 会把补齐的 <pad> 也当作时间步处理。

### 7 短答

**Q:** 7. `padding_idx=0` 有什么作用？

Explain: padding_idx=0 告诉 Embedding 第 0 号通常是补齐符号，训练时不把它当作普通字符去更新。这样 pad 向量不会学成某种真实语义，后续配合 lengths/packing 更稳。

<!-- end-practice-group -->

<!-- practice-group: 错误诊断 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 8 诊断

**Q:** 8. Embedding 报错 expected LongTensor，应该检查什么？

Explain: 检查输入 ids 的 dtype。 nn.Embedding 需要整数索引，通常是 torch.long ；如果你把 one-hot、float 或归一化后的数值传进去，就会报 expected LongTensor 一类错误。

### 9 诊断

**Q:** 9. 双向 GRU 后 Linear 报 shape mismatch，可能漏了什么？

Explain: 双向 GRU 会把正向和反向的 hidden 拼在一起，分类头输入维度通常要用 hidden_size * 2 。如果 Linear 仍按单向 hidden_size 写，就会 shape mismatch。

### 10 诊断

**Q:** 10. 推理时新建了字符表，为什么预测会乱？

Explain: 因为模型学到的是“某个 id 对应某个字符”的关系。推理时如果重新建字符表，同一个字符可能变成不同 id，Embedding 查到的向量就错了，预测自然会乱。训练、验证、推理必须复用同一套词表。

<!-- end-practice-group -->

<!-- practice-group: 场景应用 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 11 场景

**Q:** 11. 名字长度不同，为什么不能直接堆成普通二维数组？

Explain: 长度不同的名字无法直接堆成规则二维数组，因为每行列数不一样。通常先用 <pad> 补齐到同一长度，再记录 lengths，让模型知道哪些位置是真实字符。

### 12 场景

**Q:** 12. 未知字符应该怎样处理？

Explain: 未知字符通常映射到固定的 <unk> id，而不是临时扩展词表。这样输入仍能通过 Embedding，且训练、推理使用同一套 id 约定。

<!-- end-practice-group -->

<!-- practice-group: 概念区分 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 13 匹配

**Q:** 13. 匹配 embedding / GRU / linear：查表、读序列、分类。

Explain: embedding 负责把离散 id 查成向量；GRU 负责按顺序读取这些向量并形成序列表示；linear 负责把最后的序列表示映射成类别 logits。

### 14 短答

**Q:** 14. padding 和 packing 的区别是什么？

Explain: padding 是把不同长度的序列补到同一长度，方便组成 batch；packing 是把真实长度告诉 RNN，让它跳过补齐位置。前者解决“能不能堆起来”，后者解决“别把 pad 当内容学”。

<!-- end-practice-group -->

<!-- practice-group: 面试追问 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 15 短答

**Q:** 15. GRU 相比普通 RNN 主要改进什么？

Explain: GRU 用门控机制控制保留多少旧信息、写入多少新信息，比普通 RNN 更容易处理较长依赖，也更不容易在长序列里把早期信息完全冲掉。

### 16 短答

**Q:** 16. 为什么名字分类可以只取最后隐藏状态？

Explain: 名字分类是整段序列对应一个国家类别，不是每个字符都要分类。最后隐藏状态可以看作读完整个名字后的摘要，再把这个摘要送进 Linear 输出类别 logits。

<!-- end-practice-group -->

<!-- practice-group: 检查清单 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 17 清单

**Q:** 17. 写变长序列分类器前列 5 项检查。

Explain: 检查 5 项：训练和推理是否复用同一词表；输入 ids 是否是 long；padding id 和 padding_idx 是否一致；lengths 是否对应真实长度；GRU hidden 接 Linear 前是否考虑方向数和 hidden_size。

<!-- end-practice-group -->

<!-- practice-group: 代码修正 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 18 诊断

**Q:** 18. 使用 `pack_padded_sequence` 但 lengths 在 GPU 上导致问题，如何处理？

Explain: 常见做法是在调用前传 lengths.cpu() ，因为一些 PyTorch 版本或路径要求 lengths 在 CPU 上。输入序列可以在 GPU，但 lengths 作为打包元信息要按接口要求放到正确设备。

<!-- end-practice-group -->

<!-- practice-group: 费曼解释 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 19 费曼

**Q:** 19. 用 6-9 句话解释：名字分类器如何从字符串得到国家类别。

Explain: 名字先用训练时保存的词表转成字符 id，未知字符映射到 <unk> 。不同长度的名字用 <pad> 补齐，同时记录真实 lengths。Embedding 把 ids 变成连续向量序列。GRU 按时间步读取向量，必要时用 packing 跳过 pad。读完整个名字后，取最后 hidden 作为序列表示。双向时要合并两个方向的 hidden。Linear 把这个表示映射成国家类别 logits，训练时用类别索引和 CrossEntropyLoss 。

<!-- end-practice-group -->

<!-- practice-group: 自我检查 -->
<!-- quick-review: 作答提示 先说判断依据，再说后果或修法。遇到代码题时，写清输入、输出和状态变化。 -->

### 20 短答

**Q:** 20. 学完本页后，你现在能做什么？

Explain: 你现在应该能搭一条变长序列分类的数据流：复用词表，把字符串转 ids，用 padding 和 lengths 组成 batch，经 Embedding 和 GRU 得到序列表示，再检查 Linear 的输入维度和类别输出。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- [来源说明.md]()：本页的来源说明、锚点样本和教学意图。
- [12 讲](../12_%E5%BE%AA%E7%8E%AF%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C%E5%9F%BA%E7%A1%80%EF%BC%9A%E5%BA%8F%E5%88%97%E3%80%81%E9%9A%90%E8%97%8F%E7%8A%B6%E6%80%81%E4%B8%8ERNNCell/%E5%AD%A6%E4%B9%A0%E9%A1%B5.html)：RNN 基础和 hidden state。
- [课程地图](../index.html)：本页在整门课中的位置。

---
