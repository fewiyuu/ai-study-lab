---
course: "Harness Engineering"
unit: "03"
title: "任务分解与上下文边界"
kicker: "课程第 03 节"
summary: "这一页把“让 Agent 做事”压成两个动作：先把模糊需求拆成一个能执行、能验收的任务卡，再把它需要读取的上下文收窄到刚好够用。这里不追更复杂的调度系统或多 Agent 编排，先练会写清楚目标、输入、边界、证据和停手条件。"

side_note: "先把需求拆成一个能验收的任务，再限制 Agent 需要读的上下文。"

can_do:
  - "把一句模糊需求拆成目标、输入、产出、验收证据和禁止范围。"
  - "为 Agent 选择足够的上下文包，避免读少了乱猜、读多了跑偏。"
  - "从一次任务失败里判断是目标不清、边界过宽、证据不足还是上下文过期。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../02_应用可读性与反馈回路/学习页.html"
  - text: "下一节"
    href: "../04_架构约束与品味不变量/学习页.html"
  - text: "原文"
    href: "https://openai.com/zh-Hans-CN/index/harness-engineering/"

---

# 学习目标 {#goals}
<!-- tag: start here -->
<!-- layout: grid2 -->

## box
这一节承接前两页：第 01 节说人类要设计 Agent 的工作环境，第 02 节说结果要能被证据回看。现在要处理中间最容易松掉的一环：任务到底怎么交给 Agent。
一个好任务不能只写“帮我改一下登录页”。它至少要说明目标状态、当前输入、允许改动的位置、不能碰的边界、验收证据和停手条件。少了其中一项，Agent 可能仍然很努力，但它会把努力花在错误范围里。

## box soft
### 本页的练习对象
我们用一个持续出现的小场景：结算页提交按钮会卡住。你会把它从一句报错拆成任务卡，再为它选择上下文包，最后判断失败应该写回哪里。


---

# 主线：先拆任务，再给上下文 {#overview}
<!-- tag: map -->
<!-- layout: grid2 -->

## box
Harness engineering 的重点是环境。任务分解就是环境的一部分：它决定 Agent 看到什么目标、能动什么文件、用什么证据证明完成。上下文边界也是环境的一部分：它决定 Agent 读哪些规则、样例和反例，哪些材料暂时不该进入窗口。
直觉上，你可以把任务卡看成给 Agent 的工单，把上下文包看成它开工前桌面上放的材料。工单太粗，Agent 会自己补意图；材料太多，Agent 会把无关规则也当成线索。两者都要刚好够用。

## box info
### 一句工作定义
可执行任务 = 目标状态 + 输入材料 + 改动边界 + 验收证据 + 停手条件。
上下文边界 = 当前任务必须读取的规则、样例、命令和证据，不包含会诱导跑偏的历史材料。

对象
它回答什么
典型材料
失控症状


任务卡
这次到底要完成什么
目标、输入、输出、验收、禁止范围
Agent 改了很多地方，但没人知道算不算完成


上下文包
开工前必须读什么
AGENTS、README、相关文件、测试命令、相似实现
Agent 读少了乱猜，读多了引入无关规则


边界条件
哪些东西不能碰
目录范围、接口契约、权限、数据模型、不变量
局部修复破坏架构或产品约束


反馈写回
失败后该沉淀什么
模板字段、文档补丁、测试、反例、脚手架
同类任务下一次继续踩坑


---

# 把模糊需求拆成能力块 {#capability}
<!-- tag: task split -->

能力块关注 Agent 动作的可判定粒度，和模块拆分、TODO 清单不同。比如“修结算页卡住”太粗；“复现按钮卡住、定位状态来源、补失败提示、跑通提交路径、留下证据包”就更容易执行。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

能力块
具体动作
验收方式
边界


复现
按给定路径重放按钮卡住
截图或日志能证明同一现象
不先改代码


定位
判断卡在 UI 状态、请求、后端还是下游
有 request_id、错误码或 DOM 状态
不靠猜测归因


修复
只改本次缺陷需要的状态处理或提示
相关测试和浏览器复跑通过
不顺手改支付架构


写回
把新增证据字段、复现步骤或模板缺口补回仓库
后续任务能复用
不写无关大文档


---

# 上下文边界怎么画 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
上下文边界的核心判断是：这份材料会改变当前任务的正确动作吗？会，就放进上下文包；不会，就不要让它占用注意力。Agent 可以读很多东西，但读得多不等于理解得准。无关材料会制造错误联想，过期材料会制造旧规则。
边界要按任务收，不按“看起来相关”收。修结算按钮时，相关的是结算页状态、提交请求、错误提示、测试命令和近期失败日志；支付网关长期重构计划、旧版埋点讨论、另一个页面的视觉规范，除非任务明确需要，否则先不放进来。

## box warn
### 三个判定问题
- 没有这份材料，Agent 会不会改错对象？
- 这份材料会不会让 Agent 扩大任务范围？
- 这份材料是否比当前代码、测试或模板更新？

材料
放入条件
不放入时的风险
放错时的风险


AGENTS / 项目约定
影响测试、风格、权限或目录规则
违反项目约定
读到上级泛规则，忽略本目录规则


README / 启动说明
需要运行项目或复现问题
无法启动或跑错命令
把历史命令当成当前命令


相关源码
包含当前行为或相似实现
只能凭描述改代码
读入太多相邻模块，改动越界


测试与失败日志
能证明问题或验收修复
无法复查
用旧失败覆盖当前现场


设计讨论
包含当前不可破坏的不变量
破坏隐藏约束
把长期设想误当成本次目标


---

# 上下文包：给 Agent 的开工桌面 {#context-pack}
<!-- tag: context pack -->
<!-- layout: grid2 -->

## box
上下文包应该像一张小桌面：左边是任务卡，中间是必须读的规则和文件，右边是验收证据。不要把整间办公室都搬进来。一个好的上下文包让 Agent 能立刻回答三件事：我要达成什么、我能改哪里、我怎么证明自己做完了。
如果任务需要跨前端和后端，包里可以有两条路径，但仍然要写清主线。比如结算页卡住：主线是提交状态和失败提示；后端日志只是用来确认原因；支付网关重构不是本次目标。

## box info
### 最小上下文包
- 任务卡：目标、输入、边界、验收。
- 入口文件：相关页面、接口、测试或脚手架。
- 项目规则：AGENTS、目录 README、命令说明。
- 证据：复现步骤、截图、日志、失败输出。

上下文包模板复制

```text
任务：修复结算页提交按钮卡住
目标状态：提交后进入下一步；失败时显示明确错误提示
输入材料：复现路径、失败截图、request_id、最近一次日志
允许改动：结算页状态处理、错误提示、相关测试
必须读取：本目录 AGENTS、结算页 README、提交接口测试
禁止范围：不重构支付网关，不改账户体系，不扩大结算流程
验收证据：测试输出、浏览器截图、日志字段、复跑步骤
停手条件：验收通过后只写回必要规则或模板缺口
```

组成
作用
检查问题
常见缺口


任务卡
固定本次目标
有没有明确完成状态
只写“优化”“修复”“完善”


文件入口
减少搜索成本
Agent 从哪里开始读
让 Agent 全仓库乱找


规则入口
避免违反约定
哪些规则会影响这次改动
只给代码不给项目约定


证据入口
让验收可复查
用什么证明完成
只有口头总结，没有输出


---

# 从人类意图到 Agent 交接 {#handoff}
<!-- tag: handoff -->
<!-- layout: grid2 -->

## box
交接要把人类意图翻译成工程对象：目标状态、改动范围、可读上下文和证据。这个翻译过程越清楚，Agent 越不需要自己猜产品意图、架构约束或验收标准。
交接时最危险的是隐性限制。比如“别影响老用户优惠券”“不要改支付网关”“只处理移动端布局”，这些限制如果没有写进任务卡，Agent 很可能从局部最容易修的位置下手，最后造成范围外影响。

## box soft
### 交接检查顺序
1. 先写目标状态，不先写实现方案。
2. 再写允许改动的范围。
3. 补上必须读取的上下文。
4. 列出验收证据和停手条件。

字段
好写法
坏写法
坏写法会导致什么


目标
提交成功进入下一步，失败显示错误提示
修一下提交问题
完成状态不明确


输入
复现步骤、截图、日志、相关测试
你自己看看
Agent 从错误入口开始


范围
只改结算页状态处理和错误提示
相关地方都可以改
改动扩散到无关模块


验收
测试输出、截图、日志字段齐全
看起来可以就行
审查者无法复核


停手
不重构支付网关，不改账户体系
顺便优化一下
任务变成开放重构


---

# 状态台账：读、写、失效、回写 {#ledger}
<!-- tag: ledger -->

任务交给 Agent 后，你要能追踪它读了什么、写了什么、哪些信息已经失效、失败后该写回哪里。台账的价值在于把一次失败沉淀成下一次可读的任务边界。

对象
读什么
写什么
什么时候失效
失效症状
回写位置


任务卡
目标、输入、边界
执行计划、完成说明
目标或范围变了
Agent 开始做不相关改动
issue / 任务模板


上下文包
AGENTS、README、相关源码
引用的规则和文件清单
目录结构或命令变化
运行旧命令、改错目录
README / AGENTS


验收证据
测试、截图、日志
复跑结果
测试或复现步骤更新
结果无法复查
PR 模板 / 测试说明


边界规则
权限、接口、不变量
禁止范围和例外条件
架构决策改变
局部修复破坏系统约束
架构记录 / 审查清单


---

# 失败演示：同一个需求，三种交接 {#lab}
<!-- tag: lab -->

点击不同交接版本，观察 Agent 猜测空间怎么变化。这里的重点是让目标、边界和证据足够具体，而不是把任务描述写得更长。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

交接版本
Agent 能确定什么
仍然要猜什么
审查成本


一句话需求
大概知道页面名
目标、范围、证据、上下文
高


缺边界任务
知道症状和大概验收
能不能改下游、是否要看浏览器
中


完整任务卡
目标、输入、范围、证据、停手
只剩实现细节
低


---

# 失败链路：任务怎样一步步跑偏 {#failure-chain}
<!-- tag: debug -->
<!-- layout: grid2 -->

## box
任务跑偏通常源于错误搜索空间。模糊目标会让 Agent 先猜问题类型；缺少输入会让它扩大检索；缺少边界会让它把局部修复升级成重构；缺少证据会让它在看起来能跑通的地方停下。
所以自审时不要只看最终改了哪些文件，要回放它每一步为什么会做那个动作。只要能指出“哪一个字段没给，导致哪一次猜测变大”，下一版任务卡就有明确修补点。

## box soft
### 回放顺序
- 先看目标：它能否判断完成状态。
- 再看输入：它能否复现同一个现场。
- 再看边界：它能否知道哪些方向要停住。
- 最后看证据：它能否留下可复查结果。

时间点
任务卡缺口
Agent 的合理猜测
跑偏动作
修补字段


T0
只写“修提交问题”
问题可能在 UI、接口、支付、账户或优惠券
从全仓搜索 submit 和 payment
目标状态 + 复现入口


T1
没有 request_id 和日志
只能靠代码结构猜失败点
开始读支付网关适配层
输入证据 + 最小日志片段


T2
没有禁止范围
如果支付网关可疑，就可以改它
重构 gateway timeout 分支
允许改动 + 禁止范围


T3
验收只写“测试通过”
单测绿灯即可交付
没有重新打开结算页截图
验收证据 + 浏览器复跑步骤


T4
没有写回要求
任务结束后不更新模板
下次仍然缺 request_id 字段
写回位置 + 模板补丁

任务卡修补对照复制

```text
原始任务：
帮我修一下结算页提交问题，测试跑通就行。

修补后任务：
目标：用户点击提交后，如果支付成功进入确认页；如果支付超时，在当前页显示“支付暂时不可用，请稍后重试”，按钮恢复可点击。
复现：/checkout，测试卡 4242，request_id r-2481，截图见 issue 附件。
已知信号：前端按钮一直 disabled，后端日志有 PAYMENT_GATEWAY_TIMEOUT。
允许改动：结算页状态处理、错误提示文案、相关组件测试和 e2e 复现。
禁止范围：不改支付网关协议，不改账户体系，不碰优惠券计算。
验收证据：组件测试、e2e 截图、相关日志字段、手动复跑步骤。
写回：如果 issue 模板没有 request_id / 复现入口字段，补一个最小字段。
```


---

# 范围矩阵：读、改、禁止、回写 {#scope-matrix}
<!-- tag: scope -->
<!-- layout: grid2 -->

## box
边界要靠可执行权限建立。更稳的写法是把对象拆开：哪些文件只允许读取，哪些文件允许修改，哪些模块必须禁止，哪些发现要写回模板。这样 Agent 不需要靠风格猜测你的意图，审查者也能直接判断某个改动是否越界。
范围矩阵适合放在复杂 issue、PR 描述或任务交接文档里。它把“上下文边界”从抽象原则变成可检查条目：一列对象，一列动作权限，一列验收证据，一列写回位置。

## box info
### 矩阵的使用时机
- 需求涉及多个目录，但本次只想改其中一层。
- 存在高风险模块，例如支付、权限、迁移、计费。
- 任务需要读旧决策，但不能顺手推翻旧决策。
- 验收需要 UI、日志、测试、文档一起对齐。

对象
可读
可改
禁止动作
验收证据
写回位置


结算页组件
状态流、错误文案、提交按钮逻辑
允许
不能顺手重做整页布局
截图、组件测试、e2e 复跑
PR 描述


支付网关适配层
错误码、超时语义、日志字段
默认不允许
不改协议、不改重试策略
日志字段引用
任务卡边界


账户与权限模块
只读相关调用链
禁止
不改鉴权、不改角色判断
无改动说明
PR 审查说明


测试目录
既有用例、fixture、e2e 脚本
允许
不删除覆盖异常分支的旧用例
测试输出
测试说明


issue / PR 模板
现有字段
只补最小缺口
不重写整套流程
模板 diff
仓库模板

边界自审清单复制

```text
交付前自审：
1. 我能说清本次目标对应的用户可见状态或系统可见状态。
2. 我能列出 Agent 必须读取的上下文，并删掉不会改变当前动作的材料。
3. 我能指出至少一个禁止范围，尤其是高风险模块和长期架构决策。
4. 我能解释每个改动文件为什么属于“允许改动”。
5. 我留下的证据能让另一个人复跑，不需要回到聊天记录猜过程。
6. 如果任务暴露出模板或文档缺口，我知道写回到哪个稳定位置。
```


---

# 自审闭环：交付前最后看一次边界 {#review-loop}
<!-- tag: review -->
<!-- layout: grid2 -->

## box
任务完成前，要把“我做完了”翻译成“别人能复查”。这一步经常暴露上下文边界的真实质量：如果你说不清为什么读了这些文件、为什么改了这些文件、为什么没有改高风险模块，那么任务卡里的边界还不够硬。
自审闭环的顺序很简单：先拿原始任务卡对照结果，再拿范围矩阵对照改动文件，接着拿验收证据对照目标状态，最后把暴露出来的缺口写回稳定位置。这个动作不需要很重，但它能阻止同类任务每次都重新踩坑。

## box soft
### 闭环判断
- 目标：结果能否用一句用户可见状态说明。
- 范围：每个改动文件都能落到允许范围。
- 证据：截图、日志、测试输出可以互相印证。
- 写回：模板、README、AGENTS 或测试说明得到最小补丁。

自审问题
通过信号
失败信号
修补动作


目标是否完成
成功路径和失败路径都有可见状态
只能说“代码改了”或“测试过了”
补用户状态、系统状态和复跑步骤


上下文是否足够
引用的日志、截图、文件都解释了当前动作
读了大量无关背景，交付说明讲不清原因
删掉无用材料，保留会改变动作的证据


范围是否守住
改动集中在允许目录，高风险模块只有只读证据
顺手改了协议、权限、计费或全局样式
拆出新任务，当前任务回到原边界


证据是否可复查
另一个人能按命令、截图和日志复跑
结论依赖聊天记录或个人记忆
补命令输出摘要、截图路径、request_id


经验是否写回
缺失字段回到模板，常见误区回到清单
下次任务仍要重新解释同一个规则
补最小模板字段或目录级规则

交付说明模板复制

```text
交付说明：
目标结果：说明用户或系统现在能看到什么变化。
关键输入：列出本次使用的截图、日志、request_id、复现入口。
改动范围：列出改动文件，并说明它们分别落在哪个允许范围。
未改范围：列出读过但未改的高风险模块，说明为什么停在只读。
验收证据：贴测试命令、浏览器截图、日志字段、手动复跑步骤。
写回结果：说明是否补了模板、README、AGENTS、测试说明或反例清单。
后续任务：如果发现更大的架构问题，单独开新任务，不混进当前交付。
```

## box warn
### 案例：把结算页任务拆错以后会发生什么
原始任务如果只写“修提交问题”，Agent 通常会从最显眼的按钮或日志开始搜。它可能先修一个看起来相关的支路，然后把时间花在更大的支付链路上，却忘了最初要的是“提交成功后进入下一步，失败时显示明确错误提示”。这种偏差很常见，因为任务卡没有把目标状态、边界和证据绑在一起。
一旦出现这种偏差，最有效的动作把任务卡重写成可执行版本，再把这次暴露出来的缺口写回模板。这样下一次拿到同类任务的人，能直接读到复现入口、允许改动和停手条件。

步骤
看到的信号
错误推进
正确修正


读任务
只有“修提交问题”
把范围理解成整个支付链路
补目标状态、输入和停手条件


找上下文
看见按钮 disabled、日志超时
直接修改适配层和重试逻辑
先确认允许改动的文件和禁止模块


做改动
测试通过，但页面状态没恢复
只盯单测结果，不回看浏览器
补 e2e、截图和手动复跑步骤


交付后
模板仍然缺 request_id 字段
同类任务下次继续靠猜
把缺口写回 issue / PR 模板

坏卡与修正版复制

```text
坏卡：
修一下结算页提交问题，测试跑通就行。

修正版：
目标：提交成功后进入下一步；提交失败时停留在当前页并显示明确错误提示。
输入：/checkout 的复现路径、request_id、截图、最后一次失败日志。
允许改动：结算页状态处理、错误提示、相关测试。
禁止范围：支付网关协议、账户体系、优惠券逻辑。
验收：组件测试、e2e 截图、日志字段、手动复跑步骤。
写回：如果模板缺少复现入口或日志字段，把字段补回 issue / PR 模板。
```

## box soft
### 练习
拿你手边的一个任务，把它按上面的结构重写一遍。先写目标状态，再补输入和边界，最后列出验收证据和写回位置。写完以后，你应该能把它直接放进 issue 或交给 Agent。

## box
### 现场诊断
如果你发现任务已经开始改高风险模块，就先停下来，把结果退回到任务卡修正，而不是继续追着代码打补丁。这个动作能把“局部修复”重新拉回“可验收交付”。
诊断时只看四件事：目标是否完整、输入是否可复现、边界是否固定住高风险模块、证据是否能复查。四件事里缺任何一件，任务卡就还不算完成。
一旦范围越过边界，后面再加说明也只能扩大误差，正确动作是拆新任务并把缺口写回模板内。


---

# 例子：一张可执行任务卡 {#example}
<!-- tag: example -->
<!-- layout: grid2 -->

## box
下面这张任务卡适合交给 Agent，也适合放进 issue 或 PR 描述。它没有把所有背景都塞进去，只保留这次会影响正确动作的材料。注意它同时有目标、输入、改动范围、禁止范围、验收证据和写回要求。

## box soft
### 读任务卡时看什么
- 目标是否描述了用户可见状态。
- 输入是否足够复现同一现场。
- 边界是否阻止任务被扩大。
- 验收是否能被另一个人复查。

任务卡样例复制

```text
标题：修复结算页提交按钮卡住
目标：提交成功时进入下一步；提交失败时显示明确错误提示
复现：打开 /checkout，填入测试卡，点击提交后按钮持续 disabled
输入：截图、request_id r-2481、错误日志 PAYMENT_GATEWAY_TIMEOUT
允许改动：结算页状态处理、错误提示、相关测试
禁止范围：不重构支付网关，不改账户体系，不改优惠券逻辑
验收证据：测试输出、浏览器截图、日志字段、复跑步骤
写回：如果缺少日志字段或复现模板，补到 issue / PR 模板
```


---

# 落到仓库：哪里写任务，哪里写规则 {#bridge}
<!-- tag: repo -->
<!-- layout: grid2 -->

## box
任务分解要落到仓库。稳定的做法是把可复用部分写回仓库：任务模板写在 issue 或 PR 模板里；目录规则写在 AGENTS 或 README 里；测试命令写在开发文档或脚手架里；反复出现的错误写成反例或检查清单。
写回时要克制。一次任务失败不一定需要一篇长文档，它可能只需要在模板里加一个字段：复现入口、禁止范围、验收证据、日志字段。字段越靠近使用场景，下次越容易被读到。

## box info
### 写回位置速查
- 任务字段缺失：补 issue / PR 模板。
- 目录规则缺失：补 AGENTS 或目录 README。
- 命令经常跑错：补开发文档或脚手架提示。
- 同类错误反复出现：补测试、反例或审查清单。

失败信号
说明缺什么
写回哪里
下一次怎么受益


Agent 总问从哪里开始
入口不清
任务模板 / README
直接找到入口文件和复现路径


Agent 改到无关目录
边界不清
AGENTS / 目录说明
知道哪些目录不能碰


Agent 跑错测试
验收命令不清
开发文档 / 脚手架
按任务类型选正确命令


PR 反复要求补证据
证据字段不清
PR 模板
默认提交截图、日志、测试输出


---

# 常见误区和修法 {#mistakes}
<!-- tag: fix -->

误区
为什么会发生
后果
修法


把目标写成动词
习惯写“优化”“修复”“整理”
完成状态不清
改成用户或系统可见状态


上下文越多越好
担心 Agent 漏信息
无关材料制造错误联想
只放会改变当前动作的材料


没有禁止范围
以为 Agent 会自觉克制
局部问题变成开放重构
写清不能碰的模块和决策


只写验收命令
把测试输出当成全部证据
UI、日志或状态问题漏掉
按任务类型补截图、日志、指标或 trace


失败后只重说一遍
没有写回机制
下次同类任务继续踩坑
把缺口写回模板、README 或检查清单


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box
练习目标
这组题考的是你能不能把需求压成可执行任务，再判断上下文包是否刚好够用。不要只写“补充信息”，要说清补哪类信息、为什么会影响执行。

## box soft
检查重点
先看目标是否可验收，再看边界是否能限制改动，最后看证据是否足够复查。开放题按“目标、输入、边界、证据、写回”来答。

<!-- end-practice-intro -->

症状
可能缺口
先补什么
写回哪里


Agent 改了很多无关文件
禁止范围不清
允许改动和禁止范围
任务模板 / AGENTS


Agent 不知道从哪里开始
入口文件缺失
相关文件、复现路径、测试命令
README / issue 模板


PR 审查反复问证据
验收证据不清
测试输出、截图、日志字段
PR 模板


同类任务反复失败
反馈没有写回
反例、检查清单、脚手架提示
README / 测试 / 模板

<!-- practice-group: 一、拆目标 -->
<!-- quick-review: 复盘提示 目标要写状态，不只写动作。 -->

### 1 choice

**Q:** 1. 哪个目标最可验收？

- [ ] 优化结算体验
- [ ] 修一下提交问题
- [ ] 提交成功进入下一步，失败时显示错误提示

Answer: 提交成功进入下一步，失败时显示错误提示

### 2 true/false

**Q:** 2. 判断：任务卡只要写清实现方案，就不需要写目标状态。

- [ ] 对
- [ ] 错

Answer: 错

### 3 fill

**Q:** 3. 可执行任务至少要有目标、输入、边界、验收和______。

Answer: 停手条件,停手,禁止范围

### 4 short-answer

**Q:** 4. 把“优化结算页”改写成可执行任务目标。

Explain: 合格答案会把“优化”改成可观察状态，例如：用户在结算页点击提交后，成功时进入确认页；失败时按钮恢复可点并展示明确错误提示；相关测试通过，并保留一次浏览器截图或日志字段作为证据。只写“提升体验”不够。

<!-- end-practice-group -->

<!-- practice-group: 二、画边界 -->
<!-- quick-review: 复盘提示 边界要保护任务不被扩大。 -->

### 5 choice

**Q:** 5. 修结算页按钮卡住时，最应该写进禁止范围的是？

- [ ] 不重构支付网关
- [ ] 补错误提示
- [ ] 跑相关测试

Answer: 不重构支付网关

### 6 true/false

**Q:** 6. 判断：禁止范围会降低 Agent 能力，所以最好不写。

- [ ] 对
- [ ] 错

Answer: 错

### 7 diagnosis

**Q:** 7. 一个 Agent 把 UI 修复扩大成支付网关重构，任务卡缺了什么？

Explain: 应指出任务卡缺少边界：允许改动应该限定在结算页状态处理、错误提示和相关测试；禁止范围应该写清不重构支付网关、不改账户体系、不扩展结算流程；停手条件应该说明验收通过后只写回必要模板或规则缺口。只说“Agent 理解错了”太空。

<!-- end-practice-group -->

<!-- practice-group: 三、配上下文 -->
<!-- quick-review: 复盘提示 材料要能改变当前动作。 -->

### 8 choice

**Q:** 8. 上下文包最应该优先包含哪一组？

- [ ] 任务卡、相关文件、项目规则、验收证据
- [ ] 所有历史会议记录和全部设计稿
- [ ] 只给一句需求

Answer: 任务卡、相关文件、项目规则、验收证据

### 9 true/false

**Q:** 9. 判断：上下文是否放入，应该看它会不会改变当前任务的正确动作。

- [ ] 对
- [ ] 错

Answer: 对

### 10 fill

**Q:** 10. 如果 Agent 经常跑错命令，最应该补哪类上下文？

Answer: README,开发文档,脚手架提示,命令说明

### 11 scenario

**Q:** 11. 你会给“修结算页卡住”准备哪些上下文？哪些先不放？

Explain: 放入项可以包括任务卡、复现路径、截图、request_id、错误日志、结算页相关文件、提交接口测试、本目录 AGENTS 或 README。暂不放入项可以包括支付网关长期重构计划、另一个页面的视觉规范、过期设计讨论。评分重点是理由：材料是否会改变当前动作，是否会让任务越界。

<!-- end-practice-group -->

<!-- practice-group: 四、诊断失败并写回 -->
<!-- quick-review: 复盘提示 失败要沉淀到下一次能读到的位置。 -->

### 12 choice

**Q:** 12. PR 审查总要求补截图和日志，最应该写回哪里？

- [ ] PR 模板
- [ ] 支付网关源码
- [ ] 随便写进聊天记录

Answer: PR 模板

### 13 fill

**Q:** 13. 同类错误反复出现时，写回的目标是______。

Answer: 下次能读到,下次可复用,减少重复踩坑

### 14 synthesis

**Q:** 14. 用 6-8 句话解释：为什么任务分解和上下文边界是 harness 的一部分。

Explain: 好的答案会说：harness Agent 工作的工程环境。任务分解决定 Agent 要达成什么、能改哪里、怎么证明完成；上下文边界决定它开工前应该读哪些规则、文件和证据。目标不清会让 Agent 自己补意图，边界不清会让改动越界，上下文过多或过少都会造成错误判断。失败后把模板、规则、测试或反例写回仓库，下一次 Agent 才能直接读到。答案要有因果链，不要只说“更高效”。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- [Harness engineering: Leveraging Codex in an agent-first world](https://openai.com/zh-Hans-CN/index/harness-engineering/)，OpenAI Engineering，2026-02-11。本页沿用原文对 agent-first 工程环境、任务边界、证据和反馈机制的讨论。
- [Harness Engineering 课程地图](../index.html)，用于确认本节在课程中的位置：先理解角色转变和反馈证据，再练习任务分解、上下文边界、约束和熵管理。
- [课程原料与拆解](../课程原料与拆解.md)，记录了本课程对 harness、应用可读性、仓库知识、不变量和熵管理的术语边界。

---
