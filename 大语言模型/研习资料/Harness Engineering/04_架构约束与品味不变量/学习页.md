---
course: "Harness Engineering"
unit: "第 4 讲"
title: "架构约束与品味不变量"
kicker: "工程约束"
summary: "这一页讲的是：当你觉得“代码写得好不好”时，怎样把这种判断落到可检查的规则上。重点不是把所有审美都变成硬规矩，而是把会长期影响系统的部分写成不变量、反例、测试和审查标准，让 Agent 也能稳定执行。"

side_note: "把口头偏好写成可检查的规则、反例和测试，Agent 才能稳定执行。"

can_do:
  - "能把“代码要优雅”改写成一条可检查的不变量或审查规则。"
  - "能用一个反例说明某条架构偏好为什么必须被保护。"
  - "能判断某个约束该进入 lint、测试、审查清单，还是只保留为建议。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../03_任务分解与上下文边界/学习页.html"
  - text: "下一节"
    href: "../05_吞吐自治与熵管理/学习页.html"
  - text: "原文"
    href: "https://openai.com/zh-Hans-CN/index/harness-engineering/"

---

# 来源与边界 {#source}
<!-- tag: SOURCE -->
<!-- layout: grid2 -->

## box
这一页只处理会长期影响维护的判断：依赖方向、权限来源、目录职责、证据字段和审查标准。普通风格偏好可以保留为建议，但只要它会反复制造返工，就该被写成可检查对象。
本页的例子都来自同一条主线：把“感觉不对”翻成能被人检查、能被工具拦住的句子。

## box soft
### 来源支持
- OpenAI Engineering 原文：支持“品味要落到约束、反例、测试和审查标准”这条主线。
- Harness Engineering 课程地图：支持这一页在第 04 节的位置，以及它和第 03、05 节的连接。

来源
支持什么
本页怎么用


Harness engineering: Leveraging Codex in an agent-first world
品味、不变量、反例、测试、审查标准
作为这一页的工程主线，日期以 2026-02-11 为准


Harness Engineering 课程地图
单元位置、前后关系、学习顺序
说明这一页为什么排在第 04 节，以及后面该读什么


---

# 收尾注：规则为什么必须写回最近的位置 {#closing-note}
<!-- tag: closing-note -->
<!-- layout: grid2 -->

## box
如果一条规则只在聊天里出现过一次，它就很容易在下次被忘掉。工程品味要真正变成约束，必须把它写回最近的位置：目录 README、lint、测试、PR 模板和 review 清单。这样做以后，规则不再依赖某个人记得，而是依赖结构本身。
这也是为什么上面的例子都要先拆成边界，再配反例，再写验证，最后写回。这让同一条规则在不同环节都能被同一批人和工具读到。你要保住的“下次能拦住”。

## box soft
### 最小回写句式
1. 这条规则在阻止什么。
2. 它应该被谁先看到。
3. 它会放到哪个最近入口。

句子
写回位
下次谁会读到


这个目录只读权限结果，不生成权限判断
README
进入目录的人和 Agent


service 不依赖 UI
lint / 测试
合并前的检查流程


必须附验证命令和截图
PR 模板
审查人和复查人


---

# 深潜链：一个支付目录的完整回路 {#deep-dive}
<!-- tag: deep-dive -->
<!-- layout: grid2 -->

## box
把上面的判断再往前推一步，就会得到一个完整回路。支付目录先收到 review 争议，再把争议拆成边界、不变量、验证和回写四件事；然后把每一件事放到最近的位置，保证下一次出现时能直接读到。这个回路之所以重要，是因为它让“口头提醒”变成了“固定结构”。
如果目录边界没写清，Agent 就会自己补权限判断；如果 lint 没拦住，跨层依赖会继续发散；如果 PR 模板没补证据，复查会一直来回问；如果 review checklist 里没写规则，老问题还会反复出现。问题这几条没有一起闭环。
这也是为什么你不能只盯着一处。目录 README 讲职责，lint 讲机械边界，模板讲证据，checklist 讲复审。四个地方一起工作，才能把工程品味从“感觉不对”收紧到“下次必拦”。

## box soft
### 完整闭环的最小动作
1. 从 review 争议里抽出边界句。
2. 把边界句收成不变量。
3. 把不变量送进 lint、测试或模板。
4. 把最小反例写回最近入口。

环节
输入
输出
失败后果


边界
“这里像是自己在算权限”
目录只读权限结果
目录行为继续漂移


拦截
“跨层依赖看起来不稳”
lint / 架构测试
越层引用在主线里继续出现


证据
“交付证据不够”
截图和验证命令
复查一直无法结束


回写
重复出现的同类问题
checklist 和 README
规则一直停在聊天里


---

# 收尾故事：规则真正落地时长什么样 {#closing-story}
<!-- tag: closing -->
<!-- layout: grid2 -->

## box
真正把工程品味写成约束，把一条争议拆成几层。先看这条争议是不是会反复出现，再看它能不能被机械检查，再看它应该靠近哪个入口，最后看它会不会被下一位维护者直接读到。这个过程为了让规则在真实工作里能活下来。
如果一个目录总被问权限来源，说明它的责任边界没有读清。此时你不需要把所有架构理论拉进来，只要把“这个目录只读权限结果，不生成判断”写到最近的 README，把跨层引用写进 lint，把验证命令和截图写进 PR 模板，再把重复问到的问题写进 checklist。这样四个位置一起工作，下一次争议就不必重新从头开始。
这条链路的结尾“新人和 Agent 进来时会被同一条规则拦住”。真正稳定的约束，就是能被读、能被查、能被复用、还能在下次改动前先挡一下。

## box soft
### 如果你要自己复现这一页的做法
1. 抓一条会反复出现的争议。
2. 写成一句可以机械检查的不变量。
3. 把它放回最近的入口，而不是堆进大文档。
4. 补一个最小反例和一个最小验证。

争议类型
先写什么
再写什么
最后写什么


权限来源
目录边界
lint / 测试
PR 模板和 checklist


依赖方向
不变量
架构测试
例子和反例


交付证据
模板字段
验证命令
截图和日志要求


---

# 终局案例：从一条评论到一套规则 {#final-case}
<!-- tag: final-case -->
<!-- layout: grid2 -->

## box
再把视角收紧一点。假设你在支付目录里第一次收到评论：“这里像是自己在算权限。”这个句子没有告诉你具体哪一行错了，但它已经足够说明一件事：目录的责任边界没有被读者稳定识别出来。下一步不是辩解，而是去找一个最小的可检查对象。
你可以把它收成两层。第一层是目录边界：这个目录只能读权限结果，不能自己发起权限判断。第二层是验证方式：如果出现新来源，lint 或架构测试必须先报错。第三层是回写位置：README 里要写明该目录的职责、禁区和验证命令。这样做以后，评论就不再只是感觉，而是变成能执行、能复查的规则。
这条链路里最重要的地方，是你把“觉得不稳”拆成了几个具体动作。每一个动作都比前一个更强一点：评论只是提醒，不变量可以被读，lint 可以拦错，README 可以回写。规则真正稳定下来，靠的这几层一起工作。

## box soft
### 把评论收紧时的顺序
1. 先找这条评论真正想阻止什么。
2. 再写成一句边界明确的不变量。
3. 然后决定它是否能被工具拦住。
4. 最后把它写回离问题最近的位置。

评论层
对应动作
对应位置
失效症状


“这里像是自己在算权限”
识别目录边界
README
读者不知道这目录在管什么


“跨层依赖看起来不稳”
检查依赖方向
lint / 测试
任务会继续越层碰撞


“交付证据不够”
补验证和截图
PR 模板
复查只能靠口头补充


“下次还会问同一句”
写成 checklist
review 清单
同样的问题重复出现

终局模板复制

```text
评论：
边界：
验证：
回写：
下次读取位置：
```


---

# 学习目标 {#goals}
<!-- tag: goal -->
<!-- layout: grid2 -->

## box
这页要练的是把“这里不对劲”改写成能检查、能阻止、能回看的规则。你要会分清什么只是建议，什么已经该升格成不变量。
看到一条判断时，先判断它适合写进哪里，再决定写成 lint、测试、目录说明，还是 review 清单。

## box soft
### 读完后你应该能做什么
- 把一条模糊的品味判断改写成不变量。
- 把规则放到离问题最近的位置。
- 给反例、测试和审查标准配上同一条约束。

你先学会什么
对应动作
验收方式


区分建议和不变量
把口头偏好缩成一句规则
能说清它为什么不能再靠人记忆


选择落点
写进目录 README、lint、测试或 review
知道哪一层最适合拦住错误


补回写位置
把规则写到最近的地方
下次遇到同类问题能直接读到


---

# 主线：把品味拆成可执行对象 {#overview}
<!-- tag: map -->
<!-- layout: grid2 -->

## box
这页的起点很简单：人看到一段代码时，会觉得“这里不对劲”。Agent 不会自动理解这种不对劲，所以要把判断拆成它能读、能检查、能拒绝的对象。
这一页只关心四种对象：不变量、反例、测试和审查标准。先有判断，再选载体，最后写回到最近的位置。这样一条偏好才会变成可执行规则。

## box info
### 学习路线
先看什么是判断，再看什么能变成规则，接着看规则怎样落到 lint、测试和 review，最后看怎样把一次失败写回仓库。

顺序
你先看什么
要回答什么
下一步动作


1
品味判断
哪里会让维护变难
把判断缩成一句话


2
不变量和反例
什么不能破，破了会怎样
写成可检查规则


3
测试和审查标准
怎样机械拦住错误
把规则放进工具和模板


4
写回位置
同类错误下次在哪里读到
补最近的目录或模板


---

# 品味阶梯：从判断到约束 {#ladder}
<!-- tag: ladder -->

层级
它回答什么
最适合落到哪里
错了会怎样


品味判断
这里会不会让后续维护痛苦
脑中判断、review 讨论
只能靠经验解释，不能复用


不变量
什么形状绝不能破
目录 README、审查清单、lint
偏好被写成口号，没人真遵守


反例
边界外会长什么样
说明文档、review 备注
只知道对法，不知道错法


测试
怎样机械拦住错误
单测、lint、CI
人总会漏看，规则没有刹车


审查标准
什么时候该放行，什么时候必须拦住
PR 模板、review checklist
每次都重新争论同一件事


---

# 读写矩阵：一条规则怎么落到三处 {#mechanism}
<!-- tag: matrix -->

对象
读什么
写什么
什么时候会失效
失效症状


目录 README
目录职责、禁区、常见命令
最近的规则和边界
目录职责变化
Agent 改到无关层


lint / test
规则声明和检查输入
机械拦截结果
规则没同步
口头规则和工具不一致


review checklist
本次任务的风险点
放行条件和拦截条件
任务类型变化
同类错误反复出现

$$
 \text{可执行约束} \approx \text{不变量} + \text{反例} + \text{测试} + \text{审查标准}
 $$
 这里的“约束”不是口号。它必须能被人读到，也最好能被工具拦住。


---

# 转化观察：同一条判断，四种强度 {#trace}
<!-- tag: trace -->

<!-- interactive: ['shape-tracer'] → keep as HTML -->

输入
中间层
最强落点
保住了什么


“别让复杂逻辑散在各处”
职责集中
目录边界 + review 清单
维护者知道哪里该找，哪里不该碰


“错误别被吞掉”
失败必须可见
测试 + 日志断言
问题不会悄悄消失


“把可复用的东西收起来”
重复度控制
审查标准 + 重构建议
不会越写越散


---

# 示例：把“不要乱写权限”改成规则 {#example}
<!-- tag: example -->
<!-- layout: grid2 -->

## box
假设一个模块需要处理账户操作。你本来会说：“权限要谨慎一点。”这句话有方向，但对 Agent 没有约束力。它不知道什么叫谨慎，也不知道哪些地方算越界。
现在换成一条不变量：这个目录只能读取权限结果，不能自己生成权限判断；所有 allow / deny 必须来自统一 policy 层。这样写以后，Agent 至少知道两件事：哪里不能自己发明判断，哪里需要去找真正的决策源。
再往前一步，把这条规则写进 lint 或架构测试，错误就会在合并前被拦住。最后再在 review checklist 里补一句“是否有目录越权、是否有新的权限来源”，人和工具就会检查同一件事。

## box warn
### 这个例子里有哪些可检查对象
- 目录边界：模块能不能自己做权限判断。
- 规则来源：allow / deny 是否都来自 policy 层。
- 验证方式：lint、架构测试、review checklist。

最小规则片段复制

```text
目录边界：
这个目录只读权限结果，不生成权限判断。
allow / deny 必须来自统一 policy 层。
发现新的权限来源时，先停下来做 review。
```

如果你看到
说明什么
写回哪里


模块自己开始算权限
目录边界太软
目录 README


不同地方各写一套 allow / deny
规则来源分叉了
lint / 架构测试


review 一直追问“权限从哪来”
交付标准没写清
PR 模板或 checklist


---

# 审查台：同一条规则，三种处理方式 {#lab}
<!-- tag: lab -->

<!-- interactive: ['terminal-lab'] → keep as HTML -->

版本
信息密度
能否直接执行
适合留在哪里


口号版
低
不能
最多留在讨论里


不变量版
中
可以检查
目录 README / review


工具版
高
可以自动拦截
lint / test / CI


---

# 写回桥：把一条规则放回最近的位置 {#bridge}
<!-- tag: bridge -->
<!-- layout: grid2 -->

## box
写回的原则只有一个：让下一个遇到同类问题的人，能在离问题最近的地方读到它。目录级边界写到目录 README，机械能检查的规则写到 lint 或测试，交付时需要看的东西写到 PR 模板。
不要把所有内容都堆进一份大文档。那样做看上去很完整，实际上下一次还是要从头搜索。

## box soft
### 最小写回顺序
1. 先说清规则是什么。
2. 再说它应该落到哪里。
3. 最后说它会阻止什么错。

写回最小清单复制

```text
1. 目录边界：写进最近的目录 README。
2. 机械规则：写进 lint 或测试。
3. 交付证据：写进 PR 模板。
4. 复审关注点：写进 review checklist。
```


---

# 边界：这一页先追到哪里 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
这一页不负责讨论所有工程美学，也不负责把每一种代码风格都升格成硬规则。它只处理那些会长期伤害维护和协作的部分：依赖方向、权限来源、目录职责、证据字段和审查标准。
如果你已经在想“哪些东西可以只留作建议”，那就是下一层的问题。先把必须硬化的部分守住，再谈保留余地。

## box warn
### 先别追的附近概念
- 代码风格统一。
- CI 平台选型。
- 形式化证明。
- 框架内部实现细节。

问题
优先写到哪里
为什么


这个目录不能碰支付协议
目录 README
它是当前目录边界，不是全仓库总则


service 不能依赖 UI
lint / 架构测试
可以机械判断，适合工具拦截


这次改动必须附截图
PR 模板
这是交付证据字段，不是实现细节


---

# 常见误区：把偏好写成口号 {#mistakes}
<!-- tag: mistakes -->

误区
错在哪里
后果
怎么修


只写“要优雅”
没有可执行对象
Agent 继续猜
改成不变量或审查标准


所有偏好都升格成硬规则
硬约束太多
规则噪声大，维护困难
只保留会长期伤害系统的部分


只有正例，没有反例
边界外长什么样没讲
知道对法，不知道错法
补一个错法、后果、修法


能自动化却只靠 review
机械规则没进工具
人一直重复检查
把规则写进 lint 或测试


交付只看结果，不看证据
没有验收字段
复查困难
在 PR 模板里补证据位


---

# 易混概念对照 {#compare}
<!-- tag: compare -->

| 概念组 | 容易混的地方 | 区分方式 | 工程判断 |
| --- | --- | --- | --- |
| 品味 / 规则 | 都影响代码形状。 | 品味是判断来源，规则是可传递表达。 | 能表达清楚的品味尽量规则化。 |
| 建议 / 不变量 | 都可能写在文档里。 | 建议可权衡，不变量违反会破坏系统。 | 不变量要少而硬。 |
| 反例 / 测试 | 都能暴露错误。 | 反例用于理解边界，测试用于自动阻止。 | 先用反例讲清，再看能否自动化。 |
| 审查 / CI | 都守质量。 | CI 守机械底线，审查看设计权衡。 | 不要让人类重复做 CI 的事。 |


---

# 落地流程：把判断变成约束 {#workflow}
<!-- tag: workflow -->
<!-- layout: grid2 -->

## box
先看一个很常见的场景：支付模块 review 时，你不断听到“这里不稳”“这里像是越权”“这里会不会散”。这三句话都在指向同一类问题，但它们还不是约束。你真正要做的，是把这些判断拆成几个可检查的对象：依赖方向、权限来源、目录职责、验证命令和回写位置。
拆完以后，再决定每个对象写到哪里。能机械检查的，放进 lint、测试或架构校验；会被人反复问到的，放进目录 README 或 PR 模板；只用于理解边界的，放进反例说明。这样做的目的很直接：同类错误下次不用重新解释，工具和人会同时看到同一条线。
如果你只把它写成一段长文，下一次还是得搜索；如果你把它写进最近的入口，Agent 就会在执行时撞到它。约束的价值，不在于说得更重，而在于读得到、查得到、拦得住。

## box soft
### 最小落地顺序
1. 先抓信号，确认它不是一次性的吐槽。
2. 再把信号缩成一句规则，别让它继续漂着。
3. 然后选落点，优先选离问题最近的地方。
4. 最后补验证和反例，让规则真的能工作。

阶段
你在读什么
你在写什么
如果漏了会怎样


抓信号
review 里重复出现的担忧
问题句和边界句
每次都从头讨论，无法积累


缩规则
判断背后的共同点
一条不变量
偏好只剩口号，没人知道何时违背


选落点
当前任务的入口和最近读者
目录 README、lint、测试或 PR 模板
规则写远了，下一次还是看不到


补证据
这个规则如何被验证
反例、检查项、验证命令
人只能讲道理，工具没法拦住错法


回写
同类问题下次从哪读
最近的位置和稳定表述
同类错误继续回流到聊天记录

约束写法模板复制

```text
判断：
1. 先把模糊感受说成可检查对象。
2. 只保留会长期伤害维护的部分。
3. 给出一个最小反例。
4. 指明最近的写回位置。
5. 补上一个能在合并前运行的检查。
```

$$
 \text{约束强度} \approx \text{可检查性} + \text{就近性} + \text{可复用性}
 $$
 一条规则越容易被读到、越容易被验证、越容易复用，下一次越不需要再解释同一件事。


---

# 失败回写：一次 review 争议怎样变成规则 {#repair}
<!-- tag: repair -->
<!-- layout: grid2 -->

## box
再看一个更接近真实工作的例子。某个支付目录每次改动都会被追问同一句话：“权限从哪来？”这说明问题不在单次实现，而在规则没有写清。你如果每次都靠口头解释，Agent 和人都只是在临时补洞；下次换一个人、换一个任务，洞还是会出现。
正确的做法是把争议转成稳定对象：目录边界、规则来源、验证方式、回写位置。这样一来，review 看到的“这里的权限只能来自 policy 层；如果出现新来源，lint 和架构测试会先拦住”。
这一步的关键是少而硬。规则不是越多越好，规则多了以后，真正影响维护的部分反而会被噪声盖掉。你要保留的是能长期阻止返工的那部分，不是把所有审美都变成硬限制。

## box warn
### 把争议变成规则时先问三件事
- 它会反复让谁多花时间？
- 它能不能在合并前被检查？
- 下次遇到同类问题，最先应该在哪读到它？

原始说法
改成什么
放到哪里
拦住什么错


不要乱写权限
这个目录只读权限结果，不生成权限判断
目录 README
模块自己开始算权限


别写得太散
service 不依赖 UI，跨层引用视为违规
lint / 架构测试
依赖方向继续发散


交付时要补证据
PR 必须附截图和验证命令
PR 模板
只给结果，不给复查材料


老是被追问同一件事
把问题写成 review checklist
review 清单
人每次都重复问一遍

回写最小清单复制

```text
1. 找到重复争议。
2. 写成一句不变量。
3. 选择最近的写回位。
4. 加上一个最小验证。
5. 保留一个能说明边界的反例。
```


---

# 规则集：四种写回位分别负责什么 {#ruleset}
<!-- tag: ruleset -->

写回位
它负责什么
适合放什么句子
不适合放什么


目录 README
写清职责、禁区和最近的入口
目录边界、依赖方向、常用验证命令
抽象的大道理和全仓库总则


lint / 测试
拦住能机械判断的错误
跨层引用、缺少字段、违反结构的实现
审美偏好和需要讨论的权衡


review checklist
帮人重复检查最容易漏的点
权限来源、证据字段、回归验证、反例
每次任务都不同的临时思路


PR 模板
把交付证据固定下来
截图、命令、结果、风险说明
实现细节和冗长复述

## box
这四个位置不是互相替代的。目录 README 负责最近的职责边界，lint 和测试负责机械刹车，review checklist 负责重复关注点，PR 模板负责证据。把它们混在一起，结果就是每次都在错误的位置读规则。
真正稳定的写法是少而硬。你只保留足够拦住返工的句子，别把每个审美都写成红线。这样 Agent 在执行时才知道什么不能碰，人类在审查时才知道什么必须拦。

## box soft
### 判断顺序
1. 先问这条规则是不是机械可查。
2. 再问它应该靠近哪个入口。
3. 最后问它会阻止哪一种重复错误。


---

# 案例链：支付目录怎样把争议变成规则 {#casebook}
<!-- tag: casebook -->
<!-- layout: grid2 -->

## box
假设一个支付目录连续三次被 review 卡住。第一次，审查人说“这里像是自己在算权限”；第二次，审查人说“跨层依赖看起来不稳”；第三次，审查人说“交付证据不够”。这三个反馈看上去不是一件事，其实都指向同一件事：目录边界没有被写成稳定规则。
这时候不要继续写解释信。你应该先把争议拆开：权限来源写到哪个层，跨层依赖要不要机械拦，交付证据写到哪里，验收命令放哪里。拆的时候不要追求面面俱到，只保留会长期让人返工的部分。真正该写进规则的，是以后还能复用、能被读到、能被检查的那几条。
然后再把它们放回最近的位置。目录 README 负责讲边界，lint 负责拦跨层引用，PR 模板负责收证据，review checklist 负责把“再看一眼”变成固定动作。这个顺序的意义很简单：下次同类问题出现时，读的人不用重新问一遍，Agent 也不会再把它当成自由发挥的空间。

## box soft
### 这类争议最容易漏掉的东西
- 争议到底是边界问题，还是实现选择问题。
- 它是否已经重复出现，足够升格成规则。
- 它应该被谁先看到，目录还是工具还是模板。
- 它会不会在别的目录再次发生。

反馈
它真正想说什么
应该写成什么
放到哪里


这里像是自己在算权限
权限来源不稳定
目录只读权限结果，不生成判断
目录 README


跨层依赖看起来不稳
依赖方向没收住
service 不依赖 UI，跨层引用视为违规
lint / 架构测试


交付证据不够
复查材料缺失
截图、命令、结果写进模板
PR 模板


下次还是会问同一句
审查关注点没沉淀
把问题写成 checklist
review 清单

目录规则片段复制

```text
目录边界：
1. 这个目录只读权限结果，不生成权限判断。
2. service 层不能直接依赖 UI 层。
3. 交付时必须附验证命令和截图。
4. 新争议出现时，先写回 README，再补 lint 或 checklist。
```


---

# 跟踪链：同一条判断怎么一路收紧 {#trace2}
<!-- tag: trace -->

<!-- interactive: ['shape-tracer'] → keep as HTML -->

输入句子
收紧后怎么写
落点
会挡住什么


不要乱写权限
目录只读权限结果，不生成权限判断
README
目录自己造判断


别让依赖乱掉
service 不依赖 UI
lint / 测试
跨层引用发散


交付时要更清楚
必须附验证命令和截图
PR 模板
只有口头说明，没有证据


老是被问同一件事
写成 review checklist
review 清单
同类问题继续复现


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、先分清能不能硬化 -->
<!-- quick-review: 复习 先找风险，再找规则。 -->

### 04-q1 choice

**Q:** 1. 工程品味要传给 Agent，最好先变成什么？

- [ ] 具体风险和约束
- [ ] 更强烈的语气
- [ ] 更多赞美或批评

Answer: 具体风险和约束

### 04-q2 true/false

**Q:** 2. 所有风格偏好都应该变成不变量。

- [ ] 对
- [ ] 错


### 04-q3 fill

**Q:** 3. 一个反例最好包含错法、后果和______。

Answer: 修法

### 04-q4 choice

**Q:** 4. 能机械判断的规则优先放到哪里？

- [ ] lint 和测试
- [ ] 代码行数
- [ ] Commit message

Answer: lint 和测试

<!-- end-practice-group -->

<!-- practice-group: 二、把规则落到位置 -->
<!-- quick-review: 复习 落点决定约束强度。 -->

### 04-q5 choice

**Q:** 5. CI 和 review 各适合守什么？

- [ ] CI 守机械底线，review 守设计权衡
- [ ] CI 守审美，review 守格式
- [ ] review 守一切

Answer: CI 守机械底线，review 守设计权衡

### 04-q6 checkbox-diagnosis

**Q:** 6. 下面哪些句子已经适合升格成不变量？

- [ ] service 不依赖 UI
- [ ] 代码要写得优雅
- [ ] allow / deny 必须来自统一 policy 层
- [ ] 多写注释更清楚

Explain: 应选“service 不依赖 UI”和“allow / deny 必须来自统一 policy 层”。“代码要写得优雅”“多写注释”都太泛，先留作建议。

### 04-q7 synthesis

**Q:** 7. 把“不要乱写权限”改成一条不变量，并说明落点。

Explain: 答案应同时包含三件事：不变量本身、它落到哪里（目录 README、lint、架构测试或 PR 模板）、以及它会阻止什么错误。只写“要谨慎”不算完成。

### 04-q8 fill

**Q:** 8. 目录级边界优先写到哪里？

Answer: 目录 README

### 04-q9 choice

**Q:** 9. 如果 review 一直追问“权限从哪来”，说明缺了什么？

- [ ] 规则来源
- [ ] 命名风格
- [ ] 测试脚本

Answer: 规则来源

<!-- end-practice-group -->

<!-- practice-group: 三、把规则写成可执行句子 -->
<!-- quick-review: 复习 先写句子，再写证据。 -->

### 04-q10 choice

**Q:** 10. 下面哪句话更适合作为反例的作用说明？

- [ ] 告诉边界外会怎样
- [ ] 让文档更好看
- [ ] 替代测试

Answer: 告诉边界外会怎样

### 04-q11 synthesis

**Q:** 11. 为一个支付模块写 3 条少而硬的不变量。

Explain: 好的答案要少而硬，最好覆盖权限来源、数据一致性、跨层依赖、失败时是否可回滚、是否需要人工确认等主题。只写“要安全”不算完成。

### 04-q12 synthesis

**Q:** 12. 用 6-9 句话讲清：怎样把工程品味变成可执行约束。

Explain: 合格答案应串起四步：先识别让维护变难的地方，再把判断缩成不变量或反例，再选择 lint、测试、review 或目录说明作为落点，最后说明边界和写回位置。能写出具体例子会更好。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- [Harness engineering: Leveraging Codex in an agent-first world](https://openai.com/zh-Hans-CN/index/harness-engineering/)，OpenAI Engineering，2026-02-11。本页主要取“品味要变成约束、反例、测试和审查标准”的工程主线。
- [Harness Engineering 课程地图](../index.html)，用于确认本页在课程中的位置，以及它和前后页面的关系。

---
