# Source Pack: 01_tokenizer_embedding_contextwindow

## Source Boundary

- Course root: `LLM基础与应用工程`
- Unit: `01_tokenizer_embedding_contextwindow`
- Snapshot / 访问日期: `2026-06-21`
- Source material used in this run:
  - course-root `index.html`
  - existing unit page `学习页.html`

## Source-To-Unit Notes

- Course-root `index.html` provides the course order and confirms this unit is the
  first foundation block before Transformer, role boundaries, HuggingFace,
  retrieval, prompt, eval, RAG, Agent, and production-constraint units.
- Existing `学习页.html` supplies the content spine for this unit:
  tokenizer, token id, embedding, context window, padding/truncation/mask,
  shape trace, worked examples, diagnostic questions, and practice export.
- This rewrite should preserve the same learning target but make the page
  consistent with the learning-kit shared shell and Chinese prose standard.

## Unit Role

- Foundation unit.
- Teaches the first input boundary for LLM work: how text becomes tokens, ids,
  vectors, and a length-bounded prompt.
- Keeps the scope before attention, training/inference role boundaries, RAG,
  Agent, and production constraints.

## Gaps And Notes

- No external web sources were used in this run.
- Any ordering beyond the explicit course map is an editorial recommendation,
  not a source claim.
- The page should stay source-faithful to the existing lesson rather than
  inventing new operational claims.
