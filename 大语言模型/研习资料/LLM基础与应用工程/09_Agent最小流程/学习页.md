---
course: "LLM 基础与应用工程"
unit: "09｜Agent最小流程"
title: "LLM 基础 09｜Agent 最小流程：让工具循环能停下来"
kicker: "agent loop"
summary: "这一页把 Agent 讲成一个受控循环，避开“让模型自己想办法”的黑盒想象。你会沿着同一个订单查询例子看清：模型只提出下一步动作，程序负责校验工具、执行工具、记录观察和判断是否停止。学完后，你应该能设计一个小工具集，也能看日志判断 Agent 是卡在计划、工具、状态、权限，还是停止条件。"

side_note: "这一页把 Agent 压到一个可检查的工具循环：模型提出动作，程序校验和执行，观察回到状态，系统能继续也能停。"

can_do:
  - "区分固定 workflow、单次 tool use 和能根据观察继续/停止的 Agent 循环。"
  - "写出最小 Agent 的状态字段、动作 schema、工具规格、权限校验和停止条件。"
  - "用日志和样例诊断循环不停、重复提问、误调用工具、越权执行和高风险动作未确认。"

course_links:
  - text: "上一节：RAG 最小流程"
    href: "../08_RAG最小流程/学习页.html"
  - text: "课程地图"
    href: "../index.html"
  - text: "下一节：延迟、成本、可靠性"
    href: "../10_延迟成本可靠性约束/学习页.html"

---

# 学习目标 {#goals}
<!-- tag: goals -->
<!-- layout: grid2 -->

## box soft
### 先把 Agent 缩到能检查的一步
这节课不从“自主智能体”这种大词开始。工程上更稳的入口是一个小循环：给定目标和状态，模型选择一个结构化动作；应用代码校验动作和权限；工具执行后返回观察；系统更新状态，再决定继续、澄清、人工确认或停止。
08 页的 RAG 链路是固定路径：检索、拼上下文、生成回答。Agent 在这里多出“路径选择”这一层。它可以根据观察决定下一步查订单、查政策、问用户，或者直接回答。路径一旦可变，就必须把工具边界、状态和停止条件写清楚。

## box
### 本页不追的内容
- 不讲多 Agent 协作和复杂 handoff，只保留单个 Agent 的闭环。
- 不讲长期记忆系统，只讲当前任务需要的短期状态。
- 不绑定某个框架，把 SDK、planner、浏览器自动化放到后续专题。
- 不让模型直接执行高风险动作，程序侧始终保留权限和确认。


---

# 先抓主线 {#overview}
<!-- tag: map -->

目标→状态→模型选择动作→程序校验→工具执行→观察→继续或停止

判断一个流程是否进入 Agent 形态，先看模型有没有参与下一步选择。固定执行“检索 → 生成 → 返回”的链路通常是 workflow；单次让模型生成工具参数是 tool use；模型能看见观察结果，再选择下一步工具或停止，才进入 Agent 的最小形态。

Workflow
路径由程序固定安排。模型只在某个步骤里填答案或提取字段。优点是可控，风险主要来自每一步质量。


Tool use
模型可以请求工具，应用代码执行工具并把结果交回。它可能只有一次调用，也可能被放进循环。


Agent
模型根据目标、状态和观察选择下一步。风险多了路径选择、重复调用、越权动作和无法停止。


---

# 核心机制 {#mechanism}
<!-- tag: mechanism -->
<!-- layout: grid2 -->

## box
### 模型提出请求，程序执行动作
Agent 里的模型不直接联网、查库、发邮件。模型输出的是结构化动作，例如 `ask_user`、`call_tool`、`final`。应用代码负责解析动作、检查 schema、校验权限、调用工具、记录日志，再把观察结果写回状态。
这个分工很重要。模型擅长在语义上下文里选择下一步；权限、事务和审计属于程序侧。只要工具能读写外部资源，执行权就应该留在程序侧。

## box
### 循环能继续，也必须能停
每一步结束后都要问两个问题：信息是否足够，风险是否可接受。足够就返回 `final`；缺关键输入就 `ask_user`；达到步数、预算、失败次数或风险阈值就停止或转人工。
停止条件是硬边界。缺少停止条件的 Agent 会反复搜索、重复提问、持续消耗 token，或者把一次工具失败误当成继续行动的理由。

<!-- interactive: ['shape-tracer'] → keep as HTML -->


---

# 状态台账 {#ledger}
<!-- tag: ledger -->

Agent 的状态是当前任务决策需要的压缩事实，不需要保存整段聊天记录。下面这张表用订单查询例子展示每一步读什么、写什么、错了会出现什么症状。

对象进入本轮前本轮读取本轮写入错了的症状下一步小检查

`goal`解释订单为何未发货用户问题和任务边界通常不改，只可收窄Agent 扩权去退款或改地址目标里是否写了不做事项
`known_facts`用户已登录，给了订单号身份、订单号、已有澄清补充新事实重复问已经给过的信息日志里是否写回用户输入
`observations`空或上一轮工具结果工具返回、错误码、来源追加本轮观察下一步不受工具结果影响观察是否包含可用字段
`steps_left`3当前步数和最大步数每轮递减循环停不下来是否在每轮末尾检查
`risk_flags`无高风险动作动作类型和资源范围标记是否需人工确认自动发送补偿承诺高风险工具是否默认阻断

## box info
### 读日志时先看状态
如果 Agent 行为奇怪，先把本轮输入状态、模型动作、工具观察和停止判断打印出来。很多问题来自状态没有写回、工具观察太模糊、停止条件没有被检查，改 prompt 只能缓解表面症状。


---

# 动作协议 {#action-schema}
<!-- tag: schema -->
<!-- layout: grid2 -->

## box
### 把“下一步”收进三种动作
第一版先控制动作面。常见最小集合是：`final` 返回答案，`ask_user` 请求澄清，`call_tool` 请求工具。动作必须有固定字段，程序只接受 schema 合法、权限合法、工具名在白名单里的请求。
动作里可以保留 `reason` 字段用于日志，但理由不能当作权限证明。模型说“我需要查这个订单”不代表它有权查；权限仍由程序根据用户身份、资源归属和工具范围决定。

## box soft
### 最小 action JSON
agent-action-schema.json复制
```json
{
 "type": "call_tool",
 "tool": "get_order_status",
 "args": {
 "order_id": "A-1024"
 },
 "reason": "需要确认订单当前发货状态",
 "risk": "read_only"
}
```


动作类型模型输出程序侧处理常见错误

`final`最终回复检查是否有足够证据、是否越界承诺证据不足却编答案
`ask_user`一个澄清问题结束本轮，等待用户输入反复问同一个问题
`call_tool`工具名和参数schema 校验、权限校验、执行工具未知工具、参数错、越权资源
`handoff`转人工或转专门流程暂停自动执行，写明原因高风险动作仍自动执行


---

# 工具规格 {#tool-spec}
<!-- tag: tools -->
<!-- layout: grid2 -->

## box
### 工具描述要写“什么时候不用”
很多工具描述只写能力，比如“查询订单状态”。这不够。Agent 还需要知道什么时候不该用：缺订单号不能查，订单不属于当前用户不能查，查询政策不等于承诺补偿，发送邮件需要人工确认。
工具越强，边界越要硬。只读工具可以自动化得多一些；写入外部系统、发消息、改订单、退款、删除文件这类动作，要默认进入确认或人工审批。

## box
### 工具面先小后大
工具多会扩大动作空间，也会增加误选工具和参数错配。最小 Agent 先给 2-3 个工具就够：查订单、查政策、请求人工确认。等 Eval 覆盖了常见路径，再逐步增加工具。
如果一个任务用固定 workflow 就能稳定完成，优先用 workflow。Agent 适合路径需要根据观察动态变化的场景。

字段`get_order_status``lookup_shipping_policy``request_human_review`

用途查询当前用户订单状态查发货和缺货政策暂停并请求人工处理
参数`order_id``policy_topic``reason`、`risk_level`
返回状态、预计时间、错误码政策摘要、来源、更新时间工单号、等待状态
限制只能查当前用户订单不能生成赔付承诺高风险动作必须走这里
失败处理无权限、没找到、服务超时资料缺失、版本过旧创建失败时停止并说明


---

# 订单例子 {#example}
<!-- tag: worked case -->
<!-- layout: grid2 -->

## box
### 先看固定 workflow
固定 workflow 可以很好用。比如客服问答固定执行“查 FAQ → 生成答案 → 记录日志”。模型没有选择下一步路径，只在固定位置生成文本。这种结构更容易控，也更容易测。
fixed-workflow.py复制
```python
def answer_with_rag(question):
 chunks = retrieve(question)
 answer = generate_answer(question, chunks)
 log({"question": question, "chunks": chunks, "answer": answer})
 return answer
```


## box
### 再看受控 Agent 循环
下面的循环让模型选择动作，但执行权仍在程序侧。注意三个保护点：`max_steps` 限制循环，`validate_action` 检查 schema 和权限，未知工具被写成观察结果，不能直接执行。
minimal-agent-loop.py复制
```python
def run_agent(goal, state, model, tools, max_steps=3):
 for step in range(max_steps):
 action = model.decide(goal=goal, state=state, tools=tool_specs(tools))

 if not validate_action(action, state):
 state["observations"].append({
 "ok": False,
 "error_code": "invalid_or_unauthorized_action"
 })
 continue

 if action["type"] == "final":
 return action["message"]

 if action["type"] == "ask_user":
 return action["question"]

 if action["type"] == "call_tool":
 tool = tools.get(action["tool"])
 if tool is None:
 state["observations"].append({
 "ok": False,
 "error_code": "unknown_tool"
 })
 continue

 observation = tool.call(action["args"])
 state["observations"].append(observation)
 update_state_from_observation(state, observation)

 return "我先停止处理：信息不足或已达到自动处理上限。"
```


## box info
### 这段代码该怎么读
模型每轮只产生动作，程序负责决定能不能执行。工具返回后，观察要写回状态。下一轮模型看到的是新的状态，随后基于新事实继续。循环末尾的停止文本也不是失败，它说明系统没有把不确定性继续放大。


---

# 一轮日志怎么读 {#trace}
<!-- tag: trace -->

Agent 调试最容易漏掉的是“这一轮到底把什么交给了下一轮”。下面用同一个订单例子走一轮日志。最终回复只是一部分，还要同时看模型动作、程序校验、工具观察和状态更新。

时间点可见对象内容应该检查什么

输入状态`state``goal=解释未发货`，`order_id=A-1024`，`steps_left=2`订单号是否来自当前用户，本轮是否还有步数
模型动作`action``call_tool get_order_status(order_id=A-1024)`动作类型、工具名、参数是否符合 schema
程序校验`guardrail``owner(user_id)==current_user`，工具是只读权限是否由程序判断，而不是由模型自述判断
工具观察`observation``ok=true`，`status=paid_waiting_stock`，`eta=tomorrow`观察是否足够支持回答，是否带错误码或来源
状态更新`known_facts`追加“订单已付款，仓库缺货，预计明天补货”下一轮能否看到这个事实，避免重复查询
停止判断`stop_check``enough_info=true`，`risk=read_only`是否应该 final，而不是继续调用工具

## box
### 如果状态没更新
下一轮模型只知道“查过工具”，却不知道查到了什么。它可能再次查询同一个订单，或者问用户已经回答过的问题。这个时候改 prompt 通常没用，因为问题出在状态写回：观察结果没有变成下一轮可读的 `known_facts`。

## box
### 如果停止没执行
即使观察已经足够回答，Agent 仍可能继续查政策、查物流、查仓库，最后把一个简单问题拖成多轮工具链。修法是把 `enough_info`、`steps_left`、`failure_count` 和 `risk_flags` 放进每轮末尾的硬检查，prompt 提醒只能作为辅助手段。

## box soft
### 练习区会怎么用这条链
后面的诊断题会要求你按这条日志链定位问题。循环不停先看停止判断和观察写回；越权查询先看 guardrail；重复提问先看 `known_facts`；高风险动作先看风险标记和人工确认。
实际排查时可以把每一轮日志压成五行：输入状态、模型动作、程序校验、工具观察、停止判断。五行里哪一行缺字段，就先修哪一层。比如输入状态没有 `steps_left`，就先补状态和递减逻辑；工具观察只有一句“失败”，就先补错误码；停止判断没有读取 `risk_flags`，就先把高风险动作接到转人工。
修完以后，下一次日志应该能说明“为什么这轮没有继续”：例如 `enough_info=true` 导致 `final`，或 `risk=write` 导致 `request_human_review`。这就是最小验收，也方便后续稳定回归测试。


---

# 故障实验 {#failure-lab}
<!-- tag: failure lab -->

Agent 出问题时，先看它在哪个边界坏掉：状态没写回、工具太宽、权限没拦住，还是停止条件没执行。提示词调整放在定位之后。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

症状优先看哪里常见根因修法

循环不停停止判断和工具观察没有 max_steps、观察没写回、模型不知道信息已够加停止条件，压缩观察，要求足够信息时 final
重复提问`known_facts`用户已提供信息没有进入状态把输入写入结构化状态，并在下一轮传入
误调用工具工具描述和 schema工具用途太泛，没有写不适用条件缩小工具面，写限制和失败类型
越权查询权限校验把资源范围交给模型自觉判断程序侧校验用户、资源和动作权限
高风险动作未确认风险标记和 handoff写工具直接暴露给模型默认阻断，转人工或确认后执行


---

# 评测路径 {#eval}
<!-- tag: eval -->
<!-- layout: grid2 -->

## box
### Agent Eval 不只看最终答案
07 页已经讲过样本、指标、基线和回归。到了 Agent，评测对象要扩展到路径：是否该调用工具、工具选得对不对、参数是否合规、是否越权、步数是否合理、需要停时有没有停。
如果只看最终答案，一个 Agent 可能碰巧答对，但中间查了不该查的资源，或者多调用三次工具才得到结果。这种路径在生产里会变成成本、延迟和合规风险。

## box soft
### 最小验收样本
- 信息足够：应直接 final，不额外查工具。
- 缺订单号：应 ask_user，不猜订单。
- 有订单号且属于当前用户：应调用只读查询工具。
- 订单属于别人：应拒绝或澄清，不执行查询。
- 需要补偿承诺：应转人工，不自动发送。
- 工具超时：应记录失败并按重试/停止规则处理。

指标看什么通过标准失败后怎么查

任务完成最终是否解决用户问题回答与观察一致看证据和 final 文本
工具选择是否调用了正确工具该查才查，该问才问看 action log 和工具描述
参数合规工具参数是否满足 schema类型、必填项、资源范围合法看 validate_action 输出
权限安全是否访问不该访问的资源无越权读写看 user_id、resource_owner、guardrail
停止质量是否该停时停下不空转，不过度重试看 steps_left、failure_count、enough_info


---

# 边界 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
### 什么时候不要上 Agent
路径固定、风险高、可用规则明确时，优先 workflow 或普通工具调用。比如“把一段文本改成 JSON”“从固定知识库检索再回答”“按表单字段生成摘要”，路径本身无需动态选择，引入 Agent 只会增加不确定性。
Agent 的价值在于路径需要随观察变化：缺字段就问用户，查到缺货就查政策，发现高风险就转人工。没有这种分叉，先别引入循环。

## box
### 下一页会追什么
这页把循环跑通，下一页要检查循环能不能上线：每多一步工具调用都会带来延迟，每次模型决策都会消耗成本，工具失败和重试会影响可靠性。09 页的停止条件和日志，是 10 页讨论成本、延迟、可靠性的前置条件。
现在先把最小 Agent 控住：小工具、小状态、强校验、清日志、能停止。之后再扩展框架、长期记忆、多 Agent 和更复杂的任务编排。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box soft
### 练习目标
这组题检查你能不能从日志和场景里判断 Agent 的边界，并把“计划、行动、观察”落到具体修法。客观题可本地检查，开放题用参考答案自评。

## box
### 作答提醒
遇到诊断题，按“症状 -> 根因 -> 修法”写。不要只说“优化 prompt”，先看状态、schema、工具、权限和停止条件。

<!-- end-practice-intro -->

<!-- practice-group: 一、概念边界 -->
<!-- quick-review: 复习 Agent 的最小特征是模型能根据观察选择下一步，并且循环能停止。 -->

### 1 单选

**Q:** 1. 最能体现 Agent 的特征是什么？

- [ ] 回答更长
- [ ] 模型能根据观察选择下一步工具或停止
- [ ] prompt 里写了 Agent

Answer: 模型能根据观察选择下一步工具或停止

### 2 判断

**Q:** 2. 固定的“检索 -> 生成 -> 返回”流程一定是 Agent。

- [ ] 对
- [ ] 错

Answer: 错

### 3 填空

**Q:** 3. Agent 最小循环常见链路是目标、状态、动作、工具、______、停止。

Answer: 观察

### 4 排序

**Q:** 4. 排序：A 工具返回观察，B 模型决定 action，C 程序校验 action，D 更新状态，E 判断是否停止。

Answer: B-C-A-D-E

<!-- end-practice-group -->

<!-- practice-group: 二、工具和状态 -->
<!-- quick-review: 复习 工具描述要有用途、参数、返回、限制、失败类型和权限；状态只保留当前任务要用的事实。 -->

### 5 多选

**Q:** 5. 哪些字段应该进入一个最小工具规格？

- [ ] 用途
- [ ] 参数
- [ ] 返回
- [ ] 限制
- [ ] 权限
- [ ] 夸张名称

Answer: 用途,参数,返回,限制,权限

### 6 多选

**Q:** 6. 订单查询 Agent 的短期状态应优先保存哪些字段？

- [ ] goal
- [ ] known_facts
- [ ] observations
- [ ] steps_left
- [ ] risk_flags
- [ ] all_chat_tokens

Answer: goal,known_facts,observations,steps_left,risk_flags

### 7 单选

**Q:** 7. 用户给了订单号后，谁负责判断这个订单能不能查？

- [ ] 模型凭上下文判断
- [ ] 程序侧校验
- [ ] 不需要校验

Answer: 程序侧校验

### 8 简答

**Q:** 8. 为什么工具描述要写“什么时候不用”？

Explain: 因为工具描述不仅影响模型会不会调用，也影响它什么时候停手。写清不适用条件能减少误调用、越权查询和高风险动作自动执行。

<!-- end-practice-group -->

<!-- practice-group: 三、代码和日志阅读 -->
<!-- quick-review: 复习 模型提出 action，程序校验和执行；未知工具和无权限都应该变成观察或停止，不能直接执行。 -->

### 9 代码阅读

**Q:** 9. 代码里的 `max_steps` 解决什么问题？

Explain: max_steps 是硬停止条件，防止 Agent 在工具失败、观察无效或模型反复选择同一动作时无限循环和持续消耗成本。

### 10 代码阅读

**Q:** 10. 为什么未知工具不应该直接执行，而要记录成观察结果？

Explain: 工具执行权在程序侧。未知工具说明 action 不在白名单里，直接执行会突破边界；记录观察可以让模型知道请求失败，同时保留日志用于诊断。

### 11 多选

**Q:** 11. Agent 每一步日志至少应包含哪些字段？

- [ ] action
- [ ] tool
- [ ] args
- [ ] observation
- [ ] error_code
- [ ] latency_ms
- [ ] marketing_slogan

Answer: action,tool,args,observation,error_code,latency_ms

### 12 诊断

**Q:** 12. 如果删除 `tools.get(...)` 的白名单检查，会有什么风险？

Explain: 模型可能请求不存在或不该开放的工具。程序失去工具边界后，prompt 里的请求容易变成外部动作，带来越权、误写入和审计缺失。

<!-- end-practice-group -->

<!-- practice-group: 四、故障诊断 -->
<!-- quick-review: 复习 先定位坏在哪个边界，再决定改状态、工具、schema、权限还是停止条件。 -->

### 13 单选

**Q:** 13. Agent 反复调用同一个搜索工具，最先检查哪一项？

- [ ] 停止条件
- [ ] 标题字体
- [ ] 是否用了更长 prompt

Answer: 停止条件

### 14 诊断

**Q:** 14. Agent 已经问过用户订单号，下一轮又问同样问题。可能哪里设计错了？

Explain: 用户输入没有写回 known_facts ，或者下一轮没有把结构化状态传给模型。修法是把订单号写入状态，并让模型看到已知事实。

### 15 诊断

**Q:** 15. Agent 用用户提供的订单号查到了别人的订单。错在哪里？怎么修？

Explain: 错在把资源范围交给模型或工具默认放行。程序侧要校验当前用户、订单归属和动作权限；无权限时返回 observation 或拒绝，而不是执行查询。

### 16 诊断

**Q:** 16. Agent 自动给用户发了补偿承诺邮件。为什么危险？应该加什么护栏？

Explain: 这是外部写入和业务承诺，风险高。应把发送动作默认转人工或要求显式确认，工具规格里标记风险，并在程序侧阻断未确认执行。

<!-- end-practice-group -->

<!-- practice-group: 五、迁移设计 -->
<!-- quick-review: 复习 先做小工具、小状态、强停止，再扩工具和复杂路径。 -->

### 17 设计

**Q:** 17. 为“会议纪要整理助手”设计 3 个以内工具，并说明哪些动作需要人工确认。

Explain: 可用工具：读取会议文本、查参会人/议题、生成待办草稿。发送纪要、创建外部任务、通知参会人需要人工确认。状态至少保存会议 id、已读文本摘要、待办草稿和确认状态。

### 18 面试

**Q:** 18. 用 2-3 句话区分 workflow、tool use 和 Agent。

Explain: Workflow 的路径由程序固定，模型只执行其中某个步骤。Tool use 是模型请求一个工具，应用代码执行并返回结果。Agent 把 tool use 放进循环，模型能根据观察选择下一步或停止。

### 19 应用

**Q:** 19. Agent 的 Eval 除了最终答案，还应该评估什么？

Explain: 要评估工具选择、参数合法性、权限安全、步数和成本、是否该问用户、是否该转人工、是否该停不停，以及日志能否解释路径。

### 20 综合

**Q:** 20. 用 6-9 句话解释：什么是 Agent 的最小流程，为什么它必须有工具边界和停止条件。


<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- 课程地图：用于确认本页处在 RAG 和生产约束之间，重点是从固定检索链路进入可控工具循环。
- 上一节《RAG 最小流程》：承接“证据进入上下文后再生成”的链路，说明 Agent 只是把某些步骤变成可选择动作。
- OpenAI API 文档：Using tools、Function calling、Agents SDK 相关指南，用于确认工具调用、应用侧执行、状态、guardrails 和 human review 的工程边界。
- OpenAI Agents SDK 文档：Agents、Tools、Guardrails 页面，用于说明 agent、工具类型和护栏的基础分工。
- 本课程旧版 09 页：保留订单查询 Agent、工具规格、停止条件、错误诊断和练习骨架，并重构为 learning-kit 1.0 shared runtime 页面。

---
