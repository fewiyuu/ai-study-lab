---
course: "为什么大模型微调需要 PEFT"
unit: "01"
title: "为什么大模型微调需要 PEFT｜学习页"
kicker: "第 1 块"
kicker_en: "论文动机"
summary: "从全量微调的代价读懂 LoRA 的问题意识"

side_note: "先把问题讲清楚，再谈方法。LoRA 不是为了显得参数少，而是为了让大模型能服务多个下游任务。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "下一节"
    href: "../02_LoRA低秩更新公式/学习页.html"
---

# 1. 全量微调贵在哪里 {#problem}
<!-- tag: problem -->

直觉先说清楚：全量微调不是只在训练时贵，它还会把每个下游任务都变成一份新的大模型。模型越大，这件事越不像“多存几个文件”，更像给每个任务都准备一套完整机器。GPT-3 175B 这种规模下，复制一个任务 checkpoint 就已经是几百 GB 级别；如果一个服务要支持上百个任务，存储和切换都会变成实际系统问题。

术语上，full fine-tuning 是从预训练参数 `Phi_0` 出发，更新所有参数，得到 `Phi_0 + Delta Phi`。它的好处是表达能力强，模型所有层都能为新任务调整。代价也来自这里：每个任务学到的 `Delta Phi` 和原模型一样大，训练时还要为绝大多数参数保存梯度和 Adam 优化器状态。

论文的问题意识“当一个大模型要服务很多下游任务时，全量微调难以部署”。这点很重要。LoRA 要保住全量微调的任务质量，同时把任务相关的可训练参数压到很小，并且不要像一些 adapter 那样增加推理延迟。

判断句：如果一个方法让每个任务都要保存一整份 175B 参数，它就没有解决多任务部署的主要成本。你现在应该能把“训练成本”和“部署成本”分开说。

$$
full fine-tuning:   Phi = Phi0 + Delta Phi,   |Delta Phi| = |Phi0|
$$

数值对比scale**单个 GPT-3 任务全量权重**约 350GB FP16
**100 个任务全量微调**约 35TB
**共享底座 + 100 个 LoRA**约 354GB

上面的数字来自论文的工程估算：底座模型仍然需要存在，但每个任务不再复制整份模型，只增加很小的 LoRA 权重。你现在应该能解释为什么“每任务参数量”比“总模型参数量”更适合作为 PEFT 的核心指标。


---

# 2. 论文里的问题定义 {#objective}
<!-- tag: objective -->

论文把任务写成条件文本生成：给定输入 `x`，模型生成目标序列 `y`。训练目标是在下游数据集上最大化每个 token 的条件概率。这个公式看起来像语言模型常规训练，但这里的重点参数从哪里来、更新到哪里去。

全量微调直接优化 `Phi`。PEFT 改成优化一个更小的参数集合 `Theta`，再由 `Theta` 间接决定模型更新 `Delta Phi(Theta)`。换句话说，模型最终仍然要适配任务，但我们不允许每个任务自由改动所有权重，而是给它一个受控、低成本的更新通道。

这个写法帮你看清各类方法的共同点：adapter 学的是额外模块参数，prefix 学的是特殊 token 或层激活，BitFit 学的是 bias，LoRA 学的是低秩矩阵。它们都把“更新整个模型”改写为“学习小型任务参数”。

$$
PEFT:   maxTheta Σ log pPhi0 + Delta Phi(Theta)(yt | x, y<t),   |Theta| << |Phi0|
$$

判断句：只要能指出 `Theta` 是什么、它如何影响模型输出，你就能读懂一个 PEFT 方法的核心设计。你现在应该能从公式里分出“底座参数”和“任务参数”。


---

# 3. 已有方法解决了什么，又留下什么问题 {#baselines}
<!-- tag: baselines -->

Adapter 的想法很直接：在模型内部插入小的瓶颈层，只训练这些新层。它确实减少了可训练参数，但新层通常沿着网络深度顺序执行。在线推理时，特别是 batch size 小、序列短的时候，这些额外顺序计算会带来明显延迟。论文在 GPT-2 medium 上测到，adapter 可能带来 20% 到 30% 以上的延迟增加。

Prefix tuning 走另一条路：不改模型权重，训练一组连续提示向量。它的代价占用可用序列长度，并且优化可能不稳定。特殊 token 多了以后，下游任务真正能使用的上下文变短，输入分布也可能偏离预训练时见过的形式。

BitFit 只训练 bias，参数更少，但表达能力有限。Top-k fine-tuning 只训练最后几层，比全量微调省一些，但仍然不是极小任务模块。LoRA 的位置就在这里：它想像 adapter 一样参数少，但推理时能并回原权重；想像 prefix 一样不复制整模型，但不占上下文长度。

Adapter优点是模块小、直观；问题是插入顺序计算，可能增加推理延迟。
Prefix tuning优点是不直接改权重；问题是占用上下文长度，训练和参数规模不一定单调变好。
LoRA训练低秩更新，部署时可 merge 进原线性层，目标是参数少且零额外推理延迟。

判断句：参数少只是 PEFT 的一个维度；还要问是否增加推理延迟、是否占上下文长度、是否方便切换任务。你现在应该能用这四个维度比较方法。


---

# 4. PEFT 方法的对照地图 {#map}
<!-- tag: map -->

全量微调只训少量参数保持质量控制延迟方便切换任务

LoRA 的论文贡献要放在这个地图里读。它不是第一个参数高效方法，也不是唯一能微调大模型的方法。它的关键组合是：冻结预训练权重、用低秩矩阵表达权重更新、推理时把更新并回原权重、在多个模型和任务上达到接近或超过全量微调的效果。

学习论文时最容易犯的错，是只记“LoRA 把参数量减少 10000 倍”。这句话本身没错，但它不是完整理解。你还要知道这个数来自 GPT-3 175B、Adam 优化器、只适配部分 attention 权重、每任务 checkpoint 等具体语境。没有语境的数量级会变成宣传口径。

交互观察interactive

全量微调
Adapter
Prefix
LoRA

**选择一个场景**这里会显示判断结果。
项目观察
判断提示：读 PEFT 方法时，不要只看参数量。把训练参数、推理延迟、上下文长度、任务切换放到同一张表里。


---

# 5. 常见误区和修正 {#pitfalls}
<!-- tag: pitfalls -->

误区：PEFT 一定比全量微调弱参数少不等于表达力完全不足。论文实验显示 LoRA 在多组任务上可达到或超过全量微调，但这不是所有任务的保证。
误区：adapter 参数少所以零延迟adapter 的小矩阵仍然在网络里顺序执行。延迟取决于硬件、batch size、序列长度和实现。
误区：prefix 不改权重就没有代价prefix 会占用位置和序列长度，特殊 token 太多还可能让输入分布变怪。
误区：LoRA 不需要底座模型LoRA 只保存任务更新。推理时仍然需要完整预训练模型。
误区：10000 倍是所有场景通用数字这是论文在特定 GPT-3 配置下的数量级。换模型、rank、target modules 后要重新算。
误区：只看平均指标就够了PEFT 还要看训练吞吐、显存、部署延迟、任务切换和可维护性。


---

# 6. 试一试：把问题说成工程判断 {#examples}
<!-- tag: examples -->

例题 1一个 7B 模型要为 30 个客户做定制分类器。全量微调的主要部署问题是什么？参考思路：每个客户一份大 checkpoint，存储和加载切换成本高，训练时优化器状态也重。
例题 2adapter 和 LoRA 都有小矩阵，为什么推理延迟不同？adapter 是顺序新层；LoRA 的线性更新可提前并回原权重。
例题 3prefix tuning 为什么可能影响长上下文任务？特殊 token 占用序列位置，任务 token 可用长度减少。


---

# 综合练习 {#practice}
<!-- tag: mixed drills -->

<!-- practice-intro -->
<!-- end-practice-intro -->

**概念**能说清术语含义。
**公式**能解释符号和形状。
**诊断**能指出错误后果。
**表达**能用自己的话复述。

<!-- practice-group: 一、基础概念 -->
<!-- quick-review: 检查点 问自己：这个方法到底训练什么，部署时付出什么？ -->

### ? 单选

**Q:** 1. LoRA 论文中 PEFT 最直接要缓解的问题是哪一个？

- [ ] 每个下游任务复制和训练整套大模型太贵
- [ ] 让模型完全不需要下游数据
- [ ] 让所有推理都不用底座模型


### ? 判断

**Q:** 2. 全量微调的任务参数增量 Delta Phi 通常和原模型参数规模相同。

- [ ] 正确
- [ ] 错误


### ? 判断

**Q:** 3. 只保存 LoRA 权重就可以脱离预训练底座模型单独推理。

- [ ] 正确
- [ ] 错误


### ? 填空

**Q:** 4. PEFT 公式里，小型可训练任务参数通常记作 ____。


### ? 填空

**Q:** 5. 全量微调中，每个任务学到的参数增量通常记作 ____。


### ? 短答

**Q:** 6. 为什么“训练完以后保存一个新模型”在 GPT-3 175B 级别会变成部署问题？


<!-- end-practice-group -->

<!-- practice-group: 二、方法对照 -->
<!-- quick-review: 检查点 比较时至少说训练对象和推理影响。 -->

### ? 匹配

**Q:** 7. 把 full fine-tuning、adapter、prefix tuning、LoRA 分别匹配到它们训练的对象。


### ? 短答

**Q:** 8. 为什么 adapter 参数很少，仍然可能增加在线推理延迟？


### ? 短答

**Q:** 9. prefix tuning 可能牺牲什么资源？为什么？


### ? 单选

**Q:** 10. BitFit 主要训练什么？

- [ ] bias 参数
- [ ] 全部 attention 权重
- [ ] 额外 prefix token 的文本内容


### ? 多选

**Q:** 11. 评估 PEFT 方法时应该看哪些维度？

- [ ] 可训练参数量
- [ ] 推理延迟
- [ ] 是否占用上下文长度
- [ ] 任务切换成本


### ? 短答

**Q:** 12. 为什么“占用上下文长度”在长文档、摘要或代码任务里会变成真实成本？


<!-- end-practice-group -->

<!-- practice-group: 三、错误诊断与表达 -->
<!-- quick-review: 检查点 诊断题按错因、后果、修法写。 -->

### ? 诊断

**Q:** 13. 诊断：“adapter 参数不到 1%，所以它一定不会影响推理速度。”按错因、后果、修法回答。


### ? 诊断

**Q:** 14. 诊断：“我训练了 LoRA，所以部署时只上传 35MB LoRA 权重就够了。”按错因、后果、修法回答。


### ? 场景

**Q:** 15. 你要给一个共享大模型接 50 个客户任务，会优先关注 PEFT 的哪 4 个工程指标？


### ? 面试追问

**Q:** 16. 用 1-3 句话解释 LoRA 相对 adapter 的推理延迟优势。


### ? 面试追问

**Q:** 17. 为什么论文不能只证明 LoRA 参数少，还必须证明它在任务指标上接近或超过全量微调？


### ? 场景

**Q:** 18. 如果下游任务只有几百条样本，你会如何看待全量微调和 PEFT 的风险？


### ? 短答

**Q:** 19. “LoRA 没有额外推理延迟”这句话默认依赖什么部署动作？


### ? 费曼解释

**Q:** 20. 用 6-9 句话向刚学大模型的人解释：为什么 LoRA 论文要从 PEFT 问题讲起。


<!-- end-practice-group -->


---

# 导出练习记录 {#final}
<!-- tag: record -->

完成综合练习后生成一份记录，便于回看哪些判断还不稳。

生成记录复制记录

尚未生成。


---

