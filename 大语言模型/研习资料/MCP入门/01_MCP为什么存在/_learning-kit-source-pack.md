# Source Pack: MCP 为什么存在

## Source Boundary

- Course: MCP 入门
- Unit: MCP 为什么存在
- Snapshot: 本单元只使用课程内材料重构，来源是 `01_MCP为什么存在/学习页.html` 旧页、`02_MCP基本架构/学习页.html`、`03_MCP采用判断与学习路径/学习页.html` 和课程地图 `index.html`。
- Boundary: 只整理课程内对 MCP 的教学顺序、概念边界、角色拆分、能力类型、安全要求和采用判断；不引入课程外资料、论文、厂商文档或网页。

## Source-To-Unit Notes

- `01_MCP为什么存在/学习页.html` 提供本单元的主线：上下文集成为什么会碎片化、旧式连接器为什么难维护、MCP 想解决什么、为什么安全边界不能省。
- `02_MCP基本架构/学习页.html` 提供本单元的结构层：Host / Client / Server 的职责分工，tools / resources / prompts 的区别，一次初始化到返回结果的链路，以及传输层和能力层的边界。
- `03_MCP采用判断与学习路径/学习页.html` 提供本单元的决策层：什么时候值得采用 MCP，什么时候先用脚本或普通 API 更合适，不同角色分别该先学什么，demo 到 production 要补哪些东西。
- `index.html` 提供课程顺序与入口关系：这是一门三段式课程，01 是“为什么存在”，后面自然接到 02 的架构理解和 03 的采用判断。
- 写作顺序保持为：问题 -> 定义 -> 角色 -> 能力类型 -> 生命周期 -> 状态与失效 -> 安全边界 -> 误区 -> 采用判断。

## Gaps

- 这里没有外部规范文本可对照，所以页面中的定义、例子和判断都来自课程内旧页的整合与改写，属于课程内讲解口径。
- 课程内材料没有继续展开后续单元的完整目录，因此本页只把边界收在“为什么需要 MCP”这一层，不延伸到完整协议参考。
- 如果后续补出更多单元，本页的职责仍然应保持为“解释 MCP 为什么出现，以及什么时候值得用”。
