# Source Pack: MCP 基本架构

## Source Boundary

- Course: MCP 入门
- Unit: MCP 基本架构
- Snapshot: 本单元只使用课程内材料重构，来源是 `01_MCP为什么存在/学习页.html`、`02_MCP基本架构/学习页.html` 旧页、`03_MCP采用判断与学习路径/学习页.html` 和课程地图 `index.html`。
- Boundary: 只整理课程内对 MCP 的角色分工、能力类型、调用流程、状态边界、安全边界和排错顺序；不引入课程外规范、厂商文档或网页。

## Source-To-Unit Notes

- `01_MCP为什么存在/学习页.html` 提供本单元的前提：为什么连接层会碎片化、为什么需要统一入口、为什么权限和审计不能省。
- `02_MCP基本架构/学习页.html` 旧页提供本单元的主体材料：Host / Client / Server 的职责分工，tools / resources / prompts 的区别，一次会话从初始化到回写的流程，状态账本、配置片段和故障例子。
- `03_MCP采用判断与学习路径/学习页.html` 提供本单元的后续边界：什么时候值得采用 MCP，什么时候先用脚本或普通 API，什么时候只是先把连接问题讲明白。
- `index.html` 提供课程顺序与入口关系：这门课先讲为什么存在，再讲基本架构，最后讲采用判断。
- 写作顺序保持为：边界 -> 角色 -> 能力类型 -> 生命周期 -> 状态账本 -> 安全边界 -> 常见故障 -> 下一步怎么读真实 server。

## Gaps

- 这里没有外部规范文本可对照，所以页面中的定义、例子和判断都来自课程内旧页的整合与改写，属于课程内讲解口径。
- 课程内材料没有继续展开完整协议参考，因此本页只把结构讲到“能看懂、能排错、能判断”，不把自己写成协议手册。
- 如果后续补出更多单元，本页的职责仍然应保持为“把 MCP 的组件、消息流和状态边界讲清楚”。
