---
course: "RAG 论文精读"
unit: "02｜概率模型：RAG-Sequence 与 RAG-Token"
title: "概率模型：RAG-Sequence 与 RAG-Token"
kicker: "RAG 论文精读"
kicker_en: "02"
summary: "这一页只盯一件事：检索到的文档 z 在概率上怎么进入生成链路。先看文档为什么是 latent variable，再看两种模型把求和放在什么位置，最后把它们和解码、分类任务连起来。"

side_note: "这页只讲公式里的求和位置、解码方式和分类退化。DPR、BART、MIPS 和实验表放到后面的单元。"

can_do:
  - "说清 p_η(z|x) 、 p_θ(y|x,z) 、top-K 截断和边际化分别负责什么。"
  - "区分 RAG-Sequence 和 RAG-Token 的求和位置。"
  - "解释为什么分类任务长度为 1 时，两种模型会等价。"
  - "知道 RAG-Sequence 的解码为什么不能直接照搬一次 beam search。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节：论文导读"
    href: "../01_论文导读_参数记忆与非参数记忆/学习页.html"
  - text: "下一节：组件与训练"
    href: "../03_组件与训练_DPR_BART_MIPS/学习页.html"

---

# 学习目标 {#goals}
<!-- tag: goal -->

先分清对象你要先知道文档 z 是怎么从候选集里被抽出来的，再看它怎么进入生成分布。别把检索分数和最终答案混成一个量。
再看位置你要能指出公式里求和是包住整段，还是包住每个 token。这个位置一变，模型假设就变了。
最后看用途你要能把这两种形式接到解码和分类任务上，知道论文为什么说它们不是纯粹的数学变体。


---

# 先看主线 {#overview}
<!-- tag: overview -->
<!-- layout: grid2 -->

## box
### 这一页在回答什么
论文先把输入 x 送去检索，再拿回 top-K 文档 z。真正关键的是：z 一个要被边际化的潜变量。模型先假设“哪篇文档负责生成”这件事有多个可能，再把这些可能的贡献加起来，得到总体的 `p(y|x)`。
这里最容易读错的地方求和位置。RAG-Sequence 让同一篇文档负责整段输出；RAG-Token 允许每个 token 都重新在候选文档里边际化一次。一个是整段路径上的求和，一个是逐 token 的求和。

## box soft
### 一条可追的链路
输入 x 变成检索信号读：x
top-K 文档进入候选集读：index
生成器读 x 和 z写：pθ(y|x,z)
对文档路径求和写：p(y|x)
一层错了会怎样症状
**before**输入只是文本。读取 x 以后，query encoder 先给出检索表示。
**after**候选文档进来。检索器返回 top-K 文档，不是最终答案。
**reads**文档 z 和输入 x。生成器在证据条件下估计 token 概率。
**writes**总体输出概率。把多篇文档路径的贡献合起来。
**failure**检索偏了，后面都偏。常见症状来源不对。


---

# 术语和符号 {#terms}
<!-- tag: terms -->

| 符号或术语 | 直觉 | 精确意思 | 这一页先怎么读 |
| --- | --- | --- | --- |
| p_η(z|x) | 给定输入后，哪篇文档更值得被拿出来。 | 检索器对文档 z 的条件分布。 | 先把它看成“文档选择概率”。 |
| p_θ(y|x,z) | 看见输入和文档后，输出怎么写。 | 生成器在证据条件下的输出分布。 | 先把它看成“在文档条件下的写作概率”。 |
| top-K 截断 | 不对所有文档求和，只看最相关的几篇。 | 只在检索到的候选集上做近似边际化。 | 它是近似范围，不是最终答案集合。 |
| 边际化 | 把“哪篇文档负责生成”这件事加总起来。 | 对潜变量 z 的可能取值求和。 | 它是概率上的合成，不是多数投票。 |
| 潜变量 | 训练时看不见，但模型必须对它积分或求和。 | 文档选择在观测上不直接给出，只能隐式估计。 | 先记住“隐藏选择”。 |

> **先别追的东西** 这页先不展开 DPR 的训练细节、BART 的结构、MIPS 索引和后面的实验表。现在只要把“文档是潜变量、概率要边际化、求和位置会改变模型”这三件事抓住。


---

# RAG-Sequence {#sequence}
<!-- tag: model -->
<!-- layout: grid2 -->

## box
### 它在说什么
RAG-Sequence 假设同一篇文档负责整个输出序列。也就是说，模型先选一篇文档，再在这篇文档的条件下把整段 y 写完。文档是一个潜变量，但这个潜变量在整段输出期间保持不变。
这带来的直觉很简单：如果答案本来就是围绕一份稳定证据展开的，整段共用一篇文档会更自然。它也让公式里“先对文档求和，再对 token 连乘”的结构特别清楚。

## box soft
### 公式长什么样
$$p_{\text{RAG-Sequence}}(y \mid x) \approx \sum_{z \in \text{top-}k(p(\cdot \mid x))} p_{\eta}(z \mid x)\, p_{\theta}(y \mid x, z)$$
 如果把 `p_θ(y|x,z)` 再拆成 token 乘积，`z` 的求和就包住了整段序列。读的时候别漏掉：这里的文档选择只做一次。

| 步骤 | 读什么 | 写什么 | 错了会怎样 |
| --- | --- | --- | --- |
| 检索 | 输入 x | top-K 文档候选 | 候选偏了，后面再流畅也没用。 |
| 条件生成 | 输入 x 和某篇文档 z | 整段输出的条件概率 | 如果 z 没真正参与，模型只是表面接了检索。 |
| 边际化 | 多篇文档路径的贡献 | 总体 p(y|x) | 如果只看 top-1，会把模型学成硬检索器。 |


---

# RAG-Token {#token}
<!-- tag: model -->
<!-- layout: grid2 -->

## box
### 它在说什么
RAG-Token 允许每个 token 都重新对文档做边际化。直觉上，这比整段共用一篇文档更灵活：不同 token 可以依赖不同证据片段，尤其适合一个答案里要拼接多条事实的场景。
它和 Sequence 的关键区别，不在“有没有检索”，而在“检索选择是整段一次完成，还是每个 token 都参与一次”。这个差别会直接影响生成自由度和解码方式。

## box soft
### 公式长什么样
$$p_{\text{RAG-Token}}(y \mid x) \approx \prod_i \sum_{z \in \text{top-}k(p(\cdot \mid x))} p_{\eta}(z \mid x)\, p_{\theta}(y_i \mid x, z, y_{1:i-1})$$
 这里求和包住的是每个 token 的条件概率。读的时候要注意：外层是连乘，内层是逐 token 的文档边际化。

两种模型最短对照text

```text
Sequence: 先选一篇文档，再用同一篇文档生成整段。
Token: 每个 token 都可以在候选文档里重新边际化一次。
```


---

# 解码差异 {#decode}
<!-- tag: decode -->
<!-- layout: grid2 -->

## box
### 为什么解码不能一把梭
论文在测试时要近似 `arg max_y p(y|x)`。RAG-Token 可以像标准自回归 seq2seq 那样，把每一步的混合分布塞进 beam decoder；但 RAG-Sequence 的似然不是常规的 per-token 分解，不能直接照搬一次 beam search 解决。
所以解码差异模型分解方式带出来的后果。模型怎么边际化，解码就得怎么近似。

## box info
### 对应到论文里的意思
RAG-Token 可以被看成普通的 autoregressive seq2seq 变体；RAG-Sequence 则要针对每篇文档单独跑 beam，再把候选再比较一次。前者更像逐步混合，后者更像多条整段路径的比较。

| 模型 | 解码直觉 | 论文里的结果 | 你要记的点 |
| --- | --- | --- | --- |
| RAG-Token | 像标准自回归生成器那样逐 token 解码。 | 可以把混合后的每步概率送进 beam decoder。 | token 级边际化和自回归很接近。 |
| RAG-Sequence | 按文档分别估计整段候选，再比较整段路径。 | 不能只跑一次常规 beam 就结束。 | 整段路径上的似然是关键。 |


---

# 分类为什么会等价 {#classify}
<!-- tag: classification -->
<!-- layout: grid2 -->

## box
### 论文的那句话怎么理解
论文说，sequence classification 任务里如果把类别看成长度为 1 的目标序列，那么 RAG-Sequence 和 RAG-Token 会等价。原因不复杂：目标只有一个 token 时，就没有“整段共用文档”和“每个 token 换文档”的展开空间。
这也是为什么 FEVER 这类三分类任务能直接被写成 seq2seq 的输出形式。你在把标签写成一个长度为 1 的序列。

## box warn
### 这页想提醒你什么
分类任务里，RAG 的核心不在“输出长文本”，而在“输出可以被写成序列”。一旦目标长度是 1，两种 RAG 形式在概率结构上的差别就消失了。

长度为 1 时的最小判断pseudo

```text
target y = "supports" # length = 1
if len(y) == 1:
 RAG-Sequence 和 RAG-Token 的文档边际化位置没有实际差别
```


---

# 一眼区分 {#compare}
<!-- tag: compare -->

| 维度 | RAG-Sequence | RAG-Token | 判断方法 |
| --- | --- | --- | --- |
| 文档选择 | 一篇文档负责整段。 | 每个 token 都可重新边际化。 | 看求和包住的是整段还是单个 token。 |
| 生成风格 | 更像稳定证据上的整段写作。 | 更像逐步混合多份证据。 | 看答案是否常常要拼接多处事实。 |
| 解码 | 要按文档路径近似整段似然。 | 可以直接接进常规 beam decoder。 | 看似然是否还能按 token 常规展开。 |
| 分类任务 | 与 Token 等价。 | 与 Sequence 等价。 | 看目标长度是否只有 1。 |


---

# 两种模型手算 {#worked-example}
<!-- tag: worked example -->
<!-- layout: grid2 -->

## box
### 先算 Sequence
还是用“Washington 的首都是哪里？”这个例子。假设检索器返回两篇文档：`z1` 讲 Washington state，`z2` 讲 Washington, D.C.。RAG-Sequence 先对整篇文档做一次选择，再在这一篇文档条件下生成整段答案。
文档
`p_η(z|x)`
`p_θ(y|x,z)`
路径贡献


`z1`：Washington state
0.55
0.18
0.099


`z2`：Washington, D.C.
0.45
0.91
0.4095


合计
0.55 × 0.18 + 0.45 × 0.91
0.5085
这一步要看的“高检索分 × 高生成分”能不能把最终概率抬起来。Sequence 的整段概率来自整段路径的加总。

## box info
### 再算 Token
现在把答案拆成两个 token，假设第一个 token 更依赖 `z2`，第二个 token 也更依赖 `z2`。RAG-Token 允许每一步都重新对文档做边际化，所以每个位置都能有自己的文档混合。
token
文档 1 贡献
文档 2 贡献
该步总概率


第 1 个 token
0.55 × 0.30 = 0.165
0.45 × 0.90 = 0.405
0.570


第 2 个 token
0.55 × 0.40 = 0.220
0.45 × 0.80 = 0.360
0.580


整段
0.570 × 0.580
0.3306
这个数值未必比 Sequence 大或小，关键是它表达了另一种假设：不同 token 可以从不同文档里取证据。你在论文里看到“Token 更灵活”，本质上就是这张表。

$$\text{Sequence: } \sum_z p_\eta(z\mid x)\prod_i p_\theta(y_i\mid x,z,y_{1:i-1})\qquad \text{Token: } \prod_i \sum_z p_\eta(z\mid x)p_\theta(y_i\mid x,z,y_{1:i-1})$$
 把两个式子并排看，差别只在求和的位置，但模型假设完全不同。一个是整段共用证据，一个是逐 token 混合证据。


---

# 逐 token 追踪 {#token-trace}
<!-- tag: trace table -->

## box
如果你在调试一个 RAG 系统，最容易看丢的“这一位 token 到底从哪篇文档里来”。把输出拆成 token，能帮你判断 Sequence 和 Token 在什么地方真正不同。下面这张表为了让你能看见状态在每一步怎么变化。

| 位置 | Sequence 里保持什么不变 | Token 里允许什么变化 | 读到的东西 | 写出去的东西 | 如果这里错了，症状是什么 |
| --- | --- | --- | --- | --- | --- |
| 第 1 个 token | 整段只用同一篇文档。 | 可以给第 1 步一个最强相关文档。 | 输入 x、候选文档和前缀空上下文。 | 第 1 步的 token 概率。 | 如果文档选错，后面再顺也会把整段带偏。 |
| 中间 token | 文档选择还是同一篇。 | 可以把不同 token 的证据重新混合。 | 前缀 `y1:i-1`、输入 x、文档 z。 | 当前 token 的条件概率。 | 如果把这里误读成“重新检索整页”，你会错判成本。 |
| 收尾 token | 整段证据的主路径不变。 | 可以在结尾更偏向总结类证据。 | 已生成的前缀和最后一轮候选文档。 | 整段 `p(y|x)` 的最后一项。 | 如果只盯检索分，常会把结尾的证据变化看漏。 |


---

# 按论文原文读 {#source-reading}
<!-- tag: source map -->
<!-- layout: grid2 -->

## box
### Figure 1、2.1 和 2.5 分别在做什么
Figure 1 先告诉你整条链路怎么连：retriever 提供文档，generator 在文档条件下写输出，最后对文档路径做边际化。它回答的是“这个模型框架长什么样”。
2.1 Models 进入公式，告诉你 Sequence 和 Token 的求和位置有什么差别。它回答的是“模型假设到底是什么”。
2.5 Decoding 进一步告诉你，模型分解方式一变，解码近似就必须跟着变。它回答的是“测试时怎么近似 arg max”。

## box info
### 读的时候别滑过去的句子
论文里有一句很短，但很关键：sequence classification 任务里，如果目标类被看成长度为 1 的序列，那么两种模型等价。很多人把这句当成附带说明，实际上它是在告诉你，RAG 的公式不是只服务长文本生成，它也能把标签当短序列来处理。
所以你读这一页时，最好把“框架图、概率公式、解码、分类退化”当成一条链，而不是四个孤立片段。这样后面回头看实验表，就不会只剩“哪个分数更高”这一层。

## box soft
### 把原文拆成四句来读
第一句先看 Figure 1：retriever、generator、latent document、marginalization 都在图里。你读图时只需要确认谁负责选文档，谁负责把证据变成输出，不要在这一步就跳到训练细节。
第二句看 2.1 Models：Sequence 和 Token 的差别不在词面，而在求和放在整段外面还是每个 token 里面。读公式时，先把“谁被积掉、谁被保留”圈出来，再看生成器条件里到底带不带前缀。
第三句看 2.5 Decoding：模型一旦换了分解方式，测试时的近似也要换。这里最容易犯的错，是把训练时的目标和测试时的搜索当成两件互不相干的事。
第四句看分类等价：目标长度为 1 时，Sequence 和 Token 不再拉开差距。这个结论的用处提醒你，RAG 不是只有长回答场景才成立。

| 原文位置 | 这一页要抓的句子 | 不要过度外推的地方 |
| --- | --- | --- |
| Figure 1 | retriever + generator + latent document + marginalization。 | 别把结构图直接读成训练细节。 |
| 2.1 Models | Sequence 与 Token 的求和位置不同。 | 别把位置差异误读成实现上的小修小补。 |
| 2.5 Decoding | 不同模型要用不同近似方式做 `arg max`。 | 别默认所有 seq2seq 解码都能直接复用。 |
| 分类等价那句 | 长度为 1 时，两种模型没有差别。 | 别扩成“所有分类都没有检索差异”。 |


---

# 落到排错日志 {#implementation}
<!-- tag: operation bridge -->

## box
如果你把这页知识拿去做系统，最有用的知道哪里该留日志。你至少要能看见：检索器选了什么，生成器读了什么，最后是哪条路径把答案抬起来的。Sequence 和 Token 的差别，会直接体现在日志的长度和粒度上。

## box
### 日志最小字段
如果你把这一页的思路塞进真实系统，日志至少要保留 query、top-K 文档、每篇文档的分数、生成前缀、最终输出和人工复核标签。少于这个集合，很多错误只能猜，不能查。
Sequence 场景里更要留“整段共用哪篇文档”的记录；Token 场景里更要留“每一步选了哪几篇文档”的记录。两者的差别不在名词，而在你能不能把错误定位到某一个位置。

## box info
### 三类常见失真
第一类是检索压偏：问题编码把关键词压丢了，top-K 看起来相关，实际上答不上。第二类是生成没读证据：文档找对了，但解码器没把文档信息接到输出。第三类是边际化错位：路径权重算错，最后把本来应该合并的证据拆散了。
你排查时不要先问“模型为什么不聪明”，先问“错误发生在哪一层”。先定位层，再讨论参数和数据，效率会高很多。

| 对象 | 读什么 | 写什么 | 哪里失真 | 常见症状 |
| --- | --- | --- | --- | --- |
| query encoder | 输入 x | 检索信号 q(x) | 把问题压偏或漏掉关键词。 | top-K 文档看起来像“相关”，但就是答不到点上。 |
| retriever | query 和索引 | 候选文档 z | 候选集里根本没有正确证据。 | 生成器再强也只能写出半对半错的答案。 |
| generator | 输入 x 和文档 z | token 条件概率 | 文档读了，但没有真正用于生成。 | 输出很顺，却和证据对不齐。 |
| decoder | 混合后的条件分布 | 最终序列 y | 把 Sequence 当成 Token，或把 Token 当成整段共用。 | 测试时近似方式错，结果会在同一类任务里反复波动。 |

排错时最小的三步text

```text
1. 先看 top-K 文档里有没有正确证据
2. 再看生成器有没有真正读到这篇证据
3. 最后看解码和边际化有没有把路径权重算错
```

| 先看什么 | 要确认的证据 | 如果错了通常表现什么 | 这一层对应谁 |
| --- | --- | --- | --- |
| 检索候选 | top-K 里有没有真正相关的文档 | 答案很像“会说话”，但就是不答题 | retriever |
| 证据使用 | 生成前缀和文档内容是否对得上 | 输出流畅，却引用了错误事实 | generator |
| 路径合并 | Sequence 或 Token 的求和位置有没有放对 | 同一批样例在不同长度上波动很大 | decoder / marginalization |
| 复核记录 | 有没有把 query、候选、分数、输出一起存下来 | 事后只能猜“可能是检索错了” | operation log |

## box warn
如果你要把这一页迁移到项目里，最先搭的可复查日志面板。先让人看到证据链，再谈自动评分，定位速度会快很多。


---

# 完整追踪案例 {#trace-case}
<!-- tag: trace case -->
<!-- layout: grid2 -->

## box
### 具体场景：同一个问题，两个文档，两种模型
假设用户问的是“Washington 的首都是哪里？”。检索器返回 `z1 = Washington state` 和 `z2 = Washington, D.C.`。如果你只看 top-1，很容易把整段答案写向 `z1`，最后输出 Olympia；如果你在 Token 级别逐步混合证据，又没有把路径记清楚，答案可能一半像州、一半像首都。
这个场景的关键“哪一层负责把证据和输出对齐”。Sequence 需要整段共用一条文档路径，Token 允许每个位置重新混合文档，但两者都要求你把边际化放在正确的位置。

## box info
### 你应该盯住的信号
先记 query，再记 top-K 和分数，再记第一个 token 的候选文档贡献，最后记第二个 token 的贡献。只要这四类信息都在，你就能把错误切成三块：候选没进来、生成没用上、或者求和位置错了。
这比直接看最终字符串更有用，因为最终字符串只告诉你“坏了”，不会告诉你“坏在谁手里”。

| 步长 | Sequence 观察点 | Token 观察点 | 如果这一步错了 |
| --- | --- | --- | --- |
| 第 1 步 | 整段只认一篇文档，先确认是哪一篇。 | 给当前 token 算出各文档的混合权重。 | 如果文档选错，后面 token 再顺也救不回来。 |
| 第 2 步 | 前缀继续沿着同一条文档路径走。 | 允许新 token 再偏向另一篇文档。 | 如果前后文档混乱，答案会前后打架。 |
| 最终汇总 | 整段路径的概率被一次性合并。 | 每个位置的混合结果再连乘。 | 如果求和位置错了，看起来像近似，实际上是模型假设变了。 |

最小日志片段text

```text
query: Washington capital
top_k: [z1=Washington state, z2=Washington, D.C.]
step_1: sequence_path=z2, token_path=z2, p_theta(y1|x,z2)=high
step_2: sequence_path=z2, token_path=z2, p_theta(y2|x,z2)=high
remark: if sequence_path and token_path diverge, check marginalization order first
```

## box warn
失败后果很直接：如果你把 Sequence 的“一次选文档”误写成 Token 的“每步选文档”，日志看上去会一直正常，但整段答案其实已经把证据切碎了。排错时不能只看最终输出，要把路径和 token 一起看。

## box soft
### 练习钩子
回到第 8 题时，先用这张表把回答说完整：你会保留哪些字段，哪一行说明 Sequence，哪一行说明 Token，哪一行说明边际化顺序。能说清这三点，就说明你已经能把公式落到真实调试里了。

## box
### 什么时候选 Sequence，什么时候选 Token
如果任务更像单段回答，而且证据在整段里基本一致，Sequence 往往更省心，因为它把整段放进同一条文档路径里，日志和解释都更集中。你会更容易回答“这段话到底靠哪篇文档支撑”。
如果任务更像逐步生成，前后 token 可能分别依赖不同证据，Token 会更顺手，因为它允许每个位置重新混合文档。它不是“更高级”，只是把边际化放到了更细的粒度上。
```text
先问：证据是否需要整段共用？
再问：输出是否会在 token 级别切换证据？
如果前者是，优先 Sequence。
如果后者是，优先 Token。
两种都不是默认正确，关键是任务形状要和边际化假设对齐。
```


---

# 先别追的细节 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
### 这一页先不要跳去的地方
别在这一页里追 DPR 的训练 loss、BART 的层结构、MIPS 的索引库实现、论文里的每个表格数字，也别急着讨论检索监督是否一定更好。那些问题都真实，但它们不是这一页要解决的中心。
你现在要带走的只有一个判断：文档 z 要进入概率链路的潜变量。Sequence 和 Token 的差别，不只是“写法不同”，而是模型对“哪篇文档负责哪一段输出”的假设不同。

## box warn
### 下一步再回来
当你读第 03 节的 DPR、BART、MIPS，或者第 04 节的解码适配时，再回来重看这一页，会更容易把“模块怎么连起来”看成一条完整的概率链，而不是几个名字的堆叠。


---

# 常见误区 {#mistakes}
<!-- tag: mistakes -->
<!-- layout: grid2 -->

## box redsoft
### 误区 1：把 top-K 当最终答案
top-K 是近似边际化的候选范围，不是答案集合。模型后面还有生成概率要算。

## box redsoft
### 误区 2：以为 Token 每步都重新检索
论文公式里 top-K 仍然来自同一个 `p_η(z|x)`。变化的是边际化位置，不是每一步真的重新查索引。

## box redsoft
### 误区 3：只看检索分
某篇文档检索分高，不代表它对最终答案贡献一定大。贡献是检索概率和生成概率一起乘出来的。

## box redsoft
### 误区 4：把边际化理解成平均
边际化是按概率加权求和，不是简单平均，也不是多数投票。


---

# 下一节怎么接 {#bridge}
<!-- tag: bridge -->
<!-- layout: grid2 -->

## box
### 接到第 03 节时看什么
下一节要把这套概率结构落到组件上：DPR 怎么检索，BART 怎么生成，MIPS 怎么把候选找出来。读那一节时，你应该带着三个问题进去：谁在学、学什么、哪个环节更新会影响索引。

## box info
### 如果这一页卡住了
先回到 `p_η(z|x)` 和 `p_θ(y|x,z)` 两个符号，把“谁负责选文档，谁负责写 token”再说一遍。大多数混乱都来自把文档概率、生成概率和最终输出混成了一句。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box soft
### 作答目标
这组题要检查你有没有把“文档是潜变量、Sequence 和 Token 的差别、解码方式、分类等价”连成一条线，而不是只会复述名字。

## box info
### 检查重点
先看概念分不分得清，再看你能不能追到公式位置，最后看你能不能把它讲给没读过论文的人。

<!-- end-practice-intro -->

<!-- practice-group: 一、概念辨认 -->
<!-- quick-review: 快速回看 潜变量、top-K、边际化、RAG-Sequence、RAG-Token。 -->

### 1 choice

**Q:** 1. 这一页最核心的判断是哪一个？

- [ ] . 检索到的文档要进入概率链路，不能只当成检索结果看。
- [ ] . 只要 top-K 足够大，就不需要生成器。
- [ ] . 概率公式只是在做排序，不涉及边际化。

Answer: . 检索到的文档要进入概率链路，不能只当成检索结果看。

### 2 true/false

**Q:** 2. 既然检索结果可检查，最终生成就一定事实正确。

- [ ] 对
- [ ] 错

Answer: false

### 3 fill

**Q:** 3. 用自己的话解释“潜变量”。


### 4 open

**Q:** 4. 写出你最容易混淆的一组概念，并说明差别。

Explain: 可以写：检索概率 vs 生成概率，或者 RAG-Sequence vs RAG-Token。合格答案至少要说出对象、求和位置、以及为什么这个差别会影响解码。

<!-- end-practice-group -->

<!-- practice-group: 二、流程与公式 -->
<!-- quick-review: 快速回看 p_η(z|x) 、 p_θ(y|x,z) 、求和位置。 -->

### 5 choice

**Q:** 5. 哪一种写法最接近 RAG-Sequence？

- [ ] . 每个 token 都先对文档求和，再整段相乘。
- [ ] . 先对文档求和，再由同一篇文档生成整段。
- [ ] . 只保留 top-1 文档，不再边际化。

Answer: . 先对文档求和，再由同一篇文档生成整段。

### 6 true/false

**Q:** 6. 边际化的意思是只看 top-1 文档，不再考虑其他路径。

- [ ] 对
- [ ] 错

Answer: false

### 7 choice

**Q:** 7. 哪个配对最准确地表示“谁选文档，谁写 token”？

- [ ] . p_η(z|x) 负责选文档， p_θ(y|x,z) 负责生成。
- [ ] . p_θ(y|x,z) 负责选文档， p_η(z|x) 负责生成。
- [ ] . 两个符号都只表示检索排序。

Answer: . p_η(z|x) 负责选文档， p_θ(y|x,z) 负责生成。

### 8 open

**Q:** 8. 如果有两篇文档，一篇检索高但生成差，另一篇检索稍低但生成好，边际化在做什么？

Explain: 合格答案要说出：边际化会把两条路径的贡献加起来，而不是只取检索第一名。这样才能看见“检索高”和“生成好”都可能影响最终输出。

<!-- end-practice-group -->

<!-- practice-group: 三、诊断与迁移 -->
<!-- quick-review: 快速回看 答案不是文档本身，分类不是特例。 -->

### 9 diagnosis

**Q:** 9. 有人把这页内容简化成“接一个向量库就行”。最直接会漏掉哪一层？

- [ ] . 只会漏掉输入编码。
- [ ] . 会漏掉生成概率和边际化这两层。
- [ ] . 只会漏掉文档标题。

Answer: . 会漏掉生成概率和边际化这两层。

### 10 choice

**Q:** 10. 为什么分类任务里两种模型会等价？

- [ ] . 因为目标长度为 1，没法再区分逐 token 和整段共用文档。
- [ ] . 因为分类不需要任何文档。
- [ ] . 因为分类永远不需要检索。

Answer: . 因为目标长度为 1，没法再区分逐 token 和整段共用文档。

### 11 scenario-transfer

**Q:** 11. 如果把这页思想用到企业制度问答，你会保留哪些中间日志？

Explain: 至少保留：原始问题、query、top-K 文档、文档分数、生成结果和人工复核标记。这样才能回看是检索错、生成错，还是资料版本旧。

### 12 synthesis

**Q:** 12. 用 2-3 句话概括这页在 RAG 论文里的作用。

Explain: 合格答案要说出三件事：这页先解释文档为什么是潜变量，再把 Sequence 和 Token 的差别讲清，最后把概率结构接到解码和分类任务上。不要只复述“RAG 有两种模型”。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- **论文：**[Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks](https://arxiv.org/abs/2005.11401)。本页主要用它的 Figure 1、2.1 Models、2.5 Decoding 和分类等价说明。
- **本地 PDF：**[2005.11401v4.pdf](../../../../../50_资源/论文/2005.11401v4.pdf)。用于核对公式、段落顺序和解码描述。
- **本地抽取文本：**[2005.11401v4.txt](../../../../../50_资源/论文/2005.11401v4.txt)。用于核对术语、公式表述和后续单元的边界。

---
