---
course: "从零训练小语言模型"
unit: "07"
title: "从教学代码到开放训练配方"
kicker: "训练配方"
summary: "这页不教你再跑一次训练，而是教你看懂一份公开训练报告：哪一段是在证明代码链路跑通，哪一段是在证明数据和评估可审计，哪一段只是下载说明。读完后，你应该能把一次 run 拆成数据、超参、评估、阶段记录和产物链路五块。"

side_note: "这一页先把一次训练结果拆成可检查的证据链：数据、超参、评估、阶段和产物。先读主线，再去练习区写一份能复查的报告。"

can_do:
  - "能区分 toy run、open weights 和 fully open training。"
  - "能从报告里抽出数据配方、超参账本、评估协议和 checkpoint 谱系。"
  - "能写出一份小模型训练报告骨架，并补上不能外推的边界。"

course_links:
  - text: "上一节：Scaling、验证集与评估"
    href: "../06_Scaling验证集与评估/学习页.html"
  - text: "课程地图"
    href: "../index.html"
  - text: "下一节：小规模复现实验设计"
    href: "../08_小规模复现实验设计/学习页.html"

---

# 学习目标 {#goals}
<!-- tag: goal -->

这页的目标把一份公开训练材料拆成可检查的证据。每一类证据都回答不同的问题：这条 run 是否跑通、这份数据从哪来、训练设置是否够完整、评估协议是否站得住、最终模型来自哪些阶段、哪些结论只能保守地说。

**读懂层级**
能把 toy run、open weights、fully open training 分开说清，不把“能下载”误读成“可审计”。


**抽出证据**
能从数据、超参、评估、阶段记录和产物链路里抽出一份能回查的清单。


**写出边界**
能在报告结尾明确写出可靠结论和不能外推的部分，避免把一次 run 说成全部结论。

看到什么
先问什么
为什么要问


只有最终权重和一张截图
数据、超参、评估协议在哪里
这只说明结果存在，不说明结果可查。


有配置但没有数据版本
数据来源、混合比例、过滤和去重是否记录
数据变了，训练结论也会跟着变。


有分数但没有评估说明
验证集、prompt、shot、解码参数和污染检查在哪里
没有协议的分数，很难比较。


有 stage 名称但没有 checkpoint 谱系
起止 checkpoint、token budget 和选择规则在哪里
最终模型通常来自多阶段决策。

> **先知道，先别钻** 这页先不追 benchmark 排名、数据清洗流水线细节和完整 scaling fit；它只负责把报告里的证据链理顺。


---

# 证据链 {#map}
<!-- tag: trace -->

把一份公开训练材料看成一条证据链，先看每一步读了什么，再看每一步写出了什么。前一步写的内容，会决定后一步有没有资格下结论。把鼠标移到左侧步骤上，你会看到证据从“原始 run”变成“可审计报告”的过程。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

> **读法** 先看每一步“读了什么”，再看“写出了什么”。如果一环只给结论，不给来源，后面的判断就会变轻。


---

# 术语误区 {#terms}
<!-- tag: vocabulary -->

先把几个最容易混在一起的词拆开，后面写计划和看报告才不会乱。这里的重点知道每个词对应哪一层证据。

**toy run**教学用的小规模训练，主要验证代码路径、loss 下降、恢复和采样是否正常。
**recipe**训练配方。它数据、模型、优化、系统、评估和阶段决策的组合说明。
**open weights**只发布最终权重或少量模型文件。它能支持推理或微调，但未必能审计数据和训练决策。
**fully open**更高层级的开放：权重、训练数据、代码、配置、日志、评估和中间检查点尽量可访问。
**data mix**数据混合比例。它回答“每类数据在训练中占多少”，不是简单列数据集名字。
**checkpoint lineage**检查点谱系。它说明最终模型来自哪些中间点，经历了哪些继续训练、筛选或回退。
**contamination**评估污染。训练数据中如果包含测试题或近似答案，benchmark 分数会被抬高。
**reproducibility boundary**可复现边界。它告诉你哪些结论能重现，哪些只能近似，哪些不能外推。

> **先别追的旁支** 先不要把注意力放在 leaderboard 排名、数据集命名争议，或者完整 benchmark 作者学术史上；这页只处理“报告里该写什么”。


---

# 记录账本 {#records}
<!-- tag: ledger -->

训练配方一组能互相对上的账本。你只要能从记录里复原有效 batch、token budget、评估协议和阶段边界，这份报告就已经比“只给一个 config”更接近可审计。

$$
 B_{\text{eff}} = B_{\text{micro}} \times N_{\text{GPU}} \times A_{\text{acc}}
 \qquad
 C_{\text{rough}} \approx \frac{T_{\text{tokens}}}{r_{\text{tok/s}} \times 3600} \times p_{\text{gpu}}
 $$
 `B_micro` 是每卡 micro-batch，`N_GPU` 是 GPU 数，`A_acc` 是梯度累积步数。`T_tokens` 是总 token 数，`r_tok/s` 是吞吐，`p_gpu` 是每小时 GPU 单价。这个估算只给数量级，不替代真实 profiler。

账本
要写什么
漏掉后会怎样


数据账
来源、版本、token 数、混合比例、过滤、去重、许可证、污染检查。
别人知道你用了什么名字，却不知道你到底喂给了模型什么文本。


模型账
layers、hidden、heads、context、vocab、位置编码、norm、初始化。
参数量有了，结构没有，结果就很难复查。


优化账
optimizer、学习率计划、warmup、weight decay、clip、effective batch。
训练动态会变，但你看不出为什么变。


系统账
GPU、dtype、并行策略、checkpointing、吞吐、显存和依赖版本。
你只能说“跑过了”，不能说“为什么只跑到这个速度”。


评估账
验证集大小、评估间隔、prompt、shot、解码参数、污染检查、失败样例。
分数会看起来很好，但比较口径不清。


结论账
哪些发现稳、哪些退步、哪些结论不能外推。
报告会把一次 run 说得比它真正支持的更多。

> **手感** 你先把“有效 batch”和“总训练量”写对，再谈结果是不是公平。只写 micro-batch 不够，单写 GPU 型号也不够。


---

# 阶段记录 {#stage}
<!-- tag: state -->

真实训练经常分阶段：先做 sanity run，确认链路稳定；再做 base pretraining，建立主曲线；最后进入 annealing 或继续训练，收束到更高质量的数据和更谨慎的学习率。阶段记录要把起止 checkpoint、token budget、数据切换和选择规则写成能回头查的形式。

阶段
读什么
写什么
写错后会怎样


Stage 0
sanity run
代码路径、loss、恢复、采样、显存。
确认训练链路没断，先排查 NaN、OOM 和错误 shape。
你会把实现问题误当成数据问题。


Stage 1
base pretraining
通用数据、学习率计划、验证 loss、吞吐。
记录起点 checkpoint、token budget、有效 batch 和评估间隔。
后面看不到主曲线从哪里开始变稳。


Stage 2
annealing / continued training
高质量数据、数据切换、退火策略、污染检查。
记录数据混合、学习率变化、结束 checkpoint 和选择依据。
最终模型像是“一步到位”，其实是阶段结果。

阶段记录模板复制

```text
stage:
 name: base_pretraining
 start_checkpoint: step_0
 end_checkpoint: step_180000
 token_budget: 300B
 data_mix:
 web: 60%
 code: 20%
 math: 20%
 lr_schedule: warmup + cosine
 eval_every: 2000 steps
 selection_rule: lowest validation loss with stable contamination check
```

> **边界提示** 这里先不展开完整 scaling-law 拟合和阶段预算推导；这页只需要把 stage 记录写成能回头找的形式。


---

# 产物链路 {#artifacts}
<!-- tag: artifact -->

开放训练不是只交一个权重文件。更稳的做法是把产物排成一条可追踪的链：模型卡指向权重，权重指向配置，配置指向训练代码，日志指向 checkpoint，评估表指向脚本和数据版本。只要一环断了，别人就很难复查。

链路
应该对上什么
常见断点


模型卡 → 权重
版本号、下载地址、适用范围。
卡片更新了，文件还是旧版。


权重 → 配置
layers、hidden、context、tokenizer、dtype、seed。
只给权重，不给结构。


配置 → 训练代码
启动脚本、参数解析、依赖版本、训练入口。
人能看见参数，不能复跑。


日志 → checkpoint
step、stage、token budget、恢复点。
只保留最终曲线，断掉阶段谱系。


评估表 → 脚本和数据版本
prompt、shot、解码参数、验证集与污染检查。
分数看得见，协议找不到。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

> **最小开放包** 小团队不一定能公开所有原始数据，但至少可以公开：训练脚本、配置、数据来源清单、处理脚本摘要、token 统计、loss 曲线、评估脚本、最终权重、若干中间 checkpoint 和训练报告。


---

# 报告断点实验室 {#audit-lab}
<!-- tag: failure lab -->

这一小节把“看报告”变成“排查报告”。假设你拿到三种常见材料：只给权重、只给分数、或者给了一部分配置和日志。你要先判断哪一环断了，再决定下一步追问什么。这里不追华丽排版，只追证据链是否闭合。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

看到的症状
最可能缺的证据
下一步先问什么
如果不补会怎样


只有下载按钮和权重文件
数据账、超参账、stage 记录、评估协议。
数据从哪来，训练怎么配，分数怎么算。
你只能说“有模型”，不能说“模型怎么来的”。


有 MMLU 分数，但没说明 prompt 和 shot
评估协议。
版本、prompt、shot、解码参数、污染检查。
分数看起来高，但没法横向比较。


有 stage 名字，却不知道前后 checkpoint
checkpoint 谱系。
起止 checkpoint、token budget、数据切换和选择依据。
最终模型像是一个黑盒，不像一个阶段化结果。


有配置，但没有依赖版本和 seed
环境账和随机性账。
框架版本、dtype、并行策略、seed 和恢复点。
同一份配置，别人跑出来的曲线可能完全不同。

证据链审计口诀复制

```text
先看数据从哪来，再看训练怎么配；
先看 stage 从哪起，再看 checkpoint 怎么收；
先看分数按什么协议算，再看结果能不能比；
先看产物互相能不能对上，再决定要不要相信这份报告。
```

> **失败后要做什么** 如果材料只剩权重或只剩分数，不要先补修辞；先补协议和版本。修辞只能让页面看起来像报告，不能让报告真的可审计。


---

# toy run 对照 {#comparison}
<!-- tag: compare -->

同样是“训练模型”，toy run 和公开训练报告要回答的问题完全不同。前者先问链路有没有 bug，后者先问结论是否可审计、可查、可复现到合理程度。

维度
toy run 常见写法
公开训练报告应补齐


目标
“跑通 GPT 训练，loss 会下降。”
模型目标、预算、成功标准、不能外推的边界。


数据
“用了 TinyShakespeare / 一个小数据集。”
来源、版本、token 数、混合比例、过滤、去重、污染检查、许可证。


超参
只写 batch、learning rate、step 数。
模型结构、optimizer、LR schedule、warmup、dtype、并行、有效 batch、seed、硬件。


评估
看训练 loss、验证 loss、生成几个样例。
验证集定义、benchmark 协议、污染检查、失败任务、样例选择规则。


阶段
一个命令从头跑到尾。
stage 划分、数据切换、退火、checkpoint 谱系、异常恢复、最终选择依据。


产物
代码片段、最终 loss、少量截图。
权重、配置、训练脚本、日志、评估脚本、模型卡、中间 checkpoint。

> **关键判断** toy run 只回答“这条训练链路有没有 bug”；公开训练报告还要回答“这个模型结果是否可信、可查、可复查”。两者不能互相代替。


---

# 一页报告怎么读 {#example-read}
<!-- tag: example -->

下面用一个教学示例演示读法。这个片段是虚构的，只用来练习“先找证据，再下结论”的顺序，不代表某个真实项目。你要看的数字后面有没有协议、版本和链路。

教学示例：报告片段复制

```text
# 教学示例：小语言模型训练片段

## 数据配方
- sources: cleaned web 55%, code 25%, math 20%
- snapshot: 2026-06-21
- token_count: 320B
- filtering: length > 64, dedup, contamination scan

## 训练设置
- model: 120M / 12 layers / 768 hidden / 12 heads / 1024 context
- optimizer: AdamW, lr 3e-4, warmup 2k, cosine decay
- batch: micro 8 x 4 GPU x accum 4 = 128 effective batch

## 评估
- valid loss: 2.31
- contamination: pass on held-out set
- benchmark: report only, protocol separate
```

片段里的线
它支持什么结论
还缺什么
下一步追问


数据配方、snapshot、token_count、filtering
这次 run 有明确的数据来源和处理痕迹。
具体数据版本、许可证、样本抽查和切分种子。
能否把同一版数据再找回来，是否会碰到污染。


model、optimizer、batch
训练设置足够判断有效 batch 和模型规模。
依赖版本、dtype、并行策略、恢复点和 seed。
如果训练动态变了，问题更像模型、优化还是系统。


valid loss、contamination、benchmark protocol separate
作者知道验证损失和基准协议不能混着说。
完整 benchmark 协议、任务级结果、失败样例和选择依据。
分数是否可信，是否能和别的报告比较。

> **这一页能支持什么** 它支持你判断“这次 run 的骨架写出来了”；它还不能单独证明可复现、可比较和 fully open。要把这些结论补齐，还需要日志、脚本、数据版本和 checkpoint 谱系。


---

# 报告骨架 {#template}
<!-- tag: template -->

下面是一份小模型也能使用的公开配方模板。你不用一字不差地照抄，但每一栏都应该能被事实填满。把这些占位符填成可追踪信息，比写一段总结更重要。

训练报告骨架复制

```text
# 小语言模型训练报告

## 1. 目标与边界
- 目标能力：
- 模型规模：
- 预算：GPU 类型 / GPU 小时 / token budget
- 成功标准：
- 不可外推结论：

## 2. 数据配方
- 数据来源与版本：
- tokenizer 与 token 统计：
- 混合比例：
- 过滤、去重、质量控制：
- 评估污染检查：
- 许可证与可公开性：

## 3. 模型与训练设置
- 架构：layers / hidden / heads / context / vocab
- optimizer：type / betas / weight decay / grad clipping
- LR schedule：warmup / peak / decay / annealing
- batch：micro-batch / GPU 数 / accumulation / effective batch
- dtype 与系统：BF16 / FP16 / FP32、并行策略、FlashAttention、checkpointing
- seed、依赖版本、硬件：

## 4. 阶段记录
- Stage 0：sanity run
- Stage 1：base pretraining
- Stage 2：continued training or annealing
- 每阶段：起止 checkpoint、token 数、数据混合、LR、异常、选择依据

## 5. 评估
- 训练监控：loss、grad norm、吞吐、OOM / NaN 记录
- 验证集：来源、大小、评估间隔
- 下游 benchmark：版本、prompt、shot、解码参数、脚本
- 样例与失败案例：

## 6. 开放产物
- 权重与模型卡：
- 训练代码与配置：
- 数据清单与处理脚本：
- 日志、曲线、中间 checkpoint：
- 评估脚本与结果：

## 7. 结论
- 可靠发现：
- 失败与退步：
- 下一步实验：
```

**先填事实**每个空格都尽量写成可查的信息，而不是感觉词。
**再写边界**凡是不能外推的地方，直接写出来，别留给读者猜。
**最后对链路**报告、代码、日志和 checkpoint 要能彼此指向。


---

# 复现边界 {#boundary}
<!-- tag: boundary -->

好报告诚实说明哪些部分可复现、哪些部分只能近似、哪些结论不能外推。边界写清楚以后，别人更容易相信你的结果。

这一页能帮助你写什么
这一页先不追什么
下一步应该补什么


训练报告的证据链、数据账、超参账、阶段记录和产物链路。
完整 benchmark 实现、数据清洗流水线和 scaling-law 拟合细节。
把自己手头的小模型材料填进模板，再检查哪一环缺证据。


可复查的结论和不可外推的结论。
把一次小规模实验直接写成大模型承诺。
在 08 页里把预算、数据、模型和停机线写成自己的版本。


公开材料之间的对应关系。
把模型下载页当成训练报告的全部。
补上脚本、配置、日志、评估协议和 checkpoint 谱系。

> **下一页再看** 这里先不追 benchmark leaderboard、数据清洗实现细节、完整 scaling fit、提示词工程和后训练。它们在后面的单元里会重新出现。


---

# 下一步 {#bridge}
<!-- tag: bridge -->

把 07 的字段带到 08：问题、预算、数据、模型、指标和停机线。你不用先追“完整方案”，先把这几个字段写成自己的版本，下一页就能直接落成实验计划。

**问题**
先写一句话实验问题：你要验证什么关系，控制了什么变量。


**预算**
把 GPU 小时、token 数、重跑次数和人工排查时间写成上限。


**数据**
列出来源、版本、过滤、去重、切分和污染检查，不要只写数据集名称。


**边界**
把这次实验不能外推的地方提前写出来，下一页就能更快收束。

> **只做一件事** 打开 08 页之前，先把你手头那次小实验的四个字段写下来：问题、预算、数据、模型/指标。写完再继续，速度会更快。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box soft
### 怎么答才算会
先分层级，再补证据，最后写边界。你不需要把报告写得很长，但每个答案都要能指到具体字段、协议或产物。

## box info
### 检查重点
结构化题尽量直接选；开放题要写出对象、依赖和缺失项。只说感觉不算过关。

<!-- end-practice-intro -->

<!-- practice-group: 先分层级 -->
<!-- quick-review: 先看结论层级，再看是否可审计。能下载，不等于能复查。 -->

### 1 choice

**Q:** toy run 的真实任务

- [ ] 跑到更大规模时保证 SOTA
- [ ] 证明训练链路、loss、恢复和采样基本正常
- [ ] 证明公开报告已经 fully open
- [ ] 证明 benchmark 分数可直接外推

Answer: 证明训练链路、loss、恢复和采样基本正常
Explain: toy run 的重点是训练链路、loss、恢复和采样是否正常，不是验证大规模能力。

### 2 true/false

**Q:** open weights 与 fully open

- [ ] 对
- [ ] 错

Answer: 错
Explain: open weights 只说明最终权重可下载；fully open 还要看数据、代码、配置、日志、评估和中间 checkpoint。

### 3 choice

**Q:** 先补哪类证据

- [ ] 数据来源、版本、token 数、混合比例、过滤和污染检查
- [ ] 一个更大的 hero 图
- [ ] 训练吞吐截图
- [ ] 再贴一张 sample 图就够了

Answer: 数据来源、版本、token 数、混合比例、过滤和污染检查
Explain: 先补数据来源、版本、token 数、混合比例、过滤和污染检查，才知道模型到底学了什么。

<!-- end-practice-group -->

<!-- practice-group: 读证据 -->
<!-- quick-review: 数据账和超参账分开写，报告才有复查入口。名字不够，版本和数量更重要。 -->

### 4 debug-checklist

**Q:** 数据账清单

- [ ] 来源与版本
- [ ] token 数和混合比例
- [ ] 过滤与去重
- [ ] 许可证和污染检查
- [ ] 最终权重下载链接

Explain: 前四项属于数据账的核心证据；最后一项是开放产物的一部分，但不能替代数据账。判断时先问“模型学了什么”，再问“文件从哪来”。

### 5 choice

**Q:** 可追溯的信号

- [ ] 数据 URL + 下载日期 + hash + 切分种子
- [ ] 最终 sample 图
- [ ] GPU 型号
- [ ] loss 曲线截图

Answer: 数据 URL + 下载日期 + hash + 切分种子
Explain: 数据 URL + 下载日期 + hash + 切分种子，才能把同一版数据重新找回来。

### 6 synthesis

**Q:** 把口语改成配方

Explain: 合格答案要能读出数据来源、版本或下载时间、每类数据的 token 数和混合比例、过滤和去重方式、评估污染检查、许可证或可公开性边界。只把数据集名称换长，不算完成。

<!-- end-practice-group -->

<!-- practice-group: 阶段和产物 -->
<!-- quick-review: 阶段记录和产物链路要互相指向。只给最终分数，信息是不够的。 -->

### 7 code-reading

**Q:** 阶段记录缺口

- [ ] 数据混合和结束 checkpoint
- [ ] 一个更漂亮的名字
- [ ] 最终 benchmark 截图
- [ ] 模型介绍页

Answer: 数据混合和结束 checkpoint
Explain: 要回溯一段 stage，必须知道它从哪里开始、到哪里结束，以及数据是否变化。

### 8 synthesis

**Q:** 写一段 stage 记录

Explain: 合格答案至少要写出每阶段的起止 checkpoint、token budget、数据混合、学习率变化、评估间隔、选择标准。若只写“先打底，再收束”，还不够。

### 9 debug-checklist

**Q:** 产物链路

- [ ] 权重
- [ ] 模型卡
- [ ] 配置
- [ ] 训练代码
- [ ] 日志
- [ ] 评估脚本
- [ ] 数据版本
- [ ] 最终分数表

Explain: 关键“文件之间能不能互相指认”。权重要能指向配置，配置要能指向代码，日志要能指向 checkpoint，评估表要能指向脚本和数据版本。

<!-- end-practice-group -->

<!-- practice-group: 综合判断 -->
<!-- quick-review: 只给最终权重、最终分数和几张样例，证据还不够完整。 -->

### 10 choice

**Q:** 最该补的证据

- [ ] 评估协议和污染检查
- [ ] 更多宣传语
- [ ] 更复杂的封面
- [ ] 更长的简介

Answer: 评估协议和污染检查
Explain: 缺的是评估协议和污染检查，因为分数需要协议才能比较，需要污染检查才能解释可信度。

### 11 synthesis

**Q:** 为什么一次 run 不是配方

Explain: 合格答案要说明：run 只是一回执行，recipe 还要包括数据账、超参账、评估协议、阶段记录和产物链路；没有这些证据，别人只能看到结果，看不到结果怎么来的。

### 12 scenario-transfer

**Q:** 带去下一页

Explain: 至少要写出：要验证的问题、预算上限、数据来源和处理方式、模型规模或主要指标、停机线和不可外推结论。写成自己的版本，比照搬模板更有用。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- [Stanford CS336: Language Modeling from Scratch](https://cs336.stanford.edu/spring2025/index.html)（课程主线，访问：2026-06-21）。
- [karpathy/llm.c](https://github.com/karpathy/llm.c)（教学实现，访问：2026-06-21）。
- [OLMo 2 论文](https://arxiv.org/abs/2501.00656)（公开训练配方的参照，访问：2026-06-21）。
- [Ai2 OLMo 2 项目页](https://allenai.org/olmo2)（权重、数据、代码和评估的公开示例，访问：2026-06-21）。
- 本页只把这些公开材料当成学习边界，不复制课程作业、仓库说明或论文长段内容；更细的实现差异以后以最新公开文档为准。

---
