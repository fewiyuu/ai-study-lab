---
course: "提示工程学习笔记"
unit: "02 示例与指令"
title: "零样本、少样本与指令优化"
kicker: "第 2 节"
kicker_en: "示例与指令"
summary: "这一节先把 zero-shot、few-shot、instruction prompting 和 role prompting 分开看。重点不是把 prompt 写长，而是先判断最小有效写法：任务已经很清楚时先试 zero-shot，输出格式或标签边界不稳时再补 few-shot，任务边界和结果形状不够清楚时再把指令、角色和格式拆开写。CoT、RAG、工具调用这些内容放到后面的单元再展开。"

side_note: "先从最小提示起步，再决定要不要补示例、角色或更严格的格式。这个单元要解决的是起手和排错，不是把 prompt 写长。"

can_do:
  - "能先判断一条任务该从 zero-shot、few-shot、instruction prompting 还是 role prompting 起手。"
  - "能把模糊请求拆成任务、材料边界、输出格式和缺失处理。"
  - "能看出 few-shot 示例里的标签偏差、顺序敏感和“语气对了、交付物没对”的问题。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../01_提示工程基础与提示词结构/学习页.html"
  - text: "下一节"
    href: "../03_推理型提示：CoT、Self-Consistency与生成知识/学习页.html"

---

# 学习目标 {#goals}
<!-- tag: goal -->

这一页只解决一件事：当任务还没有复杂到要上推理链、检索或工具时，怎么先选对提示方式。先把任务动作、样本、角色、输出格式这四个东西分开，再谈怎样把它们组合起来。

你要会的事怎么检查常见失手点


判断先用 zero-shot 还是 few-shot看任务是否已经有清楚的标签和格式一上来就堆示例
把模糊请求改成可执行 prompt能写出任务、边界和输出形状只写“详细一点”“专业一点”
看出 few-shot 的偏差来源能指出标签分布、顺序和格式不一致把示例当成越多越好


---

# 先抓主线 {#overview}
<!-- tag: overview -->

提示工程先把最小有效提示写对。模型会按你给出的任务说明、材料边界和输出格式去补全内容，所以真正决定结果的，往往你先补了哪一层。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

方法主要补什么解决什么问题什么时候别急着加


zero-shot只有任务快速验证任务是否能做结果已经稳定时，不必先加示例
few-shot任务 + 示例固定标签、格式和风格任务本身还没说清时，先补指令
instruction任务 + 约束把动作、边界和输出写明白只是想换语气时，不要过度写长
role视角 + 语气让回答更像某个身份或场景不要拿角色代替交付物


---

# 状态账本 {#ledger}
<!-- tag: ledger -->

把 prompt 当成一个小流程看，会更容易排错。你写进去的内容、模型读到的内容、最后必须保住的内容，不是同一件事。下面这张账本先把它们分开。

对象读什么写什么写错后的症状


instruction任务目标要做什么模型知道要回应，却不知道要完成哪种动作
context场景、角色、边界在什么条件下做输出方向漂，或者跑去别的场景
input data待处理材料把要分析的内容圈起来模型把说明和正文混在一起
output indicator交付要求格式、字段、长度、缺失处理结果看着像答完了，但后续不能直接用

> **先抓一个判断** 如果你发现结果能读，但不能直接接到程序或文档里，先查 output indicator；如果模型连该做什么都没看准，先查 instruction。


---

# 代码读写 {#structure}
<!-- tag: structure -->

把 prompt 写稳，靠的不是堆词，而是把几层内容分开。先写任务，再写材料，再写输出形状，最后再加角色或风格。这样模型更容易知道哪些是输入，哪些是要求，哪些是最后必须交出来的结果。

## box soft
### 最小可检查 prompt
抽取任务骨架复制
```text
任务：请从下面文本中提取公司名称和成立时间。

输入材料：
"""
{text}
"""

输出格式：
1. 只输出 JSON
2. 字段固定为 name 和 establish_time
3. 如果找不到时间，写 null
```

这类骨架先把任务、材料和输出形状分开。后面再补角色、示例或更严格的语言风格，通常才不会改一层坏一层。

## box info
### 读写顺序
构件作用何时加入常见失手


instruction告诉模型要做什么几乎总是先写只写“帮我看看”
context告诉模型在什么场景下做任务依赖背景时加入把背景写成废话
input data告诉模型处理什么材料有具体文本、表格或问题时加入把材料和指令混在一起
output indicator告诉模型结果长什么样需要列表、表格、JSON 时加入只写“简洁一点”
role告诉模型用什么身份说话需要视角或语气时加入以为角色能替代任务
**先记一个规则**简单任务可以只用 instruction；要是你已经在交付格式上吃过亏，就先把 output indicator 写死，再考虑要不要加角色或示例。


---

# 方法选择 {#decision}
<!-- tag: decision -->

真正有用的知道什么时候该停在最小写法，什么时候该加一层。下面这张表就是一个实用的选择顺序：先看任务是否已经清楚，再看输出是否稳定，最后才考虑要不要补示例或角色。

现场信号先试什么为什么什么时候升级


标签很清楚，任务很短zero-shot先看模型能不能直接完成如果多输出解释或标签漂移，再补 instruction
格式容易跑偏instruction + output indicator先把结构钉住如果仍然学不稳，再加 few-shot
示例能明显教会标签边界few-shot让模型模仿输入输出模式要长期复用时，把角色和格式一起固定
需要稳定语气、身份或场景role + instruction角色先定说话姿态，任务再定交付物如果角色写了但结果仍散，再补示例

> **顺手检查** 如果你发现自己已经写了三层要求，但输出还乱，先停一下。常见原因任务、边界和格式还没有被拆开。


---

# 示例偏差 {#bias}
<!-- tag: bias -->

few-shot 不是“示例越多越好”。示例一旦开始带偏标签分布、顺序或词形，模型就会学到错误的模式。这个问题示例设计的问题。

偏差触发条件现场症状怎么缓和


多数标签偏差示例里某个标签占比太高模型倾向重复那个标签让标签分布更平衡
近因效应偏差最后一个示例的标签特别显眼模型容易追着末尾标签跑打散顺序，别让同一标签总排最后
常见令牌偏差标签词本身太常见或太强势模型偏向生成常见词检查标签是否过于含糊或过于惯用
顺序敏感示例排列不稳定同样的题，换个顺序就变了固定一版顺序，再做对比测试

## box soft
### 一个常见的校准思路
用 N/A 估计偏差复制
```text
文本：N/A
情感分类：积极、消极、中性
```

这个输入本身没有内容，目的是看模型对标签空间有没有天然偏向。它一个校准点。

## box info
### 别把示例当真相
few-shot 的样例是为了教模式，不是为了给事实背书。要是示例都不统一，模型学到的就是混乱的映射：有时学格式，有时学标签，有时只学到末尾那一个词。


---

# 真实排错 {#debug}
<!-- tag: debug -->

把这页学到的东西落到实战时，顺序很重要：先判断任务是不是已经清楚，再决定要不要补示例，最后才补角色和更严格的输出格式。这个顺序能让你少走很多弯路，也能让后面的 prompt 更容易维护。

步骤先看什么改写时写什么如果做反了会怎样


1. 任务动作模型到底是要分类、抽取还是改写把动词写出来模型不知道自己要做什么
2. 材料边界哪些内容算输入，哪些不算用分隔符或标签圈起来输入和说明混在一起
3. 输出形状结果是列表、表格还是 JSON把字段、长度和缺失规则写清楚答案看得懂，但不能直接用
4. 最后收束是否需要角色、语气或示例只保留真正影响结果的约束prompt 被空话和姿态词撑大

## box
### 一个可直接拿去改写的模板
结构化 prompt 模板复制
```text
你是一位{{角色}}。
任务：{{明确任务}}
输入：{{材料}}
要求：{{长度、字段、边界、缺失值}}
输出：{{结果格式}}
```


## box warn
### 失败实验室
坏 prompt
补齐后
先查哪里

输入：帮我分析一下这个产品，写详细点，不要太长，也不要乱说。
症状：任务动作模糊，长度要求互相打架，输入边界没收。
先查：instruction、output indicator、缺失处理。补齐后：请从用户场景、核心问题和下一步建议三个角度分析这个产品，每部分 60 到 80 字。只看下面给出的评审材料，缺少资料时请直接说明缺什么。输出只用三段，不要加额外寒暄。排查顺序：先查任务动作，再查输入边界，再查输出格式，最后查角色和语气。
判断规则：如果结果能看懂但不能直接用，先修 output indicator；如果模型不知道自己要做什么，先修 instruction。
这里在看症状怎么对应缺口。真正有用的修法，是先补最先坏掉的那一层。


---

# 先别追 {#boundary}
<!-- tag: boundary -->

这一页先不展开 CoT、Self-Consistency、RAG、APE、Active Prompting 和 Directional Stimulus Prompting。它们会在后面的单元继续出现，但前提都是你已经知道怎么把一条 prompt 的任务、材料和输出形状写稳。

如果你现在还在犹豫“要不要加示例”，先回到前面的选择表：任务已经清楚时先试 zero-shot；格式和标签开始漂时再补 few-shot；需要身份、语气或场景时，再把 role 和 output indicator 一起补上。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box soft
### 怎么答才算会
先看任务是否已经清楚，再判断要不要补示例、补角色或补输出格式。选择题和判断题要能当场核对；改写题要把错因、后果和修法写出来，不要只交一个更长的 prompt。

## box info
### 检查重点
这页最看重三件事：能不能选对最小有效策略、能不能把模糊请求改成结构化 prompt、能不能识别 few-shot 示例设计里的偏差。

<!-- end-practice-intro -->

<!-- practice-group: 一、概念辨认 -->
<!-- quick-review: review 把每道题当成“对象识别”题，先说清它在 prompt 里扮演什么角色。 -->

### 1 choice

**Q:** 1. 先试哪种

- [ ] zero-shot
- [ ] few-shot
- [ ] 角色提示

Answer: zero-shot
Explain: 任务清楚、标签也清楚时，先用 zero-shot 看最小写法能不能跑通。

### 2 choice

**Q:** 2. 角色最接近什么

- [ ] 上下文 / 角色提示
- [ ] 输入数据
- [ ] 输出指示

Answer: 上下文 / 角色提示
Explain: 角色提示主要改的是视角和语气，不是交付物本身。

### 3 true/false

**Q:** 3. 判断题：四个要素必须全有

- [ ] 对
- [ ] 错

Answer: 错
Explain: 简单任务可能只需要其中一部分。

### 4 fill

**Q:** 4. 术语填空

- [ ] 第一个空
- [ ] 第二个空

Explain: 一个负责任务动作，一个负责结果形状。

<!-- end-practice-group -->

<!-- practice-group: 二、方法选择 -->
<!-- quick-review: review 把“先试什么”说清楚，比把 prompt 堆长更重要。 -->

### 5 ordering

**Q:** 5. prompt 结构排序

- [ ] 先任务指令，再背景说明，再输入材料，再输出格式
- [ ] 先输入材料，再任务指令，再输出格式，再背景说明
- [ ] 先背景说明，再输入材料，再任务指令，再输出格式
- [ ] 顺序无所谓，只要句子够长就行

Answer: 先任务指令，再背景说明，再输入材料，再输出格式
Explain: 先任务指令，再背景说明，再输入材料，再输出格式，是最容易检查的顺序。

### 6 choice

**Q:** 6. 何时用 few-shot

- [ ] 把用户反馈分成“产品问题 / 物流问题 / 售后问题”，而且你想固定标签和输出格式
- [ ] 翻译一个短句
- [ ] 临时问一个常识定义

Answer: 把用户反馈分成“产品问题 / 物流问题 / 售后问题”，而且你想固定标签和输出格式
Explain: 格式和标签容易漂时，few-shot 能把模式固定下来。

### 7 true/false

**Q:** 7. 示例顺序是否重要

- [ ] 对
- [ ] 错

Answer: 错
Explain: few-shot 对顺序敏感，尤其是标签分布不均时。

<!-- end-practice-group -->

<!-- practice-group: 三、改写与诊断 -->
<!-- quick-review: review 改写题先写错因，再写结构，不要直接往上加字数。 -->

### 8 diagnostic

**Q:** 8. 模糊请求改写

Explain: 重点是补任务动作、长度边界和禁止项，不是单纯加长。

### 9 choice

**Q:** 9. 哪种任务更适合结构化提示

- [ ] 一次性翻译一句话
- [ ] 长期复用的反馈助手，要固定字段和输出格式
- [ ] 临时问一个简单定义

Answer: 长期复用的反馈助手，要固定字段和输出格式
Explain: 长期复用的助手最需要稳定结构和可检查输出。

### 10 scenario

**Q:** 10. 角色 prompt 改写

Explain: 要补学习者水平、讲解范围和输出结构，不能只补角色。

<!-- end-practice-group -->

<!-- practice-group: 四、迁移与边界 -->
<!-- quick-review: review 先把边界说清楚，再去谈更复杂的方法。 -->

### 11 true/false

**Q:** 11. 先补什么更省

- [ ] 对
- [ ] 错

Answer: 对
Explain: 当结构已经稳定时，先补 instruction 和 output indicator 往往比堆很多示例更省。

### 12 synthesis

**Q:** 12. 费曼解释

Explain: 答案要说出选择顺序，也要说出这页的边界。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- 《提示工程学习笔记：来源材料》中的“基本原则”与“提示工程技术”段落，支撑本页的 zero-shot、few-shot、instruction prompting、role prompting 和输出格式约束。
- Prompt Engineering Guide 与 OpenAI 官方提示工程指南，支撑“先写清任务、用分隔符隔开材料、尽量具体、明确输出格式”这条实践主线。
- Matt Nigh 的 CRISPE Prompt Framework，支撑结构化提示词的骨架说明。
- Tom Brown et al. 2020、Sewon Min et al. 2022、Tony Z. Zhao et al. 2021、Ouyang et al. 2022、Seonghyeon Ye et al. 2022，支撑从 zero-shot 过渡到 few-shot、instruction tuning 和 in-context instruction learning 的课程路线。
- Jiacheng Liu et al. 2023、Yongchao Zhou et al. 2023 属于后续单元会继续用到的内容，本页只做边界提示。

---
