---
course: "李宏毅 LLM 课程"
unit: "01 P01 第1讲：一堂课搞懂 LLM 大模型的原理"
title: "第1讲：一堂课搞懂 LLM 大模型的原理"
kicker: "P1 核心讲义"
summary: "网页聊天界面看起来像“输入问题，模型回答”。P1 要拆掉这层界面感：LLM 每一步看到的是一串 token，它根据已经出现的内容，为 vocabulary 里的候选 token 打分，再选出下一个 token。完整回答只是这个动作反复执行后的结果。"

side_note: "这节先把 LLM 看成可检查的生成流程：输入怎样变成 token，模型怎样给下一个 token 打分，聊天模板和上下文怎样改变结果。"

can_do:
  - "用“prompt -> token -> 概率分布 -> 采样 -> 追加 token”解释一段回答怎样长出来。"
  - "区分 token、token id、vocabulary 和 Hugging Face token，避免把同名词混用。"
  - "判断 chat template、system prompt、历史消息和 RAG 证据分别改变了模型看到的哪一部分输入。"
  - "读懂最小 Hugging Face 路线：tokenizer 管文字和编号，model 管概率，generate 管循环。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "下一节：剖析大型语言模型"
    href: "../02_P03_第3讲：剖析大型语言模型/学习页.html"

---

# 先把本讲的检查点立起来 {#goals}
<!-- tag: goals -->
<!-- layout: grid2 -->

## box soft
### P1 的主线
这一讲的核心是看懂大语言模型怎样把一段文字回答拆成可执行的步骤，背模型名字放在后面。用户看到的是聊天框，模型内部处理的是 token 序列；用户等待的是完整回答，模型每一步真正产出的是下一个 token 的概率分布。
所以学习顺序可以很朴素：文字先被 tokenizer 切成 token，再映射成 token id；模型根据已经出现的 token 计算下一个位置的分数；生成策略从这些分数里选一个 token；新 token 追加回上下文，循环继续，直到遇到停止条件。

## box
### 本页只要求你掌握这些
- 用一个流程图解释 prompt 如何变成回答。
- 说明 token、vocabulary、token id 和 Hugging Face token 的差异。
- 判断一个输出异常更可能来自 prompt、chat template、上下文、采样还是证据不足。
- 能读懂 tokenizer、model、generate 和 pipeline 在最小代码里的分工。
现在你应该能做什么：看到一个 LLM 回答时，先按“输入表示、模型打分、生成策略、上下文更新”四层来问问题。


---

# 全流程地图 {#map}
<!-- tag: trace -->

先把一轮生成放在同一张表里。这里的“模型”可以先当成一个已经训练好的函数，它接收 token id 序列，输出下一个位置对 vocabulary 中每个候选 token 的分数。真实系统会有更多工程包装，但这张地图足够定位大部分入门误解。

步骤对象模型或工具做什么常见检查点


1用户文字整理成 prompt，聊天模型还会套入角色模板。system、user、assistant 的边界是否清楚。
2tokentokenizer 把文字切成模型认识的单位。中文、空格、特殊符号是否被切成意料之外的片段。
3token id每个 token 被映射成 vocabulary 里的编号。编号只是索引，不携带人类可读含义。
4logits / probability模型给下一个 token 的所有候选项打分，再转成概率。最高分 token 与采样策略可能不一致。
5next token贪心、top-k、top-p、temperature 等策略选出一个 token。随机性、重复、跑题常从这里放大。
6更新后的上下文新 token 被追加，下一轮继续。超出上下文窗口时，旧信息可能被截断。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

现在你应该能做什么：拿到一段模型输出时，先问它是在“表示层”错了，还是在“生成层”错了。


---

# token 边界 {#tokens}
<!-- tag: tokenizer -->
<!-- layout: grid2 -->

## box
### 同一个词，两个 token
本讲最容易混的地方是 token。LLM 里的 token 是模型处理文本的基本单位，可以是一个中文字、一个英文词片段、一个标点、一个空格模式，也可以是特殊控制符。Hugging Face 网站或 API 里说的 access token 则是权限凭证，和模型切词没有关系。
vocabulary 是模型认识的 token 清单。token id 是 token 在这张清单里的编号。编号方便模型计算，但编号本身没有语义顺序，不能说“id 越大含义越复杂”。
术语这一讲怎么用不要误会成


token模型处理文字的片段单位。总是一个完整词或一个中文字。
vocabulary模型可选 token 的全集。自然语言里的全部词语。
token idtoken 在 vocabulary 里的整数索引。带有大小顺序的语义标签。
Hugging Face token访问模型仓库或 API 的权限凭证。LLM 生成时的文本 token。

## box soft
### 最小观察代码
实际学习时，不需要先训练模型。先拿 tokenizer 做 encode / decode，就能看到“文字”和“模型输入”之间的边界。
tokenizer encode/decode复制
```python
from transformers import AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-0.5B-Instruct")
text = "LLM 会逐步预测下一个 token。"

ids = tokenizer.encode(text)
tokens = tokenizer.convert_ids_to_tokens(ids)

print(ids)
print(tokens)
print(tokenizer.decode(ids))
```

这段代码检查三件事：切出来多少个 token，token id 是什么，decode 回来后是否仍然是原文。之后遇到“模型为什么不懂我的格式”，先看 tokenizer 输出往往比猜模型性格更有效。


---

# 生成循环 {#generation}
<!-- tag: next-token -->

大语言模型看起来一次写出一整段，内部更接近一个循环：根据已有序列预测下一个 token，选出它，追加，再预测下一个。这个动作也叫自回归生成，重点是“新输出会成为下一步输入的一部分”。

$$
 P(t_i \mid t_1, t_2, \ldots, t_{i-1}) = \mathrm{softmax}(z_i)
 $$
 `t_i` 是下一个 token，前面的 token 构成条件；`z_i` 可以理解为模型给 vocabulary 中每个候选 token 的原始分数。

<!-- interactive: ['shift-demo'] → keep as HTML -->

## box
### 采样策略影响风格
如果每一步都选概率最高的 token，输出通常更稳定，但也可能死板、重复。加入 temperature、top-k 或 top-p 后，模型会从一小批候选里采样，回答更有变化，也更可能偏离原题。
现象可能原因先检查
答案每次都完全一样temperature 很低或贪心解码。采样参数是否允许随机性。
答案跑题prompt 约束弱，采样过松。system prompt、temperature、top-p。
一直重复句子局部高概率循环被放大。重复惩罚、停止词、最大长度。

## box soft
### 停止条件很关键
模型本身只会继续给下一个 token 打分。何时停下来，通常要靠最大 token 数、结束符、停止词、工具调用协议或应用层规则。很多“回答被截断”和“模型停不下来”的问题，根源常在停止条件配置，而不在模型知识本身。
现在你应该能做什么：解释“一个回答”为什么其实是一连串 token 决策，并能把重复、截断、随机性归到生成参数里检查。


---

# 聊天模板 {#chat-template}
<!-- tag: prompt -->
<!-- layout: grid2 -->

## box
### 聊天要先套模板
聊天模型通常在训练时见过固定格式：system 指令、user 消息、assistant 回复之间有特殊标记。推论时如果没有使用同样或兼容的 chat template，模型看到的输入格式就会陌生。结果可能表现为角色错乱、把用户问题复述成答案、或者漏掉系统约束。
apply_chat_template复制
```python
from transformers import AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-0.5B-Instruct")
messages = [
 {"role": "system", "content": "你是一个严谨的课程助教。"},
 {"role": "user", "content": "用三句话解释 LLM 的生成流程。"},
]

prompt = tokenizer.apply_chat_template(
 messages,
 tokenize=False,
 add_generation_prompt=True,
)
print(prompt)
```


## box soft
### 模板改变的是模型实际看到的输入
如果只在应用层保存了“system: 你要严谨”，但没有把它按模型需要的格式写进 prompt，模型就不会真正看到这条约束。反过来，如果历史消息堆得太长，新的关键要求可能被挤到上下文窗口外。
角色用途排错时看什么
system设定回答规则、身份和边界。是否进入最终 prompt；是否被后续用户指令冲淡。
user表达当前任务和输入材料。任务是否具体，证据是否完整。
assistant历史回答或生成起点。历史中是否有错误结论被继续引用。


---

# 上下文与幻觉 {#context}
<!-- tag: context -->

LLM 的回答只根据当前上下文和模型参数产生。它不会自动知道你心里省略的资料，也不会天然区分“我记得的模式”和“这次输入里有证据”。这就是幻觉问题的基本入口：当模型没有足够证据时，它仍然会按语言模式补出看起来顺畅的内容。

## box
### 上下文窗口像工作台
多轮对话、系统指令、检索到的资料、工具返回值、用户这次的问题，最后都要放进上下文窗口。窗口有限，内容有顺序，越靠近当前生成位置的内容通常越容易影响输出。RAG 的价值就在于把外部证据放到当前上下文里，让模型有材料可引用。
材料进入上下文后的作用风险


系统指令限制风格、身份、边界和安全要求。过长或冲突时难以稳定执行。
历史消息保持对话连续性。旧错误会被继续沿用。
检索证据给回答提供外部事实依据。证据错、缺、过时都会污染回答。
当前问题确定这一步真正要解决的任务。问题模糊会迫使模型补假设。

## box warn
### 幻觉先按证据问题处理
不要一上来把幻觉归结为“模型坏了”。更可操作的检查顺序是：用户问题是否需要外部事实；上下文里有没有足够证据；证据是否被明确引用；生成参数是否太松；应用层是否要求模型在不确定时承认不知道。
这也是后续 RAG、工具调用、评估和安全章节会继续展开的原因。P1 只要先记住：LLM 可以先看成基于上下文生成下一个 token 的模型，事实校验要靠证据、工具和评估链路补上。


---

# Hugging Face 最小观察路线 {#hf-route}
<!-- tag: implementation -->

本讲不要求你训练模型，但可以用 Hugging Face 的工具把“tokenizer 管表示，model 管打分，generate 管循环”看清楚。下面代码的目的在于把各层职责分开，速度优化先放到后面的部署主题里。

generate 最小路线复制

```python
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer

name = "Qwen/Qwen2.5-0.5B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(name)
model = AutoModelForCausalLM.from_pretrained(name, torch_dtype="auto")

messages = [
 {"role": "system", "content": "你是一个严谨的课程助教。"},
 {"role": "user", "content": "用三句话解释 LLM 的生成流程。"},
]

prompt = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
inputs = tokenizer(prompt, return_tensors="pt")

output_ids = model.generate(
 **inputs,
 max_new_tokens=96,
 temperature=0.7,
 top_p=0.9,
)

print(tokenizer.decode(output_ids[0], skip_special_tokens=True))
```

代码对象负责什么出问题时的症状


`AutoTokenizer`文字、token、token id 之间的转换；聊天模板。特殊标记错、角色格式错、切词意外。
`AutoModelForCausalLM`给下一个 token 的候选项打分。模型类型不匹配、权重加载失败、设备或精度问题。
`generate`循环采样、追加 token、处理停止条件。重复、截断、太随机、太保守。
`pipeline`把 tokenizer、model、生成参数包装成易用接口。方便但隐藏细节，不适合第一次排错。

现在你应该能做什么：看到一段 transformers 代码时，指出哪几行在处理输入格式，哪几行在触发生成循环。


---

# 排错演练 {#failure-lab}
<!-- tag: failure -->

下面三个案例都来自 P1 的基本模型观：模型只根据当前上下文预测下一个 token。点击案例时，先不要急着改模型，先找“模型实际看到的东西”和“你以为模型看到的东西”之间的差距。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

输出异常优先检查为什么


角色混乱chat template、system/user/assistant 边界。模型实际看到的格式可能和训练格式不一致。
事实编造上下文证据、RAG 返回内容、不确定性规则。模型会补齐语言模式，不会自动查证。
回答太短或突然停下`max_new_tokens`、停止词、结束符。停止条件可能先于完整回答触发。
重复或绕圈temperature、top-p、重复惩罚、prompt 约束。生成循环会把前一步输出继续喂给下一步。


---

# 微型排错记录 {#mini-debug}
<!-- tag: debug -->

把 P1 的概念用于真实代码时，最有用的习惯是留下排错记录。下面这条记录模拟一个常见问题：你明明写了“请用三句话解释”，模型却输出一长段，甚至开始补充无关背景。先把一次请求拆开看。

## box
### 症状
用户输入是“用三句话解释 LLM 的生成流程”。输出却有七段，还夹带“作为一个人工智能模型”的套话。这个症状至少可能来自三处：chat template 没有正确加上 assistant 起始标记；system prompt 对长度约束太弱；采样参数让模型在高概率客套话上继续扩写。
第一步先别看最终自然语言回答，改为打印模型实际收到的 prompt。很多问题在这里就能暴露：角色边界有没有出现，system 指令是否在 user 前面，最后是否有生成提示符，历史消息里有没有旧的冗长回答。

## box soft
### 最小检查代码
打印 prompt 与长度复制
```python
prompt = tokenizer.apply_chat_template(
 messages,
 tokenize=False,
 add_generation_prompt=True,
)
inputs = tokenizer(prompt, return_tensors="pt")

print(prompt)
print(inputs["input_ids"].shape)
print(tokenizer.convert_ids_to_tokens(inputs["input_ids"][0][-20:]))
```

这段代码不负责改答案，它只负责让你看见输入。最后 20 个 token 可以帮助你确认生成起点附近有没有正确的 assistant 标记，以及当前问题是否真的靠近序列末尾。若这里已经错了，继续调 temperature 只会让问题更难定位。

检查动作你要看到什么如果不符合，下一步怎么改


打印最终 promptsystem、user、assistant 边界清楚，末尾有生成起点。改用模型 tokenizer 自带的 `apply_chat_template`。
看 `input_ids.shape`长度没有逼近上下文窗口，当前问题没有被挤掉。压缩历史，移除无关轮次，保留当前任务和证据。
看末尾 token最后一段 token 对应当前用户问题或 assistant 起始标记。检查消息顺序，避免把回答模板放进 user 内容。
收紧生成参数长度、随机性和停止条件与任务匹配。降低 temperature，设置 `max_new_tokens` 和明确停止规则。

这条记录的价值在于把“模型不听话”改写成可验证的问题。你能看到 prompt，就能判断模型是否真的读到了你的约束；你能看到 token 长度，就能判断旧消息是否占掉窗口；你能看到生成参数，就能解释为什么回答会变长、重复或发散。P1 讲的 token 和 next-token 预测，最终要服务于这种排错能力。

现在你应该能做什么：遇到输出异常时，先打印最终 prompt 和 token 序列，再判断模型到底读到了什么。


---

# 本讲边界 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
### 这节先不展开
- Transformer 内部的 attention、MLP、位置编码和层归一化。
- 预训练、监督微调、偏好对齐和 RLHF 的完整训练链路。
- RAG、工具调用、评估、安全、多模态和 agent 工程。
这些主题后面都会回来。现在先把最底层的观察语言统一：输入是什么，token 怎么来，模型输出什么，生成循环怎样停。

## box soft
### 带走一条判断线
LLM 的“会说话”可以先拆成一个机械过程：把上下文变成 token id，模型对 vocabulary 里的下一个 token 打分，生成策略选一个 token，再把它接回去。这个解释不覆盖所有工程细节，但足够支撑你继续学 tokenizer、Transformer、RAG、评估和部署。
现在你应该能做什么：不用玄学词汇，给别人讲清楚一段 LLM 回答是怎样一步一步长出来的。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

choiceconcept
LLM 每一步真正预测什么？
从本讲的简化模型看，LLM 生成回答时，每一步最直接的动作是什么？
 A. 一次性生成整篇文章的最终语义树
 B. 根据已有上下文预测下一个 token
 C. 在互联网实时搜索每一个事实
检查


fillconcept
模型处理文字的基本片段叫什么？
请填一个英文术语：模型把文字切成一小段一小段来处理，这个片段通常叫做什么？

检查


fillconcept
模型可选 token 的全集叫什么？
请填一个英文术语：模型可以从哪张清单里选择下一个 token？

检查


choiceconcept
token id 的含义
下面哪句话最准确？
 A. token id 越大，词义越抽象
 B. token id 是 Hugging Face 的登录凭证
 C. token id 是 token 在 vocabulary 里的整数索引
检查

orderingprocess
写出生成循环
用 4 到 6 步写出一轮 LLM 回答怎样从 prompt 长出来。要求包含 token、概率分布、采样和追加上下文。
目标：能把“完整回答”拆成一串可检查动作。

展开反馈

参考答案 / 评分要点
较完整的链路是：prompt 被聊天模板整理；tokenizer 切成 token 并转成 token id；模型根据当前序列输出下一个位置的 logits；softmax 后得到候选 token 的概率；生成策略选出一个 token；把它追加到上下文，直到结束符、停止词或最大长度触发。


choiceprocess
top-p 和 temperature 主要影响什么？
这些参数最直接影响生成流程中的哪一环？
 A. tokenizer 如何切词
 B. 从候选概率里选择下一个 token
 C. vocabulary 的训练语料来源
检查


debug checklistprocess
回答突然截断，先看哪些配置？
一次生成经常半句就停。请选择你会优先检查的项。
 `max_new_tokens` 是否太小
 停止词或结束符是否被过早触发
 页面字体是否太小
 上下文是否已接近窗口限制

展开反馈
优先检查 `max_new_tokens`、停止词 / 结束符、上下文窗口。页面字体不影响模型生成。排错时先看生成停止条件，再看 prompt 是否要求了过短输出。

choicetemplate
角色混乱时优先检查什么？
模型把 system 规则当普通正文复述。你最先检查哪一项？
 A. 是否使用了匹配模型的 chat template
 B. token id 的数字大小是否异常
 C. 输出字体是否换成等宽字体
检查


debug checklisttemplate
幻觉排查清单
模型编出一个听起来很真的事实。请选择优先检查项。
 当前上下文里有没有可靠证据
 RAG 或资料检索是否返回了正确片段
 prompt 是否要求不确定时说明不知道
 token id 是否按语义大小排序

展开反馈
优先看证据、检索结果和不确定性规则。token id 的大小不表达事实可靠性。LLM 会生成高概率文本，事实约束必须通过上下文、工具或评估系统补上。


diagnosistemplate
历史消息怎样制造错误？
解释一个场景：第一轮 assistant 给了错误结论，后面几轮用户继续追问，模型越来越确信。请说明错误是怎样通过上下文延续的，并给一个修复动作。
目标：把“模型变固执”翻译成上下文台账问题。

展开反馈

参考答案 / 评分要点
要点包括：历史 assistant 回复也会进入后续 prompt；模型会把这些 token 当作当前上下文的一部分继续生成；旧错误如果没有被显式纠正，会像证据一样影响后续回答。修复可以是压缩历史、移除错误轮次、加入明确纠错说明，或重新检索可靠证据。

fillcode
transformers 中负责生成循环的常用方法是什么？
请填一个方法名：模型对象通常调用哪个方法来执行生成循环？

检查


choicecode
tokenizer 和 model 的分工
下面哪句话最符合本页的 Hugging Face 路线？
 A. tokenizer 负责事实校验，model 负责把网页下载到本地
 B. tokenizer 负责文字和 id 转换，model 负责下一个 token 的分数
 C. model 只负责显示网页，generate 只负责复制代码
检查


diagnosiscode
给一段最小排错顺序
你运行本页的 transformers 代码，结果回答跑题且格式混乱。写出你会检查的 5 个点，顺序要从输入表示开始，到生成参数结束。
目标：把“模型表现不好”拆成可执行的排查清单。

展开反馈

参考答案 / 评分要点
可接受顺序：打印最终 prompt；确认 chat template 和角色边界；查看 tokenizer 输出和长度；确认上下文里有必要证据；检查 `max_new_tokens`、temperature、top-p、停止词；必要时降低随机性并缩短问题。答案不必逐字相同，但要体现从表示到生成的排查顺序。


synthesissummary
用自己的话讲给别人听
用 6 到 9 句话解释：LLM 为什么看起来在“理解并回答”，但底层可以先用 next-token 预测来描述。必须包含 token、vocabulary、概率分布、上下文和停止条件。
目标：能把 P1 的主线讲完整，不只记住几个词。

展开反馈

参考答案 / 评分要点
优秀答案会说明：输入先被聊天模板和 tokenizer 处理；token 来自 vocabulary；模型根据当前上下文给下一个 token 的候选项打分；softmax 后形成概率分布；生成策略选出 token 并追加；循环持续到停止条件触发；上下文和证据决定模型这一步能利用什么信息；幻觉可以先理解为证据不足时仍生成顺畅文本。


---

# 参考资料 {#sources}
<!-- tag: source -->

- 课程根 `README.md`：确认 P1 是本课程第一个核心讲义，并提供原始字幕文件名线索。
- 课程地图 `index.html`：确认本页属于“建立入口”阶段，后续会接大型语言模型剖析、评估、训练、RAG、Transformer、安全和多模态等主题。
- 旧版 `01_P01/学习页.html`：作为本地内容重构来源，保留 token、采样、chat template、幻觉与 Hugging Face 实作路线。

---
