---
course: "李宏毅 LLM 课程"
unit: "09"
title: "P10｜语音语言模型发展史"
kicker: "LEE HUNG-YI LLM 2026"
kicker_en: "P10"
summary: "这一讲不是完整 survey，而是从李宏毅团队参与过的研究路径出发，回看语音语言模型如何从语音检索、无监督语音辨识、representation model、初代 speech token 接龙，走到 GPT-4o / Moshi 之后的语音对话模型。"

side_note: "这讲是第一人称研究史。不要只背年份，要学会问：模型听到什么、输出什么、中间有没有文字、语音 token 承载什么信息、是否能实时互动。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../08_P09_第9讲：图像与声音生成策略_Diffusion_Flow_matching系列与自回归_Autoregressi/学习页.html"
  - text: "下一节"
    href: "../10_P11_作业1：理解生成式人工智能的基础知识作业/学习页.html"
---

# 一、概念骨架：先看整条技术链 {#skeleton}
<!-- tag: SKELETON -->

这一讲用第一人称研究史回答一个问题：如果模型要直接听人说话、直接用语音回答，它需要先解决哪些前置问题？主线可以压缩成一条链：先分清任务边界，再比较系统路线，然后从语音片段表示、语音版自监督模型、语音 token、文字语音混合生成，最后走到对话对齐和实时互动。

这条链的关键直觉是“语音比文字多”。文字主要承载说了什么，语音还带着谁在说、怎么说、在什么环境说、是否迟疑、是否讽刺、是否打断。把语音先转成文字会得到一个工程上很有用的系统，但它天然会把许多非文字信息压掉。端到端语音语言模型困难得多，却是为了追求更高的信息上限。

任务边界→
系统路线→
语音表示→
语音 token→
文字语音混合→
对话与实时

2010**深度学习进入语音辨识早期**早期结果未必立刻压倒传统系统，但它改写了“声音怎样被表示”的研究方向。
2015**端到端 ASR 早期尝试**错误率看起来很高，却展示了一个神经网络直接从声音到文字的可能性。
2016-2019**audio word2vec、segmental 表示、无监督 ASR**核心问题从听写扩展到：声音能不能自己形成片段、向量和可检索单位。
2019-2021**Mockingjay、TERA、APC、wav2vec 2.0、HuBERT、SUPERB**语音 representation model 成为多种下游任务的基础，也为 speech token 提供来源。
2023-2025**GSLM、GPT-4o、Moshi、hybrid decoding、TASTE、STITCH**研究焦点从语音接龙走向可对话、可对齐、能利用播放空档思考的语音模型。

## box
### 你现在应该能做什么
读完这一段，你应该能把 P10 当成一条“表示学习 → token 化 → 生成策略 → 对话对齐 → 实时交互”的发展线，而不是只背模型名称和年份。


---

# 二、术语边界：别被模型名字带偏 {#terms}
<!-- tag: TERMS -->

课程里所说的 speech language model，默认指输入语音、输出语音，并且用于对话的模型。你对它说 “How are you”，它应该回应 “I am fine” 之类的内容，而不是复述你的句子。这个边界很重要，因为文献里的用词还没有稳定下来。

有人把能听懂语音指令、输出文字答案的系统也叫 speech language model；很多 TTS 模型因为内部也在做语音 token 接龙，也会把自己叫 speech language model。读论文时不能只看名字，要看输入、输出、任务、是否对话，以及它是不是把 ASR、LLM、TTS 串起来伪装成单一模型。

| 术语 | 直觉解释 | 精确定义与常见误解 |
| --- | --- | --- |
| 对话式语音语言模型 | 直接听你说话，再直接说话回应。 | 输入语音、输出语音，并把输入当成对话轮次。误区是把任何语音进语音出的系统都算进来。 |
| Command mode / speech-aware LM | 语音是待处理资料，指令决定任务。 | 常见任务是情绪识别、翻译、分类或问答。它可能懂语音，却不一定是语音对话助手。 |
| TTS | 把文字念出来。 | 输入通常是文字，输出是语音。即使内部用语音 token 接龙，也不等同于听懂并回应用户。 |
| Cascade | ASR、文字 LLM、TTS 三段串接。 | 容易落地，短期强，但 ASR 会丢掉说话人、情绪、环境等非文字信息。 |
| Agentic solution | 文字模型作为核心调度语音工具。 | 能调用 ASR、TTS、speech enhancement、voice conversion 等工具。灵活，但系统状态和延迟更复杂。 |
| End-to-end | 一个模型直接语音进、语音出。 | 能力上限高，能利用完整声音信息；短期训练难、资料贵、成熟方案少。 |
| Semantic token | 从语音表示模型离散化得到的 token。 | 通常来自 representation model + clustering / VQ。名字不完全准确，因为它不一定只含语义。 |
| Acoustic token | 由语音 codec 压缩得到的 token。 | tokenizer 与 detokenizer 一起训练，更重视还原音质和发音细节，但序列可能更长。 |
| Hybrid decoding | 文字 token 和语音 token 协同生成。 | 文字保留知识和语义，语音 token 负责说法、语气、时长等文字外信息。 |
| Paralinguistic information | 文字内容以外的语音信息。 | 包括语气、年龄、性别、情绪、停顿、环境、讽刺或真诚程度。语音 RLHF 不能只看转写文本。 |

## box soft
### 你现在应该能判断什么
看到一篇论文说自己是 speech language model，先问四件事：它直接听语音吗？直接输出语音吗？是在对话还是做指令任务？它是否只是 TTS 或 cascade 系统？这四问能帮你避开大多数术语混乱。


---

# 三、核心讲义：从可用系统到能力上限 {#lecture}
<!-- tag: LECTURE -->

### 1. 三条路线的取舍

要做语音进语音出，不一定要端到端。最容易想到的是 cascade：ASR 把语音转文字，文字 LLM 生成回复，TTS 把回复念出来。今天很多看起来能语音对话的产品，底层仍然可能是这种串接方案。

另一种是 agentic solution：以文字模型为核心，让它调用 ASR、TTS、语音增强、语音转换等工具。它更像一个会调工具的系统，而不是一个单一模型。端到端路线则是一个模型直接吃语音、直接吐语音。短期看，cascade 借助成熟 API 往往更稳；长期看，端到端路线更可能保留语气、说话人、环境和打断等完整语音信息。

Cascade优点是容易做，现成 API 多，短期效果常常最好。缺点是 ASR 会把说话人、语气、环境等信息压掉，多个模型串接也会增加延迟和工程复杂度。
Agentic优点是灵活，可以按任务调用不同语音工具。缺点是系统边界复杂，真实实时对话里的工具选择、状态同步和延迟控制都不简单。
End-to-end优点是理论上能保留完整语音信息，延迟上限也更好。缺点是技术仍在快速发展，训练成本高，短期内不一定超过 cascade。

### 2. 历史教训：弱结果不等于没价值

李宏毅用语音辨识的发展提醒我们：新技术刚出现时，表现常常不亮眼。早期深度学习用于语音辨识时，并没有立刻震撼所有人；早期端到端 ASR 的错误率也远高于当时成熟的 cascade 系统。但多年迭代后，Whisper 这样的端到端模型成了很多人理解 ASR 的默认入口。

这给语音语言模型一个重要判断框架：2025 年 cascade 可能仍然是短期最强方案，但这不说明端到端没有价值。研究者更应该问：哪条路线的信息上限更高？哪条路线未来可以自然利用语气、环境、打断、双向对话和时间流逝？

### 3. 前传：从语音检索到 segmental audio word2vec

在语音语言模型出现之前，李宏毅团队已经在做“把一段语音变成向量”的问题。最早动机来自 spoken content retrieval：如果要在大量录音里搜某个词，直接比较两段不同长度的声音很慢，DTW 这类算法难以实时扩展。若能把每一小段声音变成固定维度向量，就可以用向量检索加速。

Audio word2vec 的结构类似 autoencoder：一个 LSTM encoder 读入一段声音，压成一个向量；另一个 LSTM decoder 根据向量还原原声音。这个想法和 P9 的 tokenizer / detokenizer 很接近，只是当时还没有今天这套语音 token 术语。

难点是：声音不像文字有清晰空格和标点。词和词、音素和音素之间没有天然边界。Segmental audio word2vec 试图同时学习两件事：哪里该切段，以及每段该变成什么向量。训练目标一方面要重建原声音，另一方面要用尽量少的 segment 表示整段语音，避免模型偷懒把每个 frame 都切成一段。

### 4. Representation model：语音版 BERT 与 GPT

2018 年以后，NLP 有 BERT，语音领域也开始训练“语音版 BERT”。Mockingjay 把一段语音的连续 frame 遮住，让 Transformer 根据上下文还原被盖住的部分。语音和文字不同，如果只 mask 一个 frame，模型可能简单插值就能猜出来，所以必须连续 mask 多个 frame，甚至在频带方向 mask，才能迫使模型学到更有用的信息。

同一时期还有 APC，它像语音版 GPT，给前半段语音，预测未来 frame。它预测更远一些的 frame，因为相邻 frame 太容易被局部平滑猜出来。wav2vec 2.0 和 HuBERT 则把 speech representation model 推向更强的阶段。这些模型最初主要被看作下游任务的表示抽取器，而不是拿来直接生成语音的模型。

SUPERB benchmark 的意义在于，它证明这些自监督语音表示不只适合 ASR。不同层包含不同信息：后层更适合语音内容识别，前层往往保留说话人信息。用 weighted sum 把每一层表示按任务自动加权，可以让同一个 representation model 适配语音辨识、说话人验证、情绪等多种任务。

### 5. 初代语音语言模型：tokenizer、接龙、detokenizer

最早接近今天想象的语音语言模型之一是 Meta 的 GSLM。它的结构和 P9 讲的多媒体生成非常一致：先把声音变成 token，再训练自回归模型做 token 接龙，最后把 token 还原成声音。

语音 token 有两类常见来源。第一类从 speech representation model 改造而来：模型输出一串向量，对这些向量做 clustering 或加 VQ layer，把相似向量归到同一个编号。这类常被称为 semantic token。第二类来自 neural speech codec：tokenizer 和 detokenizer 一起训练，目标是把语音压缩成 token，再高质量还原语音。这类通常叫 acoustic token。

课程强调：如果有多组 token，问题就从“下一个 token 是什么”变成“不同 token 谁先生成、谁后生成、如何对齐”。AudioLM 等工作会同时使用 semantic token 和 acoustic token，生成策略本身就成为研究主题。

### 6. 现代路线：文字和语音 token 必须协同

初代语音语言模型只做 pretrain，本质上是在做语音接龙：给它前半句话，它把后半句话讲下去。课程里展示的早期结果语义不连贯，也没有自然的 in-context learning。这背后有数据和训练目标两重困难。

语音数据很贵。100 万小时语音听起来很多，换成文字 token 量却远小于大型文字模型的训练规模。更麻烦的是，语音 token 的频率通常接近 50 Hz，而文字 token 只有每秒三四个左右。语音序列长度至少是文字的十倍，Transformer attention 成本又随长度平方增长，训练成本可能是文字模型的百倍量级。

GPT-4o 和 Moshi 之后，大家越来越清楚一件事：强语音语言模型不能只靠语音 token。它需要借用文字模型已经学到的知识，同时训练时继续生成文字 token，避免 catastrophic forgetting。文字是语义的高效压缩，语音 token 则负责文字以外的信息，如说话方式、声音、情绪、停顿和语速。

$$
语音语言模型的一个核心训练目标：
 保留文字模型的知识 + 学会生成语音 token + 让文字 token 与语音 token 协同。
如果只教文字模型生成语音 token，又不持续练习文字 token，它可能忘掉文字阶段学到的能力。
$$

| 混合方式 | 做法 | 代价 |
| --- | --- | --- |
| 先文字后语音 | 先生成完整文字，再根据文字合成语音 token。 | 像 TTS，容易产生等待延迟，不适合极自然实时对话。 |
| 词级 interleaving | 生成一个文字 token，再生成对应语音 token。 | 需要较好的文字和语音对齐关系。 |
| chunk-wise interleaving | 固定比例交替生成文字 token 和语音 token。 | 不要求精确对齐，但模型要自己学会错位关系。 |
| 双 head 并行 | 每个时间点同时生成文字 token 和语音 token。 | 仍要处理两种序列长度不一致的问题。 |

### 7. TASTE、SFT/RLHF 与 STITCH

如果未来主流是文字和语音 token 混合生成，那么语音 token 的设计就不该再假设“自己包山包海”。既然文字 token 已经说明要讲什么，语音 token 可以更专注于文字表达不了的部分：怎么讲、多长、什么语气、什么发音风格。TASTE 的目标就是让语音 token 数量和文字 token 数量更接近，甚至一对一。

只做 pretrain 的语音模型只会接龙，不一定会对话。要让它像助手一样回应用户，需要 SFT：用一问一答的语音对话资料教模型“听到一句话以后应该回答”。RLHF 也可以迁移到语音场景，但不能只把输出转成文字来评分，因为人类偏好还会看语气、时机、身份和安全边界。

STITCH 的问题意识是：语音对话不能总是沉默等待思考。若模型每次合成一小段约两秒语音，而生成下一段只要 0.5 秒，中间就有播放 buffer。STITCH 把 reasoning token 插在两段语音 token 之间，让模型边播放上一段语音，边产生内部思考，再生成下一段语音。用户听起来没有额外停顿，模型却获得了 reasoning 的好处。

## box
### 你现在应该能做什么
你应该能解释为什么“短期可用”和“长期上限”会指向不同路线，也能说明语音语言模型要重新处理信息保留、token 设计、训练目标和实时互动。


---

# 四、来源脉络：原字幕里有哪些关键证据 {#source}
<!-- tag: SOURCE -->

本页依据 P10 原字幕手工修订。原讲座开头明确说明：这一讲从李宏毅和团队的第一人称视角回看语音语言模型的发展。因此，本页没有把它改写成百科式 survey，而是保留“研究者怎样从旧问题走到新问题”的脉络。

字幕中的关键线索有三类。第一类是定义线索：speech language model 的术语混乱、对话模式和 command mode 的差别、TTS 论文也可能自称 speech language model。第二类是路线线索：cascade 容易做但会丢失语音额外信息，端到端短期弱但上限高，agentic 解法以文字模型调度语音工具。第三类是研究线索：从 spoken content retrieval 到 audio word2vec，再到 representation model、SUPERB、GSLM、hybrid token、TASTE、SFT/RLHF 与 STITCH。

| 字幕线索 | 本页处理方式 | 学习时要抓住的判断 |
| --- | --- | --- |
| 课程不是完整综述 | 放入目标与来源说明，避免把页面写成论文大全。 | 学研究演进的判断框架，而不是背全领域列表。 |
| 术语混乱 | 集中到 terms 区，先分清对话模型、command mode、TTS。 | 读论文先看任务边界和输入输出。 |
| Cascade 与 end-to-end 对比 | 放入 skeleton、lecture、viz 三处，既讲直觉也给交互判断。 | 短期效果不等于长期上限。 |
| Audio word2vec 与 segmental 训练 | 在讲义和例子区重建伪代码，不直接堆字幕。 | 语音片段边界本身就是学习对象。 |
| Representation model 与 SUPERB | 强调不同层保存不同信息，以及多任务评估的意义。 | 语音表示不只服务 ASR，也服务说话人、情绪等任务。 |
| 现代语音模型 | 用 hybrid decoding、TASTE、RLHF、STITCH 串成最后一段。 | 强语音模型要同时解决知识、声音、对话和实时性。 |

## box warn
### 资料边界
字幕没有提供完整论文公式、实验表格和所有模型细节，所以本页只补足学习必需的概念解释，不把未在讲座主线中展开的论文细节强行扩写成结论。


---

# 五、最小例子：把抽象路线落到可判断对象 {#example}
<!-- tag: EXAMPLE -->

这一节的例子训练“看到系统就能拆结构”的能力。语音语言模型的发展史里最容易错的地方，是把所有语音系统都看成同一种东西。下面三个最小例子分别对应：怎么切语音片段、怎么区分生成策略、怎么利用播放 buffer 思考。

伪代码：segmental audio word2vec 的精神concept复制

```text
for frame in speech_frames:
 encoder.read(frame)
 if encoder.decides_boundary():
 vector = encoder.emit_segment_vector()
 decoder.reconstruct_segment(vector)
 encoder.reset_memory()

loss = reconstruction_error + penalty_for_too_many_segments
```

这段伪代码的重点是两个目标同时存在：重建误差要小，segment 数量也要少。如果只追求重建，模型最省事的做法是每个 frame 都切一段，等于没有学到真正可复用的声音单位。加入 segment 数量惩罚后，模型才会被迫寻找“常出现、好压缩、能还原”的片段。

判断示例：一个语音系统属于哪条路线reading复制

```text
if system.uses_asr and system.uses_text_llm and system.uses_tts:
 route = "cascade"
elif system.text_llm_calls_speech_tools:
 route = "agentic"
elif system.input == "speech" and system.output == "speech" and system.single_model:
 route = "end-to-end"
else:
 route = "read the paper more carefully"
```

这段判断代码刻意很朴素，因为读论文第一步就应该朴素。先不要被模型名或 demo 效果吸引，先确认它是不是偷偷经过 ASR 文本、是不是以文字模型为调度核心、是不是单一模型直接处理语音进出。只有先分清路线，后面的效果、延迟和信息保留才有可比性。

伪代码：语音 token 与 reasoning token 交错STITCH复制

```text
while answering:
 speech_chunk = generate_speech_tokens()
 play(detokenizer(speech_chunk))

 reasoning = generate_hidden_text_tokens(buffer_time)
 next_speech_chunk = generate_speech_tokens(conditioned_on=reasoning)
```

STITCH 的关键不在“多生成一些隐藏文字”这么简单，而在时机。语音播放有物理时间，模型可以利用上一段语音正在播放的空档生成 reasoning token。这样用户听到的是连贯语音，模型内部却获得了额外推理步骤。你现在应该能判断：一个语音助手是否实时，不只看回答质量，还要看它怎样切 chunk、怎样利用 buffer、怎样避免沉默。


---

# 六、可视化：路线、TASTE 与实时 buffer {#viz}
<!-- tag: VIZ -->

路线取舍小实验
选择目标，观察哪条路线更占优。

快速做 demo
理解语气情绪
低延迟实时互动

Cascade**高**
Agentic**中**
End-to-end**低**

快速做 demoASR + LLM + TTS 是最稳妥的工程起点，能快速得到可用系统。

TASTE 的目标是让语音 token 数量和文字 token 数量更接近，甚至一对一。它使用预训练 speech encoder，例如 Whisper encoder，从不同层抽出表示；再用文字 token 作为 query，对语音表示做 attention，得到和文字 token 对应的一组语音 token。detokenizer 同时吃文字 token 和语音 token，像增强版 TTS：文字决定内容，语音 token 决定说法。

语音输入

speech encoder
不同层表示

文字 query
attention 汇聚

语音 token


文字 token

detokenizer
内容 + 说法

还原语音

课程里的 token 交换实验展示了这些 token 可以控制长度：把慢速句子里某几个词对应的语音 token 换成快速句子的 token，detokenizer 还原时这些词会突然加速；反过来也会减速。也就是说，语音 token 确实学到“这个文字 token 应该怎么被念出来”。

观察：buffer 越多，可思考 token 越多

可用 buffer

**100**

推理帮助**较好**
延迟风险**中**
图示只是帮助理解。真实系统还取决于 GPU、chunk 长度、detokenizer 速度和是否用外部文字模型提供 reasoning。

课程最后还指出两个尚未解决的重要方向：第一是 full duplex，也就是自然对话中两个人可以同时发声、插话、被打断；第二是物理时间感，例如让模型真正理解“停十秒再说话”。这些语音语言模型走向真实交互必须面对的问题。


---

# 七、错误诊断：常见误区和修正清单 {#diagnosis}
<!-- tag: DIAGNOSIS -->
<!-- layout: grid2 -->

## box
### 四个第一问题
- 它是对话模型、command mode、TTS，还是语音任务模型？
- 它是 cascade、agentic，还是端到端？是否偷偷用 ASR/TTS？
- 它使用 semantic token、acoustic token、continuous token，还是文字和语音混合 token？
- 它是否能处理语气、说话人、情绪、插话、延迟和时间感？

## box redsoft
### 六个常见误区
- 把语音语言模型和 TTS 混为一谈。
- 以为 cascade 已经够强，所以端到端没有研究价值。
- 只看 ASR 后的文字内容，忽略语音里的额外信息。
- 以为 semantic token 真的只含语义。
- 只看回答质量，不看实时延迟和是否能边听边说。
- 以为 pretrain 语音接龙模型天然会对话和 in-context learning。

| 错误说法 | 错在哪里 | 怎么修正 |
| --- | --- | --- |
| “输入语音输出语音，所以一定是端到端。” | 它可能只是 ASR + LLM + TTS 串接。 | 追问中间是否出现文字转写、是否调用多个模块。 |
| “TTS 也用 speech token，所以就是对话式语音语言模型。” | TTS 不负责理解用户语音并生成回应。 | 区分输入文字念出来，和听语音后回答。 |
| “semantic token 只保存语义。” | 这个命名是历史约定，representation model 未必真的只抽语义。 | 看 token 来源和下游任务表现，不只看名称。 |
| “pretrain 后自然就会聊天。” | 预训练语音模型多半只会接龙。 | 需要 SFT 教对话格式，再用偏好训练改进合适性。 |
| “语音 RLHF 只要看 ASR 后文字答案。” | ASR 会丢掉语气、情绪、年龄、环境等偏好因素。 | 评价语音回应时同时看内容和 paralinguistic information。 |
| “实时语音只看模型生成速度。” | 真实体验还取决于 chunk、detokenizer、buffer、打断和双工能力。 | 同时检查延迟、可打断性、是否能边播放边思考。 |

你现在应该能做什么：拿到一个新的语音模型，不再被模型名牵着走，而是能从任务边界、系统路线、token 设计、文字信息利用、对齐训练、实时交互能力六个角度拆开判断。


---

# 课后题：把研究史变成判断力 {#practice}
<!-- tag: 22 QUESTIONS -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: A. 定义与路线 -->
<!-- quick-review: 快速复盘 对话式 SLM、command mode、TTS 经常被混叫；cascade 容易做，end-to-end 上限高。 -->

### ? 判断

**Q:** 1. 判断：所有输入语音、输出语音的系统都一定是端到端语音语言模型。

- [ ] 对
- [ ] 错


### ? 对照

**Q:** 2. 对话式语音语言模型和 TTS 的核心差别是什么？


### ? 解释

**Q:** 3. Command mode 为什么不能直接等同于对话式语音语言模型？


### ? 机制

**Q:** 4. Cascade 方案的三个组件是什么？它最大的信息损失在哪里？


### ? 判断

**Q:** 5. 为什么 2025 年 cascade 可能短期更强，但 end-to-end 仍值得研究？


<!-- end-practice-group -->

<!-- practice-group: B. 历史与前传 -->
<!-- quick-review: 快速复盘 audio word2vec 解决固定向量，segmental 解决边界，无监督 ASR 展示无标签学习潜力。 -->

### ? 研究判断

**Q:** 6. 早期端到端 ASR 表现不好，为什么仍然有研究价值？


### ? 解释

**Q:** 7. Audio word2vec 为什么能帮助语音检索？


### ? 诊断

**Q:** 8. Segmental audio word2vec 为什么要惩罚 segment 数量太多？


### ? 机制

**Q:** 9. 无监督 ASR 用 GAN 思路时，discriminator 分辨的是什么？


<!-- end-practice-group -->

<!-- practice-group: C. 表示模型与 token -->
<!-- quick-review: 快速复盘 表示模型先服务下游任务，后被改造成语音 token 来源。 -->

### ? 解释

**Q:** 10. Mockingjay 为什么不能只 mask 一个语音 frame？


### ? 解释

**Q:** 11. SUPERB 为什么证明 speech representation model 可能是通用模型？


### ? 对照

**Q:** 12. 为什么不同层适合不同语音任务？请用 ASR 和说话人辨识举例。


### ? 对照

**Q:** 13. Semantic token 和 acoustic token 分别从哪里来？各自偏向解决什么问题？


### ? 解释

**Q:** 14. 初代 GSLM 式模型为什么只会语音接龙，不等于会对话？


<!-- end-practice-group -->

<!-- practice-group: D. 现代语音语言模型 -->
<!-- quick-review: 快速复盘 文字 token 保留知识，语音 token 表达说法；TASTE 让两者更好对齐。 -->

### ? 成本分析

**Q:** 15. 为什么只用语音资料训练出 Llama 级能力极其困难？


### ? 机制

**Q:** 16. 训练语音语言模型时，为什么继续生成文字 token 可以缓解遗忘？


### ? 对照

**Q:** 17. 词级 interleaving 和 chunk-wise interleaving 各有什么代价？


### ? 解释

**Q:** 18. TASTE 的语音 token 为什么不需要完整保存文字内容？


### ? 应用

**Q:** 19. 什么是 paralinguistic information？请举两个模型应该利用它的例子。


<!-- end-practice-group -->

<!-- practice-group: E. 对齐与实时交互 -->
<!-- quick-review: 快速复盘 SFT 教会对话，RLHF 教会合适，STITCH 利用播放 buffer 做无额外延迟的思考。 -->

### ? 诊断

**Q:** 20. 语音 SFT 为什么需要对话资料？只用演讲资料会怎样？


### ? 机制

**Q:** 21. STITCH 如何让语音模型一边说话一边 reasoning？


### ? 费曼解释

**Q:** 22. 费曼题：用 8-10 句话讲清语音语言模型从前传到现代系统的主线。


<!-- end-practice-group -->


---

