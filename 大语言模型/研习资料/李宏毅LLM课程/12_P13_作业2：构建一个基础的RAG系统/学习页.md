---
course: "李宏毅 LLM 课程"
unit: "12"
title: "P13｜作业2：构建一个基础的 RAG 系统"
kicker: "LEE HUNG-YI LLM 2026"
kicker_en: "P13"
summary: "这份作业要求你实现一个 RAG 系统。给定《红楼梦》全文作为 knowledge base，再给一组问题，你的系统要先检索相关片段，再让语言模型根据片段生成答案，最后把 100 个回答提交到 Judge Boy 批改。"

side_note: "这节是 RAG 实作作业。读完后，你应该能看懂 Colab 里哪些位置在调检索、切块和 prompt，以及为什么分数会因为这些改动而变化。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../11_P12_BonusHW加分作业/学习页.html"
  - text: "下一节"
    href: "../13_P14_作业3：生成式人工智能/学习页.html"
---

# 一、概念骨架：整理书架、找书、请老师回答 {#skeleton}
<!-- tag: SKELETON -->

课程用图书馆比喻 RAG。第一步，你把地上一堆书整理上架，这对应 data preprocessing：把原始知识库切成 chunk。第二步，朋友问你问题，你请图书管理员去书架里找相关书，这对应 retrieval：根据 query 找最相关的 chunk。第三步，你把问题和找到的书交给老师，请老师整理出通顺答案，这对应 generation：让语言模型根据检索片段回答。

passages→
chunking→
knowledge base→
query→
retrieval→
relevant chunks→
prompt→
LLM answer

Data preprocessing
切 chunk 建知识库

Retrieval
找相关片段

Generation
根据片段回答


用户通常只看见 query 和 final output，中间组件都藏在系统内部。

Data preprocessing把 passages 切成可检索的 chunk。chunk 是比较相似度的最小单位，不是随手切碎文本。
Retrieval用 query 找 relevant chunks。这里的检索质量决定模型能不能看到答案证据。
Generation把 query 和相关片段写进 prompt，让指定语言模型根据证据生成 response。

你现在应该能做什么：看到任何 RAG 系统，都先拆成“知识库怎么切、怎么检索、怎么把检索结果塞给模型、怎么评估答案”四个问题。


---

# 二、关键术语：先把名字和边界对齐 {#terms}
<!-- tag: TERMS -->

RAG 的难点常常很多词同时出现：query、passage、chunk、retriever、response、baseline、public dataset、private dataset。先把它们放到同一张表里，后面读 Colab 和分数说明才不会混乱。

| 术语 | 直觉解释 | 准确用法与常见误解 |
| --- | --- | --- |
| RAG | 先找资料，再让模型根据资料回答。 | 完整说法是 retrieval augmented generation，中文常译为检索增强生成；它不是把整本书一次性塞进 prompt。 |
| knowledge base | 系统可查的资料库。 | 本次作业是《红楼梦》全文，也可以换成网页、内部文档或搜索结果。 |
| passage / document | 原始长文本或资料段落。 | 它通常太长，不能直接作为检索单位，需要切成 chunk。 |
| chunk | 检索时拿来比较相似度的最小文本块。 | 太大容易带噪声，太小容易切断上下文；chunk size 和 stride 就是在调这个边界。 |
| query | 用户要问的问题。 | 它是检索的起点，也是最终 prompt 的核心输入。 |
| retriever | 负责找相关 chunk 的工具。 | BM25、embedding model、hybrid retrieval 都属于检索方案。 |
| RRF | 把多个检索器的排名合并。 | 它看排名，不直接混合原始分数，因为不同检索器的分数尺度不一致。 |
| context window | 模型一次能读入的最大长度。 | 窗口大不等于越塞越好；输入变长后，模型可能看得到但抓不住重点。 |

### 为什么要 RAG

如果只问语言模型，它只能靠参数里记住的知识回答。它可能答对，也可能胡说，而且你很难知道答案从何而来。RAG 把相关片段放进 prompt，让答案有可追溯依据；如果答错，也能回头检查到底是检索错了、片段错了，还是生成模型没有读懂。

RAG 还适合 domain-specific 和 up-to-date knowledge。比如繁体中文、红楼梦细节、企业内部资料、刚更新的政策，这些都不一定在模型训练资料中充分出现。把资料整理成 knowledge base，需要时检索给模型。

减少幻觉答案被要求根据检索片段生成，错误来源更容易追查。
领域知识把模型不熟的资料放进知识库，不必重新训练整个模型。
私有资料资料只进入当前 prompt，不必写进模型参数。
节省上下文不把整本书塞给模型，只给最相关片段。

## box redsoft
### 常见误区
RAG 不等于“把全部知识库都贴进 prompt”。如果输入太长，模型虽然看得到，但未必看得懂；而且推理成本更高。检索的价值就是先把信息筛出来，让生成模型专注回答。

你现在应该能做什么：读到 query、chunk、retrieval、generation、response 时，能指出它们在流程中的位置，并能解释为什么 RAG 比直接问模型更容易排错。


---

# 三、课程讲义：红楼梦数据集与评分规则 {#lecture}
<!-- tag: LECTURE -->

作业使用《红楼梦》120 个章回、70 多万字作为 knowledge base。题目由模型根据章回生成，再由 TA 检查合理性。每个题组包含三个组件：query、golden passage index、answer。

Public dataset 是前 50 题，query、golden passage index、answer 都公开，像练习题。你可以遮住 answer，用自己的 RAG 系统生成 response，再和标准答案比对，做错误分析。Private dataset 是后 50 题，作业截止前只公开 query，不公开答案和出处，像正式考试。

| 字段 | 含义 | 用途 |
| --- | --- | --- |
| query | 要回答的问题。 | 系统输入，也是检索的起点。 |
| golden passage index | 出题时参考的章回或片段索引。 | public 题可用于分析检索是否找对出处。 |
| answer | 标准答案。 | public 题可用于调参，private 题截止后公布成绩。 |
| response | 你的 RAG 系统生成的答案。 | 提交 Judge Boy 批改。 |

Judge Boy 会用模型批改你的 response。为了控制成本，只取每个回答前 512 个字符评估。课程说明里强调，题目通常一两百字以内就能完整回答，所以写很长不一定有利，反而可能引入矛盾内容。

每题分数有四种：1 分表示完全正确或几乎完全正确；0.5 表示部分正确；0 分表示无法回答或没有有效信息；-1 表示答案明显错误或产生与标准答案矛盾的幻觉。因此，prompt 里可以鼓励模型在没有证据时说不知道，避免胡编导致倒扣。

| 单题分数 | 含义 | 调试启发 |
| --- | --- | --- |
| 1 | 答案正确，和标准答案一致或近似。 | 检索和生成都成功。 |
| 0.5 | 部分正确，有遗漏或小错误。 | 可能缺少关键 chunk，或模型没有整理完整。 |
| 0 | 无法回答或没有有效答案。 | 比胡编更安全，但要检查检索召回。 |
| -1 | 答案明显错误或矛盾。 | 通常是幻觉、错误检索或 prompt 约束不足。 |

$$
最终成绩 = 成功提交保底 2 分 + public baseline 最多 4 分 + private baseline 最多 4 分。
public 分数可即时查看；private 分数截止后公布。
$$

你现在应该能做什么：拿到一条错误 response 时，能判断它可能是检索没召回、生成没读懂，还是答案过长引入了矛盾。


---

# 四、资料与评分：baseline、Colab 和提交边界 {#source}
<!-- tag: SOURCE -->

课程把成绩拆成 simple、medium、strong、boss 四条 baseline。Simple baseline 直接让语言模型回答问题，不做检索；medium baseline 跑完整 RAG workflow，并用 BM25 做 retrieval；strong baseline 加入 embedding model，再把 BM25 和 embedding 的排名融合；boss baseline 继续调 data preprocessing 和输入长度。

Colab 中的关键位置有三类。第一类是 data preprocessing：改 chunk size 和 stride。第二类是 retrieval：载入 BM25、embedding model，并用 RRF 融合排名。第三类是 generation：把 query 和 relevant chunks 放入 prompt，让指定的 Llama 3 4B IT 模型生成 response。

1**选择运行环境**需要 GPU，因为作业会用到 embedding model 和 generation LLM；免费 Colab 通常选择 T4。
2**下载资料与套件**public/private test dataset 和《红楼梦》全文会被下载到运行环境，后续代码再载入问题与知识库。
3**只跑目标 baseline**四个 baseline 的生成格会产生同名 response。后跑低阶方案可能覆盖前面高阶方案的输出。
4**提交 prediction file**文件是 100 个字符串组成的 JSON list，大小不能超过 2MB；每天通常只有三次提交机会。

## box redsoft
### 公平性限制
最终生成答案的语言模型只能使用 sample code 指定模型。Data preprocessing 和 retrieval 可以使用开源模型改进，但不能调用闭源付费 API 来代替作业要求的系统。

你现在应该能做什么：打开作业 Colab 后，能分辨每个关键代码格属于建库、检索、融合、生成，还是文件输出。

## box
### 离线自学任务模板
1. 先选一道 public 题，把问题、正确答案、检索到的 top chunks 分开记录。
2. 逐层判断错误来源：证据没召回、证据召回但 prompt 没用上、还是输出格式不合要求。
3. 每次只改一个变量，例如只改 chunk size、只改 stride、只改 top-k 或只换检索器。
4. 提交前确认生成的是目标 baseline 的 prediction file，且没有被后续 cell 覆盖。
你现在应该能做什么：低分时先定位 RAG 的哪一层坏了，而不是同时乱改检索和生成。

## box
### worked example：一题排错
如果问题问“贾宝玉初见林黛玉时注意到什么”，top chunks 里完全没有初见段落，优先查 retrieval；如果 top chunks 已有初见段落但模型回答了别的情节，优先查 prompt 和 top-k 噪声；如果回答内容正确但 JSON 结构坏了，优先查提交格式。


---

# 五、最小例子：从 BM25 到 hybrid retrieval {#example}
<!-- tag: EXAMPLE -->

Medium baseline 要实现完整 RAG workflow，并使用 BM25 做 retrieval。BM25 属于 sparse retrieval，它会把 query 和 document/chunk 切成离散词项，然后根据关键字匹配和词频等公式计算相关性。

BM25 的优点是快、公式明确、不需要 GPU，尤其适合专有名词、罕见词、domain-specific 词汇。只要 query 和 chunk 字面上出现相同词，BM25 很容易抓到。但是它不懂同义词和语义改写。“地震”和“地牛翻身”语义相关，字面不同，BM25 可能认为不相关。

## box soft
### 适合 BM25 的问题
人名、地名、物件名、章回小说中的专有称呼，或者问题中直接出现答案附近的关键词。

## box warn
### BM25 的弱点
同义词、隐喻、跨语言、语义相近但字面不同的表达。它像查书索引，不像真正理解句意。

伪代码：BM25 检索retrieval复制

```text
chunks = chunk_documents(hongloumeng_text)
bm25 = BM25(tokenize(chunks))

query_tokens = tokenize(query)
scores = bm25.score(query_tokens)
top_chunks = take_top_k(chunks, scores, k=5)
```

Strong baseline 会加入 embedding model。Embedding model 把 query 和 chunk 转成连续向量，再用 cosine similarity 比较相似度。它属于 dense retrieval，因为每段文本被表示成稠密浮点向量，而不是离散关键词集合。

Embedding 的优势正好补 BM25 的弱点：字面不同但语义相近，也可能被放到向量空间中相近位置。甚至跨语言、跨模态模型也可以把不同语言或图像文字对齐到同一语义空间。缺点是需要模型推理，计算和储存都比 BM25 贵；如果资料领域超出 embedding model 训练范围，效果也会下降。

Hybrid retrieval 的直觉是：BM25 和 embedding 各有盲区，那就同时用。BM25 给一份 chunk 排名，embedding model 给另一份排名，再用 reciprocal rank fusion 合成综合排名。

RRF 不直接合并原始相似度分数，因为不同检索器的分数尺度不同。它只看排名：某个 chunk 在每个检索器中排得越前，综合分越高。常见公式是对每个检索器求 `1 / (k + rank)`，再把结果相加；课程里提到 `k` 常设为 60。

$$
RRF(c) = Σr ∈ retrievers 1 / (k + rankr(c))
c 是某个 chunk；r 是某个检索器；rank 越小，贡献越大。
$$

伪代码：融合 BM25 与 embedding 排名hybrid retrieval复制

```text
bm25_rank = rank_chunks_by_bm25(query, chunks)
embed_rank = rank_chunks_by_embedding(query, chunks)

rrf_scores = {}
for rank_list in [bm25_rank, embed_rank]:
 for rank, chunk_id in enumerate(rank_list, start=1):
 rrf_scores[chunk_id] += 1 / (60 + rank)

merged = sort_by_score(rrf_scores, descending=True)
```

你现在应该能做什么：看见 BM25、embedding、RRF 的代码时，能说出每一步读入什么、输出什么，以及为什么要用排名融合。


---

# 六、交互观察：检索器信号和输入长度 {#viz}
<!-- tag: VIZ -->

下面两个小观察用来建立调参直觉。第一个看 BM25、embedding、hybrid retrieval 的侧重点；第二个看 chunk size、stride、top-k 对上下文完整性、检索纯度和噪声风险的影响。它们不是分数预测器，只帮助你判断改参数时应该观察什么。

观察一：字面匹配 vs 语义匹配
切换检索器，观察它更偏向哪类信号。

BM25
Embedding
Hybrid

关键字命中**高**
语义理解**低**
计算成本**低**

BM25适合字面关键词明显的问题，速度快，但不擅长同义和语义改写。

Boss baseline 的重点从检索模型转到 data preprocessing 和输入长度。你要调三个参数：chunk size、stride，以及最终喂给 LLM 的 chunk 数量。它们共同决定检索颗粒度、重叠程度、输入长度和噪声量。

Chunk size 决定每个 chunk 包含多少字符或 token。大 chunk 能保留完整上下文，适合动机、因果、跨段落问题；小 chunk 更纯净，适合事实型问题。Stride 决定相邻 chunk 的起点间隔，stride 小意味着 overlap 大，更不容易把完整词或事实切断，但 chunk 数会变多，检索更慢。

Top-k 决定给 LLM 多少个相关 chunk。给太少可能漏掉答案；给太多会让 prompt 冗长，模型注意力被噪声稀释。即使模型 context window 很大，也不代表把它塞满就是好策略。课程建议从较小 top-k 开始，逐步增加，观察 Judge Boy public 分数何时开始下降。

观察二：chunk 与 top-k 的取舍
chunk size**512**
stride**512**
top-k**15**
上下文完整**中**
检索纯度**中**
噪声风险**中**
这是帮助理解的启发式，不是作业最佳参数。真实分数要用 public dataset 和 Judge Boy 反馈验证。

你现在应该能做什么：调参数前先预测它会让“召回、纯度、成本、噪声”哪一项变好或变坏，再用 public 分数验证。


---

# 七、错误诊断：分数掉了先查哪一层 {#diagnosis}
<!-- tag: DIAGNOSIS -->

RAG 的好处之一是错误可以分层排查。不要只盯着最终 response。先看检索有没有把包含答案的 chunk 召回，再看 prompt 有没有要求模型根据片段回答，最后才看生成模型是否忽略证据或编造内容。

| 现象 | 优先怀疑 | 检查方式 | 修法 |
| --- | --- | --- | --- |
| 答案完全跑题 | retrieval 没召回证据 | 打印 top chunks，看是否含有标准答案附近文本。 | 改检索器、tokenization、chunk size、stride 或 top-k。 |
| 片段里有答案，但 response 漏掉 | prompt 或 generation 没抓重点 | 查看 user prompt 中片段顺序、问题位置和回答约束。 | 让模型优先引用片段证据，答案不确定时说不知道。 |
| public 分数先升后降 | 输入变长后噪声压过证据 | 对比不同 top-k 的输出长度和错误类型。 | 回退到峰值附近，逐步微调而不是继续塞 chunk。 |
| 文件提交失败 | prediction file 格式错误 | 确认是 100 个字符串的 JSON list，大小小于 2MB。 | 重新执行输出格，别手动改文件结构。 |
| 高阶输出被低阶结果覆盖 | 重复运行了后面的生成格 | 查看 response 来源与运行顺序。 | 一次只执行目标 baseline 的生成格。 |

错误示例：只靠最终答案猜原因anti-pattern复制

```text
if score_is_low:
 change_prompt_randomly()
 increase_top_k_to_max()
 submit_again()
```

## box warn
### 为什么这段做法危险
它同时改 prompt 和 top-k，无法知道哪项造成变化；把 top-k 拉满还可能让噪声增加。更稳的做法是先打印 top chunks，确认答案证据有没有被召回，再一次只改一个变量。

你现在应该能做什么：遇到低分时，能按“检索证据、prompt 约束、生成输出、提交格式”的顺序排查，而不是盲目堆参数。


---

# 八、综合练习：把 RAG 调试能力练出来 {#practice}
<!-- tag: 22 QUESTIONS -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: A. RAG 基础 -->
<!-- quick-review: 作答参考 RAG = 先检索，再把相关资料交给模型生成。 -->

### ? 判断

**Q:** 1. 判断：RAG 的做法就是把整个知识库一次性塞进 prompt。

- [ ] 对
- [ ] 错


### ? 解释

**Q:** 2. 用图书馆比喻解释 RAG 的三段流程。


### ? 机制

**Q:** 3. RAG 为什么能减少 hallucination？


### ? 应用

**Q:** 4. 为什么 RAG 适合 up-to-date 或 domain-specific knowledge？


<!-- end-practice-group -->

<!-- practice-group: B. 数据集与评估 -->
<!-- quick-review: 作答参考 Public 用来调试，private 用来真实评估。 -->

### ? 解释

**Q:** 5. 本次作业的 knowledge base 是什么？为什么不能直接全塞给模型？


### ? 定义

**Q:** 6. Query、golden passage index、answer 分别是什么？


### ? 对照

**Q:** 7. Public dataset 和 private dataset 在调试中的角色有什么不同？


### ? 评分

**Q:** 8. 为什么错误答案会被扣 -1，而不知道通常是 0？


<!-- end-practice-group -->

<!-- practice-group: C. 检索模型 -->
<!-- quick-review: 作答参考 BM25 看字面，embedding 看语义，hybrid 合并两者。 -->

### ? 解释

**Q:** 9. BM25 为什么属于 sparse retrieval？


### ? 清单

**Q:** 10. BM25 的两个优点和两个缺点是什么？


### ? 解释

**Q:** 11. Embedding model 为什么属于 dense retrieval？


### ? 例子

**Q:** 12. “地震”和“地牛翻身”这个例子说明了什么？


### ? 解释

**Q:** 13. Hybrid retrieval 为什么通常比单一检索器稳？


### ? 机制

**Q:** 14. RRF 为什么用排名而不是直接合并原始相似度分数？


<!-- end-practice-group -->

<!-- practice-group: D. 调参与提交 -->
<!-- quick-review: 作答参考 Chunk size 控制片段长度，stride 控制重叠，top-k 控制喂给模型的片段数。 -->

### ? 对照

**Q:** 15. Chunk size 调大和调小分别适合什么问题？


### ? 解释

**Q:** 16. Stride 小为什么能减少“词被切断”的问题？代价是什么？


### ? 诊断

**Q:** 17. 为什么 top-k 不是越大越好？


### ? 调试

**Q:** 18. 如果 public 分数上升后又下降，你会怎样解释？


### ? 操作

**Q:** 19. 为什么只执行一个 baseline 生成 cell，避免后面 cell 覆盖输出？


### ? 公平性

**Q:** 20. 作业为什么限制 generation LLM 只能用 sample code 指定模型？


### ? 排错

**Q:** 21. 如果模型答错很多，你会优先检查 retrieval、prompt 还是 generation？请给出顺序。


### ? 费曼解释

**Q:** 22. 费曼题：用 8-10 句话讲清楚本次 RAG 作业的系统、评估和调参主线。


<!-- end-practice-group -->


---

