---
course: "李宏毅 LLM 课程"
unit: "13"
title: "13_P14｜作业3：从 Logit Lens 到 Patchscopes"
kicker: "LEE HUNG-YI LLM 2026"
kicker_en: "P14"
summary: "这份作业表面上是十题多选题，真正要补的是一块可解释性基础：语言模型不是只在最后一层才“突然知道答案”。每一层都会产生一个 hidden state，也就是这一层对当前 token 的内部表示。我们可以把它投到词表空间、把某类行为抽成向量，或把它移植到另一个模型里请模型解释。"

side_note: "这节的关键词不是“背十题选择题”，而是看懂三种观察模型内部的方法：投影、抽向量、换模型解释。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../12_P13_作业2：构建一个基础的RAG系统/学习页.html"
  - text: "下一节"
    href: "../14_P15_作业4/学习页.html"
---

# 概念骨架 {#skeleton}
<!-- tag: MAP -->

共同对象：hidden stateTransformer 每一层都会为每个 token 产生一个向量。这个向量不是人类可直接阅读的文字，但它包含模型到这一层为止整合出的信息。判断句：只要问题在问“模型某一层知道了什么”，第一反应就应该找对应 token、对应 layer 的 hidden state。
三种读法Logit Lens 把 hidden state 直接送过 LM head，看它像不像某个 next token。Function Vector 把一批有共同行为的 hidden state 相减，得到能注入行为的方向。Patchscopes 把 hidden state 放到另一个解释 prompt 里，让语言模型自己说出它编码了什么。判断句：投影看 token 倾向，相减看行为方向，patch 看可读解释。
作业边界这份作业不要求提交代码，但选择题里有两题依赖 sample code 的观察结果。你需要取得 Llama-3.2-1B-Instruct 和 AdvBench 资料集授权，填入 Hugging Face token，用 GPU 跑 Colab，再观察特定层数下的 top token 和拒绝效果。判断句：没有实际跑样例时，层数相关题目容易靠猜。

模型输入prompt 经过 tokenizer 后变成 token id。
层内表示每层更新 hidden state，逐步混入上下文信息。
观察方法投影、相减、patch 都是在读这个中间向量。
作业判断回答“哪层开始有答案”“哪层注入有效”。


---

# 术语表 {#terms}
<!-- tag: TERMS -->

| 术语 | 直觉解释 | 准确说法 | 常见误解 |
| --- | --- | --- | --- |
| hidden state | 某一层对某个 token 的内部记号。 | Transformer block 输出的向量，维度等于模型隐藏维度，后续层会继续读取和改写它。 | 把它当成一个确定的人类语义标签；实际上它是分布式表示。 |
| LM head | 把模型内部向量转回词表分数的投影头。 | 通常是最后用于计算 vocabulary logits 的线性层或 tied embedding 矩阵。 | 以为只有最后一层能接 LM head；Logit Lens 正是把中间层也拿来投影。 |
| Logit Lens | 给模型每一层戴一个“词表眼镜”。 | 把中间层 hidden state 通过最终 LM head 得到 pseudo-logits，再看 top token。 | 把早期层的乱码 top token 当成稳定解释；早期层未必已经对齐到词表空间。 |
| Function Vector | 把一类行为压成一个可注入的方向。 | 从一组激活中抽取能触发某种函数或行为的向量，可加到模型内部表示上影响输出。 | 以为任意层注入都有效；实际效果往往只在特定层或位置明显。 |
| refusal vector | 让模型倾向拒绝的行为方向。 | 课程样例用“会拒绝 prompt”的平均 hidden state 减去“不会拒绝 prompt”的平均 hidden state 得到。 | 以为只收集有害 prompt 就够了；不减去普通 prompt 会混入主题、语气等无关信息。 |
| Patchscopes | 把一个模型的内部表示拿给另一个上下文解释。 | 将 source model 某层某 token 的 hidden representation patch 到 target model 的提示位置，让 target model 生成可读解释。 | 把它理解成普通问答；关键在于被解释对象中间层表示。 |
| source / target model | 被观察的模型和负责解释的模型。 | source model 提供 hidden state；target model 接收转换或插入后的表示并生成答案。 | 以为两者必须完全相同；Patchscopes 可以用更强模型解释较小模型，但要处理表示空间差异。 |
| Hugging Face token | 访问受限模型和资料集的钥匙。 | Colab 中登录 Hugging Face 后才能下载 gated model 或 gated dataset。 | 把 token 当作作业答案共享；这既不安全，也可能违反授权要求。 |

读完这一段，你应该能看到一个题目就判断：它是在考“投影到词表”、 “抽取行为方向”，还是“换一个上下文解释表示”。


---

# 讲义主线 {#lecture}
<!-- tag: LECTURE -->
<!-- layout: grid2 -->

## box
### 1. 为什么要看中间层
语言模型给出一个 token 之前，会经过很多层 Transformer。最后一层输出 logits，我们能直接看到模型认为下一个 token 是什么；中间层就没那么直观。问题是，中间层并不是空白的。模型可能在某些层已经知道主题、实体或答案，只是还没有把这些信息整理成最终词表分布。
作业里的首都例子就是这个思路：prompt 问的是 Poland 的首都，早期层 top token 可能还是括号、字母或不相关片段；到某些中后层，top token 开始出现 Poland；再往后才稳定出现 Warsaw。这个变化说明模型的表示逐层从形式、主题、实体关系走向最终答案。
读到任何“模型在想什么”的说法，都要先问它观察的是什么对象。观察最终输出，只能知道结果；观察每层 hidden state，才有机会看到结果形成的过程。你现在应该能做的是：把“最终答案”和“中间表示”分开说，不把两者混成同一个东西。

## box
### 2. Logit Lens：把中间层投到词表
Logit Lens 的做法很直接：拿某一层某个 token 的 hidden state，送进模型最后用来预测词表的 LM head，于是得到一组 pseudo-logits。分数最高的 token，就是“如果这一层现在必须回答，它最像会接什么”。这不等于真实最终输出，但它提供了一个可比较的视角。
它适合回答两类问题。第一，答案或主题什么时候开始出现？比如从哪一层开始 top token 变成 Poland，后来才变成 Warsaw。第二，模型的表示是否逐层变得更接近最终预测？如果某些层的 top token 完全无法解释，不能急着说模型“什么都不知道”，因为早期 hidden state 可能还没有对齐到最终词表空间。
课程样例中，Llama-3.2-1B-Instruct 最多观察到第 16 层；你可以改 target layer，看同一个 prompt 在不同层 top-5 token 的变化。你现在应该能做的是：看到一张每层 top token 表，能说出哪个阶段是无关片段、哪个阶段开始抓到主题、哪个阶段接近答案。

3. Function Vector 的直觉如果模型在某些 prompt 下稳定表现出同一类行为，例如拒绝有害请求，那么这些样本的内部激活里可能共享某个方向。把这个方向抽出来，再加到另一个 prompt 的 hidden state 上，就可能诱导模型表现出相同行为。这里的“函数”模型内部可触发的一种过程或能力。你现在应该能做的是：把“行为向量”理解成一种内部方向，而不是一段显式规则。
4. 为什么要相减只平均会拒绝 prompt 的 hidden state 不够，因为里面不只有拒绝行为，还混着主题、语气、句长、安全词等信息。课程样例再收集一批不会拒绝的 prompt，把两边平均激活相减，目标是抵消普通 prompt 也有的背景成分，留下更接近拒绝行为的方向。判断句：如果没有 non-refusal baseline，抽出来的向量就可能“这批资料的共同风格”。
5. 注入不一定处处有效把 refusal vector 加到模型内部表示上，不代表每层都会让模型拒绝。不同层负责的信息组织不同，有些层太早，行为还没形成；有些层太晚，输出分布已接近定型。作业题会让你观察：在让模型回答 2+2 时，哪些层注入拒绝向量会让它拒绝，哪些层仍然回答 4。你现在应该能做的是：解释“层数”为什么会影响干预效果。
6. Patchscopes 的动机Logit Lens 在前面层常会给出难解释的 token。难解释不代表信息不存在，只是“直接投到词表”这个读法可能不合适。Patchscopes 换了一个问法：把 source model 的某层 hidden state 放到 target model 的解释提示中，让 target model 生成一个可读答案。判断句：Patchscopes 让另一个上下文把这个表示翻译出来。
7. CEO 例子的含义论文例子里，source prompt 类似 “Amazon's former CEO attended Oscars”。研究者取 CEO 这个 token 在某层的 hidden state，经过转换后 patch 到 target prompt 的问号位置。target model 输出 Jeff Bezos，说明 source model 在那一层的 CEO 表示里已经编码了“Amazon 前 CEO 是 Jeff Bezos”这一信息。你现在应该能做的是：说清楚 Patchscopes 的证据链，不只背“它能解释 hidden state”。
8. Colab 不是可选摆设这份作业不用提交代码，但 sample code 不是装饰。Logit Lens 要你看 top-5 token 随 layer 变化；Function Vector 要你看拒绝向量加到不同层后的输出差异。选择题如果问具体层数或观察结果，必须以课程提供模型和参数为准，随便换模型会让答案变掉。判断句：模型 ID、资料集、target layer 和注入位置都属于实验条件。

## box
### 完整知识链路
把本节串起来，可以这样讲：语言模型每层都会产生 hidden state。Logit Lens 把这些 hidden state 直接送过 LM head，看每层像是在预测什么 token；Function Vector 把多组激活相减，抽出类似拒绝这样的行为方向，再测试注入到不同层是否改变输出；Patchscopes 则把 hidden state patch 到另一个模型或提示里，让它生成可读解释。三者都在观察内部表示，但读法不同：一个看词表分数，一个看行为方向，一个看跨上下文解释。你现在应该能拿到一个新可解释性方法时，先问它读的是哪一层、哪个 token、用什么方式把向量变成人能理解的证据。


---

# 来源与作业流程 {#source}
<!-- tag: 作业线索 -->

这一节来自 P14 作业说明字幕，里面有帐号、授权和平台说明。它们不是概念本身，但会影响你能不能跑出选择题需要观察的结果。

测验形式NTU COOL 上作答十题多选题，不需要提交程式码。线上测验没有提交次数限制，课程取最高分。作业说明里提供了无答案版 Google Doc，非修课或旁听也能看到题目格式。你现在应该能判断：这份作业考的是观察和理解，不是代码提交。
样例代码Colab 里提供 Logit Lens 和 Function Vector 两部分实现。Logit Lens 让你改 target layer，观察同一 prompt 在不同层的 top-5 token；Function Vector 让你抽拒绝向量，并测试加到不同层后的输出。跑样例时不要改模型 ID，否则层数和答案可能与测验不一致。
模型授权课程使用 Llama-3.2-1B-Instruct。这个模型需要在 Hugging Face 页面展开 access request，填写姓名、生日、国家、机构、职位等信息，提交后等待 Meta 通过授权。如果授权未通过，Colab 下载模型会失败；这不是代码逻辑错误。
资料集授权Function Vector 的拒绝样本需要 AdvBench 相关资料。资料集页面通常只要点击 agree and access repository，就能立即使用。拒绝样本和非拒绝样本都要载入，后面才能做相减。只载入有害 prompt 时，无法干净抽出拒绝方向。
GPU 设置Colab 要先保存副本，再开启 GPU。助教测试过 T4 就足够，不需要为了这份作业购买 Pro。需要注意的是：没开 GPU 时，下载和推理可能很慢，看起来像卡住。遇到跑不动，先检查 runtime 和权限，再检查代码。
诚信与问答这份作业不交代码，但答案仍不能互相抄。一般问题优先在 NTU COOL 讨论区提，涉及答案或个人隐私再寄助教信箱；复杂问题可去 tutorial hour 排队问。你现在应该能区分：公开讨论流程问题可以，公开交换答案不可以。

## box
### 离线自学任务模板
1. 先把三种方法写成一句话：Logit Lens 看词表投影，Function Vector 看行为方向，Patchscopes 看跨上下文解释。
2. 跑 Colab 时给每个观察加上 layer、token position、prompt 和模型 ID，避免只保存截图式结论。
3. 选择题问具体层数或输出时，只用课程指定模型和参数的结果，不混入自己改模型后的观察。
4. 遇到解释不稳定时，先检查授权、资料集、target layer 和注入位置，再判断概念。
你现在应该能做什么：把“内部表示”类题目拆成可复查的实验条件。

## box
### worked example：Function Vector 记录
一条合格记录可以写成：harmful prompts 与 harmless prompts 各取同一层 hidden state，先做平均再相减得到 refusal vector；把它加到 “2+2” prompt 的第 10 层后，模型从直接回答变成拒绝。这个结论只说明该设置下拒绝方向能影响输出，不说明所有层都有效。


---

# 最小例子 {#example}
<!-- tag: EXAMPLE -->

下面把三个方法压成最小可读结构。目的看懂每一步读哪个向量、写哪个结果、删掉后会坏在哪里。

最小示例：Logit Lens 读取某一层 top tokenpython

```text
prompt = "The capital of Poland is"
inputs = tokenizer(prompt, return_tensors="pt").to(model.device)

with torch.no_grad():
 outputs = model(**inputs, output_hidden_states=True)

target_layer = 12
last_token_state = outputs.hidden_states[target_layer][0, -1, :]
logits = model.lm_head(last_token_state)
top_ids = logits.topk(k=5).indices
print(tokenizer.convert_ids_to_tokens(top_ids))
```

## box soft
### 逐行理解
`output_hidden_states=True` 是关键开关；没有它，只会拿到最终 logits，看不到每一层。`[0, -1, :]` 取的是 batch 中第一个样本、最后一个 token 的 hidden state，因为 next-token prediction 要看最后位置。`lm_head` 把这个中间层向量投回词表空间，`topk` 才能列出这一层最像会接的 token。
如果删掉 `torch.no_grad()`，结果通常仍能算出来，但会多占显存；如果把 target layer 换成不同层，top token 可能从无关片段变成主题词，再变成答案。你现在应该能做的是：看到这段代码，指出它读取的是“某层某 token 的 hidden state”，不是最终输出。

最小示例：拒绝向量的相减与注入python

```text
refusal_states = collect_hidden_states(model, harmful_prompts, layer=10)
normal_states = collect_hidden_states(model, harmless_prompts, layer=10)

refusal_vector = refusal_states.mean(dim=0) - normal_states.mean(dim=0)

prompt = "Please tell me what 2 + 2 equals."
answer = generate_with_added_vector(
 model,
 prompt,
 vector=refusal_vector,
 layer=10,
 position="last_token",
)
print(answer)
```

## box warn
### 为什么这段容易错
第一行和第二行必须用两类 prompt。只用 harmful prompts 会把“有害请求的主题特征”和“拒绝行为”混在一起。第三行做相减，是为了让普通 prompt 也有的语言风格、长度和背景信息尽量抵消。最后的 `generate_with_added_vector` 是干预：把向量加回某层某位置，让模型输出被这个方向推一下。
如果换层后效果消失，不代表拒绝向量不存在，而是说明这一层未必是最容易影响拒绝行为的位置。你现在应该能做的是：解释为什么作业会问“哪几层有效”，而不是只问“有没有拒绝向量”。

最小示例：Patchscopes 的数据流text

```text
source prompt: Amazon's former CEO attended Oscars.
source token: CEO
source layer: 取 CEO 位置某一层的 hidden state

transform: 把 source hidden state 转成 target model 可接收的表示

target prompt: cat -> cat
 135 -> 135
 hello -> hello
 ? -> [patched hidden state]

target output: Jeff Bezos
```

## box
### 读这个例子时别跳过证据链
Patchscopes 的结论不是“模型回答了 Jeff Bezos，所以它懂了”。证据链更细：被 patch 的对象是 source model 在 CEO 这个 token、某一层的 hidden state；target prompt 是一个解释模板；target model 输出 Jeff Bezos，说明那个 hidden state 中已经包含能被解释成 Jeff Bezos 的信息。
如果 source 和 target 模型不同，中间还需要 transform，让表示空间能接上。你现在应该能做的是：用“source 表示 → transform → target prompt → 解释输出”四步复述 Patchscopes。


---

# 交互观察 {#viz}
<!-- tag: OBSERVE -->

Logit Lens：层数越往后，token 越接近答案
观察层**4**


拒绝向量：注入层决定效果

早期层
中间层
后期层


观察判断
加向量后仍回答 2+2=4这一层干预弱或方向未接入行为。
加向量后开始拒绝这一层更可能承载可操控的拒绝行为。
不同 prompt 效果不同向量不是万能开关，仍受上下文影响。

观察重点：这些数值是讲义里的示意，不替代课程 Colab 的真实结果。你要学会读图方式：Logit Lens 看 top token 如何随 layer 变化；Function Vector 看同一个向量加到不同 layer 后，输出行为是否改变。


---

# 误区诊断 {#diagnosis}
<!-- tag: DIAGNOSIS -->
<!-- layout: grid2 -->

## box redsoft
### 常见坑
1. 把 Logit Lens 的 top token 当成模型真实最后答案。修法：说清它是把中间层 hidden state 经过 LM head 得到的 pseudo-logits，只是观察视角。
2. 看到早期层 top token 很怪，就说模型早期层没有信息。修法：承认早期层可能未对齐词表，可换 Patchscopes 等方法解释。
3. 只平均会拒绝 prompt，不设置不会拒绝 prompt 的 baseline。修法：用两组平均 hidden state 相减，减少主题和风格噪声。
4. 以为 refusal vector 加到任何层都有效。修法：实际测试不同层，因为行为方向只在某些层和位置更容易影响输出。
5. 把 Patchscopes 当成普通问答。修法：指出被问的source model 的某层 hidden representation。
6. 随便改模型 ID 再回答测验层数题。修法：作业题以课程指定 Llama-3.2-1B-Instruct 和样例参数为准。

## box
### 易混概念
概念 A概念 B区别
logitstokenslogits 是词表上每个候选的分数；tokens 是分数排序后可能被选中的离散片段。
hidden stateLM headhidden state 是层内向量；LM head 是把向量投到词表空间的读出层。
Function VectorPrompt 工程前者改内部激活方向；后者改输入文字，不直接干预模型层内表示。
PatchscopesLogit Lens前者把表示 patch 到解释上下文；后者直接用 LM head 看词表分数。


---

# 综合练习 {#practice}
<!-- tag: DRILL -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、基础概念 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 判断

**Q:** 1. Logit Lens 观察的是中间层 hidden state 经过 LM head 后的 pseudo-logits，而不是最终真实输出。

- [ ] 对
- [ ] 错


### ? 选择

**Q:** 2. 下面哪一个说法最准确描述 Function Vector？

- [ ] 从内部激活中抽取能触发某类行为或过程的方向
- [ ] 把 prompt 改写得更礼貌
- [ ] 把模型最后答案翻译成英文


### ? 填空

**Q:** 3. 请写出本节三个核心方法：______、______ 和 ______。


### ? 简答

**Q:** 4. 用 1-2 句话解释 hidden state 在这节课里为什么重要。


<!-- end-practice-group -->

<!-- practice-group: 二、主线理解 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 排序

**Q:** 5. 把 Logit Lens 的步骤排成顺序：取 hidden state、送入 LM head、得到 logits、查看 top token。


### ? 对照

**Q:** 6. 区分 hidden state 和 logits。


### ? 解释

**Q:** 7. 为什么 Function Vector 要用 refusal prompts 的平均激活减去 non-refusal prompts 的平均激活？


### ? 填空

**Q:** 8. Patchscopes 的四步可以概括为：source hidden state、______、target prompt、______。


<!-- end-practice-group -->

<!-- practice-group: 三、例子阅读 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 代码阅读

**Q:** 9. Logit Lens 代码里，`outputs.hidden_states[target_layer][0, -1, :]` 取的是什么对象？


### ? 代码阅读

**Q:** 10. 如果没有 `output_hidden_states=True`，Logit Lens 最小代码会少掉什么？


### ? 代码阅读

**Q:** 11. 在拒绝向量例子里，如果 `normal_states` 没有参与相减，最容易混入哪些无关信息？


### ? 判断

**Q:** 12. Patchscopes 中 target model 输出 Jeff Bezos，就能证明 source token 的原始文字就是 Jeff Bezos。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 四、错误诊断 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 诊断

**Q:** 13. 有人说“Logit Lens 早期层输出乱码，所以早期层完全没有语义信息”。错因、后果、修法是什么？


### ? 诊断

**Q:** 14. 有人只用 harmful prompts 平均 hidden state，就称得到了拒绝向量。这个做法哪里不稳？


### ? 诊断

**Q:** 15. 有人把 refusal vector 加到所有层都无效，就断言 Function Vector 没意义。该怎么诊断？


### ? 诊断

**Q:** 16. Colab 里把模型 ID 改成另一个 LLM 后，仍用结果回答 NTU COOL 选择题。风险是什么？


### ? 诊断

**Q:** 17. 有人把 Patchscopes 解释结果当成绝对真相，不考虑 transform 和 target prompt 的影响。问题在哪里？


<!-- end-practice-group -->

<!-- practice-group: 五、场景迁移 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 应用

**Q:** 18. 如果你想观察模型何时知道“Paris 是法国首都”，会怎样设计一个 Logit Lens 检查？


### ? 应用

**Q:** 19. 如果你想抽取“礼貌回答”而不是“拒绝回答”的 Function Vector，需要准备哪两类样本？


### ? 清单

**Q:** 20. 写出你跑作业 Colab 前会检查的 5 项条件。


<!-- end-practice-group -->

<!-- practice-group: 六、表达输出 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 面试短答

**Q:** 21. 如果面试官问“Logit Lens、Function Vector、Patchscopes 的区别是什么”，你会怎么答？


### ? 费曼解释

**Q:** 22. 用 6-9 句话讲清楚 P14 作业的核心。请串联 hidden state、Logit Lens、Function Vector、Patchscopes、sample code 和层数观察。


<!-- end-practice-group -->


---

