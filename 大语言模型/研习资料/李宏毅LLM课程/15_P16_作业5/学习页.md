---
course: "李宏毅 LLM 课程"
unit: "15"
title: "15_P16｜作业5：Steam 游戏评分回归"
kicker: "LEE HUNG-YI LLM 2026"
kicker_en: "P16"
summary: "这次作业让你用 PyTorch 跑一遍机器学习训练流程。输入是 Steam games dataset 里的游戏特征，输出是一个 0 到 100 的数值：Metacritic score。因为目标是连续数值，所以这是 regression task，不是分类。"

side_note: "这节要跑完整训练流程：从 Steam 游戏特征出发，预测 Metacritic 分数，并提交 submission.csv。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../14_P15_作业4/学习页.html"
  - text: "下一节"
    href: "../16_P17_作业6/学习页.html"
---

# 概念骨架 {#skeleton}
<!-- tag: MAP -->

输入与标签训练资料有 3200 个 labeled samples，测试资料有 800 个 unlabeled samples。每个游戏有 45 个 feature 和一个 label，label 是 Metacritic score。数值 feature 可以直接标准化后喂模型；文字 feature 要先通过 sentence embedding 变成向量。判断句：模型训练时看得到 train label，生成提交时 test.csv 没有 label。
训练循环神经网络可以看成带未知参数的函数。训练时把 feature 喂进 model 得到预测值，用 MSE 衡量预测值和真实分数的差距，再用 backpropagation 计算梯度，由 optimizer 更新 weight 和 bias。判断句：模型参数由训练自动更新；epoch、learning rate、batch size、optimizer、weight decay 是你手动设定的 hyperparameters。
泛化与提交训练 loss 下降不代表模型一定好。validation loss 用来估计没见过的数据表现；如果 training loss 继续降而 validation loss 上升，就可能 overfitting。最后模型对 test.csv 预测 Metacritic score，写入 submission.csv 上传 Judge Boy。判断句：选 final submission 时不要只看 public score，也要参考 validation score。

train.csv3200 笔，含 feature 与 Metacritic score。
validation split从 labeled data 切出，不参与参数更新。
test.csv800 笔，无 label，只用于预测。
submission.csv保存预测分数，上传 Judge Boy。


---

# 术语表 {#terms}
<!-- tag: TERMS -->

| 术语 | 直觉解释 | 准确说法 | 常见误解 |
| --- | --- | --- | --- |
| regression | 预测一个连续数值。 | 本作业根据游戏 feature 预测 0-100 的 Metacritic score。 | 把它当成分类问题，只关心游戏好/不好。 |
| feature / label | 输入线索和正确答案。 | feature 是游戏 ID、发行日期、游玩时间、文字描述等输入；label 是训练时可见的 Metacritic score。 | 在 test.csv 上寻找 label；测试资料本来就没有答案。 |
| sentence embedding | 把文字特征压成向量。 | 文字 feature 不能直接喂给普通数值模型，需要已训练好的句向量模型转成 fixed-length vector。 | 以为文字列会自动被 linear model 理解。 |
| MSE loss | 预测错得越远，惩罚越大。 | Mean Squared Error 是误差平方的平均值，训练时用于优化模型参数。 | 只看误差绝对值，忽略平方会放大大错误。 |
| RMSE | 把 MSE 开根号后的评估指标。 | Root Mean Squared Error 与原目标同量纲，越低代表预测越接近答案。 | 以为 RMSE 越高越好；本作业越低越好。 |
| optimizer | 帮模型更新参数的算法。 | SGD、Adam 都是梯度下降的变形，读取梯度后更新 weight 和 bias。 | 把 optimizer 当成要预测的模型本身。 |
| validation set | 训练中保留的小测试集。 | 从 labeled data 切出来，不参与训练，只用来监控没见过资料上的表现。 | 拿 validation set 继续训练，导致评估失真。 |
| overfitting | 背训练资料，遇到新题变差。 | training loss 下降但 validation loss 上升，是常见警讯。 | 只要训练 loss 很低就认为模型最好。 |
| hyperparameter | 训练前由人设定的控制钮。 | epoch、learning rate、batch size、optimizer、weight decay、hidden dimension 都属于超参数。 | 和模型自动学到的 weight/bias 混在一起。 |
| feature selection | 挑真正有用的输入特征。 | 可用 feature 与 label 的相关系数作参考，去掉无关或噪声特征。 | 以为 feature 越多一定越好。 |

读完这一段，你应该能把本节术语放进训练流程里：数据怎么进来、模型怎么学、指标怎么判、提交文件怎么生成。


---

# 讲义主线 {#lecture}
<!-- tag: LECTURE -->
<!-- layout: grid2 -->

## box
### 1. 回归任务：输入游戏特征，输出一个分数
本作业的目标是预测 Steam 游戏的 Metacritic score。这个分数是 0 到 100 的整数，反映游戏评价高低。模型输入一组 feature：发行日期、价格、正负评价数、游玩时间、游戏名称、描述等。输出也不是类别，而是一个数值，所以它是 regression。
如果是分类，你会问“这个游戏属于哪一类”；如果是回归，你会问“这个游戏大概几分”。训练模型时，train.csv 提供 feature 和 label；test.csv 只有 feature，没有 label。最后你要把模型对 test.csv 的预测写成 submission.csv，上传到 Judge Boy。
你现在应该能做的是：看到一个机器学习作业，先说清楚输入是什么、目标值是什么、训练资料和测试资料有什么差别。

## box
### 2. 数据预处理：数值可以标准化，文字要转向量
数据集共有 46 个 column，其中 45 个是 feature，1 个是 label。大部分 feature 是数值，例如 Steam ID、发行日期、平均游玩时间、价格、正负评价数。数值 feature 通常要做标准化，让不同量级的特征不会让训练变得不稳定。
另外有 4 个 text feature，例如游戏名称和描述。普通 linear model 或 MLP 不能直接吃一段文字；它们要吃 vector。课程提供的 Colab 可以用 sentence embedding model，把一句话压成一个向量，再和数值 feature 拼起来。这里的 embedding 把文字特征变成回归模型能计算的输入。
你现在应该能做的是：解释为什么 train.csv 里的文字列不能直接喂给数值模型，以及为什么 test.csv 如果使用文字 feature，也必须做同样的 embedding 处理。

3. 带未知参数的函数线性模型可以写成一组 weight 和 bias，对输入 feature 做加权组合。神经网络也是函数，只是参数更多、结构更复杂。训练让算法调整这些未知参数，让预测分数靠近真实分数。判断句：weight/bias 是模型学出来的参数，不是你手动调的超参数。
4. MSE loss 与 RMSEMSE 是预测值和真实值误差平方的平均。平方会放大大误差，所以预测离答案越远，loss 增长越明显。RMSE 是 MSE 开根号，单位回到原来的分数量纲，Judge Boy 用类似 RMSE 的指标衡量回归表现。判断句：MSE/RMSE 都是越低越好。
5. Optimizer 与 backpropagationforward 得到预测，loss 衡量错多少，backpropagation 计算每个参数该往哪边改，optimizer 根据梯度更新参数。PyTorch 已经把这套流程封装好，你要理解每一步的意义，而不是从零手推梯度。判断句：没有 zero_grad / backward / step，训练循环通常不完整。
6. Hyperparameter 怎么调epoch 控制训练跑几轮；learning rate 控制每次更新步伐；batch size 控制每次用多少样本估梯度；optimizer 可试 Adam 或 SGD；weight decay 用来抑制参数过大，降低 overfitting 风险。判断句：学习率太大可能震荡，太小可能收敛太慢。
7. Overfitting 怎么看如果 training loss 不断下降，但 validation loss 开始上升，模型可能记住训练资料细节和噪声，泛化变差。常见处理包括减少 epoch、降低模型复杂度、加入 weight decay，或重新选择更有效的 feature。判断句：validation curve 比 training curve 更接近你对未知资料的期待。
8. Baseline 推进路径simple baseline 只要跑通 Colab 并提交；medium baseline 要看 training curve 调参；strong baseline 可换 MLP；boss baseline 要做 feature selection，挑和 Metacritic score 更相关的 feature，减少噪声。判断句：更复杂的模型和更多 feature 不一定更好，仍要看 validation 表现。

## box
### 完整知识链路
把本节串起来，可以这样讲：作业5 用 Steam 游戏资料做回归，目标是预测 Metacritic score。先读 train.csv 和 test.csv，选择数值 feature，必要时把文字 feature 用 sentence embedding 转成向量；再定义 linear model 或 MLP，把 feature 喂进去得到预测分数；用 MSE loss 衡量预测和真实 label 的距离，通过 backpropagation 和 optimizer 更新模型参数；训练过程中用 validation set 监控是否收敛和是否 overfitting；最后把 test.csv 的预测写成 submission.csv 上传 Judge Boy。冲 baseline 时，先跑通，再调 hyperparameter，再换 MLP，最后做 feature selection。


---

# 作业流程 {#source}
<!-- tag: WORKFLOW -->

下面把 Colab 和评分说明压成一张操作地图。读的时候请把每一步接回前面的训练主线。

1. 跑 Colab先切到 GPU，通常 T4 就够。Colab 会导入套件、设置 random seed、下载 train.csv 和 test.csv。random seed 方便复现结果，但换 seed 也可能得到不同模型。你现在应该能判断：GPU 是为了训练效率，seed 是为了复现。
2. 选 feature默认使用数值 feature，例如 recommendations、positive、negative、price。做 feature selection 时，把想用的 feature 名称放进 Colab 指定列表。若要使用 text feature，要取消注释 embedding 相关代码，并在 test inference 处也做同样处理。训练和测试必须用同一套 feature 处理。
3. 训练模型Colab 提供 Linear model 和 MLP model。训练 loop 会定义 dataset、model、optimizer、loss function，并每隔一段 epoch 输出 train loss 和 validation RMSE，最后画出 training curve。不要只看最后一个数字，曲线形状会告诉你是否收敛或过拟合。
4. 生成提交训练完成后，把模型跑在 test.csv 上，预测每个游戏的 Metacritic score，并生成 submission.csv。左侧文件栏里下载该 CSV，再上传到 Judge Boy 的 homework 5。不要手动改 prediction CSV，这是作业规范禁止的。
5. 评分总分 10 分：成功提交有 2 分，其余 8 分来自 public/private 各 4 个 baseline。public score 很快可见，private score 要截止后公布。public 高不一定 private 高，final submission 建议参考 validation score。
6. 规范每天最多 5 次提交，23:59 重置。不能抄袭、不能分享代码或预测文件、不能用额外网络资料、不能提高提交次数、不能手动修改 prediction CSV。被偷看或外泄自己的文件也可能受罚。这份作业考模型训练，也考实验诚信。

## box
### 实验记录模板
1. 记录 feature list：哪些数值特征、是否加入 text embedding、训练和 test inference 是否一致。
2. 记录训练设置：model type、epoch、learning rate、batch size、optimizer、weight decay、seed。
3. 记录三种结果：training loss、validation RMSE、Judge Boy public score。
4. 每次只改一类变量；如果 public 变好但 validation 变差，把它标成不稳候选，不直接当最终方案。
你现在应该能做什么：把“分数变了”追溯到具体实验设置，而不是只记一次提交结果。

## box
### worked example：选择最终提交
模型 A 的 public RMSE 更低，但 validation RMSE 连续升高；模型 B 的 public 略差，但 validation 更稳。若没有更多 private 证据，模型 B 往往更适合作为 final submission，因为 private set 更接近未知资料，而不是 public leaderboard 的少量样本。


---

# 最小例子 {#example}
<!-- tag: EXAMPLE -->

例子只保留最小结构，目的是让你看懂每一步在读什么、写什么、改变什么状态。

$$
MSE = 1nΣi=1..n(ŷi - yi)2
RMSE = √MSE

例：预测 92、84、76，真实 87、80、68，平方误差为 25、16、64，RMSE = sqrt((25+16+64)/3) ≈ 5.91。
$$

最小示例：PyTorch 回归训练 looppython

```text
model = LinearModel(input_dim=num_features)
loss_fn = torch.nn.MSELoss()
optimizer = torch.optim.Adam(model.parameters(), lr=1e-3, weight_decay=1e-4)

for epoch in range(num_epochs):
 model.train()
 for x, y in train_loader:
 pred = model(x)
 loss = loss_fn(pred.squeeze(), y.float())

 optimizer.zero_grad()
 loss.backward()
 optimizer.step()

 model.eval()
 with torch.no_grad():
 val_pred = model(val_x).squeeze()
 val_rmse = torch.sqrt(loss_fn(val_pred, val_y.float()))
```

## box soft
### 逐行理解
`model.parameters()` 是 optimizer 会更新的 weight 和 bias。`pred = model(x)` 是 forward，把 feature 转成预测分数。`loss_fn` 用 MSE 衡量预测和真实 label 的距离。`zero_grad` 清掉上一轮梯度；`backward` 计算新梯度；`step` 才真正更新参数。validation 阶段用 `no_grad`，因为它只评估，不更新参数。
你现在应该能做的事：看到训练 loop，指出哪一行计算预测、哪一行计算损失、哪一行更新模型、哪一段只做验证。

最小示例：选择 feature 时别让 train/test 不一致python

```text
numeric_features = [
 "recommendations",
 "positive",
 "negative",
 "price",
]

# 如果训练时加入 text embedding，预测 test.csv 时也必须加入同样的 text embedding。
text_features = ["name", "short_description"]
```

## box warn
### 为什么这里容易出错
模型输入维度在训练时已经固定。如果训练时用了 4 个数值 feature，预测时却多拼了文字 embedding，维度会对不上；如果训练时用了文字 embedding，预测 test.csv 时忘了同样处理，模型看到的输入结构也不一致。你现在应该能做的是：改 feature list 时，同时检查训练、validation 和 test inference 三个位置。


---

# 交互观察 {#viz}
<!-- tag: OBSERVE -->

学习率：步伐太大或太小都会出问题
learning rate**1e-3**


训练曲线：看收敛还是过拟合

未收敛
正常下降
过拟合

观察重点：调参不是凭感觉乱试。先看曲线是否收敛，再看 validation loss 是否和 training loss 分歧，最后决定要调 learning rate、epoch、weight decay、模型复杂度还是 feature selection。


---

# 误区诊断 {#diagnosis}
<!-- tag: DIAGNOSIS -->
<!-- layout: grid2 -->

## box redsoft
### 常见坑
1. 把 test.csv 当成训练资料。修法：test 没有 label，只能用于最后预测；训练和调参要用 train/validation。
2. 只看 training loss 下降就认为模型变好。修法：同时看 validation loss；分歧时警惕 overfitting。
3. 把 hyperparameter 当成模型学出来的参数。修法：weight/bias 是训练更新的参数；epoch、learning rate、batch size、weight decay 是训练前设定的超参数。
4. 训练时用了 text embedding，预测 test 时忘了同样处理。修法：train、validation、test 的 feature pipeline 必须一致。
5. feature 全部塞进去就以为更好。修法：用相关系数和 validation score 做 feature selection，去掉明显噪声。
6. 手动修改 prediction CSV 或使用额外网络资料。修法：遵守作业规范，只用给定数据和模型流程产生提交文件。

## box
### 易混概念
概念 A概念 B区别
模型参数超参数前者训练中自动更新；后者训练前由人设定，控制训练过程。
MSERMSEMSE 是平方误差平均；RMSE 是 MSE 开根号，单位回到分数。
validationtestvalidation 用来调参和选模型；test 用来生成最终提交预测。
Linear modelMLPLinear 只表达线性关系；MLP 加 hidden layers 和 activation，可拟合更复杂函数。


---

# 综合练习 {#practice}
<!-- tag: DRILL -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、基础概念 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 判断

**Q:** 1. 作业5是 regression task，因为目标输出是 Metacritic score 这个数值。

- [ ] 对
- [ ] 错


### ? 选择

**Q:** 2. 下面哪一个文件用于最终生成 `submission.csv` 的预测？

- [ ] test.csv
- [ ] README.md
- [ ] public report txt


### ? 填空

**Q:** 3. 本作业的 label 是 ______，评估指标越 ______ 越好。


### ? 简答

**Q:** 4. 用 1-2 句话解释 feature 和 label 的区别。


<!-- end-practice-group -->

<!-- practice-group: 二、主线理解 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 排序

**Q:** 5. 把训练流程排成顺序：forward、loss、backward、optimizer.step、validation。


### ? 对照

**Q:** 6. 区分模型参数和超参数。


### ? 解释

**Q:** 7. 为什么 validation set 不应该参与训练参数更新？


### ? 填空

**Q:** 8. 如果 training loss 下降、validation loss 上升，可能发生了 ______。


<!-- end-practice-group -->

<!-- practice-group: 三、例子阅读 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 代码阅读

**Q:** 9. 训练 loop 里 `zero_grad`、`backward`、`step` 分别做什么？


### ? 代码阅读

**Q:** 10. 为什么 validation 阶段要用 `no_grad`？


### ? 场景

**Q:** 11. 如果训练时加入 text embedding，test inference 时忘了同样处理，会发生什么问题？


### ? 判断

**Q:** 12. RMSE 越低代表预测分数越接近真实分数。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 四、错误诊断 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 诊断

**Q:** 13. 有人把 test.csv 加入训练来提高分数。错因、后果、修法是什么？


### ? 诊断

**Q:** 14. 有人只看 training loss 很低就选择最终提交。这个评估哪里不稳？


### ? 诊断

**Q:** 15. 有人手动打开 prediction CSV 修改几个分数。应该如何判断？


### ? 诊断

**Q:** 16. MLP 加得很深后 public 变好、validation 变差。可能是什么原因？


### ? 诊断

**Q:** 17. 有人说“feature 越多一定越好”。这句话的问题在哪里？


<!-- end-practice-group -->

<!-- practice-group: 五、场景迁移 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 应用

**Q:** 18. 如果训练曲线显示还没收敛，你会优先尝试哪两类调整？


### ? 应用

**Q:** 19. 如果要冲 boss baseline，你会怎样做 feature selection？


### ? 清单

**Q:** 20. 写出你上传 Judge Boy 前会检查的 5 项清单。


<!-- end-practice-group -->

<!-- practice-group: 六、表达输出 -->
<!-- quick-review: REMIND 把答案写成能被检查的句子。遇到错误题，先定位错因，再说后果。 -->

### ? 面试短答

**Q:** 21. 如果面试官问“一个回归训练作业怎么从数据走到提交”，你会怎么答？


### ? 费曼解释

**Q:** 22. 用 6-9 句话讲清楚 P16 作业的核心。请串联 regression、Steam games dataset、Metacritic score、MSE/RMSE、optimizer、validation、overfitting、MLP、feature selection、submission.csv。


<!-- end-practice-group -->


---

