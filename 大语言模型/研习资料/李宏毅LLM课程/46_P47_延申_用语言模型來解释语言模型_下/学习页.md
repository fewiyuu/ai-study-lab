---
course: "李宏毅 LLM 课程"
unit: "46"
title: "46_P47｜（延申）用语言模型來解释语言模型 (下)"
kicker: "LI HONGYI LLM"
kicker_en: "延伸专题"
summary: "上半段已经讲过一个基本想法：让一个语言模型观察 GPT-2 某个神经元在句子上的 activation，再写出这个神经元可能在侦测什么。下半段继续追问：这个解释怎么验收？如果给 explainer 的例子本身有偏差，它会不会写出听起来合理但实际错误的解释？"

side_note: "这一页只处理 P47 下半段：解释如何打分、为什么样本选择会误导解释、怎样用模型生成反例修正解释，以及这套方法还有哪些硬伤。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../45_P46_延申_用语言模型來解释语言模型_上/学习页.html"
  - text: "下一节"
    href: "../47_P48_作业七：UnderstandingwhatAIisthinking/学习页.html"
---

# 概念骨架 {#skeleton}
<!-- tag: MAP -->

解释不是直接相信模型Explainer 写出的句子只是一份候选解释。它可能准确，也可能只是抓住了高 activation 例子里最显眼的词。课程后半段一直在补一件事：把候选解释放回模型行为里检查。判断句：只要一个解释没有经过真实 activation 或整网输出检验，它还不能算稳定解释。
打分要看两层行为第一层是神经元级别：Simulator 根据解释预测某个神经元在新句子上的 activation，再和 GPT-2 真实 activation 做 correlation。第二层是模型级别：把 GPT-2 的一个神经元换成 GPT-4 扮演的神经元，看整个 GPT-2 输出是否变化。两种分数正相关时，说明“像不像这个神经元”和“会不会影响整体输出”没有明显冲突。读到这里，你应能区分局部打分和整体打分。
样本选择会改变解释Explainer 不可能读完上万个句子的 activation，只能看一小部分例子。通常会挑 target neuron 反应最强的句子，但这种选择会漏掉负例和反例。not all 的例子就是典型：只看正例时，模型容易把神经元解释成“看到 all 就启动”。核心直觉：正例告诉你“哪些东西会亮”，反例才告诉你“边界在哪里”。没有边界的解释通常会过宽。

1. 观察让 GPT-2 读大量句子，记录某个 neuron 的 activation。
2. 解释把高 activation 片段给 GPT-4，让它写出神经元的自然语言解释。
3. 扮演让 GPT-4 根据解释预测同一神经元在新句子上的反应。
4. 修正用分数和反例检查解释，必要时生成新句子再 revision。


---

# 术语表 {#terms}
<!-- tag: TERMS -->

| 术语 | 直觉解释 | 准确说法 | 常见误解 |
| --- | --- | --- | --- |
| Subject model | 被拿来解释的模型。 | 课程例子里是 GPT-2，研究者读取它内部某个 neuron 的 activation pattern。 | 以为 Subject model 也负责写解释。它只提供被观察的行为。 |
| Target neuron | 被盯住的单一神经元。 | 在每个 token 或片段上会输出一个 activation 数值，数值大代表这个神经元反应强。 | 把一个神经元当成完整概念检测器；现实里功能可能分布在一组神经元里。 |
| Explainer | 写解释的模型。 | 读取若干句子和对应 activation，输出“这个神经元可能在侦测什么”的自然语言描述。 | 把 explainer 的文字当成事实，而不是待验证假设。 |
| Simulator | 照着解释扮演神经元的模型。 | 根据 explanation 和新句子，预测 target neuron 应该给出怎样的 activation。 | 分数低时只怪解释，其实也可能是 simulator 扮演能力不够。 |
| correlation score | 看扮演结果像不像真实神经元。 | 比较 simulator 预测 activation 与真实 activation 的相关程度，越高说明解释更能指导扮演。 | 以为分数高就等于人一定看懂解释；分数只说明这套系统内的匹配较好。 |
| revision | 用新例子修正解释。 | 先让模型按当前解释生成可能触发神经元的句子，再送回 Subject model 标注 activation，用新证据改写解释。 | 把 revision 当成随便扩充数据；关键是生成能区分旧解释和新解释的例子。 |

读完这一段，你应能把“谁观察、谁解释、谁扮演、谁打分”说清楚。术语记不住时，先回到这四个角色。


---

# 讲义主线 {#lecture}
<!-- tag: LECTURE -->
<!-- layout: grid2 -->

## box
### 1. 为什么还要检查解释
让 GPT-4 给 GPT-2 神经元写解释，这件事最容易让人误会成“模型自己说了算”。课程里没有这样处理。解释被写出来以后，还要交给 simulator 使用：如果 simulator 只看这段解释，就能预测出真实 target neuron 在不同句子上的 activation，解释才算有实际约束力。
这里的直觉很简单：好的说明书应该能指导别人复现行为。若 explanation 写成“这个神经元在寻找 all”，但 simulator 按这句话预测所有含 all 的句子都会高 activation，而真实 GPT-2 只在 not all 这种结构上高反应，那么说明书太宽了。
正式说法是，解释质量通过 simulation 任务来间接衡量。Explainer 产生一段自然语言解释，Simulator 把这段语言转成 activation 预测，最后用 correlation score 比较预测值与真实值。读到这里，你应能把“自然语言解释”看成中间变量，而不是最终答案。

## box
### 2. 局部像不像，还要问整体有没有变
只比较单个 neuron 的 activation 仍然有一个问题：某些神经元很关键，数值差一点就会影响整个 GPT-2 输出；某些神经元不重要，activation 差很多也不会影响最终输出。单看局部相关性，可能把这两类神经元混在一起。
视频里提到另一种检查：用 GPT-4 扮演的神经元替换 GPT-2 里的真实神经元，再观察 GPT-2 的整体输出有没有改变。这个实验听起来很怪，一个有许多参数的大模型被要求扮演一个神经元，但它抓住了更现实的问题：这个 neuron 的行为差异是否真的传到整网输出。
OpenAI 的结果显示，activation correlation score 和这种替换后的整体输出分数呈正相关。因此实际分析时可以优先看其中一个分数，不必每次都做代价更高的整体替换。读到这里，你应能解释为什么“像不像神经元”和“影响不影响模型输出”是两个不同层次的问题。

3. 高 activation 样本为什么必要GPT-4 的输入长度有限，不可能把 target neuron 在所有句子上的反应都塞进去。研究者通常先让 GPT-2 跑过大量文本，再挑出这个神经元 activation 最大的片段给 explainer。这样做能把最有信号的例子放到 explainer 面前。判断句：如果一个样本从来没有触发目标神经元，它对“这个神经元喜欢什么”帮助有限。
4. 高 activation 样本也会误导问题在于，正例只告诉你“这些句子会触发”，不告诉你“哪些相似句子不会触发”。如果高 activation 片段都包含 not all，explainer 可能注意到 all，却没有证据知道没有 not 的 all 不会触发。这就是解释过宽。判断句：看到一个解释时，要问它有没有被负例或对照样本挑战过。
5. revision 的作用revision 让模型按当前解释生成新句子，再让 Subject model 给这些句子标 activation。若当前解释说“侦测 all”，就生成更多含 all 的句子；若 GPT-2 对 “All the students ...” 没有高反应，旧解释就站不稳。这样得到的新资料能逼 explainer 收窄解释，从“all”改成“not all”。读到这里，你应能说明 revision 为什么比随机加样本更有针对性。
6. random revision 为什么也有用视频里提到，随机多找一些句子或从数据库抽一些句子，解释力分数也可能提升。原因更多样本会补上一些原来没见过的边界。但随机样本未必撞到关键反例。模型生成的样本如果围绕当前解释制造对照，通常能更快暴露漏洞。
7. 最后的质疑不能跳过这套方法确实漂亮，但它没有解决所有问题。单一神经元可能没有独立语义；某个神经元的真实功能也许无法用人类语言描述；explainer 和 simulator 的分工还会混淆责任。分数低时，你很难判断到底是 explanation 不好，还是 simulator 不会扮演。判断句：这节课要你会拆解这套验证框架。

## box
### 完整知识链路
把本节串起来，可以这样讲：研究者先从 Subject model 取出 target neuron 的 activation pattern，让 Explainer 写出自然语言解释；再让 Simulator 按这段解释去预测 activation，用 correlation score 衡量预测和真实反应是否一致。为了避免只看高 activation 正例造成过宽解释，可以用当前解释生成新句子，再送回 Subject model 标注，利用反例做 revision。最后还要记住，这个流程解释的是被选中的神经元，不一定解释了整个网络；语言解释也不一定覆盖神经元真正做的复杂运算。你应能从“观察、解释、扮演、打分、修正、质疑”六步复述整节课。


---

# 课程线索 {#source}
<!-- tag: 线索 -->

下面按字幕推进顺序整理。它们把视频口头讲法压成适合回看的知识块。

线索 1：三种模型角色字幕开头先回顾整体流程：被解释的是 Subject model，课程例子里是 GPT-2；Explainer 和 Simulator 都可以由 GPT-4 扮演，但它们不必和被解释模型相同，也不必固定是 GPT-4。关键是角色分工，不是模型名字。读到这里，你应能判断：哪个角色产出解释，哪个角色产出 activation 预测。
线索 2：两种评分角度第一种角度比较 simulator 输出和真实神经元输出，得到 correlation score。第二种角度把 GPT-2 的真实神经元换成 GPT-4 扮演的神经元，检查 GPT-2 整体输出改变多少。前者看局部拟合，后者看整网影响。读到这里，你应能说明为什么“局部相似”不自动等于“整体无影响”。
线索 3：分数和人类判断的关系视频提到 OpenAI 做了 human study：人觉得比较好的解释，通常在自动解释力分数上也更高。这一点缓解了一个担心：分数不只是模型内部自娱自乐，至少和人类读到的解释质量有正相关。但它不是最终证明。human study 说明有关联，不说明所有高分解释都可靠。
线索 4：为什么要选 activation 最大的句子Explainer 的上下文有限，只能看一小批例子。比较自然的做法是让 GPT-2 先读大量句子，再挑出 target neuron 输出特别大的句子。这样能让 explainer 看到最明显的触发模式。读到这里，你应能说出这一步的必要性：不是偷懒，而是受上下文长度限制。
线索 5：not all 反例字幕里的例子很关键：某个神经元看起来在看到 all 时启动，但真实功能可能是寻找 not all。只给正例时，Explainer 没有证据排除普通 all，于是会写出过宽解释。你应能用这个例子解释什么叫“缺少对照样本”。
线索 6：用 GPT-4 生成关键样本如果当前解释是“侦测 all”，就让 GPT-4 生成更多含 all 的句子，再让 GPT-2 给这些句子标 activation。若普通 all 不触发，Explainer 就能把解释修成“not all”。这种 revision 比随机抽样更容易打到旧解释的弱点。读到这里，你应能区分随机补样本和有目标地造反例。
线索 7：方法本身的质疑结尾集中列出几个边界：用 AI 解释 AI 是否可靠；解释单一神经元是否有意义；神经元行为是否能被人类语言描述；explainer 与 simulator 的合作分数是否会掩盖责任归属。你应能把这些质疑归类为“对象边界、语言边界、评估边界”。


---

# 最小例子 {#example}
<!-- tag: EXAMPLE -->

下面用几行伪代码重建课程里的 not all 例子。重点看懂样本、解释、预测和修正之间的关系。

最小示例：用反例修正神经元解释pseudo-python

```text
case = {
 "positive_examples": [
 ("not all of them agreed", 0.96),
 ("not all answers are correct", 0.91)
 ],
 "draft_explanation": "This neuron activates on the word all.",
 "generated_tests": [
 "All the students arrived early.",
 "Not all the students arrived early."
 ],
 "subject_model_labels": [0.08, 0.94],
 "revised_explanation": "This neuron activates on the phrase not all."
}
correlation_before = score_simulator(case["draft_explanation"])
correlation_after = score_simulator(case["revised_explanation"])
```

## box soft
### 逐行理解
前两条正例只告诉我们：含 not all 的句子会让 target neuron 有高 activation。它们还不能证明 all 本身就是触发条件，因为这两条正例里 all 总是和 not 一起出现。
revision 的关键动作是主动造对照。把只有 all 的句子和含 not all 的句子一起送回 Subject model，观察真实 activation 差异。若只有 not all 高，解释就必须从“all”收窄到“not all”。读完这段，你应能指出一个解释缺少哪些反例。

## box
### 代码里的关键对象
`positive_examples` 是原本挑出来的高 activation 片段，它们都支持“这个神经元会启动”的事实，但不一定能区分 all 与 not all。`draft_explanation` 是 Explainer 的初稿，它抓到了 all，却漏掉了前面的 not。
`generated_tests` 是 revision 的关键。第一句只有 all，第二句有 not all；送回 Subject model 后，真实 activation 标签显示第一句低、第二句高。这样 Explainer 才有证据把解释收窄。

## box warn
### 删掉哪一行会出问题
删掉 `subject_model_labels`，你就只有 GPT-4 自己生成的新句子，却不知道 GPT-2 真实神经元怎样反应。删掉 `generated_tests`，你就只能在旧正例里打转，无法发现“all”这个解释过宽。
判断句：revision 的价值来自“生成候选反例 + 回到 Subject model 标注”，两步缺一不可。

## box info
### Worked example：从过宽解释到收窄解释
初稿解释是“神经元侦测 all”。现在设计四个测试句：`All birds can fly`、`All files are ready`、`Not all birds can fly`、`Not all files are ready`。如果 Subject model 给出的 activation 是 `0.05, 0.07, 0.92, 0.89`，那么 all 本身不是充分条件，`not all` 结构才更像触发条件。
这就是 revision 的手感：不要只让模型再写一句更优雅的解释，而是先构造能区分两个解释的样本，再用真实 activation 逼解释收窄。你现在应该能为任意一个候选解释写出“支持句”和“挑战句”。


---

# 交互观察 {#viz}
<!-- tag: OBSERVE -->

正例比例
只看高 activation**70**


检查方式

只看正例
加入随机样本
生成对照反例

观察重点：高 activation 正例越多，Explainer 越容易抓到显眼共同点，但也越可能把共同点解释得太宽。真正有价值的检查，是找出“看起来相似但不触发”的句子。


---

# 误区诊断 {#diagnosis}
<!-- tag: DIAGNOSIS -->
<!-- layout: grid2 -->

## box redsoft
### 常见坑
1. 把 Explainer 的解释当成事实。错因：忽略解释只是候选假设。后果：遇到反例时仍相信过宽解释。修法：用 simulator 和真实 activation 打分。
2. 只看高 activation 正例。错因：没有负例或对照样本。后果：把 not all 误解释成 all。修法：生成相似但可能不触发的句子，再送回 Subject model 标注。
3. 把 correlation score 理解成整网影响。错因：局部分数只比较神经元输出。后果：无法判断这个 neuron 的误差是否影响 GPT-2 最终输出。修法：必要时做替换神经元的整体输出检查。
4. 分数低就断定解释差。错因：忽略 simulator 也可能很弱。后果：把责任全推给 explanation。修法：同时检查 simulator 能力、给 simulator 的说明和样本质量。
5. 以为单一神经元一定有清晰语义。错因：把神经网络内部表示想得太模块化。后果：解释结果可能碎片化或误导。修法：保留“功能可能分布在神经元组或方向上”的判断。
6. 以为自然语言能描述所有行为。错因：语言词汇有限。后果：高维复杂特征被压成粗糙标签。修法：把语言解释看成可用近似，不当成完整还原。

## box
### 易混概念
概念 A概念 B区别
ExplainerSimulatorExplainer 写解释；Simulator 按解释预测 activation。前者负责生成假设，后者负责检验假设能否指导行为复现。
correlation score整体输出检查前者比较单个神经元的预测和真实 activation；后者把神经元替换掉，看 GPT-2 最终输出是否受影响。
高 activation 正例对照反例正例说明哪些句子触发；反例说明哪些相似句子不触发。缺少反例时，解释容易过宽。
解释单一神经元解释神经元组单一神经元更容易观察，但功能可能不完整；神经元组或方向可能更接近实际表示。


---

# 综合练习 {#practice}
<!-- tag: DRILL -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、基础概念 -->
<!-- quick-review: REMIND Subject model 被观察；Explainer 写解释；Simulator 扮演神经元。 -->

### ? 判断

**Q:** 1. Subject model 在本节流程里负责写出神经元解释。

- [ ] 对
- [ ] 错


### ? 选择

**Q:** 2. Explainer 的输入最接近哪一组？

- [ ] 句子片段和 target neuron 的 activation
- [ ] GPT-2 的全部参数和训练资料
- [ ] 人类提前写好的标准答案


### ? 填空

**Q:** 3. 请补全流程：Subject model → activation pattern → ______ → ______ → score。


### ? 简答

**Q:** 4. 用 1-2 句话解释 target neuron 的 activation pattern 是什么。


### ? 判断

**Q:** 5. Simulator 的作用是根据解释预测神经元反应，而不是重新训练 GPT-2。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 二、评分与验证 -->
<!-- quick-review: REMIND correlation score 看局部像不像；替换神经元看整体输出变不变。 -->

### ? 填空

**Q:** 6. correlation score 比较的是哪两个量？


### ? 选择

**Q:** 7. 为什么只看神经元输出不一定够？

- [ ] 不同神经元对整网输出的重要性不同
- [ ] activation 数值永远没有意义
- [ ] 语言模型不能处理数字


### ? 解释

**Q:** 8. 把 GPT-2 的某个神经元换成 GPT-4 扮演的神经元，这个实验想检查什么？


### ? 判断

**Q:** 9. 视频提到两种分数呈正相关，所以可以把所有验证都取消。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 三、样本与 revision -->
<!-- quick-review: REMIND 旧解释越宽，越需要相似但不触发的句子来挑战它。 -->

### ? 简答

**Q:** 10. 为什么一开始通常挑 target neuron activation 特别大的句子给 Explainer？


### ? 因果

**Q:** 11. not all 例子里，错误初稿为什么会写成“侦测 all”？


### ? 设计

**Q:** 12. 请给当前解释“这个神经元侦测 all”设计两个对照句：一个只有 all，一个包含 not all。


### ? 推理

**Q:** 13. 如果 `All the students` 的 activation 很低，而 `Not all the students` 的 activation 很高，应怎样修正解释？


### ? 判断

**Q:** 14. revision random 和 GPT-4 生成关键样本的差别是：前者补充随机样本，后者围绕当前解释制造更有区分度的样本。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 四、例子阅读 -->
<!-- quick-review: REMIND 没有 Subject model 标注，新句子只是测试题，还不是证据。 -->

### ? 代码阅读

**Q:** 15. 伪代码里的 `positive_examples` 能证明“all 本身会触发”吗？为什么？


### ? 代码阅读

**Q:** 16. 如果删掉 `subject_model_labels`，revision 会失去哪一步证据？


### ? 边界

**Q:** 17. `correlation_before` 到 `correlation_after` 上升，能说明什么？还不能说明什么？


### ? 判断

**Q:** 18. 只要 `revised_explanation` 是自然语言写成的，它就一定完整描述了神经元真实功能。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 五、错误诊断 -->
<!-- quick-review: REMIND 把问题落到样本、角色、分数或语言边界上。 -->

### ? 诊断

**Q:** 19. 有人说“GPT-4 写了解释，所以这个神经元功能已经确定”。错因、后果、修法是什么？


### ? 诊断

**Q:** 20. 有人只给 Explainer 看高 activation 正例，然后接受“all”解释。错因、后果、修法是什么？


### ? 诊断

**Q:** 21. 有人看到 explanation score 低，就断定 explainer 很差。这个诊断漏掉了什么？


<!-- end-practice-group -->

<!-- practice-group: 六、表达与迁移 -->
<!-- quick-review: REMIND 好的回答会同时讲流程、分数、revision 和质疑。 -->

### ? 迁移

**Q:** 22. 如果要解释一组神经元而不是单一神经元，你会额外担心什么？


### ? 面试短答

**Q:** 23. 面试短答：为什么“用 AI 解释 AI”不必因为“AI 也是黑盒”就直接判死刑？


### ? 清单

**Q:** 24. 请写出你检查一个神经元解释时会看的 4 项清单。


### ? 费曼解释

**Q:** 25. 用 6-9 句话讲清楚 P47 下半段。请串联 Subject model、Explainer、Simulator、activation、correlation score、revision、not all、单一神经元。


<!-- end-practice-group -->


---

