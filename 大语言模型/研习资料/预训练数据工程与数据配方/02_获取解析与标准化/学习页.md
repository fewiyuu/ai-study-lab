---
course: "预训练数据工程与数据配方"
unit: "单元 02"
title: "获取、解析与标准化"
kicker: "基础单元"
summary: "这一页只处理一条入口链：把网页、WARC 记录或导出文件变成可追溯的 document。先抓住原始记录，再抽正文并保留元数据，最后把编码和字段标准化成后续过滤能直接接手的输入；质量过滤、去重和配方留到后面的单元。"

side_note: "先把网页变成可追溯的 document，再谈后面的过滤、去重和配方。"

can_do:
  - "分清 raw data、document 和后续训练样本，不把网页直接当语料。"
  - "用状态表说明正文抽取、编码标准化和元数据记录各自读什么、写什么。"
  - "拿着一份抽取失败的记录，能判断先回查哪一步。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一单元"
    href: "../01_数据工程全景_从网页到token/学习页.html"
  - text: "下一单元"
    href: "../03_质量过滤_规则模型与偏差/学习页.html"

---

# 学习目标 {#goals}
<!-- tag: foundation -->

先认入口
这一页关注网页进入数据管线时，哪些东西必须原样留下，哪些东西要先抽成 document。


先认边界
正文抽取、编码标准化和元数据记录是这一页的中心。质量过滤、去重、配方和 tokenization 都不在这一页里展开。


先留证据
只要后面还要回到原始页面、抓取记录或抽取版本，就必须保留 source_url、charset、fetched_at、extractor_version 和 content_hash 这类字段。

## box
### 这一页的使用方式
先顺着对象链读，再看抽取和标准化怎么改对象，最后用回查表判断问题应该落在哪一步。你如果一开始就跳到过滤规则，往往会把输入层的问题误判成质量问题。


---

# 对象链 {#map}
<!-- tag: trace -->

这条链只看四个可检查的状态：原始字节、解析后的页面结构、抽取后的正文记录、标准化后的 document。四个状态各有读写关系，混在一起就会丢掉回查入口。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

状态
前一步读什么
这一层写什么
错了会怎样


`raw bytes`
URL、响应头、抓取时间
原始记录
后面不知道原页长什么样


`parsed DOM`
HTML 和 charset 提示
页面结构
乱码会把结构一起带歪


`document`
正文抽取器、保留字段规则
正文 + 元数据
正文抽错后，后面的判断都不稳


`normalized doc`
统一编码、NFKC、空白规则
可比较、可检索的文本
重复和统计都容易失真


---

# 获取入口 {#ingest}
<!-- tag: source boundary -->

这一层先回答“你拿到的到底是什么”。有些来源给你的是网页字节，有些给你的是导出 CSV，有些给你的是 WARC 或 JSONL。不要一上来就把它们都叫成网页文本。

来源形态
先留什么
容易漏掉什么
为什么这一步重要


网页 HTML / WARC
URL、响应头、抓取时间、原始字节
charset、重定向、正文之外的脚本与导航
抽取和标准化都要回到原始记录


站点导出 / CMS 导出
栏目、更新时间、作者、原站链接
字段语义和权限边界
导出内容常常已经被预处理过


论坛页 / 讨论串
主帖、回复顺序、楼层、时间戳
引用嵌套和签名块
后面做正文抽取时要知道层次


本地文件 / JSONL
schema、版本、生成时间
生成脚本和字段约定
后面回查时要知道是谁产出的

## box info
### 这一层不做什么
这里不判断内容好坏，也不做质量过滤。先把对象和记录留住，再决定值不值得保留。输入边界没定清，后面每一步都容易偏题。


---

# 正文抽取 {#extract}
<!-- tag: mechanism -->

抽取要把网页里真正进入 document 的正文找出来，同时把来源和版本信息留住。导航、推荐阅读、广告、脚注和重复块常常混在同一页里，抽取器要给它们划清边界。

extract_document.py复制

```python
def build_document(raw):
 dom = parse_html(raw.bytes, charset_hint=raw.charset_hint)
 main_text = extract_main_text(dom)
 doc = {
 "source_url": raw.url,
 "fetched_at": raw.fetched_at,
 "extractor_version": "main_text_v1",
 "text": normalize_text(main_text),
 }
 return doc
```

步骤
读什么
写什么
典型症状


解析 HTML
原始字节和 charset 提示
页面树
乱码把整页结构带歪


找主文
DOM 结构、段落密度、链接密度
正文文本
只剩导航或空白


保留元数据
URL、抓取时间、抽取版本
document 记录
后面回不去原页

## box
### 一个最小对照
原始页里可能有“首页 / 分类 / 登录 / 推荐阅读 / 正文 / 评论区”。抽取后，document 应该留下正文和必要元数据，避免把所有块原封不动存进去。

## box warn
### 别把这里做成过滤
正文抽取关心“哪一块是正文”。质量过滤关心“正文值不值得保留”。把这两件事混在一起，会让坏抽取看起来像坏数据质量。


---

# 案例走读：从一个旧网页到 document {#walkthrough}
<!-- tag: walkthrough -->

下面把前面的概念压进一个可检查的小案例。假设数据源是一批旧站点页面，页面来自 WARC 或离线 HTML 归档，编码标注不稳定，正文周围还有导航、登录入口、推荐阅读、评论和页脚。目标先产出一条可以回到原始页面、可以复现实验、可以交给后续过滤器的 document。

走读时只问三件事：当前步骤读的对象是什么，写出的对象是什么，失败以后第一轮应该回到哪里。只要这三件事没有写清，后面的过滤、去重、配方和训练统计都会缺少解释力。

raw_legacy_page.html复制

```html
<!doctype html>
<html>
<head>
 <meta charset="gb2312">
 <title>如何整理训练语料</title>
</head>
<body>
 <nav>首页 | 模型 | 数据集 | 登录</nav>
 <main>
 <h1>如何整理训练语料</h1>
 <p>预训练数据工程的第一步，是把不同来源的网页变成结构稳定的文档记录。</p>
 <p>记录里要保留来源、抓取时间、抽取器版本和正文哈希，方便后续过滤与回查。</p>
 </main>
 <aside>相关阅读：十个数据集清洗技巧</aside>
 <footer>版权声明 | 站点地图 | 联系我们</footer>
</body>
</html>
```

检查点
输入对象
输出对象
验收信号


获取
URL、响应头、原始字节、抓取时间
raw record
能看到字节长度、HTTP 状态、charset 提示和抓取批次


解析
raw record 里的字节与编码线索
DOM 或文本块列表
标题、段落、导航、页脚被分成可检查的块


正文抽取
DOM、块密度、链接密度、标签位置
main_text
正文段落被保留，导航、aside、footer 没有混进主文本


标准化
main_text、字符编码、Unicode 形态、空白符
normalized_text
中文可读，空白稳定，同一内容在不同抓取批次里便于比较


封装
normalized_text 加来源字段
document
包含 source_url、fetched_at、charset、extractor_version、content_hash

这张表的作用是把“页面处理成功了吗”拆成五个可验证的小问题。获取成功，只说明你拿到了字节；解析成功，只说明字节能被拆成结构；抽取成功，才说明主文进入记录；标准化成功，才说明字符层面稳定；封装成功，才说明这条记录以后可以被解释。很多数据事故出在这里：团队只保存了最终文本，几周后发现训练样本里混入大量导航，却无法知道是哪一版抽取器产生的。

## box info
### 第一轮输出长什么样
第一轮 document 可以很朴素，但字段必须承担职责。`source_url` 负责回到原页，`fetched_at` 负责定位抓取批次，`charset` 负责解释解码路径，`extractor_version` 负责对照规则变化，`content_hash` 负责后续重复检测，`text` 才是模型最终可能看到的主体内容。

## box warn
### 这一步最怕什么
最怕把“看起来能读”当作完成。页面能读，不等于 document 稳定；正文能看到，不等于元数据完整；当前样本正常，不等于下一批旧页面也能按同样规则处理。学习这一单元时，判断标准要落在对象、字段和回查路径上。

normalized_document.json复制

```json
{
 "source_url": "https://example.org/archive/data-engineering.html",
 "fetched_at": "2026-06-20T09:31:12+08:00",
 "raw_format": "html",
 "charset": "gb2312",
 "parser_version": "html5lib-2026-06",
 "extractor_version": "main-text-density-v1",
 "normalizer_version": "unicode-nfkc-space-v1",
 "content_hash": "sha256:7c6d...",
 "text": "如何整理训练语料\n预训练数据工程的第一步，是把不同来源的网页变成结构稳定的文档记录。\n记录里要保留来源、抓取时间、抽取器版本和正文哈希，方便后续过滤与回查。"
}
```

这个 JSON 示例故意保留了处理版本。版本字段会让很多后续问题变得可回答：同一 URL 两次抽取得到不同正文，先看 `extractor_version`；同一篇文章的哈希变化，先看标准化规则和空白处理；某批旧站点大面积乱码，先看 `charset` 记录和解析日志。没有这些字段，排查只能靠猜。

看到的故障
第一轮回查
第二轮回查
暂缓处理


正文出现“Ã¥Â­Â¦”一类乱码
charset_hint、HTTP header、meta charset
解码错误策略和原始字节样本
质量过滤阈值


document 里只有首页、登录、站点地图
正文抽取规则和 DOM 块密度
解析器是否把 main 区域拆坏
去重策略


同一文章多次出现且文本略有差异
标准化规则、空白处理、Unicode 形态
content_hash 的计算位置
训练采样权重


样本无法解释来自哪个页面
source_url、crawl_id、fetched_at
raw record 是否仍能索引到
模型效果归因

注意这张故障表的顺序。它把问题放回最早可能出错的对象层：字符异常先查获取和解析，主文异常先查抽取，重复异常先查标准化与哈希，来源缺失先查 schema。这个顺序会让排查成本下降，也能避免把入口层的坏记录误判成后续算法问题。

## box
### 怎么把走读迁移到自己的数据
拿到一个新来源时，先挑 20 到 50 个样本人工走一遍这张链。每个样本都记录原始 URL、抓取时间、编码线索、抽取文本、标准化文本和最终 document。然后专门挑异常样本：乱码页、空正文页、超短页、导航占比高的页、重复标题页。只要异常样本能被字段解释，后面的批处理才值得扩大规模。
完成这个练习后，你应该能做两件事：第一，读懂一个数据管线日志时能指出它处理的是 raw、DOM、text 还是 document；第二，看到一个坏样本时能写出第一轮回查路径，而不是直接调过滤器、去重器或训练脚本。

抽样检查
合格信号
失败时动作
能不能放大批处理


随机样本
大多数 document 字段完整，正文可读
补齐 schema 或修正基础解析
字段不完整时不放大


异常样本
乱码、空正文、导航混入都能定位到具体步骤
把失败类型写进日志和回归样本集
异常无法解释时不放大


重复样本
同文不同 URL 的哈希关系可解释
检查标准化位置和哈希计算规则
哈希漂移时不放大

release_gate.yml复制

```yaml
sample_gate:
 random_docs: 50
 required_fields:
 - source_url
 - fetched_at
 - charset
 - extractor_version
 - normalizer_version
 - content_hash
 - text
 fail_fast:
 - unreadable_text_rate_over_2_percent
 - missing_source_url
 - extractor_version_not_recorded
 - navigation_blocks_in_main_text
```

## box info
### 放行标准要写成可执行问题
“页面看起来差不多”不是放行标准。更稳的写法是：50 个随机样本里缺 source_url 的比例是多少，乱码样本能不能回到原始字节，抽取器版本有没有写入每条 document，导航块是否进入 main_text，哈希是在标准化前还是标准化后计算。每个问题都对应一个字段或一步处理动作，回答不出来就说明这批数据还没有准备好交给过滤和去重。
一个可操作的抽样记录可以这样写：样本 17 来自旧站点新闻页，raw 里 charset 是 gb2312，解析后 main 区域存在两个段落，抽取后混入页脚版权，标准化文本可读但 content_hash 已生成。这个样本的结论“抽取器回归样本 + 暂停放大批处理”。练习时你可以照这个格式写三条异常记录，每条都要指出读什么字段、查哪一步、修完以后用什么信号验收。修复后再跑同一组异常样本，确认旧问题没有重新进入 document。
这也是本单元和后续单元的交界线。完成获取、解析、抽取和标准化后，你手里应该有一批“可解释的 document”。下一单元才开始讨论哪些 document 应该留下，哪些要丢掉，以及过滤规则会带来什么偏差。把放行标准写清，后面的质量判断才不会替入口层的问题背锅。


---

# 编码标准化 {#normalize}
<!-- tag: stability -->

抽取完成以后，还要把字符层面的不稳定压下去。这里处理的是可比较、可检索、可重复的文本形态，不改写内容含义。字符解码错了，后面的去重、匹配和统计都会跟着错。

normalize_text.py复制

```python
import unicodedata

def normalize_text(raw_bytes, charset_hint=None):
 text = raw_bytes.decode(charset_hint or "utf-8", errors="replace")
 text = unicodedata.normalize("NFKC", text)
 text = " ".join(text.split())
 return text
```

问题
这一步在读什么
这一步写什么
不做会怎样


错误解码
原始字节和 charset 提示
可读文本
乱码进入 document


全角半角混用
字符形式
统一表示
重复检测和关键词匹配漂移


空白不一致
换行、制表、多个空格
稳定的分隔方式
切分和统计结果不稳

## box info
### 这一步为什么放在抽取之后
先抽取正文，再统一字符，顺序更稳。你如果先做字符清理再找正文，很多页面的结构线索会被抹掉，抽取器反而更难判断哪一块是正文。


---

# 元数据记录 {#schema}
<!-- tag: schema -->

document 是一条能回查的记录。正文之外的字段承担解释职责，它们决定你以后能不能说明这条记录从哪里来、是谁抽的、什么时候抽的、用什么规则抽的。

document-record.json复制

```json
{
 "source_url": "https://example.org/article",
 "fetched_at": "2026-06-20T10:15:00Z",
 "raw_format": "html",
 "charset": "utf-8",
 "extractor_version": "main_text_v1",
 "language": "zh",
 "content_hash": "sha256:...",
 "text": "..."
}
```

字段
作用
缺了会怎样


`source_url`
回到原始页面
你不知道这条正文从哪来


`fetched_at`
标记抓取时间
无法判断页面是否后来改过


`charset`
帮助解码和排错
乱码问题很难回溯


`extractor_version`
记录正文抽取版本
没法比较新旧抽取器差异


`content_hash`
做去重和回查
同文不同名时很难发现重复


`language`
做后续路由和过滤
语言混杂会被拖到后面才暴露


---

# 工具视角 {#tools}
<!-- tag: source-to-tool -->

工具在这里承担可重复流程的角色。看工具时，只问它在链路里改了什么对象，不要把工具名当成结论。

资料
主要看哪一层
适合借鉴什么
暂时别扩到哪里


Stanford CS336 data materials
课程主线和对象链
从网页到语料的层次感
不要把它当作实现细节文档


DataTrove
raw files 到 document 的 pipeline
reader / filter / writer 的组织方式
不要把它和模型训练超参混在一起


NeMo Curator
document 后的策展和清理
过滤、分布式执行和数据整形的思路
不要直接把它当抽取器来讲


FineWeb
验证和数据配方案例
从数据决策回到训练效果
不要把它当成唯一 recipe


OLMo 2
来源透明和训练记录
记录字段和发布边界的写法
不要把它误读成数据处理库

## box
### 工具名的正确用法
当你看到一个框架或案例，先把它放回 document、sample、token 或 shard 的哪一层，再看它的实现。层次清楚以后，工具名只是一张索引卡，答案仍然在对象层。


---

# 故障回查 {#trace}
<!-- tag: diagnosis -->

这一段的目标很直接：看到问题时先回到哪一层。下面这张回查表，专门把症状和最先检查的对象对上，减少在模型、训练脚本或数据规模上绕路的时间。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

症状
先查什么
它读什么
它写什么
常见根因


乱码
charset_hint 和原始字节
解码前的字符提示
文本字符串
字符集猜错或解码顺序不对


正文为空
抽取规则
DOM 结构和正文密度
document.text
把导航当正文，或把正文误删了


缺 source_url
schema
记录字段定义
document 记录
设计时就没留回查入口


---

# 案例与修正 {#examples}
<!-- tag: worked case -->

同一页网页，最少会有三种结果：原始页面、错误抽取后的 document、修正后的 document。你要会区分它们，不然会把“页面本来就这样”误判成“处理已经完成”。

状态
看到什么
留下什么
下一步该查什么


原始页面
导航、正文、脚注、广告都在
网页字节和抓取记录
先确认解析和编码是否正常


错误 document
正文被裁掉，或者只剩导航
一条坏记录
回到抽取规则，不要先改过滤器


修正后 document
正文清楚，元数据完整，字符稳定
可以交给后续过滤的输入
再进入质量过滤和去重单元

## box
### 论坛页和新闻页的差别
论坛页要更看重主帖、回复、楼层和引用嵌套；新闻页更看重正文、时间、栏目和作者。抽取规则如果对两类页面一视同仁，结果通常会偏。

## box warn
### 这页的终点
做到这里，你应该能把一条网页记录变成可追溯的 document，并知道问题该回到哪一层。下一页才开始谈质量过滤、规则和偏差。


---

# 边界说明 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box info
### 这一页已经覆盖的
获取入口、正文抽取、编码标准化、元数据 schema 和回查顺序。你现在可以把 raw data 和 document 分开，也能解释为什么记录字段不能省。

## box warn
### 下一页才会继续的
质量过滤、规则模型、偏差、去重、污染控制、数据配方和 tokenization。它们都建立在这里产出的 document 之上，不应该在这页里提前展开。

如果你在实作时发现 document 里还残留大量导航和脚注，先回到正文抽取；如果字符已经错了，先回到编码标准化；如果根本回不了原页，说明 schema 没留 source_url。顺序错了，后面每一层都会替你放大错误。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-intro -->
## box soft
### 怎么答才算会
先分对象，再讲转换，再回到记录。能说出输入、输出、读写关系和一个常见故障，才算真的会。

## box info
### 检查重点
选择题看对象边界；填空题看术语是否稳定；开放题看原因、后果和回查对象。答案不用长，但要能指向下一步检查什么。

<!-- end-practice-intro -->

<!-- practice-group: 一、对象和边界 -->
<!-- quick-review: 先把对象说准，再谈流程。不要把网页、正文和训练样本混成一层。 -->

### 1 choice

**Q:** 哪一个对象最适合承接正文抽取结果并保留元数据？

- [ ] raw data
- [ ] document
- [ ] sample

Answer: document

### 2 true/false

**Q:** raw HTML 可以直接当 document 吗？

- [ ] 对
- [ ] 错

Answer: 错

### 3 mini-fields

**Q:** 把入口记录写清楚：输入形态、保留字段、输出对象。

- [ ] 输入形态
- [ ] 保留字段
- [ ] 输出对象


### 4 synthesis

**Q:** 用两句话解释 document 和 sample 的区别

Explain: 合格答案应包含三点：document 是抽取后的治理对象，sample 是训练输入；sample 可以由一个 document 切窗，也可以把多个短 document pack 到一起；不要把二者说成同一层。

<!-- end-practice-group -->

<!-- practice-group: 二、抽取和标准化 -->
<!-- quick-review: 抽取决定哪块是正文，标准化决定字符是否稳定。两步不能互相替代。 -->

### 5 choice

**Q:** 只删除 HTML 标签的问题是什么？

- [ ] 仍会把导航、脚注和广告留在文本里
- [ ] 一定能抓出正文
- [ ] 会自动修好乱码

Answer: 仍会把导航、脚注和广告留在文本里

### 6 true/false

**Q:** 错误解码后再做分词，通常能自动修回来吗？

- [ ] 对
- [ ] 错

Answer: 错

### 7 fill

**Q:** 正文抽取要找网页中的哪一块？


<!-- end-practice-group -->

<!-- practice-group: 三、元数据和 schema -->
<!-- quick-review: 能回查、能复现、能比较，是这个 schema 的三件事。 -->

### 8 choice

**Q:** 哪个字段最直接帮助回到原始页面？

- [ ] source_url
- [ ] content_hash
- [ ] extractor_version

Answer: source_url

### 9 fill

**Q:** 用于记录抽取器版本的字段通常叫什么？


### 10 synthesis

**Q:** 设计一份最小 document schema，至少写出 5 个字段。

Explain: 合格答案至少要覆盖：回到原页的字段、抓取时间、字符集、抽取版本、内容哈希。更完整的答案还会补 language、raw_format、crawl_id 或 similar fields。解释里要说清每个字段服务于什么后续动作。

<!-- end-practice-group -->

<!-- practice-group: 四、回查和迁移 -->
<!-- quick-review: 先查 charset，再查抽取，再查 schema。不要先动后面的过滤和训练参数。 -->

### 11 choice

**Q:** 看到乱码时，第一轮先查什么？

- [ ] charset_hint
- [ ] content_hash
- [ ] source_url

Answer: charset_hint

### 12 diagnosis

**Q:** 如果 document 里只剩导航和广告，你会先回查哪一步？

Explain: 先回到正文抽取，再看解析和编码是否正常。导航和广告占比过高，说明抽取规则出了问题；如果字符都错了，先修解码，再看正文。过滤器要等 document 本身可信以后再查。

### 13 choice

**Q:** 抽取后还有大量导航和脚注，下一轮最该先动哪里？

- [ ] 正文抽取
- [ ] 训练超参
- [ ] tokenizer 词表

Answer: 正文抽取

### 14 choice

**Q:** 放大批处理前，哪个条件最该先满足？

- [ ] 字段完整且异常可解释
- [ ] 模型 loss 已经下降
- [ ] 数据文件已经压缩

Answer: 字段完整且异常可解释

### 15 open

**Q:** 写一条你会放进抽样验收清单的规则。

Explain: 合格规则要可执行，例如要求 source_url、extractor_version、content_hash 全部存在；随机样本乱码率超过阈值就停止；导航块进入正文时回查抽取器版本。只写“质量要好”不算合格。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- [Stanford CS336 Spring 2025 data materials](https://cs336.stanford.edu/spring2025/index.html)，支撑课程主线、对象链和这页的边界。
- [DataTrove](https://github.com/huggingface/datatrove) 与 [NeMo Curator](https://docs.nvidia.com/nemo/curator/latest/)，支撑抽取、清理和流水线组织的工具视角。
- [FineWeb 数据集](https://huggingface.co/datasets/HuggingFaceFW/fineweb)、[FineWeb 技术博客](https://huggingfacefw-blogpost-fineweb-v1.static.hf.space/) 和 [FineWeb 论文](https://arxiv.org/abs/2406.17557)，支撑从 document 到后续验证的过渡。
- [OLMo 2](https://arxiv.org/abs/2501.00656)，支撑来源透明、记录字段和发布边界的写法。

---
