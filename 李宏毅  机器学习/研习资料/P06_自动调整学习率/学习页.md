---
course: "李宏毅机器学习"
unit: ""
title: "P06｜自动调整学习率"
kicker: "Lee ML 2025 / P06"
summary: "P04 说 loss 停住时要小心 critical point 的分类，P05 说 batch 和 momentum 可以改变优化路径。P06 继续往前追问：有时 loss 停住，gradient norm 其实还很大，这说明参数并不是“没有方向可走”，而是当前的 learning rate 让它走不好。"

side_note: "这一节的判断句：loss 不降时，先看 gradient norm；如果 gradient 仍大，问题可能是步伐尺度，而不是已经没有方向。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../P05_批次与动量/学习页.html"
  - text: "下一节"
    href: "../P07_损失函数也可能有影响/学习页.html"
---

# 本节的概念地图 {#goals}
<!-- tag: map -->

训练卡住时，很多人第一反应是“卡在 local minima 或 saddle point”。但这个判断需要证据。Critical point 的特征是 gradient 很小；如果 loss 不降但 gradient norm 仍然很大，那就说明问题更新方向和步伐让 loss 没能有效下降。

Adaptive learning rate 的大原则是：某个参数方向如果长期 gradient 大，说明这个方向陡，步伐应该小一点；某个参数方向如果长期 gradient 小，说明这个方向平，步伐应该大一点。于是原本统一的 learning rate 被改成每个参数、每个 iteration 都可能不同的有效学习率。

这条线最后会汇合到常用 optimizer。AdaGrad 用过去所有 gradient 的 root mean square 作为分母；RMSProp 让最近 gradient 更有影响力；Adam 可以粗略理解为 RMSProp 加上 momentum；learning rate schedule 则让整体 learning rate 随训练时间变化。

loss 卡住→
检查 gradient norm→
步伐尺度问题→
η / σ→
AdaGrad / RMSProp / Adam→
schedule

直觉同一双鞋不适合所有地形；同一个 learning rate 也不适合所有参数方向。
正式说法Adaptive optimizer 根据历史 gradient 统计量，对每个参数的更新量做归一化。
关键边界自动调整不等于不用调参；初始 learning rate、α、β、schedule 仍是 hyperparameter。

## box soft
### 现在你应该能做什么
把 P06 的主线概括为：先诊断是不是 critical point，再处理 learning rate 的尺度问题。


---

# 公开速读与即时自测 {#share}
<!-- tag: quick check -->

核心直觉固定 learning rate 像统一鞋码：陡峭方向需要小步，平坦方向需要大步，同一尺度很难两边都合适。
术语速查`gradient norm` 判断是否真没方向；`σ` 表示历史 gradient 尺度；`schedule` 控制整体 learning rate 随时间变化。
易混点Adaptive optimizer 不等于不用调参。Adam、RMSProp 仍需要初始 learning rate、β 或 α，以及可能的 decay / warm up。

Worked example：有效步伐
如果基础 `η = 0.1`，某参数方向的 `σ = 2`，有效步伐约是 `η/σ = 0.05`；若另一个方向 `σ = 0.2`，有效步伐约是 `0.5`。这正是“陡峭方向小步、平坦方向大步”的直觉。
检查迁移题


---

# Loss 不降，不一定是 critical point {#notcritical}
<!-- tag: gradient norm -->

Critical point 的定义来自 gradient：当 gradient 接近零，参数更新量也会接近零。可是训练曲线上的 loss 不再下降，只是一个表面现象。它可能来自 gradient 很小，也可能来自更新方向在地形里来回震荡，还可能来自 learning rate 让每一步都越过合适的位置。

因此，看到 loss plateau 时，第一个动作测量它。你可以记录 gradient norm，也就是把所有参数的 gradient 当成一个大向量，计算它的长度。如果 gradient norm 仍然很大，就不能说“没有方向了”。这时更合理的诊断是：有方向，但步伐和地形配合不好。

这也是本节与 P04 的衔接。P04 教你 gradient 为零后如何分类 critical point；P06 提醒你，很多训练停滞甚至还没到 gradient 为零那一步。真正的障碍可能在更前面：优化器没有把参数带到一个可以顺利下降的路径上。

$$
若 loss 不降但 ||∇L(θ)|| 仍大：
 问题更可能是 update step 的尺度与方向，而不是已经到达 critical point。
$$

## box soft
### 现在你应该能做什么
把“loss 停住”拆成两个问题：loss 现象是什么，gradient norm 证据是什么。


---

# 狭长谷地：固定 learning rate 的两难 {#ravine}
<!-- tag: ravine -->

想象一个只有两个参数的 error surface。它是凸的，理论上没有 local minima 陷阱，最低点清清楚楚就在谷底。但这个谷地很狭长：横向很平，纵向很陡。这样的地形会让普通 gradient descent 很痛苦。

如果 learning rate 太大，参数在陡峭方向会从一侧山壁跳到另一侧山壁，loss 不会稳定下降，gradient norm 可能还很大。如果 learning rate 调得很小，震荡是少了，但横向平缓方向几乎走不动，训练会慢到像停住。

这说明问题不是“只要把 learning rate 调小就好”。一个方向需要小步防止震荡，另一个方向需要大步加速前进。单一 learning rate 同时服务所有参数方向，本来就不够细。

小交互：固定 learning rate 在谷地里的表现
learning rate**0.55**


minimum


**步伐偏大：容易震荡**
在陡峭方向上，每一步都可能跨过谷底，从一侧山壁跳到另一侧山壁。
观察重点：太大和太小都不好，原因不一样。

## box soft
### 现在你应该能做什么
解释为什么一个固定 learning rate 无法同时满足陡峭方向和平坦方向。


---

# Adaptive learning rate：给每个参数自己的步伐 {#adaptive}
<!-- tag: η / σ -->

普通 gradient descent 对所有参数使用同一个 learning rate：`θ_i ← θ_i - η g_i`。Adaptive learning rate 把这个步伐改成 `η / σ_i^t`，也就是每个参数 `i`、每个时间 `t` 都可能有自己的分母。

`σ_i^t` 的直觉是“这个参数方向过去的 gradient 通常有多大”。如果某个方向的 gradient 长期很大，说明这个方向陡，`σ` 大，于是有效 learning rate `η / σ` 小；如果某个方向的 gradient 长期很小，说明这个方向平，`σ` 小，于是有效 learning rate 大。

注意这里的调整不是简单地“gradient 大就走大步”。恰好相反，gradient 本身已经乘在更新式里，adaptive 方法额外用 `σ` 去归一化它，避免陡峭方向走太猛，同时放大平坦方向的前进能力。

$$
θit+1 = θit - ησit git
 σ 越大，有效 learning rate 越小；σ 越小，有效 learning rate 越大。
$$

小交互：σ 如何缩放有效 learning rate
σ**0.50**
base η**0.10**
有效 η/σ0.20


**σ 小：放大步伐**
当某个方向过去 gradient 小，分母小，有效 learning rate 会变大，帮助平坦方向前进。
观察重点：σ 是用历史 gradient 统计出来的尺度。

## box soft
### 现在你应该能做什么
用 `η / σ` 解释 adaptive learning rate 的基本设计，而不是只背 optimizer 名字。


---

# AdaGrad：用过去所有 gradient 的 RMS 当分母 {#adagrad}
<!-- tag: AdaGrad -->

AdaGrad 的思路是用过去所有 gradient 的平方平均再开根号，作为 `σ`。这个量叫 root mean square，直译是均方根。它只看 gradient 的大小，不看正负方向，因为平方会把正负号去掉。

如果某个参数方向过去的 gradient 都比较小，RMS 就小，更新时分母小，有效 learning rate 大。反过来，如果某个方向过去的 gradient 都很大，RMS 就大，更新时分母大，有效 learning rate 小。这正好对应狭长谷地里的需求。

但 AdaGrad 也有问题。它把过去所有 gradient 都放进统计里，而且越积越多。某个方向如果长时间看到小 gradient，分母可能变得很小，导致后来在突然变陡的区域步伐过大；某些任务里也可能因为累计分母越来越大，使有效 learning rate 过早变小。

$$
σit = sqrt( 1t+1 Σk=0t (gik)² )
 θit+1 = θit - ησit + ε git
ε 是一个很小的数，防止分母为 0。
$$

| 点 | 解释 | 训练含义 |
| --- | --- | --- |
| 平方 | 去掉方向，只保留大小 | σ 衡量尺度，不决定往左还是往右 |
| 平均 | 把历史 gradient 汇总 | 让每个参数有自己的历史尺度 |
| 开根号 | 回到与 gradient 相近的单位 | 避免平方尺度直接进入分母过大 |

## box soft
### 现在你应该能做什么
解释 AdaGrad 的 σ 从哪里来，以及为什么它能让平坦方向走大步、陡峭方向走小步。


---

# RMSProp 与 Adam：最近的 gradient 也要有话语权 {#rmsprop}
<!-- tag: RMSProp / Adam -->

AdaGrad 默认过去所有 gradient 同等重要。但同一个参数方向在训练早期和后期可能处在完全不同的地形里：前面平，后面陡；或者前面陡，后面平。此时旧统计如果影响太重，σ 的反应会变慢。

RMSProp 用指数移动平均解决这个问题。它用一个系数 `α` 保留过去统计，再用 `1-α` 加入当前 gradient 的平方。`α` 越小，当前 gradient 影响越大，σ 反应越快；`α` 越大，历史统计影响越大，σ 更平滑。

Adam 则可以作为本节的合体版理解：它一边像 momentum 那样累计 gradient 方向，用来决定往哪里走；一边像 RMSProp 那样累计 gradient 平方，用来决定步子多大。方向和尺度不是互相抵消，因为 momentum 看正负方向，RMS 分母只看大小。

$$
RMSProp: st = α st-1 + (1-α) gt²，σt = sqrt(st)
 Adam ≈ momentum 的方向估计 + RMSProp 的尺度估计
$$

小交互：α 决定 RMSProp 对新梯度多敏感
α**0.90**
历史统计90%
当前梯度10%


**更平滑，反应较慢**
α 高时，旧统计影响大，σ 不容易被一次新的大 gradient 改变。
观察重点：RMSProp 的“动态”来自最近梯度能更新统计量。

## box soft
### 现在你应该能做什么
说清 RMSProp 相对 AdaGrad 的改进，以及 Adam 为什么不是把 momentum 和 σ 抵消掉。


---

# Learning rate schedule：整体步伐也随时间变 {#schedule}
<!-- tag: decay / warm up -->

前面的 `σ` 是每个参数自己的缩放因子，`η` 则是整体基准 learning rate。Learning rate schedule 指的是让 `η` 也随着时间变化，而不是永远固定。最常见的做法是 learning rate decay：训练早期离目标远，步子可以大；训练后期接近较好区域，步子逐渐变小，避免在附近乱跳。

另一个常见策略是 warm up：learning rate 先从小逐渐变大，再进入衰减。它听起来反直觉，因为大家常听到的是“学习率要越来越小”。但在 Adam、RMSProp 这类方法里，早期的 `σ` 统计还不稳定，直接用大步可能把参数带离合适区域。先小步走一段，相当于让优化器收集地形统计，之后再放大步伐。

Warm up 在 Transformer、BERT 相关训练里很常见，也并不是新模型时代才出现的技巧。它的理论解释仍然有讨论空间，但实作上很常用。你需要掌握的是操作意图：decay 是后期刹车，warm up 是前期先稳住统计，再逐渐加速。

小图：三种 learning rate schedule


decay
warm up + decay
constant


**Schedule 改的是整体 η**
Adaptive optimizer 的 σ 是参数级缩放；schedule 是全局基准步伐随时间变化。
两者可以同时存在，并不冲突。

## box soft
### 现在你应该能做什么
区分 decay 和 warm up：前者是训练后期减速，后者是训练初期先小步稳定。


---

# 最小代码：从 SGD 到 Adam 的参数更新 {#code}
<!-- tag: code reading -->

下面的代码用一维参数写更新逻辑，重点读懂状态变量。`grad` 决定当前方向，`v` 保存 momentum 的方向记忆，`s` 保存 gradient 平方的尺度统计，`lr_t` 是 schedule 后的整体 learning rate。

正确示例：AdaGrad 的核心更新python

```text
eps = 1e-8
s = 0.0

for t in range(num_steps):
 grad = compute_grad(theta)
 s = s + grad * grad # 累计过去所有梯度平方
 theta = theta - lr / (sqrt(s) + eps) * grad
```

正确示例：RMSProp + momentum 的骨架python

```text
eps = 1e-8
v = 0.0 # 方向记忆
s = 0.0 # 尺度统计

for t in range(num_steps):
 grad = compute_grad(theta)
 lr_t = schedule(base_lr, t)

 v = beta * v + (1 - beta) * grad # 考虑方向，有正负
 s = alpha * s + (1 - alpha) * grad * grad # 只考虑大小
 theta = theta - lr_t * v / (sqrt(s) + eps)
```

Adam 的完整实现还有 bias correction 等细节，框架会帮你处理。这里先抓主线：分子负责方向，分母负责尺度，schedule 负责整体步伐随时间变化。

错误示例：把 σ 当成方向python

```text
# 错：sigma 由 grad^2 来，已经没有正负方向，不能替代 grad 决定往哪里走
theta = theta - lr * sigma
```

## box soft
### 现在你应该能做什么
读 optimizer 代码时，能区分方向状态、尺度状态和 learning rate schedule。


---

# 常见误区和修法 {#mistakes}
<!-- tag: pitfalls -->

| 误区 | 错因 | 后果 | 修法 |
| --- | --- | --- | --- |
| loss 不降就是 critical point | 没有检查 gradient norm。 | 误判训练停滞原因。 | 先测 ||gradient||，再判断是否接近 critical point。 |
| learning rate 调小就一定解决震荡 | 只看到陡峭方向，忽略平坦方向。 | 训练可能慢到走不动。 | 考虑不同参数方向需要不同有效步伐。 |
| gradient 大就该走更大步 | 混淆 gradient 本身和 adaptive 缩放。 | 在陡峭方向更容易爆炸。 | 用 σ 把陡峭方向的有效学习率压小。 |
| AdaGrad 永远最好 | 忽略它对历史 gradient 同等对待。 | 统计反应慢，或有效学习率过早变小。 | 理解 RMSProp 为什么加入指数移动平均。 |
| Adam 的 momentum 和 σ 会互相抵消 | 没区分方向和大小。 | 无法理解 Adam 的设计。 | momentum 看正负方向，σ 看平方大小。 |
| warm up 是随便的玄学 | 只看形式，没看早期统计不稳定。 | 遇到大模型训练时不知道为什么要小步开始。 | 把 warm up 理解成先稳定统计，再逐步加速。 |


---

# 综合练习 {#practice}
<!-- tag: 18 questions -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、停滞诊断 -->
<!-- quick-review: 抓手 loss 不降要看 gradient norm。 -->

### ? 选择

**Q:** 1. 判断是否接近 critical point，应该优先检查什么？

- [ ] . validation set 名字
- [ ] . gradient norm 是否很小
- [ ] . batch 编号
- [ ] . 模型文件大小


### ? 判断

**Q:** 2. loss 不再下降时，gradient norm 一定接近零。

- [ ] 对
- [ ] 错


### ? 短答

**Q:** 3. loss 停住但 gradient norm 仍大，说明什么？


### ? 错误诊断

**Q:** 4. “训练卡住就是 local minima。”这句话哪里不严谨？


<!-- end-practice-group -->

<!-- practice-group: 二、固定学习率的两难 -->
<!-- quick-review: 规则 陡峭方向要小步，平坦方向要大步。 -->

### ? 短答

**Q:** 5. 为什么 learning rate 太大时会在谷地两壁震荡？


### ? 短答

**Q:** 6. 为什么 learning rate 太小时又走不动？


### ? 填空

**Q:** 7. Adaptive learning rate 希望给不同 ______ 不同步伐。


### ? 错误诊断

**Q:** 8. “统一 learning rate 对所有参数总是足够。”这句话哪里错？


<!-- end-practice-group -->

<!-- practice-group: 三、AdaGrad 与 RMSProp -->
<!-- quick-review: 关键词 RMS、平方、指数移动平均。 -->

### ? 选择

**Q:** 9. AdaGrad 的 σ 主要来自什么？

- [ ] . 过去 gradient 平方的均方根
- [ ] . label 的平均值
- [ ] . batch 的文件名
- [ ] . validation accuracy


### ? 短答

**Q:** 10. 为什么 gradient 小的方向有效 learning rate 会变大？


### ? 匹配

**Q:** 11. 匹配 AdaGrad / RMSProp 的差异。


### ? 短答

**Q:** 12. RMSProp 中 α 越大意味着什么？


<!-- end-practice-group -->

<!-- practice-group: 四、Adam 与 schedule -->
<!-- quick-review: 主线 分子看方向，分母看大小，schedule 改整体 η。 -->

### ? 短答

**Q:** 13. Adam 可以粗略理解成哪两类技巧的结合？


### ? 错误诊断

**Q:** 14. “momentum 和 σ 都用过去 gradient，所以会互相抵消。”这句话哪里错？


### ? 短答

**Q:** 15. Learning rate decay 想解决什么？


### ? 短答

**Q:** 16. Warm up 的一个可能解释是什么？


<!-- end-practice-group -->

<!-- practice-group: 五、表达输出 -->
<!-- quick-review: 关键词 gradient norm、AdaGrad、RMSProp、Adam、decay、warm up。 -->

### ? 代码阅读

**Q:** 17. 解释这行代码：`s = alpha*s + (1-alpha)*grad*grad`


### ? 费曼解释

**Q:** 18. 用 7-10 句话讲清楚：自动调整学习率为什么能帮助训练？


<!-- end-practice-group -->


---

