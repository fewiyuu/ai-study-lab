---
course: "李宏毅机器学习"
unit: ""
title: "P08｜浅谈机器学习原理"
kicker: "Lee ML 2025 / P08"
summary: "这一节用一个非常简单的分类器解释机器学习的底层问题：我们只有有限训练资料，却希望学到的函数能在未知世界里也表现好。参数越多为什么容易 overfitting？训练资料越多为什么有帮助？模型太简单又为什么不行？"

side_note: "这一节的判断句：更多资料让训练集更有代表性；更小的模型复杂度降低泛化风险，但也可能让理想模型本身变差。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../P07_损失函数也可能有影响/学习页.html"
  - text: "下一节"
    href: "../P09_CNN/学习页.html"
---

# 本节的概念地图 {#goals}
<!-- tag: map -->

机器学习有三个基本步骤：先选一类有未知参数的函数，再定义 loss，最后做 optimization 找出参数。这个步骤你在深度学习里也见过，只是神经网络的参数很多，loss 可微，optimization 用 gradient descent。P08 用只有一个参数的阈值模型，把这些概念变得可数、可画、可推。

关键问题是：我们真正想要的是在“所有可能资料”上的好表现，但手里只有训练集。训练集是从真实世界分布中抽样出来的。如果训练集刚好有代表性，训练出来的模型就接近理想；如果训练集偏了，模型可能在训练集上很好，在真实世界里很差，这就是 overfitting 的理论入口。

课程最后给出一个上界直觉：坏训练集出现的概率与训练资料数 `N` 下降相关，与模型可选择函数数量 `|H|` 上升相关。资料越多越稳，模型越复杂越容易挑到“只在训练集上刚好好”的函数。但模型太简单又会让理想性能本身变差。

edge complexity→
threshold H→
training loss→
h_train→
population loss→
generalization

直觉训练集是你抓到的一小批样本；真实世界是你没抓完的整体生态。
正式说法从 hypothesis set 里选出 empirical loss 最小的函数，希望它的 population loss 接近最优。
关键边界本节的上界是解释原理，不是实际项目里常直接拿来精算的工具。

## box soft
### 现在你应该能做什么
把本节主线说成：有限训练集如何代表真实世界，以及模型复杂度为什么影响泛化。


---

# 公开速读与即时自测 {#share}
<!-- tag: quick check -->

核心直觉训练集只是从真实世界抽到的一小份样本。模型在这小份样本上最优，不等于在整体分布上最优。
术语速查`D_train` 是抽样训练集；`D_all` 是理想整体；`𝓗` 是候选函数集合；`|𝓗|` 越大，可选择空间越大。
易混点模型复杂度太大容易挑中“只适合训练集”的函数；复杂度太小又可能连理想规律都表达不出来。

小检查表：泛化风险看两边

资料量 `N` 是否足够？越大越能降低抽样偶然性。
候选集合 `|𝓗|` 是否过大？越大越容易挑中偶然好函数。
模型是否太简单？太小的 `𝓗` 会带来 underfitting。
训练集是否代表未来资料？IID 假设不稳时，上界直觉也会失效。

检查迁移题


---

# 案例：用边线复杂度分宝可梦和数码宝贝 {#case}
<!-- tag: threshold classifier -->

宝可梦和数码宝贝有些角色很像，但画风也有明显差异。课程观察到：数码宝贝常常线条更复杂、细节更多；宝可梦通常更圆润、线条更简单。于是我们可以用 edge detection 把图片边线抽出来，再数白色像素数量，作为“线条复杂度”。

这个特征记作 `E(x)`。输入一张图片 `x`，`E(x)` 输出边线白色像素数。接着定义一个 threshold `H`：如果 `E(x) > H`，预测数码宝贝；如果 `E(x) ≤ H`，预测宝可梦。

这个模型非常简单，未知参数只有一个 `H`。但它已经包含机器学习的核心：我们不知道 `H` 应该设多少，需要从资料中学出来。所有可能的 `H` 组成 hypothesis set，记作大写 `𝓗`。

$$
fH(x) = { 数码宝贝，如果 E(x) > H；宝可梦，如果 E(x) ≤ H }
 𝓗 = {1, 2, 3, ..., 10000}，|𝓗| = 10000
$$

小交互：移动阈值 H 看分类边界
threshold H**4800**


简单复杂


H=4800


**中间阈值**
阈值太左会把很多宝可梦判成数码宝贝；太右会把很多数码宝贝判成宝可梦。
观察重点：学习就是从资料中选出一个合适的 H。

## box soft
### 现在你应该能做什么
用 `E(x)` 和 `H` 写出这个分类器，并指出唯一需要学习的参数。


---

# 机器学习三步：function、loss、optimization {#three}
<!-- tag: three steps -->

第一步是定义带未知参数的 function。这里的 function family 是所有阈值分类器 `f_H`，参数是 `H`。所有可能参数组成 `𝓗`，`|𝓗|` 表示可选择函数的数量，也就是模型复杂度的一种度量。

第二步是定义 loss。对一笔资料 `(x, y)`，如果 `f_H(x)` 预测错误，就给 loss 1；预测正确，就给 loss 0。把资料集里所有样本的 loss 平均起来，就是该阈值在这个资料集上的错误率。

第三步是 optimization。因为 `H` 只有 1 到 10000 共一万个可能，我们不需要 gradient descent，直接枚举所有 `H`，计算训练集错误率，选出错误率最低的 `H`。如果是神经网络，参数空间巨大且连续，才需要可微 loss 和 gradient descent。

$$
L(H, D) = 1N Σn=1N 𝟙[fH(xn) ≠ ŷn]
 htrain = argminH∈𝓗 L(H, Dtrain)
$$

| 步骤 | 本例对应 | 深度学习对应 |
| --- | --- | --- |
| Function | 阈值分类器 f_H | 神经网络结构和参数 |
| Loss | 错误率，错 1 对 0 | cross entropy、MSE 等 |
| Optimization | 枚举 10000 个 H | gradient descent / Adam |

## box soft
### 现在你应该能做什么
把一个简单规则模型也拆成机器学习三步，而不是只把机器学习等同于神经网络。


---

# 理想与现实：D_all、D_train、h_all、h_train {#ideal}
<!-- tag: generalization -->

理想情况下，我们拥有全宇宙所有宝可梦和数码宝贝，记作 `D_all`。如果能在 `D_all` 上找出错误率最低的阈值，这个阈值就是 `h_all`，也就是在当前 hypothesis set 里真正最好的选择。

现实中，我们没有 `D_all`。我们只有从真实分布中抽样出来的训练集 `D_train`。在训练集上错误率最低的阈值叫 `h_train`。它在训练集上可能很好，但我们真正关心的是它在 `D_all` 上的错误率。

这就是泛化问题。训练集 loss 低只是经验表现好；population loss 低才是真实世界表现好。Overfitting 的典型情况是 `L(h_train, D_train)` 很低，但 `L(h_train, D_all)` 很高。

$$
hall = argminH∈𝓗 L(H, Dall)
 htrain = argminH∈𝓗 L(H, Dtrain)
 目标：L(htrain, Dall) 不要比 L(hall, Dall) 差太多。
$$

## box info
### 好抽样
`D_train` 分布像 `D_all`，选出的 `h_train` 在整体上也接近理想。

## box warn
### 坏抽样
`D_train` 刚好偏掉，`h_train` 只适合训练集，在整体上表现差。

## box soft
### 现在你应该能做什么
解释为什么训练错误率可能低于理想全集错误率，但这不代表模型真的更好。


---

# 什么叫“好的训练集” {#gooddata}
<!-- tag: representative sample -->

一个训练集好，说它对所有候选函数都足够有代表性。更正式地说，对每一个 `H`，在 `D_train` 上算出的 loss 和在 `D_all` 上算出的 loss 都接近。

为什么要“对所有 H”都接近？因为训练过程会在所有候选函数里挑一个训练 loss 最小的 `h_train`。如果某些 H 在训练集上被严重低估，optimization 就可能选中它，导致真实世界表现很差。

课程给出的推导结论是：如果训练集对所有 `H` 都满足 `|L(H,D_train)-L(H,D_all)| ≤ ε`，那么用训练集选出的 `h_train` 在真实世界上的表现，就不会比理想 `h_all` 差太多。

$$
好训练集条件：对所有 H∈𝓗，|L(H,Dtrain) - L(H,Dall)| ≤ ε
 直觉：训练集像真实世界，所以不管拿哪个函数来测，结果都差不多。
$$

小图：每个点是一组训练集


蓝点：代表性好的训练集红点：抽样偏掉的训练集


**注意单位**
这里一个点一整组训练集。
坏训练集是指至少存在一个 H，让训练 loss 和真实 loss 差很多。

## box soft
### 现在你应该能做什么
用“对所有 H 都有代表性”解释好训练集，而不是只看训练集上某个模型的成绩。


---

# 坏训练集概率上界：Hoeffding + union bound {#bound}
<!-- tag: bound -->

先固定一个候选函数 `H`。对这个固定 H 来说，`L(H,D_train)` 是从真实世界样本 loss 里抽一部分求平均，`L(H,D_all)` 是整体平均。Hoeffding inequality 告诉我们：如果每笔 loss 在 0 到 1 之间，训练样本数 `N` 越大，样本平均偏离整体平均很多的概率越小。

但训练过程从很多 H 里挑。只要有一个 H 在训练集上和真实世界差很多，这组训练集就可能被“弄坏”。所以要对所有 H 做 union bound：坏训练集的概率不超过每个 H 把训练集弄坏的概率之和。

于是得到一个典型上界：坏训练集概率小于等于 `|𝓗| × 2 exp(-2Nε²)`。这个式子常常很松，实际算出来可能大于 1，项目里不一定可直接使用；但它清楚揭示了两个方向：增加资料数 `N` 会降低风险，增大模型复杂度 `|𝓗|` 会增加风险。

$$
P(坏训练集) ≤ |𝓗| · 2 exp(-2Nε²)
 N：训练资料数；|𝓗|：可选择函数数量；ε：训练 loss 与真实 loss 可容忍差距。
$$

小交互：N 和 |𝓗| 如何影响上界
训练资料 N**500**
|𝓗|**10000**
上界0.91


**上界偏松**
当 N 不够大或 |𝓗| 很大时，上界可能接近甚至超过 1，说明这个理论界限给不出强保证。
观察重点：N 增大让指数项下降，|𝓗| 增大让上界乘大。

## box soft
### 现在你应该能做什么
看到上界式子时，能指出它为什么同时包含资料量和模型复杂度。


---

# N 与 |𝓗| 的两难：过拟合和欠拟合同时存在 {#tradeoff}
<!-- tag: tradeoff -->

增加训练资料 `N` 通常有帮助，因为训练集更可能代表真实分布。问题是资料有成本，很多任务不能随便多收。课程里用很生活的方式说：你可能想先花钱收资料，但现实里预算、标注、业务约束都会卡住。

减小 `|𝓗|` 也能让坏训练集概率上界变小，因为可挑的函数少了，不容易挑到只在训练集上刚好好的函数。但模型太简单会有另一个问题：理想本身变差。也就是说，就算 `h_train` 很接近 `h_all`，如果 `h_all` 在真实世界上本来就很差，那接近它也没用。

这就是机器学习经典两难：模型太复杂，现实和理想容易差很远，容易 overfitting；模型太简单，理想本身就坏，容易 underfitting。深度学习后面要讨论的神奇之处，就是它看起来在某些情况下能用很复杂的模型，同时仍取得不错泛化。

| 做法 | 好处 | 风险 | 对应概念 |
| --- | --- | --- | --- |
| 增加 N | 训练集更有代表性 | 资料收集与标注成本高 | 降低泛化风险 |
| 减小 |𝓗| | 坏训练集概率上界变小 | 理想模型也可能很差 | 降低复杂度，但可能欠拟合 |
| 增大 |𝓗| | 可能找到更好的理想模型 | 更容易选到只适合训练集的函数 | 表达力更强，但可能过拟合 |

## box soft
### 现在你应该能做什么
解释“参数越多越可能 overfitting”背后的理论直觉，同时说明模型太简单也会出问题。


---

# 最小代码：枚举阈值并比较 train/test error {#code}
<!-- tag: code reading -->

这段代码模拟本节的阈值分类器。它先在训练集上枚举所有阈值，找出训练错误率最低的 `h_train`，再把这个阈值拿到测试集上评估。你要抓住两件事：optimization 用的是训练集，真正关心的是未见资料上的表现。

正确示例：阈值分类器训练与评估python

```text
import numpy as np

def predict(edge_count, threshold):
 return (edge_count > threshold).astype(int) # 1: 数码宝贝, 0: 宝可梦

def error_rate(edge_count, label, threshold):
 pred = predict(edge_count, threshold)
 return np.mean(pred != label)

thresholds = np.arange(1, 10001)
train_losses = [error_rate(train_edge, train_label, h) for h in thresholds]
h_train = thresholds[np.argmin(train_losses)]

train_error = error_rate(train_edge, train_label, h_train)
test_error = error_rate(test_edge, test_label, h_train)
```

错误示例：只看训练错误率就宣布成功python

```text
if train_error < 0.05:
 print("模型已经很好") # 错：还没看 test / population 表现
```

## box soft
### 现在你应该能做什么
读训练代码时，能指出哪里在做 empirical risk minimization，哪里在检查泛化。


---

# 常见误区和修法 {#mistakes}
<!-- tag: pitfalls -->

| 误区 | 错因 | 后果 | 修法 |
| --- | --- | --- | --- |
| 训练错误率低就代表模型好 | 混淆 training loss 和 population loss。 | 忽略 overfitting。 | 始终评估未见资料或测试集。 |
| h_all 在任何资料集上都最强 | 忘记 h_all 只是在 D_all 上最优。 | 无法理解训练 loss 可能低于理想全集 loss。 | 明确 loss 的第二个输入是资料集。 |
| 一个好训练集只要让某个模型表现好 | 忽略训练过程会挑 H。 | 可能被某个偏差 H 误导。 | 好训练集要对所有 H 都有代表性。 |
| 上界算出来大于 1 就说明推导错了 | 不了解 union bound 是松上界。 | 误解理论用途。 | 把它当解释原理，不当精准实用估计。 |
| 模型复杂度越小越好 | 只看泛化风险，不看表达能力。 | 理想模型本身变差，欠拟合。 | 在表达力和泛化之间取平衡。 |
| 连续参数让模型复杂度一定无限大且无从讨论 | 忽略有限精度和 VC dimension 等复杂度概念。 | 以为理论完全不能说神经网络。 | 理解 |H| 是离散例子的入口，连续模型需更细复杂度度量。 |


---

# 综合练习 {#practice}
<!-- tag: 18 questions -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、案例与三步 -->
<!-- quick-review: 抓手 模型只有一个参数 H。 -->

### ? 选择

**Q:** 1. 本节阈值分类器的未知参数是什么？

- [ ] . 图片文件名
- [ ] . threshold H
- [ ] . 学习页页码
- [ ] . 浏览器窗口大小


### ? 短答

**Q:** 2. `E(x)` 在案例中表示什么？


### ? 填空

**Q:** 3. 机器学习三步是：定义 ______、定义 ______、做 ______。


### ? 判断

**Q:** 4. 本节错误率 loss 对 H 可微，所以必须用 gradient descent。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 二、理想与现实 -->
<!-- quick-review: 符号 D_all 是真实整体，D_train 是抽样。 -->

### ? 匹配

**Q:** 5. 匹配 h_all、h_train、D_all、D_train 的含义。


### ? 短答

**Q:** 6. 为什么 h_train 在训练集上表现好还不够？


### ? 错误诊断

**Q:** 7. “h_all 在所有数据集上都是 loss 最低。”这句话哪里错？


### ? 短答

**Q:** 8. IID 抽样假设在这里是什么意思？


<!-- end-practice-group -->

<!-- practice-group: 三、好训练集与上界 -->
<!-- quick-review: 核心 对所有 H，训练 loss 和真实 loss 都接近。 -->

### ? 短答

**Q:** 9. 什么叫好的训练集？


### ? 选择

**Q:** 10. 坏训练集概率上界中，N 变大通常会怎样？

- [ ] . 上界下降
- [ ] . 上界必定上升
- [ ] . 没有任何影响
- [ ] . 让 loss 不可微


### ? 填空

**Q:** 11. `|𝓗|` 表示什么？


### ? 错误诊断

**Q:** 12. “上界算出来大于 1，说明理论是错的。”这句话哪里不稳？


<!-- end-practice-group -->

<!-- practice-group: 四、复杂度两难 -->
<!-- quick-review: 两难 大模型理想好但风险大，小模型稳但可能太弱。 -->

### ? 短答

**Q:** 13. 为什么 `|𝓗|` 大会增加 overfitting 风险？


### ? 短答

**Q:** 14. 为什么 `|𝓗|` 太小也不好？


### ? 场景判断

**Q:** 15. 训练集错误率 0，但测试集很差，可能发生了什么？


### ? 短答

**Q:** 16. 连续参数模型为什么仍可讨论复杂度？


<!-- end-practice-group -->

<!-- practice-group: 五、表达输出 -->
<!-- quick-review: 主线 N、|H|、泛化、过拟合。 -->

### ? 代码阅读

**Q:** 17. 解释最小代码中 `h_train = thresholds[np.argmin(train_losses)]` 的含义。


### ? 费曼解释

**Q:** 18. 用 8-11 句话讲清楚：为什么参数越多越可能 overfitting，但模型太简单也不行？


<!-- end-practice-group -->


---

