---
course: "李宏毅机器学习"
unit: ""
title: "P15｜Transformer（上）"
kicker: "Lee ML 2025 / P15"
summary: "Transformer 是一种 sequence-to-sequence model，简称 seq2seq。它处理的任务是：输入是一段 sequence，输出也是一段 sequence，而且输出长度由模型自己决定。语音辨识、机器翻译、语音翻译、聊天机器人、问答、摘要，甚至一些看起来不像序列输出的任务，都可以被改写成 seq2seq。"

side_note: "这一节的判断句：Transformer 是 seq2seq 模型；P15 先讲 encoder，decoder 留到下一节。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../P14_批次标准化_Batch_Normalization/学习页.html"
  - text: "下一节"
    href: "../P16_Transformer下/学习页.html"
---

# 本节的概念地图 {#goals}
<!-- tag: map -->

Seq2seq 的核心是输出长度不固定。前面 self-attention 讲过 sequence labeling：输入 N 个向量，输出 N 个 label。而 seq2seq 不要求输出和输入一样长，模型需要自己决定什么时候继续输出、什么时候停止。

Transformer 是 seq2seq 的代表模型。它通常分成 encoder 和 decoder 两块：encoder 读输入，decoder 产生输出。P15 只讲 encoder 的结构；decoder 因为涉及如何一步步生成输出，留到 P16。

Encoder 的基本功能是输入一串向量、输出一串同长度向量。这个功能 RNN、CNN、self-attention 都可以做；Transformer encoder 选择 self-attention，并用 residual、LayerNorm、feed-forward 组成可堆叠的 block。

input sequence→
encoder→
context vectors→
decoder→
output sequence

## box soft
### 现在你应该能做什么
用“输入序列、输出序列、输出长度由模型决定”一句话抓住 seq2seq。


---

# Seq2seq：输出多长由机器决定 {#seq2seq}
<!-- tag: sequence-to-sequence -->

语音辨识是典型 seq2seq。输入是一段声音讯号，可以表示成一串 frame 向量；输出是文字序列。声音长度和文字长度有关系，但没有固定比例。模型必须听内容，自己决定输出几个字。

机器翻译也是 seq2seq。中文“机器学习”有四个字，英文是两个词，但这不代表所有中英翻译都是 1/2 的长度关系。不同语言语序、词汇粒度和表达方式都不同，输出长度必须由模型根据内容决定。

语音翻译更直接：输入某种语言的声音，输出另一种语言的文字。课程用台语例子说明，很多语言文字系统不普及，先做语音辨识再翻译未必可行；直接训练声音到文字的 seq2seq 模型，虽然粗暴，却可能有效。

| 任务 | 输入 | 输出 | 为什么是 seq2seq |
| --- | --- | --- | --- |
| 语音辨识 | frame sequence | 文字 sequence | 声音长度不直接决定字数。 |
| 机器翻译 | 源语言词序列 | 目标语言词序列 | 两种语言长度和语序不同。 |
| 语音翻译 | 声音 sequence | 另一语言文字 | 中间不一定有可用文字转写。 |

## box soft
### 现在你应该能做什么
判断一个任务是否需要模型自己决定输出长度。


---

# Seq2seq 的威力：很多任务可以硬改写 {#apps}
<!-- tag: applications -->

聊天机器人可以看成 seq2seq：输入一句话，输出一句回应。训练资料可以来自大量对话，例如影视剧台词。模型学习“看到这句话，接下来应该输出什么回应”。

NLP 里很多任务可以改写成 question answering。翻译可以问“这个句子的德文翻译是什么”；摘要可以问“这段文字的摘要是什么”；情感分析可以问“这段文字是正面还是负面”。把文章和问题拼在一起作为输入，输出答案，就是一个 seq2seq 任务。

一些看起来不是序列输出的任务也能硬解。文法剖析输出的是树，但树可以用括号序列表示；multi-label classification 输出标签集合，标签数量不固定，也可以让模型依序输出标签；object detection 也可以把框和类别转成序列输出。课程强调：这不一定是每个任务的最佳专用模型，但说明 seq2seq 很通用。

好处统一输入输出形式，很多问题都能先跑起来。
代价像瑞士刀：能做很多事，但不一定是每个任务的最佳工具。
判断专用模型可能更好，seq2seq 是强大基线和统一框架。

## box soft
### 现在你应该能做什么
把一个非典型任务改写成“输入序列 → 输出序列”，同时指出这种硬解的局限。


---

# Encoder / Decoder：读进去，再生出来 {#encoder-decoder}
<!-- tag: architecture -->

一般 seq2seq model 会分成 encoder 和 decoder。Encoder 负责处理输入 sequence，把每个输入位置变成带上下文的表示。Decoder 负责根据 encoder 的结果生成输出 sequence，并决定输出多长。

P15 只讲 encoder，因为 decoder 涉及生成过程：如何从开始符号产生第一个 token，再把前面已经输出的 token 当条件继续产生下一个 token，直到输出结束。这部分留到 P16。

在 Transformer 里，encoder 的核心模块是 self-attention。它输入一串向量，输出另一串同长度向量。多个 encoder block 堆叠后，输出的每个向量都可以看作对输入 sequence 的更深层表示。

$$
Encoder: x₁, x₂, ..., xₙ → h₁, h₂, ..., hₙ
 Decoder: h₁...hₙ + previous outputs → y₁, y₂, ..., yₘ
$$

## box soft
### 现在你应该能做什么
解释为什么 P15 讲 encoder 还没有真正讲“如何生成输出”。


---

# Transformer Encoder Block 里到底有什么 {#encoder}
<!-- tag: encoder block -->

课程先给出简化版：一个 block 先做 self-attention，再做 fully connected feed-forward network。Self-attention 负责让每个位置看全序列；feed-forward network 负责对每个位置的向量做进一步非线性变换。

真实 Transformer encoder block 更复杂。输入先加 positional encoding，补上顺序资讯；经过 multi-head self-attention；attention 输出与原输入相加，形成 residual connection；再做 LayerNorm；接着进入 feed-forward network；feed-forward 输出再和它的输入做 residual；最后再做一次 LayerNorm。

Residual connection 的直觉是保留原始信息和梯度通路。LayerNorm 则让每个向量内部的维度分布更稳定。多个这样的 block 重复 N 次，就形成 Transformer encoder。

**1. input + positional encoding**让内容带上位置资讯
**2. multi-head self-attention**每个位置看完整输入
**3. add + LayerNorm**residual 后标准化
**4. feed-forward network**逐位置非线性变换
**5. add + LayerNorm**形成 block 输出

## box soft
### 现在你应该能做什么
按顺序复述一个 Transformer encoder block 的主要组件。


---

# LayerNorm：不是 BatchNorm 换个名字 {#layernorm}
<!-- tag: normalization -->

P14 刚讲过 BatchNorm：它对同一个 dimension，在一个 batch 的不同 examples 上计算 mean 和 standard deviation。LayerNorm 不看 batch，它对同一个 example 的一个向量内部，不同 dimensions 计算 mean 和 standard deviation。

这一区分非常关键。Transformer 经常处理长度可变的序列，也常在生成时一次处理一个样本或一个 token；如果依赖 batch statistics，会带来不稳定和实现问题。LayerNorm 只依赖当前向量本身，因此更适合 Transformer 的典型设置。

课程还提到，为什么 LayerNorm 放在 residual 之后，或者能否放到 block 输入处，这不是绝对定论。原始 Transformer 是一种经典设计，但后续研究提出了不同顺序，有时效果更好。

| 方法 | 统计维度 | 是否依赖 batch | 本节用途 |
| --- | --- | --- | --- |
| BatchNorm | 同一 feature dimension 的 batch 样本 | 依赖 | CNN 等场景常见。 |
| LayerNorm | 同一个向量内部的 dimensions | 不依赖 | Transformer encoder block 常用。 |

## box soft
### 现在你应该能做什么
说明 LayerNorm 和 BatchNorm 的统计对象差在哪里。


---

# 原始 Transformer 不是唯一最优答案 {#design}
<!-- tag: design choices -->

课程特别提醒：原始 Transformer 论文里的 encoder 结构很重要，但不代表它是永远最优的架构。比如 LayerNorm 放在哪里，就有后续论文专门研究。有些变体把 LayerNorm 放到 block input 处，也可能得到更好结果。

为什么不用 BatchNorm，而用 LayerNorm？这也不是一句“大家都这样做”就结束。Transformer 的 sequence setting、batch 变化、生成过程和 normalization 统计维度，都会影响选择。后续也有 PowerNorm 等变体尝试替代或改进 LayerNorm。

你应该把 Transformer encoder block 看成一个经过验证的强基线，而不是不能动的圣旨。学会原始结构之后，才有能力理解 Pre-LN、Post-LN、NormFormer 等后续设计。

## box warn
### 学习态度
先会画原始 block，再知道它不是唯一设计；先理解每个组件解决什么问题，再讨论换顺序或换 normalization。

## box soft
### 现在你应该能做什么
回答“为什么 Transformer 长这样”时，能区分经典设计、经验选择和可研究的替代方案。


---

# 最小代码：Encoder Block 的骨架 {#code}
<!-- tag: minimal code -->

下面是概念版代码，只表达 P15 的结构。真实实现还会包含 dropout、mask、multi-head 维度拆分、参数初始化等细节。关键是读出两个 residual 分支和两次 LayerNorm。

正确示例：Transformer encoder blockpython

```text
class EncoderBlock:
 def forward(self, x):
 # x: [length, dim]，已经加上 positional encoding
 attn_out = self.self_attention(x)
 x = self.layer_norm1(x + attn_out) # residual + LayerNorm

 ff_out = self.feed_forward(x)
 x = self.layer_norm2(x + ff_out) # residual + LayerNorm
 return x
```

错误示例：把 encoder block 说成只有 self-attentionpseudo

```text
# 错误理解：Transformer encoder = self-attention 一层。
encoder_output = self_attention(input)

# 修正：还要考虑 positional encoding、residual、LayerNorm、feed-forward 等结构。
```

## box soft
### 现在你应该能做什么
读一段 encoder 代码时，标出 attention 子层、FFN 子层、residual 和 LayerNorm。


---

# 常见误区与修正 {#mistakes}
<!-- tag: pitfalls -->
<!-- layout: grid2 -->

## box redsoft
### 误区 1：seq2seq 输出长度等于输入长度
错因：混淆 sequence labeling。后果：无法理解翻译和语音辨识。修法：seq2seq 的输出长度由模型决定。

## box redsoft
### 误区 2：所有任务都用 seq2seq 就最好
错因：只看到通用性。后果：忽略任务专用模型可能更强。修法：seq2seq 是强大通用框架，不一定是最佳专用解。

## box redsoft
### 误区 3：P15 已经讲完 Transformer
错因：只看到 encoder。后果：不知道 decoder 如何生成输出。修法：P15 是 encoder，P16 继续 decoder。

## box redsoft
### 误区 4：encoder block 只有 self-attention
错因：忽略真实 block。后果：看不懂论文图。修法：attention 后还有 residual、LayerNorm、FFN、residual、LayerNorm。

## box redsoft
### 误区 5：LayerNorm 和 BatchNorm 一样
错因：只看名字都有 normalization。后果：统计维度说错。修法：BN 跨 batch，LN 在单个向量内部。

## box redsoft
### 误区 6：原始论文结构不能改
错因：把经典设计当绝对最优。后果：无法理解后续 Transformer 变体。修法：先掌握原始结构，再理解改进动机。

## box soft
### 现在你应该能做什么
诊断别人是否把 seq2seq、Transformer encoder、LayerNorm 的概念混在一起。


---

# 术语与检查：先分清 seq2seq 和 encoder {#terms}
<!-- tag: terms / checklist -->

P15 容易混在一起的点有两个：seq2seq 是任务框架，Transformer 是一种模型架构；encoder 是读入模块，decoder 是生成模块。本页只把 encoder block 打稳，decoder 的生成细节留到 P16。

| 术语 | 一句话解释 | 易混点 |
| --- | --- | --- |
| Seq2seq | 输入一段序列，输出另一段长度可变的序列。 | 不是每个输入位置都必须对应一个输出标签。 |
| Encoder | 把输入序列读成一串上下文表示。 | 它不负责一步步吐出最终答案。 |
| Decoder | 根据 encoder 信息生成输出序列。 | P15 只铺垫概念，P16 才讲 masked attention 和生成。 |
| LayerNorm | 在单个样本的向量维度内做 normalization。 | 不要和跨 batch 统计的 BatchNorm 混用解释。 |

## box soft
### Worked example
语音翻译可以写成 seq2seq：输入是很多声学 frame，输出是目标语言句子，长度通常不同。Transformer encoder 先读声音特征，产生上下文向量；后续 decoder 再决定输出多少 token。

## box info
### 小检查表
- 任务是否真的需要长度可变输出？
- 讲 Transformer encoder 时是否包含 attention、FFN、residual、LayerNorm？
- 有没有把“通用可用”误说成“所有任务最优”？

## box soft
### 现在你应该能做什么
面对一个任务，先判断它是不是 seq2seq，再决定是否需要完整 encoder-decoder，而不是默认套一种模型。


---

# 综合练习 {#practice}
<!-- tag: 18 questions -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、Seq2seq 任务 -->
<!-- quick-review: 抓手 input seq → output seq。 -->

### ? 选择

**Q:** 1. 哪个任务最典型是 seq2seq？

- [ ] . 输入图片输出一个类别
- [ ] . 输入中文句子输出英文句子
- [ ] . 输入一个向量输出一个数值
- [ ] . 输入 frame 输出同长度 frame label


### ? 短答

**Q:** 2. Seq2seq 和 sequence labeling 最大差别是什么？


### ? 场景判断

**Q:** 3. 为什么语音翻译可以直接用 seq2seq 建模？


### ? 判断

**Q:** 4. Seq2seq 很通用，所以对任何任务都是最佳模型。

- [ ] 对
- [ ] 错


<!-- end-practice-group -->

<!-- practice-group: 二、应用改写 -->
<!-- quick-review: 方法 把答案也表示成文字序列。 -->

### ? 短答

**Q:** 5. Question answering 如何改写成 seq2seq？


### ? 短答

**Q:** 6. 文法剖析输出是树，为什么仍可用 seq2seq 硬解？


### ? 场景判断

**Q:** 7. Multi-label classification 为什么可以改写成 seq2seq？


### ? 错误诊断

**Q:** 8. “Multi-label 只要取分类器分数最高前三名就好。”这句话哪里不稳？


<!-- end-practice-group -->

<!-- practice-group: 三、Encoder / Decoder 与 Encoder Block -->
<!-- quick-review: 边界 P15 只讲 encoder。 -->

### ? 填空

**Q:** 9. Encoder 和 decoder 的基本分工是什么？


### ? 选择

**Q:** 10. Transformer encoder 中处理整段输入序列的核心模块是什么？

- [ ] . self-attention
- [ ] . K-means
- [ ] . 决策树
- [ ] . 只靠 softmax


### ? 排序

**Q:** 11. 按顺序写出一个 Transformer encoder block 的主要步骤。


### ? 短答

**Q:** 12. Residual connection 在 encoder block 里做什么？


<!-- end-practice-group -->

<!-- practice-group: 四、LayerNorm 与设计选择 -->
<!-- quick-review: 区分 BN 看 batch，LN 看向量内部。 -->

### ? 短答

**Q:** 13. LayerNorm 和 BatchNorm 的统计维度有什么不同？


### ? 判断

**Q:** 14. 原始 Transformer 的 encoder block 顺序就是永远最优，不能改。

- [ ] 对
- [ ] 错


### ? 短答

**Q:** 15. 为什么 Transformer encoder 还需要 positional encoding？


### ? 错误诊断

**Q:** 16. “Transformer encoder block 其实就是一层 self-attention。”这句话哪里不完整？


<!-- end-practice-group -->

<!-- practice-group: 五、表达输出 -->
<!-- quick-review: 主线 seq2seq 通用，encoder 读入。 -->

### ? 面试短答

**Q:** 17. 用一句话说明 P15 主要讲了什么。


### ? 费曼解释

**Q:** 18. 用 8-12 句话讲清楚：什么是 seq2seq，Transformer encoder block 又由什么组成？


<!-- end-practice-group -->


---

