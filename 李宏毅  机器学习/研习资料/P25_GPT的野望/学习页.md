---
course: "李宏毅机器学习"
unit: ""
title: "P25｜GPT 的野望"
kicker: "Lee ML 2025 / P25"
summary: "BERT 的核心训练任务是填空。GPT 的核心训练任务更简单直接：给前面的 token，预测下一个 token。把这个动作在大量文本上重复，模型就学会了语言续写。使用时，只要把模型刚生成的 token 接回输入，再预测下一个，就能不断产生文章。"

side_note: "本节判断句：GPT 训练时只学“根据前文预测下一个 token”，使用时却能把任务说明和例子也当成前文。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../P24_BERT奇闻轶事/学习页.html"
  - text: "下一节"
    href: "../P26_有效使用自督导式模型/学习页.html"
---

# 本节的概念地图 {#goals}
<!-- tag: map -->

GPT 的主线是自回归语言模型。训练时，模型看到 `BOS`，预测第一个 token；看到 `BOS + 第一个 token`，预测第二个 token；以此类推。每一步都是一个词表分类问题，target 来自原始句子本身，所以仍然是 self-supervised learning。

架构上，GPT 类似 Transformer decoder，但拿掉 encoder-decoder cross-attention，只保留 masked self-attention。Masked 的意思是预测当前位置时不能偷看未来 token。使用上，GPT 可以直接生成文字，也可以通过 prompt 给任务描述和范例，让模型在上下文中“领会”要做什么。

text corpus→
next token loss→
decoder LM→
generation→
prompt→
in-context learning

## box soft
### 现在你应该能做什么
把 GPT 的训练和使用都解释成“补完前文”的问题。


---

# Next-token Prediction：每一步都是分类 {#next}
<!-- tag: autoregressive -->

假设训练句子是“台湾大学”。GPT 会构造多个训练步：给 `BOS` 预测“台”；给 `BOS 台` 预测“湾”；给 `BOS 台湾` 预测“大”；给 `BOS 台湾大` 预测“学”。每一步都用 hidden vector 经过 linear layer 和 softmax，得到下一个 token 的概率分布。

这里的 label 不需要人工标注，因为下一个 token 就在原始文本里。模型训练目标是让正确下一个 token 的概率变高，通常用 cross entropy loss。大量句子不断重复这个过程，模型就学会语言的局部和长程模式。

小交互：训练步如何移动
训练步**1**


预测第一个 token
模型只看到 BOS，目标是预测“台”。未来 token 在训练目标里，但不能作为输入偷看。

$$
Training objective:
 maximize P(token_t | token_1, ..., token_{t-1})
$$

## box soft
### 现在你应该能做什么
给任意一句话，拆出 GPT 的多个 next-token 训练样本。


---

# GPT 架构：像 Decoder，但没有 Cross-attention {#decoder}
<!-- tag: decoder-only -->

Transformer decoder 原本有 masked self-attention、encoder-decoder cross-attention 和 feed-forward 等部分。GPT 类模型更像 decoder-only：没有 encoder 输入，因此也不需要 cross-attention。它只在已经生成或已经给定的前文上做 masked self-attention。

Masked self-attention 的作用是防止偷看未来。训练时整句都在内存里，但预测第 t 个 token 时，模型只能 attend 到它之前的位置。如果让它看到未来 token，next-token prediction 就会变成抄答案，训练目标失去意义。

保留Masked self-attention、feed-forward、残差、normalization。
拿掉Encoder-decoder cross-attention，因为没有 encoder 侧输入。
结果模型只能根据前文建模，天然适合生成。

## box soft
### 现在你应该能做什么
说明 GPT 为什么不能在预测时看到未来 token。


---

# 生成能力：预测一次，再接回去预测下一次 {#generate}
<!-- tag: generation -->

训练完成后，GPT 可以生成文字。给它一段 prompt，它输出下一个 token 的分布。选出一个 token 后，把这个 token 接到 prompt 后面，再预测下一个。不断重复，就能产生句子、段落甚至文章。

这也是 GPT 早期让人印象深刻的地方：模型可以续写看起来像新闻的文本。课程提到 GPT-2 的经典“独角兽假新闻”例子，说明语言模型不只是分类器，而是能生成长文本的模型。

但生成不等于真实。模型学习的是文本分布，可能产生流畅但虚构的内容。理解 GPT 时要同时看到它的强大续写能力和事实可靠性的风险。

## box soft
### 训练时
看真实前文，预测真实下一个 token。

## box warn
### 生成时
看 prompt 和自己生成的前文，继续预测；错误也可能被接着放大。

## box soft
### 现在你应该能做什么
解释为什么 next-token prediction 可以变成长文本生成。


---

# In-context Learning：不改参数，只改上下文 {#incontext}
<!-- tag: prompting -->

BERT 的典型用法是 fine-tuning：接任务头，使用标注资料更新参数。GPT 也可以这样做，但 GPT 论文中更强调一种更狂的用法：把任务说明和几个范例直接写进 prompt，不做梯度下降，不更新模型参数，让模型从上下文中推断任务格式。

Few-shot learning 指 prompt 里给几个例子；one-shot 给一个例子；zero-shot 只给任务说明不给例子。这里的 “learning” 不是传统训练，没有 optimizer，没有 parameter update。它更像考试说明：先告诉模型要做什么，再看它能不能接着补出答案。

小交互：zero / one / few-shot prompt

zero-shot
one-shot
few-shot


Zero-shot
只有任务说明，没有示范。模型要直接根据说明补出答案。

## box soft
### 现在你应该能做什么
判断一个 GPT 使用方式是在 fine-tuning，还是 in-context learning。


---

# 规模与效果：大模型让 in-context 更像样，但不万能 {#scale}
<!-- tag: scaling -->

GPT-3 论文测试了不同大小模型在多任务上的 zero-shot、one-shot、few-shot 表现。总体趋势是模型越大，平均表现越好；few-shot 通常又比 zero-shot 更强。这个结果支持了一个直觉：大规模预训练模型更容易从 prompt 里读懂任务。

但课程也提醒：不是每个任务都成功。有些任务，例如简单算术，模型可能表现出一定能力；有些需要严密逻辑推理的任务表现很差。把平均正确率从二十几提升到五十几，算不算真正“会了”，仍然见仁见智。

所以 GPT 的野望是：用同一个巨大语言模型，通过上下文完成许多任务。但入门时要记住边界：它不是传统意义上针对每个任务训练过，也不是所有任务都能可靠解决。

Zero-shot只给说明，最难。
One-shot给一个示范，格式更清楚。
Few-shot给多个示范，效果常更好，但仍不保证可靠。

## box soft
### 现在你应该能做什么
解释为什么模型变大和示范变多通常提升 in-context 表现，但不能保证所有任务成功。


---

# 文字之外：视觉和语音也能自监督 {#beyond}
<!-- tag: beyond text -->

课程最后提醒：BERT 和 GPT 只是 self-supervised learning 的一部分，而且主要属于 prediction 路线。自监督学习不只用于文字，也用于 computer vision 和 speech。视觉里有 contrastive learning、BYOL 等方法；语音里可以做“语音版 BERT”：遮住声音片段再预测，也可以做“语音版 GPT”：预测接下来会出现的语音。

文字领域有 GLUE 这样的 benchmark，可以用多任务评估预训练模型。语音领域也需要类似工具。课程提到 SUPERB：Speech processing Universal PERformance Benchmark，目标是用多个任务测试语音自监督模型的能力，包括内容、说话人、语气、语义等维度。

这一段的学习目标是扩展视野：自监督的核心不是文字，而是“能否从资料本身构造任务”。不同模态的任务形式不同，但理念相通。

TextMLM、next-token prediction。
Vision对比学习、遮挡重建、BYOL 等。
Speech遮住语音、预测未来语音、SUPERB benchmark。

## box soft
### 现在你应该能做什么
把自监督学习迁移到文字以外的资料类型来思考。


---

# 公开分享速查：一句话看懂 GPT {#share}
<!-- tag: quick reference -->
<!-- layout: grid2 -->

## box soft
### 核心直觉
GPT 的训练任务很窄：永远只问“前文之后最可能出现什么 token”。但如果前文里放入任务说明、格式示范和问题，模型的续写动作就会表现得像在做翻译、分类、摘要或问答。
所以 GPT 的强项把许多任务都改写成“把后面的文字补出来”。

## box warn
### Worked example
Prompt：`情绪分类。句子：这家店再也不去了。答案：`
模型在预测“答案：”后最合理的 token。若训练中见过大量类似语言模式，它可能续写“负面”。这就是把分类任务转成语言续写。

| 术语 | 精确定义 | 容易混的点 |
| --- | --- | --- |
| Next-token prediction | 给定前文，预测词表中下一个 token 的概率分布。 | 不是一次生成整篇文章；长文本来自逐步预测并接回上下文。 |
| Masked self-attention | 当前位置只能看自己之前的 token，不能看未来。 | 这里的 mask 是因果遮罩，不是 BERT 那种把词换成 [MASK]。 |
| In-context learning | 通过 prompt 中的说明和示例让模型完成任务，不更新参数。 | 看到 few-shot 不代表做了 fine-tuning；示例只是上下文。 |
| Zero/one/few-shot | 分别对应不给示例、给一个示例、给多个示例。 | 它们描述的是 prompt 里有多少示例，不是训练集大小。 |

## box info
### 小检查表
- 解释 GPT 时，先说训练目标，再说生成循环。
- 遇到 prompt 例子，先判断是否更新参数；不更新就是 in-context。
- 评价能力时，区分“能续写出格式正确答案”和“答案事实可靠”。


---

# 最小代码：next-token loss 与 prompt {#code}
<!-- tag: minimal code -->

下面伪代码展示 GPT 训练和 in-context 使用的差别：训练会更新参数，prompt 使用不更新参数。

正确示例：next-token trainingpseudo python

```text
tokens = [BOS, "台", "湾", "大", "学"]
hidden = gpt(tokens[:-1]) # cannot see future tokens
logits = linear(hidden)
loss = cross_entropy(logits, tokens[1:])
update(gpt.parameters, loss)
```

正确示例：in-context useprompt

```text
Translate English to French.
cat => chat
dog => chien
cheese =>
```

错误示例：把 in-context learning 当成参数更新pseudo

```text
# 错误理解：few-shot prompt 会用梯度下降更新 GPT 参数。
prompt = examples + question
loss = compute_loss(prompt)
update(gpt.parameters, loss)

# 修正：in-context learning 只改变输入上下文，不改变模型参数。
```

## box soft
### 现在你应该能做什么
看一个 GPT 工作流时，判断是否真的训练了模型参数。


---

# 常见误区与修正 {#mistakes}
<!-- tag: pitfalls -->
<!-- layout: grid2 -->

## box redsoft
### 误区 1：GPT 和 BERT 训练目标一样
错因：都属于自监督。后果：混淆填空和续写。修法：BERT 做 MLM，GPT 做 next-token prediction。

## box redsoft
### 误区 2：GPT 预测下一个 token 时可以看完整句子
错因：忘记 masked self-attention。后果：训练目标变成抄答案。修法：只能看当前位置之前的 token。

## box redsoft
### 误区 3：In-context learning 等于 fine-tuning
错因：看到 few-shot 就以为训练。后果：误解参数没有更新。修法：prompt 示例只是上下文，模型参数不变。

## box redsoft
### 误区 4：Zero-shot 表现不好代表 GPT 完全没学会语言
错因：把任务说明能力和语言模型能力混为一谈。后果：评价过度。修法：看任务、prompt、模型规模和评估方式。

## box redsoft
### 误区 5：模型越大一定会解决所有推理任务
错因：只看 scaling trend。后果：忽略逻辑推理仍可能很差。修法：区分平均提升和具体任务可靠性。

## box redsoft
### 误区 6：自监督学习只属于 NLP
错因：本节主要讲 BERT/GPT。后果：忽略视觉和语音应用。修法：抓住“从资料自身造任务”的通用核心。

## box soft
### 现在你应该能做什么
用“训练目标、注意力可见范围、使用时是否更新参数”三点区分 GPT 和其他模型。


---

# 综合练习 {#practice}
<!-- tag: 18 questions -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: A. GPT 训练目标 -->
<!-- quick-review: quick review GPT 看前文预测下一个 token；每一步都是词表分类；target 来自原文本。 -->

### ? 简答

**Q:** 1. 用自己的话解释 GPT 的 next-token prediction。


### ? 拆解

**Q:** 2. 对句子“台湾大学”，列出前两个 GPT 训练步。


### ? 选择

**Q:** 3. GPT 更像 Transformer 的哪一部分？

- [ ] Decoder-only，去掉 cross-attention。
- [ ] Encoder-only，和 BERT 完全一样。
- [ ] GAN generator。


### ? 解释

**Q:** 4. GPT 为什么需要 masked self-attention？


### ? 辨析

**Q:** 5. BERT 的 MLM 和 GPT 的 next-token prediction 有什么核心差异？


### ? 生成

**Q:** 6. 为什么预测下一个 token 的模型可以生成长文章？


<!-- end-practice-group -->

<!-- practice-group: B. In-context Learning -->
<!-- quick-review: quick review Zero-shot 只有说明；one-shot 有一个例子；few-shot 有多个例子。In-context 不做梯度更新。 -->

### ? 简答

**Q:** 7. 什么是 in-context learning？它和 fine-tuning 最大差别是什么？


### ? 选择

**Q:** 8. Prompt 里给了 5 个英法翻译例子，再让 GPT 翻译 cheese。这属于什么？

- [ ] Few-shot in-context learning。
- [ ] 从头预训练。
- [ ] WGAN 训练。


### ? 对比

**Q:** 9. Zero-shot、one-shot、few-shot 分别给模型什么信息？


### ? 诊断

**Q:** 10. 错误示例：few-shot prompt 会通过 gradient descent 更新 GPT 参数。指出问题。


### ? 解释

**Q:** 11. 为什么 GPT 可以把“Translate English to French...”当成任务说明？


### ? 边界

**Q:** 12. 为什么 in-context learning 表现提升不等于所有任务都可靠解决？


<!-- end-practice-group -->

<!-- practice-group: C. 扩展与边界 -->
<!-- quick-review: quick review BERT/GPT 只是 prediction 路线的一部分；视觉和语音也能做自监督任务。 -->

### ? 简答

**Q:** 13. GPT-3 实验中，模型规模变大和 few-shot 示例对表现有什么总体影响？


### ? 应用

**Q:** 14. 如何设计一个“语音版 BERT”的自监督任务？


### ? 应用

**Q:** 15. 如何设计一个“语音版 GPT”的自监督任务？


### ? Benchmark

**Q:** 16. SUPERB 这类 benchmark 想评估语音模型的哪些面向？


### ? 总结

**Q:** 17. 为什么说 BERT/GPT 只是 self-supervised learning 的一小块？


### ? 费曼题

**Q:** 18. 费曼题：用 260-420 字讲清 P25：GPT 如何用 next-token prediction 自监督训练，为什么像 decoder-only，如何生成文字，in-context learning 与 fine-tuning 有何不同，zero/one/few-shot 如何工作，以及自监督如何扩展到视觉和语音。


<!-- end-practice-group -->

<!-- practice-group: D. 低摩擦核对与迁移 -->
<!-- quick-review: quick review 先问“是否更新参数”，再问“任务是否被写成续写”。 -->

### ? 判断

**Q:** 19. Prompt 里给 3 个示例，但没有梯度下降。这是 fine-tuning 吗？

- [ ] 不是，这是 few-shot in-context learning。
- [ ] 是，因为有 3 个训练样本。


### ? 填空

**Q:** 20. GPT 训练时不能看未来 token，这个限制由什么机制实现？


### ? 迁移

**Q:** 21. 把“判断一句客服留言是否需要人工介入”改写成 GPT 可续写的 prompt。


### ? 迁移

**Q:** 22. 这个客服 prompt 即使能跑，仍然要检查哪两类风险？


<!-- end-practice-group -->


---

