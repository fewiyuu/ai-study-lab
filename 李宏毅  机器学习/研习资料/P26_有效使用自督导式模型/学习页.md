---
course: "李宏毅机器学习"
unit: ""
title: "P26｜有效使用自督导式模型"
kicker: "Lee ML 2025 / P26"
summary: "预训练语言模型已经有大量语言知识，但现实任务经常卡在两件事上。第一，某个下游任务的有标注资料很少，直接接分类器再微调容易过拟合。第二，模型本身太大，如果每个任务都完整保存一份微调后的模型，空间和推理成本都会很高。"

side_note: "本节判断句：少数据时，尽量把任务改写成预训练模型原本会做的形式；模型太大时，只训练少量任务专属参数。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../P25_GPT的野望/学习页.html"
  - text: "下一节"
    href: "../P27_语音与影像自督导式学习模型/学习页.html"
---

# 本节的概念地图 {#goals}
<!-- tag: MAP -->

把预训练模型用于下游任务时，最朴素的方法是 standard fine-tuning：拿掉原本的 language model head，接一个新的 classifier head，然后把整颗模型一起微调。这个方法简单，但在有标注资料很少时，新分类头需要从很少的样本里学映射；在任务很多时，每个任务都要保存一份完整参数。

prompt tuning 的想法是不要把任务改造成“新分类器问题”，而是把任务改写成语言模型熟悉的形式。例如自然语言推理可以写成“前提句。[MASK]，假设句”，再让模型填 yes、maybe、no。这个设计让下游任务更贴近预训练时的填空任务，也把人的任务理解放进 template 和 verbalizer 里。

参数高效微调继续往前走：如果整颗模型已经会很多东西，我们是否只训练一小块任务专属组件，让它改变 hidden representation 的方向，而不更新原始模型的大量参数？Adapter、LoRA、prefix tuning 和 soft prompt 都是在做这件事，只是插入位置和可训练对象不同。

pretrained LM
prompt form
少数据适配
少参数适配
多任务部署

## box soft
### 现在你应该能做什么
看到一个下游任务时，先判断它卡在“标注少”还是“每个任务参数太多”。


---

# 两个现实问题：资料少，模型大 {#problem}
<!-- tag: MOTIVATION -->
<!-- layout: grid2 -->

## box soft
### 问题一：有标注资料太少
预训练阶段可以用海量未标注文本，但下游任务往往只有几十、几百或几千笔有标注资料。若直接训练一个新的分类头，分类头参数虽然看起来不大，却可能已经多过可用样本提供的信息量。模型容易记住训练样本，而不是学到稳健规则。
少数据路线的核心做法是降低任务和预训练任务之间的差距。把任务写成自然语言提示，让模型继续做它熟悉的词表预测。

## box warn
### 问题二：每个任务保存一整颗模型
假设一个 BERT/RoBERTa 模型被微调用到四个任务上。standard fine-tuning 会得到四份完整模型参数。任务越多，存储成本越高；如果服务端要同时支持多个任务，加载和切换也会变麻烦。
少参数路线的核心做法是冻结共用预训练模型，只为每个任务保存很小的专属参数。这样部署多个任务时，可以共享同一个基础模型，再按任务切换 Adapter、LoRA 权重、prefix 或 soft prompt。

$$
共同问题：不要让下游任务重新学习已经存在的语言知识。
 少数据：减少任务形式差距；少参数：减少每个任务新增参数。
$$

## box soft
### 现在你应该能做什么
解释为什么“模型已经预训练过”不代表下游使用就没有成本。


---

# Prompt Tuning：保留 LM Head，用词表词表达标签 {#prompt}
<!-- tag: PROMPT -->

standard fine-tuning 会丢掉原本的 LM head，接上一个任务分类器。prompt tuning 不这么做。它保留 LM head，让模型继续预测词表里的 token；我们再用 verbalizer 把 token 映射回标签。比如 NLI 任务中，entailment、neutral、contradiction 可以映射成 yes、maybe、no。

这套做法需要三件东西。第一是 prompt template，也就是输入如何被改写成填空形式。第二是 pretrained LM，它负责根据上下文预测 mask 位置的词。第三是 verbalizer，它规定哪些词代表哪些标签。template 不是装饰，它把人的任务假设写进输入格式；verbalizer 也不是随便选词，因为不同词可能让模型调用到不同的预训练知识。

小交互：template 与 verbalizer 如何决定标签

NLI
影评
主题分类


负向词分数**18**
中性词分数**31**
正向词分数**82**


预测结果：entailment
当前 verbalizer 中，正向词分数最高，因此标签会落到“蕴含/肯定”一侧。观察重点标签来自词表词，而不是新接的分类器。

| 做法 | 保留 LM head 吗 | 新增分类器吗 | 适合观察什么 |
| --- | --- | --- | --- |
| standard fine-tuning | 通常不保留 | 会新增 | 任务数据足够时很直接 |
| prompt tuning | 保留 | 通常不新增 | 标注少时，借用预训练填空能力 |

## box soft
### 现在你应该能做什么
看到一个 prompt 方法时，指出 template、LM head 和 verbalizer 分别在哪里。


---

# 少数据路线：从 few-shot 到 zero-shot {#data}
<!-- tag: DATA EFFICIENT -->

少数据路线一组围绕 prompt 的使用策略。few-shot 场景下，资料可能只有十几笔。LM-BFF 这类方法会在 prompt tuning 之外加入 demonstration，也就是把少量示范例子放进输入，让模型看到“这个任务的输入和输出应该长什么样”。这和 GPT 的 in-context learning 形式相似，但这里可以配合较小的 RoBERTa 类模型做微调。

semi-supervised learning 场景下，我们有少量有标注资料和大量未标注资料。PET 的思路是设计多组 template 和 verbalizer，训练出多个 prompt-based 模型，再让这些模型给未标注资料产生伪标签。最后用原始标签和伪标签一起训练一个标准分类模型。这里的关键用多个 prompt 视角把预训练知识转成更多监督信号。

zero-shot 场景下，甚至没有目标任务的有标注样本。GPT-3 展示过只靠任务说明做推断的能力。一种解释是：预训练文本里本来就混着 FAQ、摘要、翻译、问答等任务形态，模型在预测下一个 token 时隐约学到多任务模式。更主动的做法是把 T5 等模型用多任务 prompt 形式训练，让它显式练习很多任务，再测试未见过的任务。

few-shot少量标签 + demonstration
PET多种模板产生伪标签
semi-supervised少量标签 + 大量未标注
zero-shot只给任务说明
multi-task prompt显式训练跨任务能力

LM-BFF给模型看 demonstration，让少量例子提供任务格式。它强调小一些的预训练模型也可以在 few-shot 上做得不错。
PET用多组 prompt 模型给未标注资料产出伪标签，再训练一个标准分类模型。它把未标注资料转成可用信号。
Zero-shot没有目标标签时，只能依赖模型在预训练或多任务训练中学到的任务理解能力。效果和任务、说明、模型规模都有关。

## box soft
### 现在你应该能做什么
把 LM-BFF、PET、zero-shot prompt 放在“标签数量逐渐减少”的同一条轴上解释。


---

# 少参数直觉：只学一个小的 Δh {#parameter}
<!-- tag: PARAMETER EFFICIENT -->

standard fine-tuning 更新整颗模型，本质上是在改变模型内部产生的 hidden representation。设原本预训练模型给出表示 `h`，下游任务想要的是更适合分类器或生成目标的 `h'`。如果可以让一个很小的任务模块产生变化量 `Δh`，使 `h' = h + Δh`，就不一定需要动到整颗模型。

这就是参数高效微调的共同直觉。原始 Transformer、embedding、attention 和 feed-forward 大多冻结；每个任务只训练插入的小模块或小向量。部署时，共用基础模型只存一份，任务 A 存自己的小参数，任务 B 存自己的小参数。任务切换时换小参数，而不是换整颗模型。

小交互：多个任务的存储量对比
任务数量**4**
小模块比例**2%**

方案需要保存的相对参数说明
整颗模型各存一份400%每个任务都保存完整模型
共用模型 + 小参数108%一份基础模型加每任务小组件


观察重点
任务越多，整颗模型各存一份会线性膨胀；小参数方案也会增长，但增长的是每个任务的小组件。这里的数字是相对直觉演示，用来理解成本差异。

$$
h' = h + Δh
 大模型负责通用语言知识；小模块负责把表示推向当前任务需要的方向。
$$

## box soft
### 现在你应该能做什么
用 `h`、`Δh`、`h'` 解释为什么冻结基础模型也能适配任务。


---

# 四种参数高效做法：插在哪里，训练什么 {#methods}
<!-- tag: METHODS -->

Adapter、LoRA、prefix tuning、soft prompt 都在减少任务专属参数，但它们插入的位置不同。Adapter 是在 Transformer layer 内部串接一个小模块，通常放在 attention 或 feed-forward 后面。这个小模块先降维、非线性转换、再升维，然后用残差方式给原表示加一个变化量。

LoRA 也是产生变化量，但它常以平行分支的形式加在某些线性层旁边。它把一个大的权重更新近似成两个低秩矩阵的乘积，因此只需要训练很少的矩阵参数。和串接 Adapter 相比，LoRA 的一个卖点是平行计算更容易，不一定增加明显的推理深度。

prefix tuning 把可训练的 prefix 放到每一层 self-attention 的 key/value 前面。普通 token 有 query、key、value；prefix 不需要主动 query 别人，它主要被后续 token attend 到。训练完成后，每个任务保存对应层的 prefix key/value。soft prompt 更简单，只在输入 embedding 前面插入可训练向量，不在每一层都插。

| 方法 | 插入位置 | 训练对象 | 容易混淆点 |
| --- | --- | --- | --- |
| Adapter | 每层内部，常在 attention/FFN 后 | 小型降维-升维模块和任务头 | 会让路径更深一点 |
| LoRA | 线性层旁边的低秩分支 | 低秩矩阵 A/B 和任务头 | 不是把原权重全量更新一遍 |
| prefix tuning | 每层 attention 的 key/value 前缀 | 每层 prefix 对应的向量 | prefix 通常不需要 query |
| soft prompt | 输入 embedding 前面 | 少量连续向量 | 不是人工写出的 hard prompt 词 |

## box info
### hard prompt vs soft prompt
hard prompt 是真实 token，例如“请判断这句话的情感”。它经过词表 embedding 得到向量。soft prompt 直接训练连续向量，不一定对应任何可读文字，因此更灵活，也更难人工解释。

## box warn
### prompt tuning 名字容易乱
有些论文把用自然语言模板称为 prompt tuning，有些把训练连续 prompt 向量称为 prompt tuning。读论文时不要只看名字，要看它到底在训练整颗模型、训练 LM head，还是只训练若干连续向量。

## box soft
### 现在你应该能做什么
看到一种方法时，先问两个问题：它插在哪里？训练哪些参数？


---

# Early Exit：容易的样本不用跑完整层数 {#exit}
<!-- tag: INFERENCE -->

前面的做法主要减少每个任务需要保存或训练的参数。early exit 更关心推理时间。大模型有很多层，但不是每个样本都需要走到最后一层才能判断。有些输入在前几层已经足够明显，继续计算后面的层只是增加成本。

一种做法是在每一层后面都放一个小分类器，再训练 confidence predictor 判断当前层输出是否已经足够可靠。如果第 2 层已经很有把握，就直接输出结果，后面的层不再计算；如果还不够有把握，就继续往后走。它没有把模型结构真正删掉，而是按样本动态选择走多远。

小交互：提前退出到哪一层
退出层**5/12**

指标当前值
相对计算量42%
节省计算58%


判断提示
越早退出，速度越快，但要依赖置信度判断。真正难的样本仍然应该走到后面层。

## box soft
### 现在你应该能做什么
解释 early exit 和 Adapter/LoRA 的区别：前者主要省推理计算，后者主要省任务专属参数。


---

# 公开分享速查：少数据与少参数 {#share}
<!-- tag: quick reference -->
<!-- layout: grid2 -->

## box soft
### 核心直觉
少数据时，不要急着训练一个全新的分类头，而是尽量把任务改写成语言模型本来擅长的形式：补一个词、接一句话、按示例输出格式。
少参数时，不要为每个任务保存一整套大模型，而是冻结主干，只给每个任务存一小块可训练参数。

## box warn
### Worked example
三分类情绪任务可以写成：`这条评论让人感觉 [MASK]`。Verbalizer 把 `差` 映射为负面，把 `普通` 映射为中性，把 `好` 映射为正面。
如果只有几十条标注，这种改写能复用 LM head 对词的知识；如果有几十个任务，再配合 Adapter 或 LoRA，就不用保存几十份完整模型。

| 术语 | 精确定义 | 易混点 |
| --- | --- | --- |
| Template | 把输入包装成语言模型可读的句式。 | 不是越长越好，关键是让目标词位置清楚。 |
| Verbalizer | 把标签映射到词表词，例如 positive -> great。 | 它解释 LM head 预测出的词。 |
| Soft prompt | 可训练的连续向量提示，不必对应可读文字。 | 不要把它理解成“写得更好的自然语言提示词”。 |
| Adapter / LoRA / Prefix | 冻结大模型，只训练插入模块、低秩分支或前缀向量。 | 它们主要省任务专属参数，不等于一定省推理时间。 |
| Early exit | 简单样本在中间层提前输出。 | 它主要省计算，和 LoRA 这类省存储方法不同。 |

## box info
### 选型检查表
- 标注很少：先考虑 prompt、verbalizer、demonstration 或伪标签。
- 任务很多：优先考虑 Adapter、LoRA、prefix tuning 或 soft prompt。
- 推理延迟敏感：再考虑 early exit，并为提前退出设计置信度门槛。


---

# 最小代码：看懂冻结与小参数训练 {#code}
<!-- tag: MINIMAL CODE -->

下面的伪代码只展示语义。重点哪些参数会更新，哪些参数保持冻结。

正确示例：prompt tuning 保留 LM headpseudo python

```text
text = premise + " [MASK], " + hypothesis
logits = pretrained_lm(text).mask_logits
label_scores = {
 "entailment": logits["yes"],
 "neutral": logits["maybe"],
 "contradiction": logits["no"],
}
loss = cross_entropy(label_scores, gold_label)
update(pretrained_lm.parameters, loss)
```

正确示例：只训练 LoRA 小分支pseudo python

```text
for p in base_model.parameters:
 p.requires_grad = False

output = base_linear(x) + lora_B(lora_A(x))
loss = task_loss(classifier(output), y)
update(lora_A.parameters + lora_B.parameters + classifier.parameters, loss)
```

错误示例：以为 soft prompt 是可读文字pseudo

```text
# 错误理解：soft prompt 一定能翻译成一句自然语言。
soft_prompt = ["请", "判断", "情感"]

# 修正：soft prompt 通常是连续向量，不一定对应词表中的真实 token。
soft_prompt = trainable_vectors(shape=(prompt_len, hidden_dim))
```

## box soft
### 现在你应该能做什么
读一段微调代码时，检查基础模型参数是否冻结，以及任务专属参数在哪里。


---

# 常见误区与修正 {#mistakes}
<!-- tag: PITFALLS -->
<!-- layout: grid2 -->

## box redsoft
### 误区 1：prompt tuning 一定不更新模型
错因：把自然语言 prompt、soft prompt 和 in-context use 混在一起。后果：判断训练成本时出错。修法：看是否有梯度更新，更新的是整颗模型还是少量向量。

## box redsoft
### 误区 2：verbalizer 只是翻译标签名
错因：以为 yes/no/maybe 随便选。后果：模型预测的词表概率和真实标签对不上。修法：把 verbalizer 当成标签到词表词的关键映射。

## box redsoft
### 误区 3：few-shot 就等于完全不训练
错因：看到 few-shot prompt 就联想到 GPT 的上下文学习。后果：混淆 LM-BFF 一类微调方法。修法：few-shot 只描述样本少，不自动说明是否更新参数。

## box redsoft
### 误区 4：Adapter、LoRA、prefix tuning 都只是换名字
错因：它们都训练少量参数。后果：无法判断插入位置和推理成本。修法：分别看串接模块、低秩分支、每层 key/value 前缀。

## box redsoft
### 误区 5：soft prompt 是人工写出的提示词
错因：名字里有 prompt。后果：期待它可读、可解释。修法：soft prompt 是可训练连续向量，hard prompt 才是真实 token。

## box redsoft
### 误区 6：early exit 会把大模型永久剪小
错因：把动态退出和模型压缩混淆。后果：误解部署方式。修法：模型层仍在，简单样本可提前输出，难样本继续往后走。

## box soft
### 现在你应该能做什么
用“训练对象、插入位置、推理路径”三点拆解各种高效使用方法。


---

# 综合练习 {#practice}
<!-- tag: 18 QUESTIONS -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、基础概念 -->
<!-- quick-review: 作答提示 不要只背名字，回答时带上“解决什么问题”。 -->

### ? 选择

**Q:** 1. standard fine-tuning 最典型的做法是哪一个？

- [ ] 保留 LM head，只改 verbalizer
- [ ] 丢掉 LM head，接 classifier，并更新整颗模型
- [ ] 只训练输入前面的连续向量


### ? 判断

**Q:** 2. prompt tuning 中 verbalizer 的作用是把词表词映射回任务标签。

- [ ] 对
- [ ] 错


### ? 填空

**Q:** 3. prompt tuning 至少需要三件东西：template、pretrained LM 和 ______。


### ? 填空

**Q:** 4. 参数高效微调的直觉可以写成 h' = h + ______。


<!-- end-practice-group -->

<!-- practice-group: 二、少数据路线 -->
<!-- quick-review: 作答提示 从 few-shot、semi-supervised、zero-shot 三个场景组织答案。 -->

### ? 匹配

**Q:** 5. 将方法和核心动作匹配：LM-BFF / PET / 多任务 prompt 训练。


### ? 选择

**Q:** 6. PET 为什么要训练多组 template 和 verbalizer 组合？

- [ ] 为了完全避免使用未标注资料
- [ ] 为了从多个 prompt 视角给未标注资料产生伪标签
- [ ] 为了把模型永久剪成更少层


### ? 场景

**Q:** 7. 你只有 16 笔影评标签和一个较小的 RoBERTa。请说明为什么 demonstration 可能比单独 template 更有帮助。


### ? 短答

**Q:** 8. GPT-3 为什么可能有 zero-shot 能力？请给出“预训练资料像多任务环境”的解释。


<!-- end-practice-group -->

<!-- practice-group: 三、少参数路线 -->
<!-- quick-review: 作答提示 每题都说清楚“冻结什么、训练什么”。 -->

### ? 排序

**Q:** 9. Adapter 模块内部通常经历哪些步骤？请按顺序写出：升维、降维、非线性、残差相加。


### ? 判断

**Q:** 10. LoRA 的低秩分支通常和原线性层平行相加，而不是替换原线性层。

- [ ] 对
- [ ] 错


### ? 短答

**Q:** 11. prefix tuning 为什么要把 prefix 放到 self-attention 的 key/value 一侧？


### ? 对照

**Q:** 12. 用两句话区分 hard prompt 和 soft prompt。


<!-- end-practice-group -->

<!-- practice-group: 四、错误诊断与迁移 -->
<!-- quick-review: 作答提示 错误示例题按“错在哪 / 会怎样 / 怎么改”写。 -->

### ? 错误诊断

**Q:** 13. 错误示例：有人说“soft prompt 就是把中文提示词写得更好，所以一定可读”。请诊断。


### ? 错误诊断

**Q:** 14. 错误示例：有人在少量标签任务中接了一个分类器，然后说这已经利用了 LM head 的知识。请诊断。


### ? 场景应用

**Q:** 15. 你要给 20 个不同文本分类任务部署同一个基础模型。你会优先考虑哪类方法？为什么？


### ? 面试短答

**Q:** 16. 用 2-3 句话回答：Adapter 和 LoRA 的共同点与差别是什么？


<!-- end-practice-group -->

<!-- practice-group: 五、表达输出 -->
<!-- quick-review: 作答提示 关键词：prompt、verbalizer、少数据、Adapter、LoRA、prefix、soft prompt、early exit。 -->

### ? 综合短答

**Q:** 17. early exit 和 LoRA 都让模型“更有效”，但有效的对象不同。请说明。


### ? 费曼题

**Q:** 18. 费曼题：请用 5-7 句话向刚学完 BERT/GPT 的同学解释，本节为什么分成“少数据”和“少参数”两条路线。


<!-- end-practice-group -->

<!-- practice-group: 六、低摩擦核对与迁移 -->
<!-- quick-review: 作答提示 先判断瓶颈是标注、存储还是推理延迟。 -->

### ? 选择

**Q:** 19. 只有 30 条标注文本，优先把分类任务改成 [MASK] 补词，主要解决什么问题？

- [ ] 少数据下复用预训练语言知识
- [ ] 压缩模型文件大小
- [ ] 让样本提前退出


### ? 配对

**Q:** 20. 在 Adapter、LoRA、Early Exit 中，哪一个最直接针对推理计算？


### ? 迁移

**Q:** 21. 为“投诉 / 咨询 / 表扬”三分类写一个 template 和 verbalizer。


### ? 迁移

**Q:** 22. 一个公司有 60 个相似文本任务、每天新增任务。请写出你会采用的参数高效方案和上线检查点。


<!-- end-practice-group -->


---

