---
course: "李宏毅机器学习"
unit: ""
title: "P27｜语音与影像上的自督导式学习模型"
kicker: "Lee ML 2025 / P27"
summary: "文字里的 BERT 和 GPT 已经说明：只要能从资料本身构造训练目标，就可以不靠人工标签学到有用表示。语音和影像也能这样做。给模型大量未标注声音或图片，让它学出一排 representation，再用少量标注资料接下游模型，就能做语音辨识、唤醒词、说话人任务、影像辨识、物件侦测、分割等任务。"

side_note: "本节判断句：语音和影像也能从未标注资料里造任务，但不能机械照搬文字方法，要考虑连续讯号、细节过多、负例选择和表示崩塌。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../P26_有效使用自督导式模型/学习页.html"
  - text: "下一节"
    href: "../P28_自编码器基本概念/学习页.html"
---

# 本节的概念地图 {#goals}
<!-- tag: MAP -->

自督导式学习的共同核心是：没有人工标签，也要从资料内部造出监督信号。文字中，BERT 盖住 token 让模型填空，GPT 根据前文预测下一个 token。语音中，可以盖住一段声音让模型还原，也可以根据前面的声音预测较远之后的声音。影像中，可以盖住 patch、预测 pixel、判断旋转角度，或让同一张图不同增强版本的表示接近。

语音和影像的特殊之处让方法分化成五类。第一类 generative 试着还原原始讯号或被 mask 的部分。第二类 predictive 不一定还原原讯号，而是预测旋转、相对位置、cluster id 等更容易的目标。第三类 contrastive 不直接生成东西，而是让正样本表示接近、负样本表示远离。第四类 bootstrap 避开负例，用 teacher/student 和 stop-gradient 训练。第五类 regularization 用 variance/covariance 约束防止所有表示变成一样。

未标注声音/图片
自造任务
学 representation
少量标注适配
多任务评估

## box soft
### 现在你应该能做什么
把“自督导”解释成从资料本身造题，而不是只背某一个模型名字。


---

# 从文字迁移到语音和影像 {#transfer}
<!-- tag: TRANSFER -->

文字版 BERT 的输入是一串 token，输出是一串向量，每个 token 对应一个 representation。语音版 BERT 的输入是一段声音讯号，通常先变成一排 acoustic feature，然后输出一排向量。下游语音辨识模型可以吃这些向量，把它们转成文字。若没有这种预训练表示，端到端语音辨识往往需要成千上万小时的标注声音；有了语音版 BERT，研究者会挑战用十分钟、几十分钟标注资料做适配。

影像也类似。预训练模型先在大量未标注图片上学 representation，再把表示用于影像辨识、物件侦测、语义分割、视觉导航等任务。课程中特别强调一个惊人的现象：某些影像自督导模型在下游任务上可以超过用大规模标注资料监督训练得到的基准。这并说明未标注资料中的结构本身已经很有学习价值。

文字输入 token 序列，遮住 token 或预测下一个 token，学语言表示。
语音输入连续声音 feature，遮住一段时间或预测未来 frame，学内容、说话人、情感等表示。
影像输入图片或 patch，预测变换、相对位置、cluster 或拉近增强视图，学视觉表示。

## box soft
### 现在你应该能做什么
说明“语音版 BERT”用类似训练目标学声音表示。


---

# 多任务评估：SUPERB 与影像下游任务 {#bench}
<!-- tag: BENCHMARK -->

文字领域有 GLUE 这样的 benchmark，用多个自然语言处理任务评估预训练模型。语音领域也有类似思路：SUPERB 是 Speech processing Universal PERformance Benchmark，用一系列任务测试语音自督导模型的能力。任务不仅有语音辨识，也包括 keyword spotting、说话人辨识、说话人验证、情感辨识、语音翻译、语音增强、语音分离等。

这很重要，因为一个表示如果只在单一任务上好，可能只是学到了窄线索。比如只做语音辨识，模型可能偏向内容信息；若还要做说话人验证、情感辨识和语音增强，就能检查它是否保留了更多层次的信息。影像领域也是一样，不能只看 image classification，还要看 object detection、semantic segmentation、navigation 等任务。

| 模态 | 典型任务 | 评估意义 |
| --- | --- | --- |
| 语音 | 辨识、唤醒词、说话人、情绪、翻译、增强、分离 | 检查表示是否覆盖内容、说话人、语气和语义 |
| 影像 | 分类、侦测、分割、导航 | 检查表示是否能迁移到不同视觉粒度 |
| 文字 | 分类、推理、问答、相似度 | 检查语言表示是否能支持不同文本任务 |

## box soft
### 现在你应该能做什么
解释为什么自督导模型不能只靠一个下游任务判断好坏。


---

# 生成式方法：直接还原被遮住或未来的讯号 {#generative}
<!-- tag: GENERATIVE -->

生成式方法最接近文字版 BERT/GPT。语音版 BERT 可以把声音某些片段 mask 掉，要求模型还原被盖住的部分；代表例子包括 Mockingjay。语音版 GPT 可以给前面的声音讯号，预测未来某个时间点的声音；代表例子包括 APC。影像也可以把 pixel 拉成序列，预测下一个 pixel，或 mask 一部分 pixel 再还原。

但照搬会出问题。语音 feature 相邻帧高度相似，如果只 mask 一个 frame，模型用左右邻居内插就能猜出来，学不到更高层的语音结构。所以语音中通常要 mask 一整段连续 feature，或者预测距离当前更远的未来 frame。影像和语音还包含大量低层细节，要求模型完整还原原始讯号很难，也不一定是下游任务最需要的能力。

小交互：mask 多长才有学习价值
连续 mask 长度**3**


中等难度
连续遮住几段 feature 后，模型不能只靠简单内插解决问题，较可能学到语音内容或结构。

## box soft
### 现在你应该能做什么
说明为什么语音中 mask 一个 frame 太容易，通常要 mask 一整段。


---

# 预测式方法：不还原原讯号，预测更简单的目标 {#predictive}
<!-- tag: PREDICTIVE -->

如果还原声音或图片太复杂，可以设计一个更简单但有用的预测任务。影像中常见例子是 rotation prediction：把图片旋转 0、90、180、270 度，让模型判断旋转角度。另一个例子是 context prediction：从大图切出两个 patch，让模型判断第二个 patch 在第一个 patch 的哪个方向。这些任务迫使模型理解物体方向、局部结构和空间关系。

语音也可以设计类似任务，比如给同一句话里切出的两段声音，让模型判断它们相距多远。更通用的做法是先把连续 acoustic feature 做 clustering 或 quantization，把复杂向量变成 discrete cluster id，再让模型预测被 mask 位置的 cluster id。HuBERT、BEST-RQ、DeepCluster 都属于这类思路的代表：目标预测一个简化后的符号或聚类标签。

原始讯号声音/图片细节很多
变换或聚类旋转、相对位置、cluster
预测目标角度、方向、id
学习表示抽出结构信息
下游使用分类/辨识/分割

## box info
### 好处
目标比还原原始讯号更简单，训练更稳定，也可能更贴近下游任务需要的抽象结构。

## box warn
### 风险
预测任务需要领域知识。太简单学不到东西，太偏门又可能学到和下游无关的线索。

## box soft
### 现在你应该能做什么
给一个新的声音或图片任务，尝试设计一个不用人工标签的小预测任务。


---

# 对比学习：不生成，只拉近正样本、推远负样本 {#contrastive}
<!-- tag: CONTRASTIVE -->

contrastive learning 的目标塑造 representation 空间。对同一张图片做两次 data augmentation，得到两个视图，它们被当作 positive pair，表示要接近。来自不同图片的视图被当作 negative pair，表示要远离。SimCLR 就是这一路线的经典方法，常用 random crop、color distortion、Gaussian blur 等增强，其中 random crop 往往很关键。

语音里也有类似路线，例如 CPC、wav2vec、vq-wav2vec 和 wav2vec 2.0。wav2vec 2.0 看起来复杂：前端 encoder 把声音变成表示，中间有 quantization 产生 discrete target，后端 Transformer 吃 continuous representation，在 mask 位置预测正确的 discrete target，并把若干错误 target 当作负例压低。换个角度看，它和 BERT 很像：BERT 在 mask 位置预测正确 token；wav2vec 2.0 在 mask 位置预测正确的语音离散单位，只是负例太多时用抽样近似。

小交互：负例难度

太简单
合适
太困难


太简单的负例
如果猫的负例是天空或海，模型可能只学颜色差异就能分开，表示不一定学到物体结构。

$$
对比学习的判断：positive pair 距离小；negative pair 距离大。
 难点：负例不能太简单，也不能把真实同类误当成负例强行拉开。
$$

## box soft
### 现在你应该能做什么
解释为什么 SimCLR 的数据增强和负例选择会直接影响学到的表示。


---

# Bootstrap：只用正样本，靠非对称路径避免 collapse {#bootstrap}
<!-- tag: BOOTSTRAP -->

如果不想选负例，能不能只让 positive pair 接近？直接这样做会 collapse：模型可以对所有输入输出同一个零向量，正样本当然都很接近，但表示完全没用了。bootstrap 路线试图在不用负例的情况下避免 collapse，代表方法包括 BYOL、SimSiam，以及语音/影像/文字都可用的 data2vec。

BYOL 的结构像 teacher/student。两张增强视图分别走两条路径：一边通常 stop-gradient，另一边有 predictor，并且只更新 student 路径；然后用 moving average 慢慢更新 teacher。SimSiam 进一步说明 moving average 不一定必要，但 predictor 和 stop-gradient 很关键。课程里也提醒：这类方法为什么不 collapse 仍然不是初学者能用一句话完全解释的事，但实验上这些机制确实很重要。

小交互：是否有防 collapse 机制

只有正样本
BYOL 风格
加 variance


容易 collapse
如果训练目标只要求正样本接近，模型可以把所有输入都输出同一个向量。

## box soft
### 现在你应该能做什么
说清楚 BYOL 为什么加入了非对称网络和 stop-gradient。


---

# 正则化路线：直接约束表示不要塌掉 {#regularization}
<!-- tag: REGULARIZATION -->

另一条避开负例的路线是加正则化。VICReg 的名字来自 Variance-Invariance-Covariance Regularization。invariance 是让同一图片的不同增强版本表示接近；variance 是要求一个 batch 中每个表示维度都有足够变化，避免所有样本输出同一个向量；covariance 是让不同维度减少冗余，让 latent space 被更充分使用。

课程中特别强调 variance 很关键。只有 invariance 会 collapse；加上 variance，就能强制表示在不同样本之间保持差异。covariance 可以让不同维度不要学成重复信息，但没有 covariance 时模型仍可能训练起来。Barlow Twins 和 VICReg 都属于这个方向，语音里也有类似方法。

| 约束 | 直觉 | 防止什么问题 |
| --- | --- | --- |
| Invariance | 同一样本不同增强视图要接近 | 学到增强不变的表示 |
| Variance | 不同样本在每个维度上要有足够变化 | 防止全部输出同一个向量 |
| Covariance | 不同维度之间不要高度重复 | 减少维度冗余，展开表示空间 |

## box soft
### 现在你应该能做什么
解释 VICReg 里三个词分别负责什么，而不是只背缩写。


---

# 公开分享速查：跨模态自监督怎么选 {#share}
<!-- tag: quick reference -->
<!-- layout: grid2 -->

## box soft
### 核心直觉
文字、语音、影像都可以自监督，但任务不能机械照搬。文字 token 离散，next-token 很自然；语音和影像连续且细节多，直接重建可能太难，预测太近又可能太容易。
方法演化的主线是：先尝试生成或预测，再用对比学习关注表示相似性，最后用 bootstrap 或正则化绕开负例选择。

## box warn
### Worked example
语音若只预测下一个 10ms frame，模型可能学到平滑内插，而不是语义。更好的任务可以遮住一整段，或预测更远位置的离散单位，让模型必须利用上下文和内容结构。
影像若完整重建每个像素，模型可能沉迷纹理细节；对比或增强不变性目标更容易把注意力放在物体和语义表示上。

| 方法 | 直觉 | 主要风险 |
| --- | --- | --- |
| Generative | 重建被遮住或未来的原始信号。 | 信号太细，任务太难或学到低层细节。 |
| Predictive | 预测离散单位、未来表示或较简化目标。 | 目标太近会变成平滑内插。 |
| Contrastive | 拉近正样本，推远负样本。 | 负例选错会把同类样本推开。 |
| Bootstrap | 用 teacher/student 非对称路径只学正样本。 | 若没有 stop-gradient、predictor 等设计，容易 collapse。 |
| Regularization | 用 variance/covariance 等约束保持表示展开。 | 只做 invariance 不够，仍可能所有样本输出同向量。 |

## box info
### 选择检查表
- 先问资料是离散 token、连续语音，还是高维影像。
- 再问训练目标是否太容易、太难，或会鼓励复制无关细节。
- 使用无负例方法时，必须说明靠什么机制防止 collapse。


---

# 最小代码：几类目标函数怎么长 {#code}
<!-- tag: MINIMAL CODE -->

下面伪代码只看训练目标。重点是区分“生成原讯号”“预测离散目标”“拉近/推远表示”和“防 collapse 约束”。

正确示例：语音 mask 一整段pseudo python

```text
features = acoustic_frontend(wave)
masked = mask_contiguous_span(features, span_len=8)
hidden = speech_encoder(masked)
loss = reconstruct_loss(hidden[mask_pos], features[mask_pos])
```

正确示例：对比学习目标pseudo python

```text
view1 = augment(image)
view2 = augment(image)
z1 = encoder(view1)
z2 = encoder(view2)
positive = similarity(z1, z2)
negative = similarity(z1, z_from_other_images)
loss = make_positive_high_and_negative_low(positive, negative)
```

错误示例：只拉近正样本，没有任何防 collapse 机制pseudo

```text
# 错误：模型可以对所有输入输出 zero vector。
z1 = encoder(augment(x))
z2 = encoder(augment(x))
loss = distance(z1, z2)

# 修正：加入负例，或使用 predictor + stop-gradient，或加入 variance/covariance 约束。
```

## box soft
### 现在你应该能做什么
看到一个自督导目标函数时，先判断它到底在还原、预测、对比，还是用约束防止表示塌掉。


---

# 常见误区与修正 {#mistakes}
<!-- tag: PITFALLS -->
<!-- layout: grid2 -->

## box redsoft
### 误区 1：语音版 BERT 就是把文字 BERT 直接拿来用
错因：只看名字相似。后果：忽略声音是连续 feature。修法：说成“类似 BERT 的训练目标和 encoder 思路”。

## box redsoft
### 误区 2：mask 一个语音 frame 就足够
错因：把语音当作离散 token。后果：模型可能只学内插。修法：mask 连续片段或预测更远未来。

## box redsoft
### 误区 3：生成式方法总是最好
错因：文字中 BERT/GPT 很成功。后果：忽略图片和声音细节太多。修法：根据模态选择预测简化目标或表示空间目标。

## box redsoft
### 误区 4：对比学习只要找不同图片当负例就行
错因：低估负例难度。后果：太简单学不到结构，太难可能误拉开同类。修法：关注增强策略和负例采样。

## box redsoft
### 误区 5：没有负例一定会 collapse
错因：只看到朴素正样本目标。后果：无法理解 BYOL/VICReg。修法：补上 predictor、stop-gradient、variance 等机制。

## box redsoft
### 误区 6：wav2vec 2.0 和 BERT 完全无关
错因：架构细节看起来复杂。后果：失去统一理解。修法：把它看作在 mask 位置预测正确语音离散单位的 BERT-like 目标。

## box soft
### 现在你应该能做什么
用“模态差异、训练目标、是否需要负例、防 collapse 机制”四点诊断一个方法。


---

# 综合练习 {#practice}
<!-- tag: 18 QUESTIONS -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、基础概念 -->
<!-- quick-review: 作答提示 回答时带上“输入是什么、目标是什么、输出表示怎么用”。 -->

### ? 选择

**Q:** 1. 语音版 BERT 最合理的描述是哪一个？

- [ ] 直接把文字 token 当声音输入
- [ ] 用大量未标注声音训练 encoder，输出一排语音表示
- [ ] 必须先有上万小时人工转写才能训练


### ? 判断

**Q:** 2. SUPERB 的价值在于用多个语音任务评估同一个自督导表示。

- [ ] 对
- [ ] 错


### ? 填空

**Q:** 3. 语音 feature 相邻帧很像，所以 mask 时通常要遮住一段 ______。


### ? 填空

**Q:** 4. VICReg 的三个组成是 invariance、variance 和 ______。


<!-- end-practice-group -->

<!-- practice-group: 二、五类方法 -->
<!-- quick-review: 作答提示 不要只列模型名，要说明它在让模型做什么。 -->

### ? 匹配

**Q:** 5. 将 generative / predictive / contrastive / bootstrap / regularization 分别用一句话解释。


### ? 选择

**Q:** 6. rotation prediction 和 context prediction 属于哪类方法？

- [ ] generative
- [ ] predictive
- [ ] regularization


### ? 短答

**Q:** 7. 为什么把 acoustic feature 先 clustering 成 cluster id，可能比直接还原声音更容易？


### ? 场景

**Q:** 8. 你要为无人标注的室内机器人图片训练视觉表示。请设计一个 predictive 小任务。


<!-- end-practice-group -->

<!-- practice-group: 三、对比学习与 wav2vec 2.0 -->
<!-- quick-review: 作答提示 回答时说清 positive pair、negative pair 和目标表示。 -->

### ? 短答

**Q:** 9. SimCLR 如何在没有标签的情况下构造 positive pair？


### ? 判断

**Q:** 10. 在 contrastive learning 中，负例越难越好，完全不用担心把同类样本拉开。

- [ ] 对
- [ ] 错


### ? 短答

**Q:** 11. 从 BERT 角度解释 wav2vec 2.0：它在 mask 位置预测什么？为什么要用离散单位？


### ? 对照

**Q:** 12. 用两句话区分 classification 和 contrastive learning 的相似点与差别。


<!-- end-practice-group -->

<!-- practice-group: 四、错误诊断与迁移 -->
<!-- quick-review: 作答提示 错误示例题按“错在哪 / 后果 / 修法”写。 -->

### ? 错误诊断

**Q:** 13. 错误示例：有人说“只要让同一图片两种增强的向量接近，就一定能学到好表示”。请诊断。


### ? 错误诊断

**Q:** 14. 错误示例：有人把语音中的下一个 frame 预测当成 GPT，且只预测紧邻 frame。请诊断。


### ? 场景应用

**Q:** 15. 如果你没有负例采样经验，又担心 contrastive learning 把同类拉开，你会考虑哪两类无负例方法？


### ? 面试短答

**Q:** 16. 用 2-3 句话回答：BYOL 为什么不等于朴素地去掉负例？


<!-- end-practice-group -->

<!-- practice-group: 五、表达输出 -->
<!-- quick-review: 作答提示 关键词：语音版 BERT、SUPERB、generative、predictive、contrastive、negative example、BYOL、VICReg。 -->

### ? 综合短答

**Q:** 17. 为什么影像自督导有时能超过“用同一批图片标签监督训练”的基准？请从未标注资料结构和下游迁移解释。


### ? 费曼题

**Q:** 18. 费曼题：请用 5-7 句话向刚学完 BERT/GPT 的同学解释，语音与影像自督导为什么会从生成式方法一路发展出对比、bootstrap 和正则化方法。


<!-- end-practice-group -->

<!-- practice-group: 六、低摩擦核对与迁移 -->
<!-- quick-review: 作答提示 不要只报方法名，要说它避开什么问题。 -->

### ? 判断

**Q:** 19. 只让同一样本的两种增强表示接近，是否足以保证不 collapse？

- [ ] 不足以，还需要非对称设计或正则化约束。
- [ ] 足以，因为正样本越近越好。


### ? 填空

**Q:** 20. VICReg 中最直接防止所有样本输出同一向量的是哪一项？


### ? 迁移

**Q:** 21. 你有大量未标注 X 光片，想训练可迁移表示。请设计一个自监督目标并说明为什么不用完整像素重建。


### ? 迁移

**Q:** 22. 为唤醒词系统预训练语音表示时，你会如何避免“预测任务太容易”？


<!-- end-practice-group -->


---

