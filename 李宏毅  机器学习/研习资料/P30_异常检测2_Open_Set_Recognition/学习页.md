---
course: "李宏毅机器学习"
unit: ""
title: "P30｜异常检测 2：Open Set Recognition"
kicker: "Lee ML 2025 / P30"
summary: "P30 处理的是有标签的异常检测设定。训练资料有已知类别标签，例如辛普森家庭里每张图片都标了具体人物。我们可以先训练一个分类器，让它判断输入图片是 Bart、Lisa、Homer 还是其他已知角色。问题是：测试时如果给它一张训练世界之外的图片，它能不能不要硬分到某个已知角色，而是说 unknown？"

side_note: "本节判断句：open set recognition 不只问“这是哪一类”，还要问“它是不是根本不属于任何已知类”。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../P29_异常检测1_问题定义/学习页.html"
  - text: "下一节"
    href: "../P31_异常检测3_评估指标/学习页.html"
---

# 本节的概念地图 {#goals}
<!-- tag: MAP -->

open set recognition 的训练阶段像普通分类：训练资料里只有已知类，而且每个样本都有标签。模型学会在这些已知类之间分类。测试阶段不同：输入可能来自训练时没有出现过的 unknown 类。此时模型不能只输出一个已知标签，还要给出“我是否有足够信心”的判断。

这一节用辛普森家庭角色分类器做例子。若输入是已知角色，softmax 可能给某个角色很高分，例如 0.97。若输入是其他动画人物，分数可能更平均，最高分也较低。于是我们可以把最高 softmax 分数当作 confidence score，也可以用 entropy 衡量分布平均程度。分数再和阈值比较，得到 known 或 unknown 的决策。

已知类训练资料
训练分类器
softmax 分布
confidence score
known / unknown

## box soft
### 现在你应该能做什么
把 open set recognition 解释成“分类 + 拒绝未知输入”。


---

# 有标签设定：已知类内部可以正常训练分类器 {#setting}
<!-- tag: SETTING -->

在 P29 的分类中，P30 属于“训练资料有标签”的 case。每张训练图片属于某个已知类别。以辛普森家庭为例，训练集中有 Bart、Lisa、Homer、Marge 等角色，并且每张图都标明是谁。用这些资料训练一个普通分类器没有问题，它可以在已知角色之间做多分类。

open set 的额外要求发生在测试时。测试样本可能是辛普森角色，也可能是其他动画人物、真人照片或完全不同的图。普通分类器的 softmax 一定会在已知类别中选一个最大值；open set recognition 要多加一步：如果信心不足，就拒绝把它分到任何已知类，输出 unknown。

## box info
### Closed set classification
假设测试样本一定属于训练见过的类别。模型只需要在已知类中选一个。

## box warn
### Open set recognition
测试样本可能来自未知类别。模型除了分类，还要判断是否拒绝。

## box soft
### 现在你应该能做什么
说明为什么 open set 比普通分类多了 unknown 判断。


---

# 信心分数：分类器输出不只拿来选类 {#confidence}
<!-- tag: CONFIDENCE -->

分类器通常输出一个 softmax 分布。这个分布给每个已知类别一个分数，所有分数加起来为 1。普通分类只关心分数最高的类别；open set recognition 还关心这个最高分到底有多高。如果最高分很高，模型可能比较肯定；如果最高分不高，模型可能只是被迫在已知类里选一个。

“比较肯定”不等于“一定正确”。softmax 分数不是完美概率，神经网络可能对未知输入过度自信。课程里的例子就出现过：某些非辛普森角色会被分类器以很高信心归到某个辛普森角色。这正是后续研究要改进的地方。但作为第一步，max softmax probability 是一个简单、便宜、常常值得先试的 baseline。

小交互：softmax 分布与 unknown 决策

已知角色
未知人物
过度自信

阈值**0.70**


判为 known
最高分超过阈值，模型接受这个样本属于某个已知类。

## box soft
### 现在你应该能做什么
读 softmax 输出时，不只看 argmax 类别，也看最高分是否足够高。


---

# 方法一：Max Softmax Probability {#maxprob}
<!-- tag: MAX SCORE -->

最直觉的 confidence score 是 `max p(y|x)`，也就是 softmax 分布里最大的值。如果模型看到 Homer 的图片，输出 Homer=0.97，其他类别接近 0，那么 max score 是 0.97。若阈值是 0.7，就接受为 known，并把类别设为 Homer。

如果输入是未知人物，softmax 可能比较平均，例如最高分只有 0.26。此时 max score 小于阈值，就输出 unknown。这套做法不需要改训练流程，也不需要额外模型，只要已有分类器就能试。它特别适合做第一版系统或 sanity check：如果这个 baseline 都没有跑，直接上复杂方法容易失去参照。

$$
confidence(x) = max_k p(y = k | x)
 if confidence(x) ≥ λ → known class argmax_k p(y=k|x)
 otherwise → unknown
$$

## box soft
### 现在你应该能做什么
给一组 softmax 分数，算出 max score，并按阈值判断 known/unknown。


---

# 方法二：Entropy 看分布是否平均 {#entropy}
<!-- tag: ENTROPY -->

另一个思路是看 entropy。entropy 衡量分布的平均程度。若某个类别分数接近 1，其他类别接近 0，entropy 低，表示分布尖锐；若各类别分数差不多，entropy 高，表示模型没有明显偏好。open set 中，可以把高 entropy 看作低信心。

max score 和 entropy 往往会给相近直觉：最高分高时，entropy 通常低；最高分低且分布平均时，entropy 通常高。但它们不是完全等价。max score 只看最大值，entropy 会看整个分布。实际使用时，文献里有多种 confidence 设计，课程强调这些简单方法的差异不一定特别大，关键是先有可解释的分数和阈值。

| 分布形状 | Max score | Entropy | 直觉 |
| --- | --- | --- | --- |
| 0.97, 0.01, 0.01, 0.01 | 高 | 低 | 像已知类 |
| 0.26, 0.24, 0.23, 0.27 | 低 | 高 | 模型犹豫 |
| 0.63, 0.08, 0.07, ... | 中高 | 中 | 可能过度自信，要小心 |

## box soft
### 现在你应该能做什么
用“尖锐/平均”描述 softmax 分布，并说明 entropy 和信心的关系。


---

# 阈值与 development set：λ 不是凭空来的 {#threshold}
<!-- tag: THRESHOLD -->

课程里把阈值写成 `λ`。如果 confidence score 大于 `λ`，就认为输入来自已知类；若小于 `λ`，就拒绝为 unknown。阈值不能随便拍脑袋，它通常要根据 development set 调整。development set 应该模拟你关心的测试情况：既有已知类样本，也尽量有一些可代表未知情况的样本。

阈值越高，模型越谨慎，更多样本会被拒绝为 unknown。这会减少把未知样本误收为已知类的风险，但也可能把真正已知类误拒。阈值越低，模型更容易接受样本为已知类，误拒会少一些，但未知样本更容易混进来。open set recognition 的实际表现常常取决于这个取舍。

## box warn
### 阈值太高
known 样本也可能被拒绝，使用体验会变保守。

## box redsoft
### 阈值太低
unknown 样本更容易被硬塞进已知类，安全或质量风险升高。

## box soft
### 现在你应该能做什么
说明为什么 open set 需要 development set 来选阈值。


---

# 实验观察：已知类通常高分，但会有例外 {#experiment}
<!-- tag: OBSERVATION -->

辛普森家庭例子里，分类器在已知角色测试集上准确率很高，且多数样本的 confidence score 接近 1。把其他动画人物丢进去时，多数未知样本的信心分数会低一些，于是 max score 的分布和已知类分布有明显差异。这说明简单信心分数确实能捕捉一部分 known/unknown 差异。

但也有例外。有些未知角色会被模型非常自信地归到某个已知角色，课程里提到某些非辛普森人物被高分判成 Carl。这通常与训练资料里的视觉偏差有关，例如某个角色肤色或发色不像典型黄色脸，导致未知人物容易被吸过去。这提醒我们：confidence score 有用，但不能迷信。

已知角色分数常集中在高信心区域，分类器比较确定。
多数未知分布更平均，最高分较低，容易被阈值拒绝。
例外未知有些未知样本会触发过度自信，成为 open set 的难点。

## box soft
### 现在你应该能做什么
解释为什么 max score baseline 有效果，也为什么它会失败。


---

# 简单 baseline 不等于弱方法 {#baseline}
<!-- tag: BASELINE -->

课程特别提醒：如果手上已经有分类器，先用 confidence score 做异常检测是很合理的第一步。它实现简单、解释清楚、成本低，而且在不少场景表现不差。工程上，简单方法可以帮你快速建立参照：后续复杂方法如果不能明显超过它，就未必值得引入。

当然，也有更专门的方法。比如训练分类器时就让它直接输出更可靠的 confidence，或者加入额外目标校准已知/未知边界。这类工作属于活跃研究方向。入门时先掌握本节的 baseline，再读更复杂论文会更稳，因为你知道复杂方法到底想改进什么。

## box info
### 先试 baseline
max score、entropy、阈值，能快速得到可解释系统。

## box soft
### 再看进阶方法
若 baseline 失败，再考虑专门训练 confidence 或其他 open set 方法。

## box soft
### 现在你应该能做什么
遇到有分类器的 unknown 检测任务，先写出一个 max score baseline。


---

# 公开分享速查：分类器加一道拒绝门 {#share}
<!-- tag: quick reference -->
<!-- layout: grid2 -->

## box soft
### 核心直觉
普通分类器永远会给出一个已知类；open set recognition 多问一步：这个最高分是否足够可信？不够可信，就不要硬分，输出 unknown。
因此流程是先分类，再拒绝。拒绝规则可以用 max softmax probability，也可以用 entropy。

## box warn
### Worked example
Softmax 输出 Bart 0.91、Lisa 0.04、Homer 0.05。若阈值是 0.80，系统接受 Bart；若最高分只有 0.42，系统拒绝为 unknown。
Entropy 视角则相反：分布越平均，entropy 越高，不确定性越强，越可能拒绝。

| 术语 | 精确定义 | 易混点 |
| --- | --- | --- |
| Known class | 训练时出现过且有标签的类别。 | 不是“世界上所有合法类别”。 |
| Unknown | 测试时不属于任何已知类的样本。 | 分类器不会自然有 unknown 类，必须设计拒绝机制。 |
| Max softmax probability | softmax 分布中最大的类别概率。 | 数值高不保证真的对，只是模型自信。 |
| Entropy | 衡量分布平均程度，不确定越高 entropy 越高。 | 用 entropy 时阈值方向和 max probability 常相反。 |
| Development set | 用来调阈值的验证资料，最好同时含 known 和 unknown。 | 不要用最终测试集反复调阈值。 |

## box info
### 上线检查表
- 分类器输出类别和信心分数必须分开记录。
- 阈值要在 development set 上调，不凭感觉选。
- 报告时同时写 known accuracy、unknown reject rate 和误拒情况。


---

# 最小代码：从 softmax 到 unknown {#code}
<!-- tag: MINIMAL CODE -->

下面伪代码展示 open set recognition 的基本数据流。重点是：先分类，再用信心分数决定是否拒绝。

正确示例：max probability 作为信心分数pseudo python

```text
probs = softmax(classifier(image))
confidence = max(probs)
predicted_class = argmax(probs)

if confidence >= threshold:
 label = predicted_class
else:
 label = "unknown"
```

正确示例：entropy 作为不确定性pseudo python

```text
entropy = -sum(p * log(p) for p in probs)
if entropy <= entropy_threshold:
 label = argmax(probs)
else:
 label = "unknown"
```

错误示例：永远强迫输出已知类pseudo

```text
# 错误：测试样本可能来自未知类，但模型没有拒绝机制。
probs = softmax(classifier(image))
label = argmax(probs)

# 修正：加入 confidence score 和 threshold。
```

## box soft
### 现在你应该能做什么
把任意 softmax 分类器改成带 unknown 输出的简单 open set detector。


---

# 常见误区与修正 {#mistakes}
<!-- tag: PITFALLS -->
<!-- layout: grid2 -->

## box redsoft
### 误区 1：分类器最高分就是可靠概率
错因：把 softmax 分数当校准过的真实概率。后果：未知样本可能被高信心误收。修法：把它当 baseline 分数，并用验证集评估。

## box redsoft
### 误区 2：open set 只要 argmax 就够
错因：沿用 closed set 分类习惯。后果：未知样本被强塞到已知类。修法：加入拒绝阈值。

## box redsoft
### 误区 3：阈值可以随便定
错因：忽略 known/unknown 代价。后果：误拒或误收不可控。修法：用 development set 调整。

## box redsoft
### 误区 4：unknown 分数低说明模型一定错
错因：混淆分类正确性和开放集拒绝。后果：把低信心已知样本都当异常。修法：看阈值取舍和验证结果。

## box redsoft
### 误区 5：entropy 和 max score 完全一样
错因：都反映信心。后果：忽略 entropy 看整个分布。修法：理解一个看最大值，一个看分布形状。

## box redsoft
### 误区 6：简单 baseline 没必要做
错因：以为简单就弱。后果：复杂方法缺少参照。修法：先跑 max score/entropy，再谈改进。

## box soft
### 现在你应该能做什么
用“softmax 分布、信心分数、阈值、拒绝机制”四点检查 open set 方案。


---

# 综合练习 {#practice}
<!-- tag: 18 QUESTIONS -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、基础概念 -->
<!-- quick-review: 作答提示 回答时明确训练类别和测试 unknown。 -->

### ? 选择

**Q:** 1. open set recognition 相比 closed set classification 多了什么要求？

- [ ] 必须输出更多已知类别
- [ ] 遇到未知类时可以拒绝并输出 unknown
- [ ] 训练时不能有标签


### ? 判断

**Q:** 2. P30 的设定中，训练资料有已知类别标签，所以可以先训练普通分类器。

- [ ] 对
- [ ] 错


### ? 填空

**Q:** 3. 若 softmax 输出为 [0.1, 0.7, 0.2]，max probability confidence 是 ______。


### ? 短答

**Q:** 4. 为什么 softmax 分布很平均时，模型信心通常较低？


<!-- end-practice-group -->

<!-- practice-group: 二、分数与阈值 -->
<!-- quick-review: 作答提示 先算 score，再和 threshold 比。 -->

### ? 计算

**Q:** 5. 阈值 λ=0.8。softmax=[0.76,0.12,0.08,0.04]，应该判为已知类还是 unknown？为什么？


### ? 选择

**Q:** 6. entropy 较高通常表示什么？

- [ ] 分布更平均，模型更不确定
- [ ] 模型一定分类正确
- [ ] 最高分一定是 1


### ? 短答

**Q:** 7. 阈值太高和太低分别会带来什么风险？


### ? 场景

**Q:** 8. 你有一个动物分类器，只认识猫狗马。测试时可能出现飞机。请写出 open set 决策流程。


<!-- end-practice-group -->

<!-- practice-group: 三、实验观察与 baseline -->
<!-- quick-review: 作答提示 不要把 baseline 写成“完美方法”。 -->

### ? 短答

**Q:** 9. 辛普森家庭实验中，为什么已知角色的 confidence score 往往接近 1？


### ? 判断

**Q:** 10. 如果未知样本 max score 很高，说明 max score baseline 永远没用。

- [ ] 对
- [ ] 错


### ? 短答

**Q:** 11. 为什么课程建议有现成分类器时先试 confidence score baseline？


### ? 对照

**Q:** 12. 用两句话区分 max probability 和 entropy 作为信心指标。


<!-- end-practice-group -->

<!-- practice-group: 四、错误诊断与迁移 -->
<!-- quick-review: 作答提示 错误示例题按“错在哪 / 后果 / 修法”写。 -->

### ? 错误诊断

**Q:** 13. 错误示例：有人说“softmax 最大值是 0.99，所以这个未知样本一定属于该类”。请诊断。


### ? 错误诊断

**Q:** 14. 错误示例：有人做 open set，但代码永远 label=argmax(probs)。请诊断。


### ? 场景应用

**Q:** 15. 你在工厂缺陷检测中有 5 类已知正常零件状态，测试可能出现新缺陷。请设计 confidence score 方案。


### ? 面试短答

**Q:** 16. 用 2-3 句话回答：为什么 development set 对 open set recognition 很重要？


<!-- end-practice-group -->

<!-- practice-group: 五、表达输出 -->
<!-- quick-review: 作答提示 关键词：classifier、softmax、confidence、max probability、entropy、threshold、unknown、baseline。 -->

### ? 综合短答

**Q:** 17. 请说明“简单 baseline 不等于弱方法”在 P30 中指什么。


### ? 费曼题

**Q:** 18. 费曼题：请用 5-7 句话向会训练分类器的人解释，如何把已知类分类器改造成 open set detector。


<!-- end-practice-group -->

<!-- practice-group: 六、低摩擦核对与迁移 -->
<!-- quick-review: 作答提示 看清分数方向：max 越低越拒绝，entropy 越高越拒绝。 -->

### ? 计算

**Q:** 19. 最高 softmax 是 0.58，阈值是 0.70。应输出已知类还是 unknown？

- [ ] unknown，因为最高分低于阈值。
- [ ] 已知类，因为 argmax 总能给类别。


### ? 判断

**Q:** 20. 如果用 entropy 做 unknown 分数，entropy 越高通常表示越确定吗？


### ? 迁移

**Q:** 21. 电商模型只认识 12 个商品类，但会遇到新类目。请设计一个 max probability baseline。


### ? 迁移

**Q:** 22. 这个 baseline 的 development set 应该包含哪些样本？为什么？


<!-- end-practice-group -->


---

