---
course: "李宏毅机器学习"
unit: ""
title: "P44｜领域自适应概述"
kicker: "Lee ML 2025 / P44"
summary: "在许多 benchmark 里，训练资料和测试资料来自同一分布，所以模型看起来很强。但真实部署常常不是这样：实验室里的手写数字是黑白的，用户上传的数字可能是彩色的；训练资料来自干净环境，实际输入来自噪声场景。训练和测试分布一变，准确率可能从 99% 直接掉到不及格。"

side_note: "本节判断句：source 上高分，只说明模型适应 source；能否用到 target，要看分布差异和可用 target 信息。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../P43_NLP模仿攻击与后门攻击/学习页.html"
  - text: "下一节"
    href: "../P45_BERT三个故事/学习页.html"
---

# 本节的概念地图 {#goals}
<!-- tag: MAP -->

这一节建立领域自适应的思考框架。你要先问：训练资料来自哪里？测试资料来自哪里？target 上有多少资料？有没有标签？source 和 target 的类别集合是否一致？这些问题决定后面能不能做适应，以及该用多强的假设。

课程先用黑白数字到彩色数字的例子说明：输入分布变化就足以让模型崩掉。接着引入 source / target 的术语，再讲无标注 target data 的典型路线：把模型切成 feature extractor 和 label predictor，并用 domain classifier 推动两个领域的 feature 对齐。

训练分布
测试分布
资料条件
特征对齐
分类保持
边界检查

## box soft
### 现在你应该能做什么
把领域自适应理解为：让 source 学到的可迁移部分能服务 target。


---

# Domain Shift：训练资料和测试资料不再同分布 {#shift}
<!-- tag: SHIFT -->

domain shift 指训练资料和测试资料的分布不同。最直观的例子是：训练时数字图片是黑白的，测试时变成彩色的。人类会说“数字形状没变”，但模型可能学到许多 source domain 的细节，例如背景、灰阶、笔画粗细、颜色通道统计。一旦这些统计变了，模型就可能失败。

这也是 benchmark 和真实部署之间的差距。benchmark 通常默认 train / test 来自同一分布；真实系统上线后，使用者、设备、场景、语言风格、光照、地区都会改变输入分布。模型在 source test 上高分，不代表它能处理 target。

分布差异45
source 表现99
target 表现75
调节分布差异，观察 target accuracy 如何下降。


domain shift45
直觉：source accuracy 只是“在原环境里做得好”。分布差异越大，直接套用到 target 的风险越高。

**现在你应该能做什么：**看到模型部署问题时，先问 train 和 test 是否真的来自同一分布。


---

# Domain Shift 不只一种 {#types}
<!-- tag: TYPES -->

课程中提醒，分布变化有多种。P44 主要关注输入分布变化，也就是图片外观、文字风格、语音环境等 `x` 的分布变了。但还有其他可能：输出标签比例变了，或者输入和标签之间的关系变了。

为什么要区分？因为不同 shift 需要不同方法。若只是输入外观变了，学 domain-invariant feature 可能有帮助；若 target 的类别先验比例变了，你可能需要校准输出分布；若 `x → y` 的关系都变了，简单对齐 feature 可能反而有害。

| 变化类型 | 直觉例子 | 风险 |
| --- | --- | --- |
| 输入分布变化 | 黑白数字训练，彩色数字测试。 | 模型依赖 source 外观统计。 |
| 输出分布变化 | 训练集中 0-9 均衡，部署中某些数字更常见。 | 预测校准和阈值可能失准。 |
| 输入-标签关系变化 | 同样图案在两个场景中代表不同标签。 | source 规则本身可能不能迁移。 |

**现在你应该能做什么：**不要把所有分布变化都叫成同一种问题；先判断变的是输入、输出，还是关系。


---

# Target 资料有多少，决定问题难度 {#settings}
<!-- tag: SETTINGS -->

领域自适应不是一个固定场景。关键是 target domain 上有什么资料。若 target 有很多标注资料，那最直接的做法就是用 target 资料训练，不必绕远路。若 target 只有少量标注资料，可以从 source model 出发做 fine-tuning，但要防止 overfitting。

P44 主要关注的是：target 有大量资料，但没有标签。这很符合真实系统：模型上线后可以收集用户输入，但很难为每个输入标注正确答案。于是我们不能直接用 supervised loss，却可以利用 target 输入分布来调整 feature space。

很多 target 标注直接用 target 训练或混合训练，问题相对简单。
少量 target 标注fine-tuning；小学习率、少轮数、正则化，避免过拟合。
很多 target 无标注无标注领域自适应：用 target 输入分布帮助学习表示。

如果 target 无标签且资料很少，甚至只有一个样本，常规对齐方法就很难工作；这会引出 testing-time training 等方向。如果 target 资料完全未知，就更接近 domain generalization。

**现在你应该能做什么：**拿到一个迁移问题时，先用“target 有多少资料、有没有标签”给它分类。


---

# 核心直觉：保留共同因素，去掉领域外观 {#intuition}
<!-- tag: INVARIANT FEATURE -->

在黑白数字到彩色数字的例子里，我们希望模型忽略颜色，只保留数字形状。更一般地，feature extractor 应该把 source 和 target 的表面差异过滤掉，抽取任务真正需要的共同因素。

如果 source image 和 target image 经过 feature extractor 后落在相似分布里，那么 source 上训练出的 label predictor 就更有机会应用到 target。注意这里说的是“更有机会”，不是保证。因为 feature 对齐还必须保留类别结构：0 还是要像 0，1 还是要像 1，不能为了对齐而把类别信息洗掉。

**source feature**target feature黑线代表分类边界示意

**现在你应该能做什么：**解释 domain-invariant feature 的目标：让领域不可分，但类别仍可分。


---

# 把分类器拆成两段：Feature Extractor + Label Predictor {#parts}
<!-- tag: MODEL PARTS -->

一个普通分类器可以拆成前半段和后半段。前半段叫 feature extractor，把输入图片或文本转成 feature vector；后半段叫 label predictor，把 feature vector 转成类别预测。CNN 里前几层输出可能是 feature map，拉平或池化后也可以看作 feature。

拆在哪里是超参数。你可以把浅层当 feature，也可以把更深层当 feature。浅层通常更接近边缘、颜色、纹理等低阶统计；深层更接近语义类别。领域自适应选择哪一层对齐，会影响最终效果。

**输入 x**source 有标签，target 可能无标签。
**Feature extractor**输出 feature vector 或 feature map。
**Label predictor**用 source label 训练分类。
**Domain classifier**判断 feature 来自 source 还是 target。

source data 可以训练整条分类链，因为它有标签。target data 没有标签，不能算分类 loss，但可以送过 feature extractor，再让 domain classifier 判断它来自哪个领域。这个二元分类任务就给了 target data 使用价值。

**现在你应该能做什么：**读懂领域对抗训练图时，知道每个模块吃什么、输出什么、用哪种资料训练。


---

# Domain Adversarial Training：让领域分类器分不出来 {#dat}
<!-- tag: DAT -->

domain adversarial training 的想法很像一个小型对抗游戏。domain classifier 想分辨 feature 来自 source 还是 target；feature extractor 则想骗过 domain classifier，让两个领域的 feature 混在一起。与此同时，label predictor 仍要在 source 上把类别分对。

如果只让 feature extractor 骗 domain classifier，它可以输出全零向量，让任何领域都不可分。但这样 label predictor 也无法分类。所以 source classification loss 是必要约束：feature 必须既能服务分类，又能消除领域差异。

$$
minF,P Lcls(P(F(xs)), ys) + adversarial alignment
F 是 feature extractor，P 是 label predictor。source 有标签，用来训练分类；source 和 target 都可用于领域对齐。
$$

$$
domain classifier: min Ld,   feature extractor: make domain hard to predict
实际实现常用 gradient reversal layer：前向不变，反向把 domain loss 的梯度乘上负系数。
$$

这里容易误解的一点是：不是让 source 变成 target，也让提取出的表示不再携带明显领域信息。理想状态下，domain classifier 只能瞎猜，但 label predictor 仍能正确分辨类别。

**现在你应该能做什么：**用“领域不可分、类别仍可分”概括 domain adversarial training。


---

# 动手观察：对齐太强，类别结构会被压坏 {#tradeoff}
<!-- tag: TRADEOFF -->

领域对齐不是越强越好。如果 alignment 权重太弱，target feature 仍和 source 分开，target 表现不好；如果权重太强，模型可能为了骗过 domain classifier，牺牲 label predictor 需要的类别结构。

领域不可分0
类别可分0
target 表现0
调节对齐权重，观察三者如何相互牵制。


对齐权重48
直觉：适中时能减少领域差异；过大时可能把有用特征也抹掉。

这也是课程中同学提问的重点：feature map 或 latent embedding 被硬拉在一起，会不会破坏原本空间结构？答案是有可能。因此实际训练要调 loss 权重、对齐层位置、模型容量，还要观察 target 表现，而不是只看 domain classifier 是否被成功骗过。

**现在你应该能做什么：**解释为什么 domain classifier 分不出来只是中间目标，不是最终成功标准。


---

# 方法限制：对齐分布不等于对齐类别 {#limits}
<!-- tag: LIMITS -->

无标注 target data 的困难在于：我们知道它来自 target，但不知道每个点属于什么类别。若只要求 source feature 和 target feature 的整体分布接近，可能出现错误对齐。比如 target 的 1 被拉到 source 的 7 附近，整体看起来混合了，但分类会错。

一个常见补充想法是让 target 样本远离分类边界。虽然不知道 target label，但希望 label predictor 输出足够集中，不要每一类概率都差不多。这类思想和 entropy minimization、DIRT-T、maximum classifier discrepancy 等方法有关。P44 只做概述，后续可以再深入。

另一个限制是类别集合未必一致。source 可能有老虎、狮子、狗，target 可能只有狗和猫，或者两边各有独特类别。若强行把所有分布完全对齐，就可能把不同类别揉在一起。这会引出 universal domain adaptation 等更复杂设定。

## box redsoft
### 错误示例
只看 source / target 的 feature 是否混合，就宣布适应成功。这样可能忽略 target 类别被错配到 source 类别的问题。

## box info
### 更稳的检查
观察 target pseudo-label、预测熵、少量人工标注验证、类别覆盖假设和不同对齐层的效果。

**现在你应该能做什么：**指出“分布对齐”与“分类正确”不是一回事。


---

# Target 资料更少时：TTT 与 Domain Generalization {#dg}
<!-- tag: BEYOND DA -->

如果 target 有大量无标注资料，可以估计 target 分布并做对齐。但如果 target 资料很少，甚至测试时只看到一个样本，就很难靠分布对齐工作。testing-time training 试图在测试时利用单个或少量样本上的自监督目标，让模型临时适应当前输入。

如果训练时完全不知道 target domain，就不再是适应某个已知 target，而是 domain generalization。它希望模型训练完后，面对未知领域也能表现稳定。若训练资料包含多个领域，可以学习跨领域共同结构；若训练资料只有一个领域，常见思路是用 data augmentation 人造多种变化，让模型提前见过更多可能性。

| 设定 | target 信息 | 代表思路 |
| --- | --- | --- |
| Domain adaptation | 训练时知道 target，有资料。 | fine-tuning、feature alignment、对抗训练。 |
| Testing-time training | 测试时才看到少量 target 样本。 | 用自监督任务临时更新或调整。 |
| Domain generalization | 训练时不知道具体 target。 | 多领域训练、增强、学习领域无关表示。 |

**现在你应该能做什么：**区分“已知 target 后适应”和“未知 target 前泛化”。


---

# 最小代码：Gradient Reversal 风格训练骨架 {#code}
<!-- tag: CODE -->

下面的伪代码展示 domain adversarial training 的核心。source batch 有标签，可以算分类 loss；source 和 target 都有领域标签，可以算 domain loss。feature extractor 在分类 loss 上正常下降，在 domain loss 上反向更新，让 domain classifier 更难分辨。

正确示例：source 分类 + domain 对齐Python-like

```text
def train_step(source_x, source_y, target_x, lamb):
 xs = concat(source_x, target_x)
 domain_y = concat(zeros(len(source_x)), ones(len(target_x)))

 feat_source = feature_extractor(source_x)
 cls_logits = label_predictor(feat_source)
 cls_loss = cross_entropy(cls_logits, source_y)

 feat_all = feature_extractor(xs)
 reversed_feat = gradient_reversal(feat_all, lamb)
 domain_logits = domain_classifier(reversed_feat)
 domain_loss = binary_cross_entropy(domain_logits, domain_y)

 loss = cls_loss + domain_loss
 loss.backward()
 update_all_modules()
```

`gradient_reversal` 前向传播时像 identity，反向传播时把梯度乘上 `-λ`。这样 domain classifier 自己学会分 source / target，而 feature extractor 收到相反信号，学习隐藏领域信息。

错误示例：只优化领域不可分Representation bug

```text
# 错误示例：没有分类约束，feature 可以坍缩成常数。
feat_all = feature_extractor(concat(source_x, target_x))
domain_loss = confuse_domain_classifier(feat_all)
domain_loss.backward()

# 缺少 source classification loss，label predictor 无法保证类别仍可分。
```

**现在你应该能做什么：**读懂领域对抗训练的训练步骤，并能指出分类 loss 和领域 loss 分别保护什么。


---

# 综合练习 {#practice}
<!-- tag: 18 QUESTIONS -->

<!-- practice-intro -->
<!-- end-practice-intro -->

<!-- practice-group: 一、Domain Shift 基础 -->
<!-- quick-review: 快速回顾 source 是训练领域，target 是希望部署或测试的领域。 -->

### ? Q01

**Q:** 什么是 domain shift？为什么黑白数字训练、彩色数字测试会让模型准确率下降？


### ? Q02

**Q:** source domain 和 target domain 分别指什么？在真实部署中如何识别它们？


### ? Q03

**Q:** 输入分布变化、输出分布变化、输入-标签关系变化有什么区别？


### ? Q04

**Q:** 为什么 benchmark 上的高准确率容易让人低估真实部署风险？


<!-- end-practice-group -->

<!-- practice-group: 二、适应情境 -->
<!-- quick-review: 快速回顾 target 有无标签、资料多少，决定是直接训练、微调、对齐还是泛化。 -->

### ? Q05

**Q:** 如果 target domain 有大量标注资料，为什么通常不需要复杂的领域自适应？

- [ ] 可以直接用 target 标注资料训练或微调，问题退化成监督学习。
- [ ] 因为 target 标注越多，越应该完全忽略 target 标签。
- [ ] 因为领域自适应只适用于没有 source 资料的情况。


### ? Q06

**Q:** target 只有少量标注资料时，fine-tuning 要注意哪些过拟合风险？


### ? Q07

**Q:** target 有大量无标注资料时，这些资料虽然没有 label，仍然能提供什么信息？


### ? Q08

**Q:** testing-time training 和 domain generalization 分别处理什么更困难的场景？


<!-- end-practice-group -->

<!-- practice-group: 三、Feature 对齐 -->
<!-- quick-review: 快速回顾 feature extractor 要让领域不可分，label predictor 要让类别可分。 -->

### ? Q09

**Q:** 为什么可以把分类器拆成 feature extractor 和 label predictor？拆分位置有什么影响？


### ? Q10

**Q:** domain classifier 的输入和输出是什么？它为什么是二元分类器？


### ? Q11

**Q:** domain adversarial training 中，domain classifier 和 feature extractor 的目标为什么相反？


### ? Q12

**Q:** 错误示例：feature extractor 为了骗过 domain classifier，对所有输入都输出 zero vector。为什么这不是有效解？


### ? Q13

**Q:** gradient reversal layer 的前向和反向分别做什么？它如何实现领域对抗目标？


<!-- end-practice-group -->

<!-- practice-group: 四、限制与评估 -->
<!-- quick-review: 快速回顾 分布对齐不是类别对齐；target 类别集合也可能不同。 -->

### ? Q14

**Q:** 为什么 source / target feature 看起来混合在一起，不一定代表 target 分类正确？


### ? Q15

**Q:** 为什么有人希望无标注 target 样本远离分类边界？用预测分布的集中程度解释。


### ? Q16

**Q:** 错误示例：source 有老虎、狮子、狗，target 有狗、猫，却强行把整体分布完全对齐。可能造成什么问题？


### ? Q17

**Q:** 请用 8-12 句话向同学讲清楚：领域自适应为什么要同时做到“领域不可分”和“类别可分”？


### ? Q18

**Q:** 你要把实验室训练的图像分类器部署到真实用户环境。请设计一个领域自适应评估方案，包含资料收集、是否标注、训练策略、验证指标和失败检查。


<!-- end-practice-group -->


---

