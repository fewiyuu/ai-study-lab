---
course: "Transformer 论文精读"
unit: "05 训练细节与实验读法"
title: "Transformer 论文精读 05｜训练细节与实验读法"
kicker: "Attention Is All You Need"
kicker_en: "training and experiments"
summary: "Transformer 原论文的实验部分容易被读成几串数字： 28.4 BLEU 、 41.8 BLEU 、 8 GPUs 、 3.5 days 。这一页把数字放回训练设置里：模型用什么配方训出来，指标在测什么，对比对象是谁，消融表能支撑哪一级结论。"

side_note: "这页把论文最后一段读成证据链：训练配方怎样稳定模型，评价指标到底测什么，实验表又怎样支持架构贡献。"

can_do:
  - "解释 Noam learning rate 为什么前期升、后期降。"
  - "说清 Adam 参数、dropout、label smoothing 各自影响训练的哪一层。"
  - "读实验结果时同时检查任务、指标、单模型/集成、模型规模和训练成本。"
  - "拿一行消融结果说出变量、现象、可能解释和边界。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../04_位置编码/学习页.html"

---

# 学习目标 {#goals}
<!-- tag: goal -->

这一页对应原论文第 5 节训练设置和第 6 节实验结果。前四页已经把 attention、multi-head、encoder/decoder、位置编码拆开；现在要回答另一个问题：这些结构放到真实机器翻译训练里，论文是怎样把“效果强、训练快、可替代循环结构”这件事讲成证据的。

## box
### 先分清三条线
第一条线是训练配方，包括 Adam、Noam 学习率、dropout、label smoothing 和 batching。第二条线是结果读法，包括 BLEU、perplexity、训练成本、base/big 模型规模。第三条线是证据边界，包括消融实验回答了什么、没有回答什么。
读论文时把这三条线拆开，会避免一个常见误读：看到 BLEU 提升，就把所有提升都归到某个单点技巧上。原论文想展示的是一套架构在当时机器翻译任务上的整体能力，训练配方和实验条件是这个结论的一部分。

## box info
### 本页的验收标准
你不需要手背每个表格数字，但要能做三种动作：给定训练代码，检查它是否接近论文配方；给定结果表，解释每个指标在支持什么；给定一行消融，判断它改变的是结构、规模、正则强度还是优化条件。
换句话说，这页训练的是论文阅读能力，不只是 Transformer 记忆点。后面读新模型论文时，同样可以按“设置、指标、对照、消融、边界”这五项验收。

你现在应该能做什么：打开实验部分时，先画出训练配方和结果证据两列，不要直接摘最高分。


---

# 训练设置先分层看 {#setup}
<!-- tag: setup -->

原论文训练的是机器翻译模型，主要报告 WMT 2014 English-German 和 English-French 任务。它的训练设置不能压缩成一句“用 Adam 训练”：优化器参数、学习率曲线、batch 组织、正则化、模型规模都会改变结果。复现或读表时，应该把它们写成一张配方表。

层级论文中的具体设置读论文时要问的问题容易误读的点


模型规模Transformer base 与 Transformer big 两档配置。当前结果来自 base 还是 big？参数量和训练成本是否一起变了？把 big 的效果和 base 的成本混在一起讲。
优化器Adam，`β1=0.9`、`β2=0.98`、`ε=10^-9`。代码是否只用了默认 Adam？学习率是否由外部 schedule 控制？以为优化器名字相同就等于训练配方相同。
学习率Noam schedule，`warmup_steps=4000`。前期为什么升高，过 warmup 后为什么衰减？把 warmup 理解成一直加大学习率。
正则化dropout 和 label smoothing。dropout 作用在哪些张量？label smoothing 改的是训练目标还是评价指标？只记一个 `0.1`，不看它放在哪里。
训练输入decoder 训练使用右移目标序列，仍保留 causal mask。teacher forcing 是否造成未来泄漏？padding 是否从 attention 和 loss 中排除？以为用了真实前缀就不需要 mask。
评价主要报告 BLEU，也讨论 perplexity 和训练成本。指标测的是概率、生成文本，还是硬件时间？把 BLEU 当作所有语言质量的完整评价。

<!-- interactive: ['shape-tracer'] → keep as HTML -->

<!-- interactive: ['shift-demo'] → keep as HTML -->

你现在应该能做什么：看到训练代码时，先检查目标右移、causal mask、padding mask、loss ignore 这四项是否同时成立。


---

# Noam warmup 学习率 {#schedule}
<!-- tag: schedule -->

论文的学习率会随训练步数变化。公式看起来像一个技巧，但它的直觉很清楚：训练初期模型表示还不稳，先用较小步子逐渐放大；过了 warmup 之后，学习率按步数平方根衰减，让后期更新更细。

$$
 lrate = d_{model}^{-0.5} \cdot \min(step\_num^{-0.5},\ step\_num \cdot warmup\_steps^{-1.5})
 $$
 原论文使用 `warmup_steps=4000`。当 `step_num` 很小时，线性 warmup 项更小，学习率随 step 上升；当 step 过大后，`step_num^{-0.5}` 更小，学习率开始衰减。

## box
### 分段读公式
这个公式的关键是 `min`。优化器真正使用的是两个候选学习率里较小的那个。warmup 前，`step_num * warmup_steps^{-1.5}` 这项随 step 线性增加；warmup 后，`step_num^{-0.5}` 这项变成较小项，于是曲线缓慢下降。
`d_model^{-0.5}` 是尺度因子。模型维度越大，初始学习率规模会被压低。这样读公式时，你会发现它把模型宽度、训练步数和 warmup 长度绑在一起。

## box warn
### 为什么不直接用默认 Adam
Adam 本身会根据梯度一阶、二阶矩调整每个参数的步长，但它仍需要一个全局学习率。原论文把 optimizer 的 `lr` 当成外部 schedule 的载体，每一步重写参数组里的学习率。只写 `torch.optim.Adam(model.parameters())`，没有复现这条曲线。
这件事在复现实验时很实际：相同架构、相同数据，如果学习率计划不同，训练速度、稳定性和最终 BLEU 都可能变。论文读法里，训练配方就是证据的一部分。

阶段较小项学习率趋势训练含义


训练刚开始`step_num * warmup_steps^{-1.5}`随 step 线性升高让早期更新从小步开始，减少震荡。
warmup 附近两项接近达到峰值附近模型已经积累了一段稳定梯度历史。
warmup 之后`step_num^{-0.5}`按平方根衰减后期逐渐收小步长，细化参数。
改变 `d_model`整体乘 `d_model^{-0.5}`曲线整体缩放模型宽度进入学习率尺度。

你现在应该能做什么：不用画图，也能靠 `min` 的两个项解释学习率为什么先升后降。


---

# 目标分布与正则化 {#targets}
<!-- tag: targets -->

训练配方里最容易被粗读的是 dropout 和 label smoothing，因为它们都常被概括成“防过拟合”。更好的读法是分清它们作用的对象：dropout 在中间表示或 attention 权重上制造扰动；label smoothing 改的是监督目标分布，让模型不要把训练标签当成绝对概率 1。

$$
 q(k) =
 \begin{cases}
 1-\epsilon_{ls}, & k = y \\
 \epsilon_{ls}/(V-2), & k \ne y,\ k \ne \langle pad \rangle \\
 0, & k = \langle pad \rangle
 \end{cases}
 $$
 这里用的是训练时常见的实现读法：正确类保留主要概率，非 padding 的其他类分到少量概率，padding 类不参与目标分布。原论文使用 `ε_ls=0.1`。

## box
### label smoothing 为什么可能让 PPL 变差
perplexity 来自模型给真实 token 的概率。label smoothing 把训练目标从 one-hot 改成软分布，会主动降低模型对正确类的极端置信度。于是它可能让 perplexity 看起来不如没有 smoothing 时漂亮，但生成译文的 BLEU 更好。
这类现象通常来自测量对象不同。PPL 更贴近概率校准和似然，BLEU 看生成结果与参考译文的 n-gram 匹配。论文里这类现象提醒你：不要把一个指标当成全部结论。

## box soft
### dropout 要问“放在哪里”
原论文在多个位置使用 dropout，包括每个子层输出后、attention 权重上，以及 embedding 与 positional encoding 相加后。读实现时应检查 dropout 是否作用在这些路径上，并确认训练/评估模式切换是否正确。
如果只在结果表里看到 dropout 比例，不看它落在哪些张量上，你只能得到很粗的结论。训练复现时，这些位置差异会变成行为差异。

组件作用对象训练后果读代码时先查


dropout子层输出、attention 权重、输入表示。减少对某些通道或连接的过度依赖。`model.train()` 下是否启用，位置是否贴近论文。
label smoothing交叉熵目标分布。降低过度自信，可能改善 BLEU。正确类概率、非 padding 类分配、padding 是否排除。
padding ignoreloss 中的无效位置。避免模型学习预测补齐 token。`ignore_index` 或手动 mask 是否生效。
causal maskdecoder self-attention 的未来位置。避免训练时偷看答案。mask 是否加在 softmax 前，方向是否正确。

你现在应该能做什么：看到 “label smoothing=0.1” 时，能说出它改的是目标分布，并解释为什么它和 BLEU/PPL 的关系不能简单等同。


---

# 指标读法 {#metrics}
<!-- tag: metrics -->

实验表要按测量对象读。BLEU 近似衡量译文与参考译文的 n-gram 重叠，perplexity 衡量概率模型对真实序列的困惑程度，训练成本衡量达到结果所花的硬件与时间。只看最高分，会把这些对象混成一个“好”字。

## box
### BLEU 读成“翻译基准上的自动指标”
BLEU 在机器翻译论文里常用，但它不能完整替代人类评价。它奖励和参考译文重叠的 n-gram，并带有长度惩罚。原论文报告 WMT 2014 En-De 和 En-Fr 上的 BLEU，是为了和当时机器翻译系统在同一基准上比较。
因此，读到 `28.4 BLEU` 时，合格表达应包括任务、数据、模型是否单模型、对比对象和训练成本。只说“BLEU 高，所以模型全面更好”太粗。

## box info
### PPL 读成“概率模型困惑度”
perplexity 越低，通常表示模型给真实序列更高概率。但 label smoothing 改了目标分布，所以 PPL 与 BLEU 可能方向不同。读这种差异时先问：指标测的是概率、译文重叠，还是训练成本。
这条经验可以迁移到其他论文：当两个指标方向不一致，不要急着说实验矛盾，先拆开测量对象。

指标或条件它在测什么能支持的结论不能直接支持的结论


BLEU生成译文与参考译文的 n-gram 重叠。在该翻译基准上输出质量强。所有语义、事实性、风格都无问题。
perplexity概率模型对真实序列的困惑度。模型给真实 token 的概率情况。生成文本一定更符合人类偏好。
single model是否不用模型集成。单个模型本身就能达到该分数。完全排除训练技巧或数据处理影响。
training cost硬件数量和训练时间。并行训练效率是贡献之一。任何硬件和实现都能复现同样时间。
base / big模型规模和容量。规模增加是否带来更好结果。架构变化和规模变化可以自动分离。

你现在应该能做什么：读实验表时至少说出“指标测量对象 + 对比条件 + 结论边界”三件事。


---

# 实验结果怎么读 {#results}
<!-- tag: results -->

原论文摘要里的关键结果很有辨识度：Transformer 在 WMT 2014 English-to-German 上达到 `28.4 BLEU`，比当时已有最好结果包括 ensembles 高出 2 BLEU 以上；在 WMT 2014 English-to-French 上，单模型达到 `41.8 BLEU`，并用 8 块 GPU 训练约 3.5 天。要把这几句话一起读，不要拆成孤立口号。

结果点应该读成什么需要同时检查什么不要读成什么


WMT14 En-De `28.4 BLEU`在该翻译基准上超过当时强基线。对比对象、是否 ensemble、数据处理。所有语言任务都已被证明最优。
WMT14 En-Fr `41.8 BLEU`单模型在该任务上达到很强结果。模型规模、训练设置、解码设置。靠多个模型集成堆出的数字。
8 GPUs，约 3.5 天并行训练效率是论文贡献的一部分。硬件、实现、batching、模型大小。任何环境都能复现同样时间。
base 和 big两档模型展示容量与效果关系。参数量、训练成本、表格行是否对应同一设置。big 的分数可以代表所有 Transformer。

## box
### “效果强”之外还有“训练快”
论文标题强调 attention，但实验叙述还强调并行训练。RNN 类模型按时间步递推，训练并行度受序列顺序限制；Transformer 用 attention 和前馈网络堆叠，更容易把训练工作摊到矩阵计算上。BLEU 和训练成本一起出现，是为了说明架构在质量和效率上都占优势。
所以，当你复述贡献时，最好不要只说“分数高”。更准确的说法是：在当时机器翻译基准上，Transformer 用较低训练成本达到或超过强基线，并通过消融支持了 attention-only 架构的设计选择。

## box warn
### 历史位置要说清
这篇论文的实验背景是 2017 年前后的机器翻译和解析任务，不包含后来大规模预训练、长上下文推理、KV cache、RoPE、FlashAttention 等生态。经典论文可以提供机制和读法，但不能把后来所有进展都反向塞回原文。
迁移到新任务时，结论需要重新验证。比如在长上下文模型里，标准 attention 的 `N^2` 成本会重新成为问题；在生成式大模型里，解码成本和缓存机制又会成为新主线。

你现在应该能做什么：复述 Transformer 实验贡献时，同时说质量、成本、对比对象和历史边界。


---

# 消融表读法 {#ablation}
<!-- tag: ablation -->

消融实验的任务是回答设计变量是否有用。原论文的消融表改变 head 数、key/value 维度、模型宽度、FFN 维度、dropout、attention 类型、位置表示等设置。每一行都应该读成一个小问题：改了什么，指标怎么变，变化能归因到哪里，边界是什么。

变量它在问什么读表时看什么结论边界


head 数多头是否比少头或单头更有效。BLEU/PPL 是否随 head 数变化，参数量是否同步变化。head 模式来自训练，不由人工固定语法功能。
`d_k` / `d_v`每个 head 的子空间维度是否足够。点积维度、输出容量、计算成本。维度变化可能同时影响参数量和优化。
`d_model` / `d_ff`模型宽度和 FFN 容量是否限制效果。效果提升是否伴随明显成本增加。容量提升不等于结构贡献本身。
dropout正则强度是否影响泛化。训练/验证指标是否出现过拟合迹象。最佳比例和数据规模、模型大小相关。
attention 类型替换 attention 机制后表现如何。是否控制其他条件，复杂度是否改变。单个任务上的结果不能覆盖所有场景。
位置表示固定正弦与可学习位置表示差别如何。长度外推、训练长度、指标变化。不等于后来所有位置机制的最终结论。

<!-- interactive: ['terminal-lab'] → keep as HTML -->

## box
### 一行消融的合格表达
合格表达一般长这样：作者改变了某个变量，在同一任务和相近训练设置下观察到某个指标变化，因此这个变量可能贡献了某种效果；但如果参数量、计算成本或训练稳定性也改变了，结论要保留边界。
这个句式很实用。它迫使你把“变量、现象、解释、边界”都说出来，也避免把整张表读成“作者调了很多参数”。

## box soft
### 消融和复现的关系
消融表能告诉你哪个设计值得优先保留。复现训练时，先保住 optimizer、schedule、mask、loss、模型规模这些基础项，再讨论是否改 head 数或 dropout。否则你看到的指标变化可能来自训练配方漂移，未必来自结构变量。

你现在应该能做什么：拿任意一行消融结果，写出变量、对照、指标变化和不能外推的范围。


---

# 训练配方最小实现 {#implementation}
<!-- tag: code -->

下面只保留本页关心的训练骨架：Noam schedule、Adam 参数、label smoothing、padding 排除、一次训练 step。它的目的在于把“训练配方”落成可以检查的代码对象。

Noam schedule 与 Adam 参数复制

```python
import math
import torch


def noam_lr(step: int, d_model: int = 512, warmup: int = 4000) -> float:
 step = max(step, 1)
 scale = d_model ** -0.5
 warm = step * (warmup ** -1.5)
 decay = step ** -0.5
 return scale * min(decay, warm)


optimizer = torch.optim.Adam(
 model.parameters(),
 lr=1.0,
 betas=(0.9, 0.98),
 eps=1e-9,
)

for step, batch in enumerate(loader, start=1):
 lr = noam_lr(step, d_model=512, warmup=4000)
 for group in optimizer.param_groups:
 group["lr"] = lr

 loss = compute_smoothed_loss(model, batch)
 loss.backward()
 optimizer.step()
 optimizer.zero_grad(set_to_none=True)
```

Label smoothing 与 padding 排除复制

```python
import torch
import torch.nn.functional as F


def smooth_targets(target, vocab_size: int, smoothing: float = 0.1, pad_id: int = 0):
 confidence = 1.0 - smoothing
 low = smoothing / (vocab_size - 2)
 flat = target.reshape(-1)

 dist = torch.full((flat.numel(), vocab_size), low, device=target.device)
 dist.scatter_(1, flat[:, None], confidence)
 dist[:, pad_id] = 0.0
 dist[flat == pad_id] = 0.0
 return dist


def smoothed_kl_loss(logits, target, pad_id: int = 0, smoothing: float = 0.1):
 vocab_size = logits.size(-1)
 log_probs = F.log_softmax(logits.reshape(-1, vocab_size), dim=-1)
 gold = smooth_targets(target, vocab_size, smoothing, pad_id)
 token_loss = F.kl_div(log_probs, gold, reduction="none").sum(dim=-1)
 valid = target.reshape(-1).ne(pad_id)
 return token_loss[valid].mean()
```

## box
### 代码验收顺序
先看 `noam_lr` 是否真的每一步更新参数组学习率，再看 Adam 的 `betas` 和 `eps` 是否贴近论文。接着检查 decoder 输入是否右移，target 是否是下一个 token，最后看 padding 是否同时从 attention 和 loss 中排除。
label smoothing 里最容易漏的是 padding。padding 类不应该分到概率，padding 位置也不应该贡献 loss。否则模型会被训练去解释补齐 token，指标看起来还能跑，但学习目标已经偏了。

## box warn
### 错误示例
固定学习率与 padding 漏处理复制
```python
optimizer = torch.optim.Adam(model.parameters(), lr=1e-3)
dist = torch.full((target.numel(), vocab_size), smoothing / vocab_size)
dist.scatter_(1, target.reshape(-1, 1), 1.0 - smoothing)
loss = F.kl_div(log_probs, dist, reduction="batchmean")
```

这段代码可能仍能训练，但它不能声称复现了论文配方：学习率没有使用 Noam schedule，Adam 参数也偏离论文设置；padding 类和 padding 位置都被放进了目标和损失。

你现在应该能做什么：用一张检查表判断训练 loop 是否接近原论文，别只看模型类名是不是 Transformer。


---

# 常见误区和修法 {#pitfalls}
<!-- tag: pitfalls -->
<!-- layout: grid2 -->

## box
### 只记 BLEU，不看条件
BLEU 必须和任务、数据、单模型/集成、模型规模、训练成本一起读。修法：复述结果时至少补上 WMT14、En-De/En-Fr、single model 或 ensemble、硬件时间。

## box
### 把 warmup 当成一直升高
Noam schedule 是先升后降。修法：用公式里的 `min` 解释两个阶段，不要只背 `warmup_steps=4000`。

## box
### 忽略 Adam 参数
原论文使用 `β2=0.98`、`ε=1e-9`，和许多默认设置不同。修法：复现时把 optimizer、schedule、warmup、batching 一起列出。

## box
### 把 label smoothing 当成随机改标签
label smoothing 改的是目标概率分布，并不会随便把正确标签换掉。修法：写出正确类、其他类、padding 类的概率分配。

## box
### 把 teacher forcing 当成答案泄漏
训练用真实前缀，但 decoder self-attention 仍要 causal mask。修法：检查右移目标和 causal mask 同时存在。

## box
### 把消融表读成调参记录
消融是为了回答变量作用。修法：每一行按“变量、对照、指标、边界”写一句解释。


---

# 下一步怎么读 {#bridge}
<!-- tag: bridge -->
<!-- layout: grid2 -->

## box
### 回到整篇论文
到这里，五个单元已经覆盖了原论文的核心阅读路径：self-attention 解释如何读全句，多头解释如何分投影空间，encoder/decoder 解释结构信息流，位置编码补回顺序，训练与实验页解释证据如何成立。
复盘时可以从最后一页倒推：实验结果为什么可信？因为有训练配方、指标对照、模型规模和消融；训练配方为什么能落地？因为前面四页的结构和形状都已经能对上代码。

## box soft
### 以后读模型论文的通用清单
1. 先分清结构贡献和训练配方。
2. 把关键公式转成一条可检查的数据流。
3. 读结果时同时看指标、数据、对比对象和成本。
4. 读消融时只问一个变量的作用。
5. 最后写出结论边界：在哪些任务、规模和条件下成立。


---

# 综合练习 {#practice}
<!-- tag: practice -->

<!-- practice-intro -->
## box
### 答题目标
练习区按训练配方、目标与指标、结果表、消融和综合表达分组。诊断题按“错因 / 后果 / 修法”写；综合题要把训练设置和实验证据连起来。

## box soft
### 复盘抓手
如果答案写散了，就回到五个词：`optimizer`、`schedule`、`regularization`、`metric`、`ablation`。每个词都要落到一个可检查对象。

<!-- end-practice-intro -->

<!-- practice-group: 一、训练配方 -->
<!-- quick-review: 复盘提示：优化器名字、超参数、学习率曲线和 batch 构造要一起看。 -->

### 1_warmup_role choice

**Q:** warmup 的作用

- [ ] 训练初期逐步增大学习率，过 warmup 后再衰减。
- [ ] 让学习率在整个训练过程中一直增大。
- [ ] 替代 Adam 的一阶、二阶矩统计。

Answer: 训练初期逐步增大学习率，过 warmup 后再衰减。
Explain: Noam schedule 在 warmup 前逐步升高，之后按 step 的平方根衰减。

### 2_adam_params true/false

**Q:** Adam 参数

- [ ] 对
- [ ] 错

Answer: 错
Explain: 原论文给出了 betas 和 eps；默认 Adam 参数不等于论文配方。

### 3_warmup_steps choice

**Q:** warmup_steps 数值

- [ ] 4000
- [ ] 512
- [ ] 0.98

Answer: 4000
Explain: 原论文学习率计划使用 warmup_steps=4000。

### 4_teacher_mask short-answer

**Q:** teacher forcing 和 mask

Explain: 合格答案要包含：teacher forcing 只是把真实前缀作为 decoder 输入；每个位置仍只能利用之前位置预测下一个 token；如果没有 causal mask，当前位置可以直接读取未来答案，训练指标会虚高，推理时会崩。

<!-- end-practice-group -->

<!-- practice-group: 二、目标分布和指标 -->
<!-- quick-review: 复盘提示：label smoothing 改目标分布；BLEU 看译文重叠；PPL 看概率困惑度。 -->

### 5_smoothing choice

**Q:** label smoothing 作用

- [ ] 随机把正确标签改成错误标签。
- [ ] 只改变 BLEU 的计算公式。
- [ ] 把 one-hot 目标改成较软的概率分布。

Answer: 把 one-hot 目标改成较软的概率分布。
Explain: 它把 one-hot 目标变成软分布，降低模型过度自信。

### 6_bleu_ppl short-answer

**Q:** PPL 与 BLEU

Explain: PPL 来自模型给真实 token 的概率，label smoothing 会降低模型对正确类的极端置信度；BLEU 看生成译文与参考译文的 n-gram 重叠，较好的泛化或置信度控制可能提升生成质量。两者测量对象不同，所以方向可以不一致。

### 7_padding_loss diagnosis

**Q:** padding 处理

Explain: padding 是无效占位。若 padding 类分到概率，或 padding 位置进入 loss，模型会被训练去解释补齐 token，损失和梯度都会偏。修法是把 padding 类概率置 0，并用 mask 或 ignore_index 排除 padding 位置。

### 8_bleu_boundary true/false

**Q:** BLEU 边界

- [ ] 对
- [ ] 错

Answer: 错
Explain: BLEU 是自动翻译指标，不覆盖所有语义、事实性和人类偏好。

<!-- end-practice-group -->

<!-- practice-group: 三、结果表读法 -->
<!-- quick-review: 复盘提示：结果数字必须带上任务、模型、是否集成、训练成本和对比对象。 -->

### 9_ende_bleu choice

**Q:** En-De 结果

- [ ] 28.4 BLEU
- [ ] 41.8 BLEU
- [ ] 4000 BLEU

Answer: 28.4 BLEU
Explain: 论文摘要报告 WMT14 English-to-German 28.4 BLEU。

### 10_enfr_cost choice

**Q:** En-Fr 结果与成本

- [ ] 只靠集成模型达到 28.4 BLEU。
- [ ] 单模型达到 4000 BLEU。
- [ ] 单模型达到 41.8 BLEU，并用 8 块 GPU 训练约 3.5 天。

Answer: 单模型达到 41.8 BLEU，并用 8 块 GPU 训练约 3.5 天。
Explain: 摘要强调 En-Fr 单模型 41.8 BLEU，并用 8 块 GPU 训练约 3.5 天。

### 11_base_big short-answer

**Q:** base 与 big

Explain: base 和 big 是不同规模模型。big 往往效果更强，但参数量、训练成本和优化行为也不同。读表时不能拿 big 的分数代表所有 Transformer，也不能用 base 的成本解释 big 的结果。

### 12_result_sentence short-answer

**Q:** 结果复述

Explain: 可以写成：Transformer 在 WMT14 En-De 和 En-Fr 机器翻译基准上达到强 BLEU，且 En-Fr 单模型在 8 块 GPU 约 3.5 天训练后达到 41.8 BLEU。这个结果要和当时强基线、是否单模型、训练成本一起读，说明论文强调质量和并行训练效率。

<!-- end-practice-group -->

<!-- practice-group: 四、消融与错误诊断 -->
<!-- quick-review: 复盘提示：消融句式是“变量 / 现象 / 解释 / 边界”。诊断句式是“错因 / 后果 / 修法”。 -->

### 13_ablation_items choice

**Q:** 消融三件事

- [ ] 变量、指标变化、结论边界。
- [ ] 作者名字、论文标题、排版风格。
- [ ] 只说哪一行最高。

Answer: 变量、指标变化、结论边界。
Explain: 读一行消融至少要说变量、指标变化和结论边界。

### 14_fixed_lr diagnosis

**Q:** 固定学习率诊断

Explain: 错因：没有使用论文的 Adam 参数，也没有 Noam schedule 和 warmup。后果：训练早期稳定性、收敛速度和最终指标都可能变，不能说复现论文配方。修法：设置 betas=(0.9,0.98) 、 eps=1e-9 ，并逐步更新 Noam 学习率。

### 15_only_bleu diagnosis

**Q:** 只看 BLEU 诊断

Explain: BLEU 只是在特定翻译基准上的自动指标，不能覆盖所有语言质量。合格表述要包含 WMT14 任务、BLEU 分数、是否单模型或 ensemble、训练成本和当时基线，并说明结论只在这些实验条件内成立。

### 16_confound diagnosis

**Q:** 消融混杂诊断

Explain: 因为多个变量同时改变，效果可能来自模型容量、FFN 宽度、head 数、优化稳定性或它们的交互。要归因给 head 数，最好控制其他条件，只改变 head 数，或至少在结论里保留“变量混杂”的边界。

<!-- end-practice-group -->

<!-- practice-group: 五、完整讲清楚 -->
<!-- quick-review: 复盘提示：费曼解释至少包含 Adam、Noam、dropout、label smoothing、BLEU、成本、base/big、消融边界。 -->

### 17_feynman synthesis

**Q:** 费曼解释

Explain: 合格答案应覆盖：训练时目标右移并保留 causal mask；Adam 使用论文给定参数；Noam 学习率先 warmup 后衰减；dropout 和 label smoothing 控制泛化与置信度；PPL 与 BLEU 测量对象不同；WMT14 En-De/En-Fr 结果要和单模型、训练成本、对比对象一起读；base/big 不能混讲；消融表用于判断变量贡献，但结论只在实验设置内成立。

### 18_paper_checklist checklist

**Q:** 论文实验验收清单

Explain: 清单至少应包含：任务和数据集是什么；指标测量对象是什么；训练配方和默认值是否写清；模型规模是否可比；对比对象是否公平；是否有消融支持关键设计；训练成本是否报告；结论边界是否明确。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- [Vaswani et al., *Attention Is All You Need*](https://arxiv.org/abs/1706.03762)。本页的训练设置、Noam 学习率、label smoothing、WMT14 BLEU、训练成本和消融读法以原论文为主。访问日期：2026-06-21。
- [PyTorch `torch.optim.Adam` 文档](https://docs.pytorch.org/docs/stable/generated/torch.optim.Adam.html)。用于确认 Adam 参数接口和参数组学习率更新。访问日期：2026-06-21。
- [PyTorch `torch.nn.functional.log_softmax` 文档](https://docs.pytorch.org/docs/stable/generated/torch.nn.functional.log_softmax.html)与[`torch.nn.functional.kl_div`](https://docs.pytorch.org/docs/stable/generated/torch.nn.functional.kl_div.html)。用于最小 label smoothing 损失实现。访问日期：2026-06-21。
- [PyTorch `Tensor.scatter_` 文档](https://docs.pytorch.org/docs/stable/generated/torch.Tensor.scatter_.html)。用于把正确类概率写入平滑后的目标分布。访问日期：2026-06-21。

---
