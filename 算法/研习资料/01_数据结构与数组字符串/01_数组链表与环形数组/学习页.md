---
course: "算法：数据结构与数组字符串"
unit: "01 数组链表与环形数组"
title: "数组链表与环形数组"
kicker: "基础单元"
summary: "这一页把线性结构的三种底座放在同一张桌面上：数组看下标和连续存放，链表看节点和指针，环形数组看固定容量里如何用 `front`、`rear`、`size` 和取模把队列首尾接起来。先学会判断题目要的是随机访问、局部重连，还是头尾进出，后面的双指针、队列和滑动窗口才会顺手。"

side_note: "先把数组、链表和环形数组的读写位置理顺，再去练习区检查状态、边界和回绕。"

can_do:
  - "能先从题目动作判断该用数组、链表，还是环形数组，不必先背 API 名字。"
  - "能手动追踪 `front`、`rear`、`size` 的变化，并说清楚为什么要对容量取模。"
  - "能写出数组覆盖、链表改连接、循环队列入队出队的最小可检查步骤。"

course_links:
  - text: "课程地图"
    href: "../index.html"

---

# 先抓主线 {#goals}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
### 先问三个问题
这题是按下标读写，还是顺着节点走？要改的是元素内容，还是节点连接？头尾会不会反复进出同一块固定空间？只要把这三个问题答清楚，结构选择就会稳定很多。
**读题顺序**先找动作，再选结构，最后才写代码。

## box soft
### 先放一边
这一页先不把哈希表、滑动窗口、双端队列、前缀和一口气拉进来。它们后面都会接上来，当前先把三种底座和状态变量站稳。

| 题目动作 | 优先结构 | 原因 |
| --- | --- | --- |
| 按下标读取、交换、覆盖 | 数组 | 编号和位置直接对应，随机访问快，原地改写也自然。 |
| 在已知节点附近插入、删除、重连 | 链表 | 已经站到前驱或当前节点旁边时，只改连接就够了。 |
| 固定容量里头尾反复进出 | 环形数组 | 用取模把尾部绕回开头，空间能继续复用。 |
| 保留连续区间的有效部分 | 数组覆盖 / 队列状态 | 用慢指针、front/rear、size 维护有效窗口，比真删真搬更稳。 |


---

# 先别追的方向 {#boundary}
<!-- tag: boundary -->
<!-- layout: grid2 -->

## box
### 这一页先不展开
哈希表、双端队列、滑动窗口、前缀和这些题型，都会在后面的单元里接上。现在先把数组的覆盖、链表的重连、环形数组的回绕练熟。

## box soft
### 到哪里再看
02 单元看受限操作；03 单元看哈希；04、05 单元看双指针；06、07 单元看窗口和前缀和。现在不用把它们混在一起。

| 后续方向 | 本页先做什么 | 后面再看什么 | 为什么现在先放下 |
| --- | --- | --- | --- |
| 队列和栈 | 先把 front / rear / size 的职责说清楚 | 一端或两端受限的操作规则 | 先别把实现技巧和结构判定混成一件事 |
| 哈希表 | 先完成数组和链表的读写位置判断 | 键值映射、查表和去重 | 现在还不需要处理映射键的组织方式 |
| 双指针 | 先学会覆盖、重连、回绕 | 快慢指针、左右夹逼、双链表走位 | 没有这层底座，双指针只会像一堆指针名词 |
| 窗口和前缀和 | 先确认连续区间的读写关系 | 滑动窗口、前缀和、差分数组 | 这几类题都依赖“边界怎么动”，但不是本页主角 |


---

# 同一组元素，三种看法 {#overview}
<!-- tag: overview -->

同一组元素可以放在数组里看下标，也可以放在链表里看连接，还可以放在固定容量的队列里看头尾。这里讲的是三种不同的动作模型。

数组按下标读写
链表按节点重连
环形数组头尾复用空间
状态变量front / rear / size
后续迁移双指针、队列、窗口

## box
### 数组
数组的强项是随机访问和原地交换。只要你知道下标，就能直接拿到元素；只要你愿意覆盖，就能把不需要的元素挪出结果前缀。

## box
### 链表
链表的强项是局部重连。只要你站在前驱节点旁边，改一次 `next` 就能插入或删除一段节点；代价是按位置走到第 k 个节点并不快。

| 结构 | 读什么 | 写什么 | 常见慢点 |
| --- | --- | --- | --- |
| 数组 | 下标、区间、值 | 覆盖、交换、移动慢指针 | 头部插入删除、中间大搬家 |
| 链表 | 当前节点、后继节点、前驱节点 | `next` 指向、局部重连 | 按位置随机访问 |
| 环形数组 | `front`、`rear`、`size` | 写入位置、读取位置、容量边界 | 空满混淆、忘记取模 |


---

# 核心机制 {#mechanism}
<!-- tag: mechanism -->
<!-- layout: grid2 -->

## box
### 数组：慢指针负责写，快指针负责看
数组题里常见的做法是用覆盖代替删除。快指针负责扫描原数组，慢指针负责下一次写入位置。这样做的好处是：读写分工清楚，结果前缀始终连续。
数组覆盖示例复制
```python
def keep_at_most_two(nums):
 slow = 0
 for fast in range(len(nums)):
 if slow < 2 or nums[fast] != nums[slow - 2]:
 nums[slow] = nums[fast]
 slow += 1
 return slow
```

这里 `fast` 只读，`slow` 只写。它和后面的链表前驱、循环队列写入位是一类思路：把“当前可用位置”固定下来，再让状态往前走。

## box
### 链表：先保后继，再改连接
链表最容易丢东西的地方，是你把 `next` 改掉以后，原来后面那串节点就再也找不回来了。只要先保存后继，再重连，就能避免断链。遇到头结点附近的操作时，虚拟头结点能把“删头”和“删中间”写成同一种写法。
链表删除示例复制
```python
def delete_after_prev(prev):
 if prev is None or prev.next is None:
 return
 nxt = prev.next
 prev.next = nxt.next
```

如果 `prev` 来自虚拟头结点，那么这段代码既能删中间节点，也能统一处理原始头结点的删除。

$$
next(i) = (i + 1)\bmod capacity,\qquad prev(i) = (i - 1 + capacity)\bmod capacity
 $$
 取模把“走到末尾”改写成“回到 0”，让固定容量的数组继续复用前面的空位。
$$

循环队列示例复制

```python
class CircularQueue:
 def __init__(self, capacity):
 self.data = [None] * capacity
 self.front = 0
 self.rear = 0
 self.size = 0

 def enqueue(self, value):
 if self.size == len(self.data):
 return False
 self.data[self.rear] = value
 self.rear = (self.rear + 1) % len(self.data)
 self.size += 1
 return True

 def dequeue(self):
 if self.size == 0:
 return None
 value = self.data[self.front]
 self.front = (self.front + 1) % len(self.data)
 self.size -= 1
 return value
```

| 对象 | 读什么 | 写什么 | 错了会怎样 |
| --- | --- | --- | --- |
| fast | 原数组中的当前元素 | 不写，只扫描 | 一边读一边改，原始顺序会乱 |
| slow | 当前结果前缀 | 下一次写入位置 | 写偏了就会漏元素或重复元素 |
| front / rear | 队首读取位、队尾写入位 | 轮流前移 | 顺序混掉或越界 |


---

# 状态追踪 {#trace}
<!-- tag: trace -->
<!-- layout: grid2 -->

<!-- interactive: ['shape-tracer'] → keep as HTML -->

## box soft
### 看追踪时先盯住什么
先看 `front` 还是 `rear` 在动，再看 `size` 是加还是减。你一旦把“读位”和“写位”搞混，空满边界就会连着错。
这也是为什么循环队列更适合配一个状态台账。

| 操作 | front | rear | size | 读写动作 | 错位症状 |
| --- | --- | --- | --- | --- | --- |
| 初始 | 0 | 0 | 0 | 还没有元素 | 空满都不能乱判 |
| 入队 | 不变 | 前移一格 | +1 | 先写后移 | rear 含义摇摆会写错位 |
| 出队 | 前移一格 | 不变 | -1 | 先读后移 | front 指向错位置会读错元素 |
| 绕回 | 按取模移动 | 按取模移动 | 按真实元素数变化 | 把末尾折回开头 | 不取模就会越界或浪费空间 |


---

# 同一组元素的三次变化 {#example}
<!-- tag: example -->

拿 `A、B、C` 这组三个元素做锚点，数组、链表和环形数组会分别给出不同的改写方式。你只要跟住这组三次变化，就能把“怎么改”从抽象词变成具体动作。

| 结构 | 做什么 | 读什么 | 写什么 | 如果写错 |
| --- | --- | --- | --- | --- |
| 数组 | 用慢指针覆盖保留元素 | 原数组顺序 | 结果前缀 | 会漏元素或重复保留 |
| 链表 | 保存后继，再改 `next` | 当前节点和后继节点 | 前驱节点的连接 | 会把后半段节点断开 |
| 环形数组 | 写入 `rear`，再取模前移 | 队首、队尾、容量 | front / rear / size | 空满混淆或尾部无法复用 |

## box
### 数组里的 A、B、C
当你在数组里处理重复元素时，`fast` 负责把 A、B、C 一个个扫过去，`slow` 负责把需要保留的值写进结果前缀。题目最后看的是 `nums[:slow]` 这段结果前缀。

## box
### 链表里的 A、B、C
当你在链表里删除某个节点时，真正要改的是前驱节点的 `next`。如果前面加了虚拟头结点，头结点也只是“前驱节点”的一种特例，写法会更统一。

手算三步复制

```text
数组：fast 扫描 A、B、C，slow 只写保留项。
链表：先保存 nxt，再把 prev.next 改到 nxt.next。
环形数组：rear 写入后前移一格，走到末尾就取模回到 0。
```

同一组元素在三种结构里会被不同地读取和写入，但核心都一样：先把“当前可用位置”说清楚，再让状态往前走。


---

# 把同一件事放进三种结构里 {#playbook}
<!-- tag: operation bridge -->

现在把目标收窄到一个具体动作：处理序列 `A、B、C、D`，让它变成“保留 A 和 C”，或者让它在容量 3 的队列里完成一次绕回。数组、链表和环形数组的写法不同，但都在回答同一件事：当前读哪里、当前写哪里、旧状态什么时候失效。

数组看的是扫描和覆盖，链表看的是前驱和后继，环形数组看的是读位和写位。只要把这三种位置固定住，题目再换外壳，你要做的动作还是那几个：比较、保存后继、写入、前移、取模、判满。区别不在“做不做”，而在“把哪个位置当成当前有效位置”。

| 具体动作 | 数组里怎么做 | 链表里怎么做 | 环形数组里怎么做 | 最常见失误 |
| --- | --- | --- | --- | --- |
| 保留某些元素 | 用 `slow` 覆盖结果前缀 | 让前驱节点跨过被删节点 | 不做删除，更多是移动读写位 | 真删、真搬，导致顺序和复杂度都乱 |
| 往中间插入一个值 | 后移后半段，再写新值 | 改两条连接就完成 | 这类操作通常不优先放在这个结构里 | 忘记先保存后继或后缀 |
| 让尾部继续接收新元素 | 尾部下标顺着走，必要时另开空间 | 尾节点的 next 指向新节点 | `rear = (rear + 1) % capacity` | 没有取模，或者没有判断满 |
| 让头部继续弹出旧元素 | 把 `fast` 扫过的值丢到前缀后面 | 头结点/前驱节点改连接 | `front = (front + 1) % capacity` | 把读位和写位混在一起 |
| 判断状态是否还能继续 | 看结果前缀是否满足条件 | 看当前节点还在不在链上 | 看 `size` 是否等于 0 或 capacity | 只靠一个表面条件，空满或断链会混淆 |

同一段状态推进复制

```python
# 数组：扫描 A,B,C,D，slow 指向结果前缀的下一次写入位
slow = 0
for fast, value in enumerate(nums):
 if keep(value):
 nums[slow] = value
 slow += 1

# 链表：先保后继，再改连接
nxt = cur.next
cur.next = prev

# 环形数组：先写 rear，再取模前移
data[rear] = value
rear = (rear + 1) % capacity
size += 1
```

这段桥的作用是让调试时立刻知道应该盯哪里：数组错了先看 `slow` 有没有写偏，链表错了先看 `nxt` 有没有提前丢，队列错了先看 `rear` 是否回绕、`size` 是否同步。

如果你能把 A、B、C、D 在三种结构里各走一遍，再去做练习 5、8、10、17，很多题面会立刻变短。


---

# 删链实验 {#debug-lab}
<!-- tag: debug lab -->

现在把链表删节点这件事缩成一个最小动作：给定一个前驱节点 `prev`，删掉它后面的节点。这个动作非常小，但它能把链表的关键机制全都暴露出来：先看到谁、先保存谁、改了哪条连接、旧节点什么时候失效。

如果你把虚拟头结点加进来，删头节点就不再需要单独分支；如果你把 `nxt` 先保存下来，重连过程就不会把后半段链丢掉。这个实验的重点是确认你能把“前驱、当前、后继”三者的位置说清楚。

| 场景 | 先读谁 | 改谁 | 容易出的错 | 正确修法 |
| --- | --- | --- | --- | --- |
| 删中间节点 | 前驱和后继 | 前驱的 `next` | 把后继链断开 | 先保存 `nxt`，再连过去 |
| 删头节点 | 虚拟头结点后的第一个节点 | 虚拟头结点的 `next` | 代码里多出一堆 if | 让虚拟头结点统一入口 |
| 删尾节点 | 前驱 | 前驱的 `next = None` | 误把尾巴之后的内容当作存在 | 确认目标节点确实存在后再改连接 |
| 连续删除多个节点 | 当前前驱和它的后继 | 循环更新前驱的 `next` | 忘记前驱位置要继续停留在原地 | 让前驱始终站在下一次删除之前 |

统一删链模板复制

```python
def remove_after_prev(prev):
 if prev is None or prev.next is None:
 return None
 target = prev.next
 prev.next = target.next
 return target
```

## box
### 这个模板怎么读
第一行守住输入条件，第二行拿到要删除的节点，第三行把前驱直接连到后继。重点是确认链表的连接关系在修改后仍然完整。

## box
### 这个模板怎么错
如果你直接写 `prev.next = prev.next.next`，通常也能跑，但调试时很难说清中间状态。把 `target` 先保存下来以后，你能更清楚地回答：我刚读到的是谁、我刚改的是谁、谁从链上消失了。

这个实验和数组覆盖的思路很像：数组里你固定写入位，链表里你固定前驱位。差别只在于，数组改的是连续槽位，链表改的是指针连接。你只要把这点说稳，后面删重复、反转、合并、插入这些题，操作顺序就会比死记模板更牢。

对应到练习区，10 题会检查你是否先保存后继，16 题会检查你是否理解顺着节点走，19 和 22 题会检查你是否能把状态约定和纸面台账讲出来。


---

# 常见误区 {#mistakes}
<!-- tag: mistakes -->

<!-- interactive: ['terminal-lab'] → keep as HTML -->

| 症状 | 原因 | 修法 |
| --- | --- | --- |
| 数组删除时越删越乱 | 一边遍历一边真删，后面的元素不断重排 | 改成慢指针覆盖，或者换更合适的数据结构 |
| 链表反转后丢尾巴 | 没保存后继就改了 `next` | 先记住 `nxt = cur.next`，再做重连 |
| 循环队列看起来空满都一样 | 只靠 `front == rear` 判断 | 补 `size`，或改成空位约定 |
| 头部出队越来越慢 | 用 `pop(0)` 反复搬移元素 | 换 `deque` 或环形数组思路 |

先把错法画出来，比只背“正确答案”更稳。你知道它会怎么错，才知道下一次要盯哪一个状态变量。


---

# 后续衔接 {#bridge}
<!-- tag: bridge -->

这一页的重点是把“动作”映射到“结构”。后面的单元会继续用这些词，但会换成更明确的题型：队列/栈、哈希表、链表双指针、数组双指针、滑动窗口和前缀和。

| 后续单元 | 会接住什么 | 这一页先别展开什么 |
| --- | --- | --- |
| 02 队列栈与受限操作 | LIFO / FIFO、只能在一端或两端操作 | 不要把实现细节和结构选择混在一起 |
| 04 链表双指针框架 | 前驱、快慢指针、虚拟头结点 | 不要先把窗口、哈希、树的套路拉进来 |
| 05 数组双指针框架 | slow / fast 覆盖、左右夹逼、原地合并 | 先把“覆盖”和“移动”分清 |
| 06 滑动窗口核心模板 | 连续区间、队首过期、区间边界 | 先别把窗口和队列混成一个概念 |


---

# 容量 3 的手算台账 {#ledger}
<!-- tag: walkthrough -->

把环形数组缩到容量 3，很多抽象就会变得很具体。你可以直接看出：什么叫写入位、什么叫读取位、什么时候会绕回、为什么空满不能只看头尾相等。

| 步骤 | 动作 | 数组内容 | front | rear | size |
| --- | --- | --- | --- | --- | --- |
| 0 | 初始化 | [_ , _ , _] | 0 | 0 | 0 |
| 1 | 入队 A | [A, _ , _] | 0 | 1 | 1 |
| 2 | 入队 B | [A, B, _] | 0 | 2 | 2 |
| 3 | 出队一次 | [_ , B, _] | 1 | 2 | 1 |
| 4 | 入队 C | [_, B, C] | 1 | 0 | 2 |
| 5 | 再入队 D | [D, B, C] | 1 | 1 | 3 |

## box
### 这张表在看什么
这张表要让你看到：rear 到末尾以后是怎么回到 0 的，空出来的位置怎么被重新用起来的，以及 `size` 怎么保证空和满不混淆。

## box
### 和代码怎么对上
每一步入队，都对应“先写当前 rear，再移动 rear，再增加 size”；每一步出队，都对应“先读 front，再移动 front，再减少 size”。只要顺序反过来，状态就会开始漂。

这张台账还可以换成另一个检查方法：每走一步，就问自己三件事。第一，刚刚读取的是当前有效元素吗；第二，刚刚写入的是空位吗；第三，size 有没有同步变化。只要有一问答不上来，就说明你的状态约定还不稳。

容量 3 的好处在于，它能把绕回和空满都压缩到很短的步数里。容量太大时，错误常常被延后；容量 3 时，错位会很快暴露，所以适合拿来做纸面自检、面试口述和代码单测的第一组样本。


---

# 错位会长什么样 {#failure}
<!-- tag: failure -->
<!-- layout: grid2 -->

## box
### 只看 front == rear
错因是把两个不同状态压成了一个条件。后果是满队列和空队列会被判断成同一件事，程序要么拒绝入队，要么把空队列当成有元素。

## box
### 不做取模
错因是把环形数组当成普通数组。后果是 rear 很快越界，或者前面明明有空位却根本用不上。

## box
### 先修什么
先固定 `front` 的含义，再固定 `rear` 的含义，最后让 `size` 负责空满判断。只要三者职责分开，空满边界就不会互相抢活。

## box
### 为什么这一步重要
后面遇到 622、641，题目不会替你定义状态。你必须自己把写入位、读取位和容量关系讲清楚，不然实现会一直在边界上抖。

如果你要把这页的内容变成可执行检查，最简单的方法就是拿容量 3 的小队列做三轮纸面测试：先只入队，看 `rear` 是否按取模前移；再做一次出队，看 `front` 是否只移动一次；最后故意再入队，看 `size` 是否能把满队列和空队列区分开。这个测试很短，但足够抓住绝大多数状态错误。

当你怀疑自己的实现有问题时，不要先看大样例。先把小样本画成状态表，再把每一步读写动作写成一句话。这样你会很快发现问题是在“写之前该不该判满”，还是在“读之后有没有忘记减 size”，或者是在“绕回时有没有忘记取模”。


---

# 把 A、B、C、D 走一遍 {#walkthrough}
<!-- tag: walkthrough -->

这一段只做一件事：拿同一组样本 `A、B、C、D`，先走数组覆盖，再走链表删节点，再走容量 3 的循环队列。三个结构都在管理“当前位置”，只是被管理的对象不一样。数组管理写入位，链表管理前驱和后继，循环队列管理读位、写位和剩余容量。

样本
数组覆盖
链表删节点
循环队列
观察点


A
`slow = 0` 时写入第一个保留值
先定位到 `prev` 的后继
`data[rear]` 先落下第一个元素
先看当前可用位置，不急着改下一步


B
若不满足保留条件，`slow` 不动
如果 B 是 target，先保留 `nxt` 再断开 B
第二次入队后 `rear` 前移一格
旧状态是否还要保留，决定你能不能继续走


C
满足条件就覆盖到当前 `slow` 位
如果 C 是后继，删完 B 后链会直接指向 C
队列还没满时继续入队，`size` 递增
读写位有没有分开，决定结构是否还完整


D
继续扫描，直到结果前缀结束
删完目标后再检查后面是否仍能追踪
容量 3 满了以后，D 应该被拒绝或改用别的位置
边界决定状态是否可信

## box
### 数组里怎么读这组样本
数组覆盖时，A、B、C、D 只是扫描顺序。你先看 `fast` 走到哪里，再看 `slow` 是否应该写入。真正要保留的是满足条件的那些位置。换成题目语言，就是“保留谁、覆盖谁、结果前缀多长”。

## box
### 链表里怎么读这组样本
链表里 A、B、C、D 由连接关系区分，不靠下标。你一旦在 B 处改掉 `prev.next`，后面的链就要靠 `nxt` 继续追。这里最关键的检查点是：修改之后，C 和 D 还能不能从新的起点追到。

## box soft
### 循环队列里怎么读这组样本
把容量固定成 3 以后，A、B、C 三次入队会把队列推满，D 再来时就必须先检查 `size == capacity`。这是数据不会被覆盖的底线。只看 `front == rear` 会把空和满混成一类，纸面样本很快就能把这个错误暴露出来。

## box soft
### 这组样本要留下什么
读完这一段，你应该能把“扫描、保存后继、写入、回绕、判满”这五个动作说出来，并且知道它们分别对应哪个结构。下一次看到 A、B、C、D 这种短样本时，不要先想代码，把位置关系先摆正。

三种结构的最小动作复制

```text
数组：fast 扫到 B 时决定是否把 B 写进 slow 所在的结果前缀。
链表：prev 站稳后，先记住 nxt，再把 prev.next 连到 nxt。
循环队列：A、B、C 入队后先看 size，再决定 D 是入队还是拒绝。
```

如果你把这段动作序列写成纸面检查，顺序应该是：先确认谁是当前读位，后确认谁是当前写位，再确认旧状态还能不能保留。这个顺序一乱，数组会写偏，链表会断链，循环队列会空满混淆。


---

# 练习 {#practice}
<!-- tag: diagnostic -->

<!-- practice-group: 一、结构选择 -->
<!-- quick-review: 回看要点 先看“读什么、写什么、改哪里”，再看代码长什么样。 -->

### 1 choice

**Q:** 1. 题目要按下标读取并频繁交换，优先选什么？

- [ ] 数组
- [ ] 链表
- [ ] 队列

Answer: 数组
Explain: 数组把位置和编号绑在一起，读下标和原地交换都直接。

### 2 判断

**Q:** 2. 链表按位置访问通常比数组慢。

- [ ] 对
- [ ] 错

Answer: 错
Explain: 链表要顺着 next 走，按位置访问不是它的强项。

### 3 填空

**Q:** 3. 环形数组里把下标往后走一步，常用什么运算？


### 4 简答

**Q:** 4. 为什么 `pop(0)` 不适合频繁模拟队列出队？

Explain: 头部删除会把后面的元素整体往前搬，单次看起来只是删一个，实际会移动一串数据。频繁调用时，总代价会变高。更稳的做法是用 `deque.popleft()` 或环形数组思路。

<!-- end-practice-group -->

<!-- practice-group: 二、状态追踪 -->
<!-- quick-review: 回看要点 先分清读位和写位，再看边界条件。 -->

### 5 choice

**Q:** 5. 约定 rear 指向下一次写入位置时，入队的正确顺序是哪一个？

- [ ] 先写 data[rear]，再移动 rear
- [ ] 先移动 rear，再写 data[rear]
- [ ] 只改 size，不改 rear

Answer: 先写 data[rear]，再移动 rear
Explain: 先写再移动，rear 才始终指向下一次写入的位置。

### 6 choice

**Q:** 6. front 的职责是什么？

- [ ] 下一次读取位置
- [ ] 下一次写入位置
- [ ] 队尾最后一个元素

Answer: 下一次读取位置
Explain: front 不表示最后一个元素，而是下一次出队要读的位置。

### 7 choice

**Q:** 7. size 的职责是什么？

- [ ] 真实元素数
- [ ] 下一次读取位置
- [ ] 数组总容量

Answer: 真实元素数
Explain: size 直接告诉你现在队列里有多少个真实元素。

### 8 边界

**Q:** 8. 容量为 3 的循环队列，入队 A、B、C 后再入队 D，会发生什么？

Explain: 如果容量已经满了，D 应该被拒绝，不能再写入。原因是固定容量已经用完，继续写会覆盖旧元素。检查点是 `size == capacity`，不要只看 `front == rear`。

<!-- end-practice-group -->

<!-- practice-group: 三、代码阅读与诊断 -->
<!-- quick-review: 回看要点 先记住后继、再改连接；先扫原数组、再写结果。 -->

### 9 choice

**Q:** 9. 删除链表当前节点后继时，先保存什么？

- [ ] 后继节点
- [ ] 头结点
- [ ] 尾结点

Answer: 后继节点
Explain: 先保存后继，才能在改完连接以后还找得到后面的链。

### 10 诊断

**Q:** 10. 如果链表反转时没保存 `cur.next`，会丢什么？

Explain: 会把原来后面的节点链断掉，因为 `cur.next = prev` 之后，原来的后继不再能直接追到。正确做法是先记住 `nxt = cur.next`，再改 `cur.next`，最后把 `cur` 挪到 `nxt`。

### 11 choice

**Q:** 11. 数组删除题里一边遍历一边 `remove` 的典型问题是什么？

- [ ] 会跳过元素并反复搬移元素
- [ ] 会自动变成 O(1)
- [ ] 只会影响最后一个元素

Answer: 会跳过元素并反复搬移元素
Explain: 真删会触发元素搬移，遍历指针还可能直接跨过新挪过来的值。

### 12 mini-fields

**Q:** 12. 慢指针覆盖数组时，fast 和 slow 各负责什么？

- [ ] fast
- [ ] slow


<!-- end-practice-group -->

<!-- practice-group: 四、题型迁移 -->
<!-- quick-review: 回看要点 先判题型，再选数据结构；先判状态，再写边界。 -->

### 13 choice

**Q:** 13. 622 设计循环队列为什么适合环形数组？

- [ ] 固定容量里头尾反复移动
- [ ] 需要随机访问任意下标
- [ ] 只是为了排序更快

Answer: 固定容量里头尾反复移动
Explain: 622 的核心是固定容量和头尾移动，环形数组正好把这两件事接住。

### 14 choice

**Q:** 14. 641 比 622 多了什么状态压力？

- [ ] 两端都要维护入队出队
- [ ] 只需要保存一个尾指针
- [ ] 必须扩容到无限大

Answer: 两端都要维护入队出队
Explain: 641 把操作扩到两头，front 和 rear 的职责都要统一。

### 15 填空

**Q:** 15. 80 删除有序数组中的重复项 II 里，slow 前面保存的应该满足什么？

Answer: 最多保留两个,每个数最多两个
Explain: 这题的结果前缀只允许同一个数字出现两次。

### 16 迁移

**Q:** 16. 为什么链表题 2 两数相加更适合顺着节点走？

Explain: 链表节点只知道下一个节点，想取第 k 个位置必须从头走过去。两数相加又是逐位推进的过程，正好可以一边走节点一边处理进位，不需要随机访问。用链表顺着走，状态更直接，代码也更贴近题意。

<!-- end-practice-group -->

<!-- practice-group: 五、容量 3 台账 -->
<!-- quick-review: 回看要点 看懂表，再看代码；看懂状态，再回答空满。 -->

### 17 choice

**Q:** 17. 在容量 3 的台账里，第 4 步入队 C 后，rear 变成多少？

- [ ] 0
- [ ] 1
- [ ] 2

Answer: 0
Explain: rear 到末尾后取模回到 0。

### 18 choice

**Q:** 18. 在容量 3 的台账里，第 5 步再入队 D 后，size 变成多少？

- [ ] 2
- [ ] 3
- [ ] 4

Answer: 3
Explain: size 记录真实元素数，满队列时等于容量。

### 19 诊断

**Q:** 19. 为什么这张台账能同时说明空和满？

Explain: 因为它把“头尾是否相等”从唯一判断里拿掉了。空看 `size == 0`，满看 `size == capacity`，`front` 和 `rear` 只负责读写位置。这样空和满就不会共用同一个条件。

<!-- end-practice-group -->

<!-- practice-group: 六、纸面单测 -->
<!-- quick-review: 回看要点 先画台账，再对照代码。 -->

### 20 choice

**Q:** 20. 如果入队前忘了判满，会最先出现什么问题？

- [ ] 覆盖旧元素
- [ ] 自动扩容
- [ ] 只会变慢，不会错

Answer: 覆盖旧元素
Explain: 满了还写，会把还在队列里的旧元素覆盖掉。

### 21 choice

**Q:** 21. 如果出队后忘了减 size，会最先出现什么症状？

- [ ] 空满判断失真
- [ ] 队列变成链表
- [ ] front 自动归零

Answer: 空满判断失真
Explain: size 失真后，后续的满/空判断都会偏掉。

### 22 诊断

**Q:** 22. 你会怎么用 3 个样本步骤检查循环队列？

Explain: 先看初始化时 `front`、`rear`、`size` 是否归零；再看一次入队后是否写到 `rear` 指向的位置、`rear` 是否前移；再做一次出队，看 `front` 是否前移且 `size` 是否减少；最后再入队，确认取模回绕后不会覆盖还在队列里的元素。

<!-- end-practice-group -->

<!-- practice-group: 七、删链实验 -->
<!-- quick-review: 回看要点 先找前驱，再找后继，最后改连接。 -->

### 23 choice

**Q:** 23. 删链实验里，prev.next = target.next 这句改的是谁？

- [ ] 前驱节点的 next
- [ ] target 自己的值
- [ ] 头结点的值

Answer: 前驱节点的 next
Explain: 它改的是前驱和后继之间的连接。

### 24 choice

**Q:** 24. 为什么删链时先保存 target 有帮助？

- [ ] 便于说明当前删除节点
- [ ] 会让链表自动扩容
- [ ] 能直接跳过判空

Answer: 便于说明当前删除节点
Explain: 先保存 target，便于调试时说明谁被删掉、谁还留在链上。

### 25 总结

**Q:** 25. 如果你要用一句话讲清删链动作，会怎么讲？

Explain: 删链就是让前驱节点直接跳到后继节点，把中间那个节点从连接关系里拿掉。先确认前驱和后继都存在，再改前驱的 `next`，这样链不会断。

<!-- end-practice-group -->


---

# 参考资料 {#sources}
<!-- tag: source -->

- **课程地图**：`index.html`，用于确认本页在 7 个单元中的位置和前后衔接。
- **本页**：`01_数组链表与环形数组/学习页.html`，用于提炼数组、链表和环形数组的主线、状态变量和常见边界。
- **相邻单元**：`02_队列栈与受限操作` 到 `07_前缀和与差分数组`，用于校准后续衔接和术语边界。
- **手算台账**：容量为 3 的循环队列状态表，用于检查 `front`、`rear`、`size` 和取模是否同步。

---
