---
course: "计算机教育中缺失的一课"
unit: "12"
title: "代码质量与自动化检查｜学习页"
kicker: "代码质量"
kicker_en: "自动化检查"
summary: "代码质量工具的目标不是让项目显得专业，而是把反馈前移：保存时格式化，提交前检查，合并前测试，主干持续集成。越早发现问题，修复成本越低。"

side_note: "质量链路的目标是更早发现问题，不是堆工具显得专业。"

course_links:
  - text: "课程地图"
    href: "../index.html"
  - text: "上一节"
    href: "../11_代码之外的工程协作/学习页.html"
  - text: "下一节"
    href: "../13_数据整理与命令行转换/学习页.html"
---

# 9. 质量链路安排反馈时机 {#deep-guide}
<!-- tag: deep guide -->

代码质量工具最容易被学成工具名清单：formatter、linter、type checker、test、pre-commit、CI。真正重要的是反馈时机。越早发现问题，修复成本越低；但越早运行的检查越应该快、稳定、低噪声。保存时适合格式化和局部 lint；提交前适合快速检查；合并前适合完整测试、构建和跨环境验证。

不要一上来把所有检查都塞进 pre-commit。提交前检查如果跑 10 分钟，开发者会想办法绕过它，最后工具形同虚设。也不要只依赖 CI。CI 虽然统一可信，但反馈晚，等 CI 红了再修，通常已经切换上下文。好的链路是分层：本地快反馈处理低级问题，CI 处理共享环境和完整回归。

每个工具还要说清边界。formatter 不证明逻辑正确；linter 不知道业务需求；type checker 不能覆盖所有运行时状态；测试只覆盖你写出来的场景；CI 也不能替代人工评审。工具负责稳定规则，人负责需求、风险和取舍。

| 时机 | 适合检查 | 不适合放这里 |
| --- | --- | --- |
| 保存时 | 格式化、局部 lint | 慢测试、联网检查 |
| 提交前 | format check、lint、密钥扫描、快速单测 | 十分钟级全量测试 |
| 推送后 / PR | 完整测试、类型检查、构建、跨平台矩阵 | 只能本地复现的交互操作 |
| 发布前 | 端到端、回归、性能或安全关键检查 | 无关风格争论 |

你现在应该能做什么：为一个小项目设计“保存时、提交前、合并前”三层检查，而不是把所有工具堆在同一个地方。


---

# 10. 测试要覆盖行为，不是讨好覆盖率数字 {#}
<!-- tag: tests -->

覆盖率能告诉你哪些代码被执行过，但不能告诉你断言是否有意义。一个测试调用了函数却没有检查结果，覆盖率会增加，质量不会增加。好测试应该描述行为：给定什么输入，期望什么输出；遇到什么错误，应该如何失败；边界条件下是否保持约定。

写测试时先选关键风险。解析函数要测合法输入、缺字段、错误类型；文件写入要测路径存在、路径不存在、权限失败；CLI 要测参数解析、默认行为、错误提示；网络逻辑要避免真实外部依赖，使用 mock 或 fake。测试越贴近风险，越能在回归时发出有用信号。

测试失败也要会读。先看失败命令，再看失败测试名，再看断言差异，再看最近改动。不要一看到红色就全量重跑。重跑只能处理偶发问题，不能替代定位。若测试偶发失败，也应该记录条件，找随机性、时间、并发或外部依赖。

错误示例调用了函数但没有验证行为复制

```text
def test_parse_user():
 parse_user({"id": "1", "name": "Ada"})
 # 没有 assert，函数返回错了也可能看不出来。
```

正确示例正常路径和错误路径都固定住复制

```text
def test_parse_user_converts_id():
 assert parse_user({"id": "1", "name": "Ada"}) == {"id": 1, "name": "Ada"}

def test_parse_user_rejects_missing_id():
 with pytest.raises(KeyError):
 parse_user({"name": "Ada"})
```

你现在应该能做什么：判断一个测试是否真的验证了行为，并为关键错误路径补断言。


---

# 11. CI 失败排查：从日志回到可复现命令 {#}
<!-- tag: ci -->

CI 失败时，第一步找失败的是哪一个 job、哪一条命令、哪一个测试或断言。CI 日志通常很长，但结构清楚：安装依赖、运行检查、运行测试、上传产物。你要从最后的错误往前找到真实失败点，而不是被前面的普通输出吓住。

常见 CI 问题分四类。第一是代码真实回归，本地也能复现。第二是环境差异，比如本地 Windows、CI Linux，路径大小写或换行不同。第三是依赖差异，本地缓存旧版本，CI 安装新版本。第四是偶发问题，来自时间、随机数、网络或并发。不同类型修法不同。

最稳的做法是把 CI 失败命令复制到本地复现。如果不能复现，就对比 Python/Node 版本、操作系统、环境变量、依赖锁文件和测试数据。CI 是共享门禁，不是神秘黑箱；它只是另一个更干净、更严格的运行环境。

逐行拆解CI 失败阅读顺序复制

```text
1. 找失败 job：lint / test / build / deploy？
2. 找失败命令：ruff check . 还是 pytest？
3. 找失败对象：哪个文件、哪个测试、哪个断言？
4. 本地复现：复制同一条命令。
5. 对比环境：系统、版本、依赖、变量、随机性。
```

你现在应该能做什么：看到 CI 红了时，能定位失败命令和失败类型，而不是只反复重跑。


---

# 12. 正则表达式：写模式时同时写反例 {#}
<!-- tag: regex -->

正则很适合处理稳定文本模式：日志行、简单编号、邮箱粗校验、文件名筛选、搜索替换。它不适合解析完整 HTML、复杂嵌套括号、编程语言语法或规则变化很大的人类文本。能用结构化解析器时，优先用解析器；正则适合当边界清楚的小工具。

写正则时不要只拿一个正例试。至少准备三类样本：应该匹配的正例、不应该匹配的反例、边界样例。比如匹配错误日志时，要想清楚大小写、行首行尾、空格数量、时间戳、错误码是否稳定。模式过宽会误匹配，模式过窄会漏掉真实情况。

复杂正则要拆解。把每个元字符写成注释，或者用命名分组，让未来读者知道它在捕获什么。看不懂的正则是维护风险，尤其在自动替换和数据清洗里，误匹配可能批量改坏文件。

正确示例用正例和反例测试模式复制

```text
pattern = r"^ERROR \[[A-Z_]+\] .+ timeout$"

should_match = [
 "ERROR [NETWORK] request timeout",
]
should_not_match = [
 "WARN [NETWORK] request timeout",
 "TERROR [NETWORK] request timeout",
 "ERROR [NETWORK] request timeout later",
]
```

错误示例过宽模式用于批量替换复制

```text
.*id.*
# 可能匹配 user_id、identity、grid、midnight。
# 如果拿它做批量替换，很容易改到无关文本。
```

你现在应该能做什么：写正则前先列正例、反例和边界样例，并判断是否应该改用结构化解析器。


---

# 综合练习 {#practice}
<!-- tag: 混合练习 -->

<!-- practice-intro -->
<!-- end-practice-intro -->

**概念**先区分 formatter、linter、测试、CI。
**应用**能把概念落到命令、工具或协作场景。
**诊断**写出反馈链路和修法。
**表达**说明自动化质量链路。

<!-- practice-group: 一、质量工具概念 -->
<!-- quick-review: 检查点 先区分 formatter、linter、测试、CI。 -->

### ? 概念检查

**Q:** 1. 单选：formatter 主要负责？

- [ ] 统一代码表面风格
- [ ] 证明业务正确
- [ ] 部署服务器


### ? 概念检查

**Q:** 2. 单选：CI 的核心价值是？

- [ ] 在共享环境自动运行检查
- [ ] 隐藏测试失败
- [ ] 替代需求分析


### ? 概念检查

**Q:** 3. 判断：只测 happy path 就足以保证质量。

- [ ] 正确
- [ ] 错误


### ? 概念检查

**Q:** 4. 填空：提交前运行快速检查的机制常叫 ____。


### ? 概念检查

**Q:** 5. 填空：正则中匹配行首常用符号是？


### ? 概念检查

**Q:** 6. 单选：linter 主要用于？

- [ ] 发现可疑写法和潜在问题
- [ ] 创建分支
- [ ] 改变屏幕亮度


<!-- end-practice-group -->

<!-- practice-group: 二、质量问题诊断 -->
<!-- quick-review: 答题脚手架 写出反馈链路和修法。 -->

### ? 诊断应用

**Q:** 7. 排序：从本地编辑到合并前检查的合理链路


### ? 诊断应用

**Q:** 8. 匹配：formatter、linter、type checker、test、CI 各负责什么？


### ? 诊断应用

**Q:** 9. 阅读：`^ERROR .+ timeout$` 每个关键部分是什么意思？


### ? 诊断应用

**Q:** 10. 诊断：团队只跑 formatter 就说质量通过。按错因/后果/修法回答。


### ? 诊断应用

**Q:** 11. 诊断：CI 失败后只反复重跑。按错因/后果/修法回答。


### ? 诊断应用

**Q:** 12. 诊断：`.*error.*` 匹配了很多不该匹配的文本。按错因/后果/修法回答。


### ? 诊断应用

**Q:** 13. 多选：最小质量门禁可以包含哪些？

- [ ] format check
- [ ] lint
- [ ] tests
- [ ] type check


### ? 诊断应用

**Q:** 14. 判断：formatter 通过就能证明业务逻辑正确。

- [ ] 正确
- [ ] 错误


### ? 诊断应用

**Q:** 15. 场景：pre-commit 每次提交要跑 10 分钟，团队开始绕过它。你会怎么调整？


<!-- end-practice-group -->

<!-- practice-group: 三、场景迁移与解释 -->
<!-- quick-review: 表达要求 说明自动化质量链路。 -->

### ? 迁移表达

**Q:** 16. 简答：哪些质量问题适合交给机器，哪些更适合人来判断？


### ? 迁移表达

**Q:** 17. 简答：pre-commit 不应该太慢，为什么？


### ? 迁移表达

**Q:** 18. 场景：你接手一个没有质量工具的小项目，会如何建立最小检查链路？


### ? 迁移表达

**Q:** 19. 面试追问：单元测试、集成测试、端到端测试的差别是什么？


### ? 迁移表达

**Q:** 20. 面试追问：为什么本地通过仍需要 CI？


### ? 迁移表达

**Q:** 21. 简答：正则适合什么，不适合什么？


### ? 迁移表达

**Q:** 22. 费曼题：向零基础同学解释一条自动化代码质量链路如何工作。


### ? 迁移表达

**Q:** 23. 诊断：一个测试只调用函数，没有任何 assert，也没有检查异常。按错因/后果/修法回答。


### ? 迁移表达

**Q:** 24. 场景：CI 在 Linux 失败，本地 Windows 通过。你会优先对比哪些信息？


<!-- end-practice-group -->


---

